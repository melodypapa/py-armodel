# Sync todo: Group 9 — Constants & MSR

Input: full-repo orphan audit 2026-09-23 (unstamped ∧ untracked, M2 only) · Queue order = row order
(resume = first class row still `[ ]`; all class rows `[x]` = sync finished — Rule 0017.3)
> **Rule — already-verified short-circuit (added 2026-09-04):** before running the 9-step
> sync for a row, check whether the class already carries `# Spec verified: <RELEASE>` or
> `# XSD verified: <xsd-file>` in its own class body (verify the marker in the source — a
> row in this todo is not proof). If it does **and** a quick deviation check finds nothing
> new (base vs the spec `Base` closure, member types vs its table, verbatim docstrings,
> reader/writer coverage, checklist shape per Rules 0002/0012), **skip the 9 steps**: mark
> the row `- [x] <Class> — already verified (<marker>, <file>)` and move on. If the check
> finds a new deviation, do **not** mark it verified — keep the row queued, run the steps
> for the deviation only, and record it in Step 8 (Rule 0012.3: an existing marker is not
> proof).

## Queue (dependency-first)

- [ ] `NumericalValueSpecification` — ValueSpecification — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Constants/__init__.py
  - [x] Step 1 — Sync members & description from spec — Table 5.114 (AUTOSAR_CP_TPS_SoftwareComponentTemplate.md L12573, p.436 via pdf_page.py); reproductions verified identical: Table D.43 (AUTOSAR_CP_TPS_BSWModuleDescriptionTemplate.md L8855) + Table F.84 (AUTOSAR_CP_TPS_SystemTemplate.md L75190 table body before title — appendix title-after-content rendering); Class table: Package M2::AUTOSARTemplates::CommonStructure::Constants matches current file; class Note "A numerical ValueSpecification which is intended to be assigned to a Primitive data element. Note that the numerical value is a variant, it can be computed by a formula."; Base ARObject, ValueSpecification → most-derived base ValueSpecification — already stamped `# Spec verified: R23-11` in this module (L29; base-stamp observation, not a blocker); single attribute row: value (Numerical, 0..1, attr, Note "This is the value itself. Stereotypes: atpVariation Tags: vh.latestBindingTime=preCompileTime" — Numerical → ARNumerical, the atpMixedString NUMERICAL-VALUE-VARIATION-POINT payload); constr_1920 existence constraint is doc-level, not a model member; Aggregated-by list is consumer-side, not modeled. XSD 00052 ground truth: group NUMERICAL-VALUE-SPECIFICATION L85701 (single VALUE minOccurs 0) + complexType L85718 (AR-OBJECT + VALUE-SPECIFICATION + NUMERICAL-VALUE-SPECIFICATION groups → XML element order SHORT-LABEL then VALUE); reader pre-exists (parser getNumericalValueSpecification arxml_parser.py L7929 via readValueSpecification base helper + getChildElementOptionalNumericalValue; tag dispatch getValueSpecification L7995) and writer pre-exists (writeNumericalValueSpecification arxml_writer.py L1775 via writeValueSpecification + setChildElementOptionalNumericalValue; consumers L1788 ArrayValueSpecification.elements / L1861 setChildValueSpecification / L3389 RecordValueSpecification.fields) — coverage complete, XSD order correct, no dropped field; model layer is old-style: 4-col checklist without `# Spec:` line, paraphrased docstrings, `ARNumerical` non-Optional hints → Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red) — new mirror file tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/Constants/test_NumericalValueSpecification.py (7 tests, style of stamped sibling test_ReferenceValueSpecification.py): inheritance isinstance ValueSpecification, initialization (value/shortLabel None), get/set round-trip with ARNumerical("3.14") asserting identity + float value, setValue(None) no-op, shortLabel base round-trip + None no-op, and typing.get_type_hints assert Optional[ARNumerical] on getValue return + setValue param; Red confirmed 1 failed/6 passed (first run KeyError'd on __init__-body hints — PEP 526 self.attr annotations in method bodies are not runtime-visible, moved the Red assert to the accessor signatures — final Red: getValue return = ARNumerical ≠ Optional[ARNumerical]); honest note: the 6 behavioral tests pass immediately — the class pre-exists with value/getValue/setValue and a None-guard (orphan intake, alignment pass); the only genuine Red is the non-Optional typing (Rule 0003)
  - [x] Step 3 — Implement model class (Green) — member set per Table 5.114 already complete (single attr value; no fabrication, no missing member, dedicated field not a registry filter); base ValueSpecification (most-derived, stamped) already provides shortLabel; Green edits: `self.value: ARNumerical = None` → `self.value: Optional[ARNumerical] = None`, getValue return `ARNumerical` → `Optional[ARNumerical]`, setValue param `ARNumerical` → `Optional[ARNumerical]` + explicit chaining return `-> NumericalValueSpecification` (stamped-sibling convention); mirror test 7/7 passed (0.16s); Step-3 referenced classes: none missing — ARNumerical (PrimitiveTypes, imported), ValueSpecification (same module, stamped)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped ALL stale docstrings in the class (old class docstring "Represents a numerical value specification…", `__init__` docstring "Initializes…", paraphrased getter "Gets the numerical value…"/setter "Sets the numerical value…" + Args/Returns blocks) then rewrote verbatim from Table 5.114 markdown: class docstring = class Note "A numerical ValueSpecification which is intended to be assigned to a Primitive data element. Note that the numerical value is a variant, it can be computed by a formula."; `__init__` no docstring (Rule 0012.2.5.2) with inline member comment = attribute Note verbatim "This is the value itself. Stereotypes: atpVariation Tags: vh.latestBindingTime=preCompileTime"; getter docstring = attribute Note verbatim (+ closing period per stamped-sibling inline shape); guarded setter docstring = attribute Note verbatim + "A None value is a no-op and does not overwrite an existing value."; shape mirrors stamped sibling ReferenceValueSpecification (multi-line triple-quoted); PEP 526 annotated member directly under its note comment, blank line between `__init__` attribute block and methods; mirror test 7/7 after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/writer/test_writer_numerical_value_specification.py (3 tests, style of stamped sibling test_writer_not_available_value_specification.py, AUTOSAR.getInstance().new() fixture): (1) write with SHORT-LABEL "nvs" + VALUE 3.14 asserting the full child tag sequence == ["SHORT-LABEL", "VALUE"] (XSD 00052 complexType L85718 group order: AR-OBJECT + VALUE-SPECIFICATION + NUMERICAL-VALUE-SPECIFICATION) and both field values; (2) empty-wrapper case — bare spec writes the tag with NO VALUE child; (3) write → ET serialize → namespace-inject → parser.getValueSpecification re-parse asserting isinstance + shortLabel "nvs" + value 3.14 (field values, not len()); honest note: 3/3 passed immediately (0.17s) — reader/writer coverage pre-exists and is correct for this class; no genuine Red found (no dropped field)
  - [x] Step 6 — Update parser & writer (Green) — NO CHANGE needed: reader arxml_parser.py getNumericalValueSpecification (L7929) already reads via readValueSpecification base helper → setShortLabel + setValue(getChildElementOptionalNumericalValue("VALUE")) (mutator form, no chaining), dispatched from the getValueSpecification tag dispatch (L7995-7996); writer arxml_writer.py writeNumericalValueSpecification (L1775) already writes tag → writeValueSpecification base → setChildElementOptionalNumericalValue("VALUE", getValue()) in XSD order, consumed from ArrayValueSpecification.elements (L1788), setChildValueSpecification polymorphic dispatch (L1861), RecordValueSpecification.fields (L3389); Rule 0013 matched-name pairs verified (readValueSpecification↔writeValueSpecification, setValue↔getValue, setShortLabel↔getShortLabel); observation: reader re-calls setShortLabel after readValueSpecification already set it — idempotent duplicate (setShortLabel None-guards), present on all value-spec siblings, left as-is; round-trip 3/3 + model 7/7 re-run green
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to 6-column Rule 0002 format: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 5.114, p.436` + Columns legend + 3 rows source order (__init__ [—]/[—], getValue writer [x] — writer calls getValue/shortLabel getter, setValue reader [x] — reader calls setValue/setShortLabel), every row release R23-11; shortLabel accessor rows stay on the stamped base ValueSpecification checklist (base owns them); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO deviations: no placeholders, no missing members (table = single attr value, all modeled), no naming (ref/tref/aggr rules n/a), no type deviations (Numerical → ARNumerical is the established mapped type, same as stamped consumers), reader+writer coverage complete, member order = displayed row order (single attribute); nothing to add to method_deviation_by_class*.md; observations recorded at Steps 1/6 (stamped base; idempotent setShortLabel re-read) are not spec deviations; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 159/159 (Constants model dir incl. new test_NumericalValueSpecification.py 7 + CommonStructure_init.py 149 + new test_writer_numerical_value_specification.py 3), full suite 11346 passed/0 failed (parent-branch baseline 11336 per Group8 final rows + 10 net-new: 7 model + 3 round-trip; the 11326 figure in the batch brief predates the last Group8 additions), npm run lint (flake8+ruff) clean, npm run black no-op (1161 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `TextValueSpecification` — ValueSpecification — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Constants/__init__.py
  - [x] Step 1 — Sync members & description from spec — Table 5.113 (AUTOSAR_CP_TPS_SoftwareComponentTemplate.md, body split across page break: header rows L12551-12557, title + attribute row L12565-12569, p.436 via pdf_page.py); reproductions verified identical: Table F.136 (AUTOSAR_CP_TPS_SystemTemplate.md L76158 title, body before title L76142-76156 — appendix title-after-content rendering) + R4.3.1 Table 5.121 (AUTOSAR_TPS_SoftwareComponentTemplate.md L10394 — same Note/Base/attr text but mult 1 vs R23-11 0..1; R23-11 + XSD minOccurs=0 authoritative, Rule 0015); Class table: Package M2::AUTOSARTemplates::CommonStructure::Constants matches current file; class Note "The purpose of TextValueSpecification is to define the labels that correspond to enumeration values."; Base ARObject, ValueSpecification → most-derived base ValueSpecification — already stamped `# Spec verified: R23-11` in this module (L29); single attribute row: value (VerbatimString, 0..1, attr, Note "This is the value itself. Note that vt uses the | operator to separate the values for the different bitfield masks in case that the semantics of the related DataPrototype is described by means of a BITFIELD_TEXTTABLE in the associated CompuMethod." — markdown `&#124;` = literal pipe; XSD documentation confirms the two-sentence text); constr_1919 existence constraint is doc-level, not a model member; Aggregated-by list is consumer-side, not modeled. XSD 00052 ground truth: group TEXT-VALUE-SPECIFICATION L122617 (single VALUE minOccurs 0 type VERBATIM-STRING) + complexType L122635 (AR-OBJECT + VALUE-SPECIFICATION + TEXT-VALUE-SPECIFICATION groups → XML element order SHORT-LABEL then VALUE); reader pre-exists (parser getTextValueSpecification arxml_parser.py L7936 via readValueSpecification base helper + setShortLabel duplicate + setValue(getChildElementOptionalLiteral "VALUE") — TYPE GAP: reads plain ARLiteral, spec type is VerbatimString; established sibling pattern reads VerbatimString fields via getChildElementOptionalVerbatimString e.g. SwValues.vt L6678, RuleArguments.vt L7871; tag dispatch getValueSpecification L7999; also consumed L7608 MetaDataItem.metaDataItemType) and writer pre-exists (writeTextValueSpecification arxml_writer.py L1769 via writeValueSpecification + setChildElementOptionalLiteral("VALUE", getValue()) — this IS the established writer convention for VerbatimString fields, no setChildElementOptionalVerbatimString exists, all VT fields write via setChildElementOptionalLiteral; consumers L1798 ArrayValueSpecification.elements / L1853 setChildValueSpecification / L3391 RecordValueSpecification.fields); model layer is old-style: 4-col checklist without `# Spec:` line, fabricated class docstring ("Represents a text value specification…", not the spec Note), paraphrased docstrings, `self.value: ARLiteral = None` non-Optional + wrong type (spec: Optional[VerbatimString]), setValue no None-guard, no chaining return type → Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red) — new mirror file tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/Constants/test_TextValueSpecification.py (7 tests, style of prior-agent test_NumericalValueSpecification.py): inheritance isinstance ValueSpecification, initialization (value/shortLabel None), get/set round-trip with VerbatimString("ON") asserting identity + string value, setValue(None) no-op, shortLabel base round-trip + None no-op, and typing.get_type_hints assert Optional[VerbatimString] on getValue return + setValue param; Red confirmed 2 failed/5 passed (test_value_annotation_is_optional — accessors carry no annotations, get_type_hints misses; test_set_value_none — setValue(None) overwrites, no None-guard); honest note: the 5 behavioral tests pass immediately — the class pre-exists with value/getValue/setValue (orphan intake, alignment pass); the genuine Reds are the unannotated accessors (Rule 0003) and the missing None-guard (stamped-sibling convention)
  - [x] Step 3 — Implement model class (Green) — member set per Table 5.113 already complete (single attr value; no fabrication, no missing member, dedicated field not a registry filter); base ValueSpecification (most-derived, stamped) already provides shortLabel; Green edits: `self.value: ARLiteral = None` → `self.value: Optional[VerbatimString] = None` (spec type VerbatimString, already imported in the module — used by sibling RuleArguments.vt), getValue return `Optional[VerbatimString]`, setValue param `Optional[VerbatimString]` + explicit chaining return `-> TextValueSpecification` (stamped-sibling convention) + None-guard; mirror test 7/7 passed (0.19s); Step-3 referenced classes: none missing — VerbatimString (PrimitiveTypes, imported), ValueSpecification (same module, stamped)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped ALL stale docstrings in the class (old class docstring "Represents a text value specification…", `__init__` docstring "Initializes…", paraphrased getter "Gets the text value…"/setter "Sets the text value…" + Args/Returns blocks) then rewrote verbatim from Table 5.113 markdown: class docstring = class Note "The purpose of TextValueSpecification is to define the labels that correspond to enumeration values."; `__init__` no docstring (Rule 0012.2.5.2) with inline member comment = attribute Note verbatim "This is the value itself. Note that vt uses the | operator to separate the values for the different bitfield masks in case that the semantics of the related DataPrototype is described by means of a BITFIELD_TEXTTABLE in the associated CompuMethod."; getter docstring = attribute Note verbatim (with closing period, markdown cell carries it); guarded setter docstring = attribute Note verbatim + "A None value is a no-op and does not overwrite an existing value."; shape mirrors prior-agent NumericalValueSpecification (multi-line triple-quoted); PEP 526 annotated member directly under its note comment, blank line between `__init__` attribute block and methods; Constants 156 + CommonStructure_init 163 total passed after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/writer/test_writer_text_value_specification.py (3 tests, style of prior-agent test_writer_numerical_value_specification.py, AUTOSAR.getInstance().new() fixture): (1) write with SHORT-LABEL "tvs" + VALUE "ON" asserting the full child tag sequence == ["SHORT-LABEL", "VALUE"] (XSD 00052 complexType L122635 group order: AR-OBJECT + VALUE-SPECIFICATION + TEXT-VALUE-SPECIFICATION) and both field values; (2) empty-wrapper case — bare spec writes the tag with NO VALUE child; (3) write → ET serialize → namespace-inject → parser.getValueSpecification re-parse asserting isinstance TextValueSpecification + isinstance VerbatimString on getValue() + shortLabel "tvs" + value "ON" (field values + spec type fidelity, not len()); Red confirmed 1 failed/2 passed (test_text_value_specification_round_trip — reader getChildElementOptionalLiteral returns plain ARLiteral, isinstance VerbatimString False — genuine Red on spec-type fidelity; the 2 writer tests pass immediately — writer coverage pre-exists and is correct, no dropped field)
  - [x] Step 6 — Update parser & writer (Green) — reader arxml_parser.py getTextValueSpecification (L7936): single-line change `setValue(getChildElementOptionalLiteral(element, "VALUE"))` → `setValue(getChildElementOptionalVerbatimString(element, "VALUE"))` — spec-type fidelity fix matching the model's Optional[VerbatimString], same pattern as sibling VerbatimString readers (SwValues.vt L6678, RuleArguments.vt L7871); setValue/setShortLabel mutator form, no chaining, dispatched from the getValueSpecification tag dispatch (L7999-8000) and consumed at L7608 MetaDataItem.metaDataItemType; writer arxml_writer.py writeTextValueSpecification (L1769) NO CHANGE needed — already writes tag → writeValueSpecification base → setChildElementOptionalLiteral("VALUE", getValue()) in XSD order; setChildElementOptionalLiteral IS the established writer convention for VerbatimString fields (no setChildElementOptionalVerbatimString exists; SwValues.vt L1724, RuleArguments.vt L3293 all write via Literal helper — VerbatimString is an ARLiteral subclass), consumed from ArrayValueSpecification.elements (L1798), setChildValueSpecification polymorphic dispatch (L1853), RecordValueSpecification.fields (L3391); Rule 0013 matched-name pairs verified (readValueSpecification↔writeValueSpecification, setValue↔getValue, setShortLabel↔getShortLabel); observation: reader re-calls setShortLabel after readValueSpecification already set it — idempotent duplicate (setShortLabel None-guards), present on all value-spec siblings, left as-is; round-trip 3/3 + model 7/7 re-run green (10 passed)
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to 6-column Rule 0002 format: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 5.113, p.436` + Columns legend + 3 rows source order (__init__ [—]/[—], getValue writer [x] — writer calls getValue/shortLabel getter, setValue reader [x] — reader calls setValue/setShortLabel), every row release R23-11; shortLabel accessor rows stay on the stamped base ValueSpecification checklist (base owns them); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO deviations: no placeholders, no missing members (table = single attr value, all modeled), no naming (ref/tref/aggr rules n/a), no type deviations remaining — reader's VALUE read upgraded ARLiteral → VerbatimString in Step 6 to match the spec Type column (pre-existing intake gap fixed, not an open deviation), reader+writer coverage complete, member order = displayed row order (single attribute); nothing to add to method_deviation_by_class*.md; observations recorded at Steps 1/6 (stamped base; idempotent setShortLabel re-read) are not spec deviations; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 10/10 (new test_TextValueSpecification.py 7 + new test_writer_text_value_specification.py 3), full suite 11356 passed/0 failed (prior-row baseline 11346 + 10 net-new: 7 model + 3 round-trip), npm run lint (flake8+ruff) clean, npm run black no-op (1163 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `ConstantReference` — ValueSpecification — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Constants/__init__.py
  - [x] Step 1 — Sync members & description from spec — Table 5.117 (AUTOSAR_CP_TPS_SoftwareComponentTemplate.md; page-split table: Class/Package/Note/Base header rows L12692-12696, title + attribute rows L12714-12720, p.441 via pdf_page.py); reproduction verified: R4.3.1 Table 5.132 (AUTOSAR_TPS_SoftwareComponentTemplate.md L10804) — identical Class/Package/Note/Base/attr Note text, only mult differs (R4.3.1 1 vs R23-11 0..1; R23-11 + XSD minOccurs=0 authoritative, Rule 0015); Class table: Package M2::AUTOSARTemplates::CommonStructure::Constants matches current file; class Note "Instead of defining this value inline, a constant is referenced." (verbatim confirmed by XSD 00052 group/complexType CONSTANT-REFERENCE documentation L22134/L22157); Base ARObject, ValueSpecification → most-derived base ValueSpecification — stamped `# Spec verified: R23-11` in this module; single attribute row: constant (ConstantSpecification, 0..1, ref, Note "The referenced constant.") → field name constantRef = spec base name + Kind ref→Ref (Rule 0001.5, existing name already correct); typed Optional[RefType] per stamped ref-sibling ReferenceValueSpecification (Table 5.115, same module); constr_1930 existence constraint is doc-level, not a model member; Aggregated-by list is consumer-side, not modeled. XSD 00052 ground truth: group CONSTANT-REFERENCE L22132 (single CONSTANT-REF minOccurs 0, AR:REF extension + DEST CONSTANT-SPECIFICATION--SUBTYPES-ENUM required) + complexType L22155 (AR-OBJECT + VALUE-SPECIFICATION + CONSTANT-REFERENCE groups → XML element order SHORT-LABEL, VARIATION-POINT then CONSTANT-REF); reader pre-exists (getConstantReference arxml_parser.py L7954 via readValueSpecification base helper + setConstantRef(getChildElementOptionalRefType "CONSTANT-REF"), tag dispatch getValueSpecification L8001) and writer pre-exists (setConstantReference arxml_writer.py L1812 via writeValueSpecification + setChildElementOptionalRefType("CONSTANT-REF", getConstantRef()); consumers L1855 setChildValueSpecification polymorphic dispatch + L3353/L3369 CompositeRuleBasedValueSpecification arguments/compound) — GAP FOUND: ConstantReference is missing from the writeArrayValueSpecification elements dispatch (arxml_writer.py L1788-1811) although Table 5.117 Aggregated-by includes ArrayValueSpecification.element and the reader handles CONSTANT-REFERENCE inside ELEMENTS via the generic tag dispatch → asymmetric round-trip (write hits notImplemented: raises, or logs-and-drops the element in warning mode); genuine Red candidate for Step 5; model layer is old-style: 4-col checklist without `# Spec:` line, fabricated class docstring, `__init__` docstring, `self.constantRef: RefType = None` non-Optional (Rule 0003), untyped setter param, no None-guard, no explicit chaining return → Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red) — new mirror file tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/Constants/test_ConstantReference.py (7 tests, style of prior-agent test_NumericalValueSpecification.py / stamped ref-sibling ReferenceValueSpecification): inheritance isinstance ValueSpecification, initialization (constantRef/shortLabel None), typing.get_type_hints assert Optional[RefType] on getConstantRef return + setConstantRef param, getConstantRef initial None, setConstantRef round-trip with RefType asserting identity + chaining + ref value, setConstantRef(None) no-op, shortLabel base round-trip + None no-op; Red confirmed 2 failed/5 passed (test_constant_ref_annotation_is_optional — accessors carry no annotations, get_type_hints misses; test_set_constant_ref_none — setConstantRef(None) overwrites, no None-guard); honest note: the 5 behavioral tests pass immediately — the class pre-exists with constantRef/getConstantRef/setConstantRef (orphan intake, alignment pass); the genuine Reds are the unannotated accessors (Rule 0003) and the missing None-guard (stamped-sibling convention)
  - [x] Step 3 — Implement model class (Green) — member set per Table 5.117 already complete (single attr constant modeled as constantRef; no fabrication, no missing member, dedicated field not a registry filter); base ValueSpecification (most-derived, stamped) already provides shortLabel; Green edits: `self.constantRef: RefType = None` → `self.constantRef: Optional[RefType] = None`, getConstantRef return `Optional[RefType]`, setConstantRef param `Optional[RefType]` + explicit chaining return `-> ConstantReference` (stamped-sibling convention) + None-guard; mirror test 7/7 passed (0.13s); Step-3 referenced classes: none missing — RefType (PrimitiveTypes, imported), ConstantSpecification (referenced type, exists in same module, queued separately as the next row), ValueSpecification (same module, stamped)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped ALL stale docstrings in the class (old fabricated class docstring "Represents a constant reference in AUTOSAR models. This class contains a reference to a constant…", `__init__` docstring "Initializes the ConstantReference with default values.", paraphrased getter "Gets the reference to the constant…"/setter "Sets the reference to the constant…" + Args/Returns blocks, stale inline member comment "Reference to the constant for this specification") then rewrote verbatim from Table 5.117 markdown: class docstring = class Note "Instead of defining this value inline, a constant is referenced."; `__init__` no docstring (Rule 0012.2.5.2) with inline member comment = attribute Note verbatim "The referenced constant."; getter docstring = attribute Note verbatim (markdown cell carries the closing period); guarded setter docstring = attribute Note verbatim + "A None value is a no-op and does not overwrite an existing constantRef."; shape mirrors stamped ref-sibling ReferenceValueSpecification (multi-line triple-quoted); PEP 526 annotated member directly under its note comment, blank line between `__init__` attribute block and methods; Constants model dir 72/72 after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/writer/test_writer_constant_reference.py (4 tests, style of prior-agent test_writer_numerical_value_specification.py, AUTOSAR.getInstance().new() fixture): (1) write with SHORT-LABEL "crs" + CONSTANT-REF asserting the full child tag sequence == ["SHORT-LABEL", "CONSTANT-REF"] (XSD 00052 complexType L22155 group order: AR-OBJECT + VALUE-SPECIFICATION + CONSTANT-REFERENCE) + DEST attribute + both field values; (2) empty-wrapper case — bare spec writes the tag with NO CONSTANT-REF child; (3) write → ET serialize → namespace-inject → parser.getValueSpecification re-parse asserting isinstance ConstantReference + isinstance RefType on getConstantRef() + shortLabel "crs" + ref value + DEST (field values, not len()); (4) array case — ArrayValueSpecification.addElement(ConstantReference) → writeArrayValueSpecification → re-parse asserting the element round-trips as ConstantReference with its constantRef; Red confirmed 1 failed/3 passed — test_constant_reference_round_trip_in_array raises NotImplementedError "Unsupported element type of <ConstantReference> of ArrayValueSpecification" (writer elements dispatch L1788-1811 lacks the ConstantReference branch although Table 5.117 Aggregated-by includes ArrayValueSpecification.element and the reader reads it via the generic tag dispatch → asymmetric round-trip, genuine dropped field on write); honest note: the 3 direct tests pass immediately — reader/writer coverage pre-exists and is correct on the direct path (init-value consumers), no dropped field there
  - [x] Step 6 — Update parser & writer (Green) — READER arxml_parser.py NO CHANGE needed: getConstantReference (L7954) already reads via readValueSpecification base helper → setConstantRef(getChildElementOptionalRefType(element, "CONSTANT-REF")) (mutator form, no chaining — and unlike the Numerical/Text siblings it does not re-call setShortLabel after readValueSpecification, so no idempotent-duplicate observation here), dispatched from the getValueSpecification tag dispatch (L8001-8002) and reaches ArrayValueSpecification.elements through the generic per-child dispatch in getArrayValueSpecification; WRITER arxml_writer.py ONE CHANGE: added the missing `elif isinstance(sub_element, ConstantReference): self.setConstantReference(elements_tag, sub_element)` branch to the writeArrayValueSpecification elements dispatch (between TextValueSpecification and ArrayValueSpecification branches, mirroring the setChildValueSpecification dispatch order) — closes the Step 5 Red (NotImplementedError drop); setConstantReference (L1812) otherwise unchanged — tag → writeValueSpecification base → setChildElementOptionalRefType("CONSTANT-REF", getConstantRef()) in XSD order, other consumers L1855 setChildValueSpecification + L3353/L3369 CompositeRuleBasedValueSpecification arguments/compound untouched; Rule 0013 matched-name pairs verified (readValueSpecification↔writeValueSpecification, getConstantReference↔setConstantReference element pair, setConstantRef↔getConstantRef, setShortLabel↔getShortLabel); round-trip 4/4 + model 7/7 re-run green (11 passed)
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to 6-column Rule 0002 format: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 5.117, p.441` + Columns legend + 3 rows source order (__init__ [—]/[—], getConstantRef writer [x] — writer calls getConstantRef, setConstantRef reader [x] — reader calls setConstantRef), every row release R23-11; shortLabel accessor rows stay on the stamped base ValueSpecification checklist (base owns them); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO deviations: no placeholders, no missing members (table = single attr constant, modeled as constantRef per Rule 0001.5), no naming (constant + Kind ref → Ref suffix correct), no type deviations (ref → RefType with DEST CONSTANT-SPECIFICATION--SUBTYPES-ENUM is the established mapped ref type, same as stamped siblings ReferenceValueSpecification.referenceValueRef and ConstantSpecificationMapping.appl/implConstantRef), reader+writer coverage complete (direct path pre-existing; ArrayValueSpecification.elements writer dispatch gap closed in Step 6), member order = displayed row order (single attribute); no ConstantReference entries in docs/examples/method_deviation_by_class*.md and nothing to add; observations recorded at Steps 1/5 (stamped base; array-dispatch gap — fixed, not open) are not open deviations; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 11/11 (new test_ConstantReference.py 7 + new test_writer_constant_reference.py 4), full suite 11367 passed/0 failed (prior-row baseline 11356 + 11 net-new: 7 model + 4 round-trip), npm run lint (flake8+ruff) clean, npm run black no-op (1165 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `ConstantSpecification` — ARElement — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Constants/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table 5.108 (AUTOSAR_CP_TPS_SoftwareComponentTemplate.md L12485, p.433 via pdf_page.py --table 5.108); reproductions verified: Table D.21 (AUTOSAR_CP_TPS_BSWModuleDescriptionTemplate.md L8480 title, body before title — appendix title-after-content rendering) identical Class/Package/Note/Base/Aggregated-by/attr text + R4.3.1 Table 5.116 (AUTOSAR_TPS_SoftwareComponentTemplate.md L10350 title, body L10340-10350) same Note/Base/attr text but mult differs (R4.3.1 1 vs R23-11 0..1; R23-11 + XSD minOccurs=0 authoritative, Rule 0015; R4.3.1 render also omits the Aggregated-by row); Class table: Package M2::AUTOSARTemplates::CommonStructure::Constants matches current file; class Note "Specification of a constant that can be part of a package, i.e. it can be defined stand-alone. Tags: atp.recommendedPackage=ConstantSpecifications" (verbatim confirmed by XSD 00052 group/complexType CONSTANT-SPECIFICATION documentation L22173/L22209); Base ARElement, ARObject, CollectableElement, Identifiable, MultilanguageReferrable, PackageableElement, Referrable → most-derived base ARElement (matches existing `class ConstantSpecification(ARElement)`); ARElement ⇒ ARPackage-level element: ARPackage.element Aggregated-by is consumer-side, not modeled in the class, and the infrastructure all pre-exists — ARPackage.createConstantSpecification (ARPackage.py L849-854, IsElementExists-guarded + getElement return, same shape as stamped sibling createConstantSpecificationMappingSet L758) + getConstantSpecifications (L1741, registry-filter convention), parser readARPackageElements tag dispatch L13763-13765 (CONSTANT-SPECIFICATION → parent.createConstantSpecification → readConstantSpecification) and writer isinstance branch L13575-13576 → writeConstantSpecification; single attribute row: valueSpec (ValueSpecification, 0..1, aggr, Note "Specification of an expression leading to a value for this constant. Stereotypes: atpSplitable Tags: atp.Splitkey=valueSpec" — 5-column page-split render, Note in the 5th cell; XSD VALUE-SPEC documentation L22180-22181 carries the long form "Specification of an expression leading to a value for this constant. This aggregation contains a variation point although it is not variant. Therefore, this variation point shall not exist in models. See constr_2638."; NOTE corrected during Step 4 — initially recorded the markdown as Note-less, the full-width row re-check found the Note cell, markdown verbatim per Rule 0015 is authoritative); constr_1917 existence constraint is doc-level, not a model member; its `value` aggregates the just-synced value-spec choice — XSD 00052 group CONSTANT-SPECIFICATION L22169 (single VALUE-SPEC minOccurs 0, 12-way choice: APPLICATION-ASSOC-MAP/APPLICATION-RULE-BASED/APPLICATION-VALUE-SPECIFICATION, ARRAY/COMPOSITE-RULE-BASED/CONSTANT-REFERENCE/NOT-AVAILABLE/NUMERICAL-RULE-BASED/NUMERICAL/RECORD/REFERENCE/TEXT-VALUE-SPECIFICATION — the dispatched siblings) + complexType L22203 (group sequence AR-OBJECT, REFERRABLE, MULTILANGUAGE-REFERRABLE, IDENTIFIABLE, COLLECTABLE-ELEMENT, PACKAGEABLE-ELEMENT, AR-ELEMENT, CONSTANT-SPECIFICATION → XML element order: Identifiable base groups then VALUE-SPEC last; writer already matches: CONSTANT-SPECIFICATION tag → writeIdentifiable → conditional VALUE-SPEC); reader pre-exists (readConstantSpecification arxml_parser.py L8023: readIdentifiable + findall("VALUE-SPEC/*") → setValueSpec via the polymorphic getValueSpecification dispatch; even covered by tests/test_armodel/parser/test_arxml_parser_handlers.py test_readConstantSpecification_nested_composite_rule_based L1257 and round-tripped by integration fixtures SoftwareComponents.arxml CONSTANT-SPECIFICATION) and writer pre-exists (writeConstantSpecification arxml_writer.py L3404); deviation tracker review COMPLETE: NO `ConstantSpecification` section exists in EITHER method_deviation_by_class.md or _v2.md — the row-note matches are sibling classes (md L2076 `ConstantSpecificationMapping` missing appl/implConstantRef rows — that class is since synced & stamped in this module; v2 L344 constantValueMapping → ConstantSpecificationMappingSet type deviation — different class); nothing to reconcile for THIS class; model layer is old-style: 4-col checklist without `# Spec:` line, fabricated class docstring ("Represents a constant specification in AUTOSAR models…"), `__init__` docstring, `self.valueSpec: ValueSpecification = None` non-Optional (Rule 0003), untyped accessor hints, setter docstring claims "Only sets the value if it is not None." but code overwrites unconditionally (no None-guard) → Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red) — new mirror file tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/Constants/test_ConstantSpecification.py (6 tests, style of prior-agent test_TextValueSpecification.py + ARElement constructor shape of test_ConstantSpecificationMappingSet.py): inheritance isinstance ARElement, initialization (getShortName "MyConstant" + getValueSpec None), typing.get_type_hints assert Optional[ValueSpecification] on getValueSpec return + setValueSpec param, getValueSpec initial None, setValueSpec round-trip with a concrete NumericalValueSpecification(ARNumerical("3.14")) asserting identity + chaining + numeric value, setValueSpec(None) no-op; Red confirmed 2 failed/4 passed (test_value_spec_annotation_is_optional — accessors carry no annotations, get_type_hints misses; test_set_value_spec_none — setValueSpec(None) overwrites, no None-guard despite the stale docstring claiming one); honest note: the 4 behavioral tests pass immediately — the class pre-exists with valueSpec/getValueSpec/setValueSpec (orphan intake, alignment pass); the genuine Reds are the unannotated accessors (Rule 0003) and the missing None-guard (stamped-sibling convention)
  - [x] Step 3 — Implement model class (Green) — member set per Table 5.108 already complete (single attr valueSpec modeled as-is; no fabrication, no missing member, dedicated field not a registry filter); base ARElement (most-derived, matches existing class header) already provides the Identifiable machinery via (parent, short_name) constructor; Green edits: `self.valueSpec: ValueSpecification = None` → `self.valueSpec: Optional[ValueSpecification] = None`, getValueSpec return `Optional[ValueSpecification]`, setValueSpec param `Optional[ValueSpecification]` + explicit chaining return `-> ConstantSpecification` (stamped-sibling convention) + None-guard (closing the docstring-vs-code mismatch found at Step 1); mirror test 6/6 passed, Constants model dir 78/78 (0.20s); Step-3 referenced classes: none missing — ValueSpecification (same module, abstract base of the just-synced dispatched siblings), ARElement (ARPackage.py, stamped base chain), ARNumerical/NumericalValueSpecification (test-only, imported)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped ALL stale docstrings in the class (old fabricated class docstring "Represents a constant specification in AUTOSAR models. This class contains a value specification for defining constants in AUTOSAR systems.", `__init__` docstring "Initializes the ConstantSpecification with a parent and short name." + Args block, paraphrased getter "Gets the value specification for this constant."/setter "Sets the value specification for this constant. Only sets the value if it is not None." + Args/Returns blocks, stale inline member comment "Value specification for this constant") then rewrote verbatim from Table 5.108 markdown: class docstring = class Note "Specification of a constant that can be part of a package, i.e. it can be defined stand-alone. Tags: atp.recommendedPackage=ConstantSpecifications"; `__init__` no docstring (Rule 0012.2.5.2) with inline member comment = attribute Note verbatim "Specification of an expression leading to a value for this constant. Stereotypes: atpSplitable Tags: atp.Splitkey=valueSpec" (5-column page-split render, Note in the 5th cell — full-width row re-check during this step corrected the Step-1 draft that had missed the Note cell; markdown verbatim per Rule 0015, XSD documentation L22180-22181 carries the long form); getter docstring = attribute Note verbatim (+ closing period per stamped-sibling inline shape); guarded setter docstring = attribute Note verbatim + "A None value is a no-op and does not overwrite an existing valueSpec."; shape mirrors prior-agent siblings (multi-line triple-quoted); PEP 526 annotated member directly under its note comment, blank line between `__init__` attribute block and methods; Constants model dir 78/78 + CommonStructure_init 98/98 after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/writer/test_writer_constant_specification.py (3 tests, style of prior-agent test_writer_numerical_value_specification.py + document-level pattern of test_invalidation_policy.py TestInvalidationPolicyRoundTrip, AUTOSAR.getInstance() + clear() + setARRelease("R23-11") fixture): (1) write via ARPackage-factory-created spec asserting the full child tag sequence == ["SHORT-NAME", "VALUE-SPEC"] (XSD 00052 complexType L22203 group order: base Identifiable groups then CONSTANT-SPECIFICATION group last → VALUE-SPEC) + SHORT-NAME text + VALUE-SPEC/NUMERICAL-VALUE-SPECIFICATION child present; (2) empty-wrapper case — bare spec writes the tag with NO VALUE-SPEC child; (3) FULL document round-trip writer.save → ARXMLParser().load asserting via getARPackages()[0].getConstantSpecifications() (field values, not len()): isinstance ConstantSpecification, shortName "MyConstant", getValueSpec() isinstance NumericalValueSpecification with value 3.14; honest note: 3/3 passed immediately (0.21s) — reader/writer coverage pre-exists and is correct; the round-trip exercises the real ARElement machinery end-to-end (writer writeARPackageElements isinstance branch L13575, parser readARPackageElements tag dispatch L13763 → createConstantSpecification factory, readConstantSpecification → polymorphic getValueSpecification dispatch); no genuine Red found (no dropped field); existing coverage also verified: tests/test_armodel/writer/test_writer_arpackage_dispatch.py parametrizes ("ConstantSpecification", "CONSTANT-SPECIFICATION") and parser test_arxml_parser_handlers.py test_readConstantSpecification_nested_composite_rule_based covers the reader
  - [x] Step 6 — Update parser & writer (Green) — NO CHANGE needed: READER arxml_parser.py readConstantSpecification (L8023-8027) already reads via readIdentifiable base helper (single call, no double-read) + `for child_element in self.findall(element, "VALUE-SPEC/*"): spec.setValueSpec(self.getValueSpecification(child_element, self.getTagName(child_element)))` — mutator form, no chaining, only ever called with a non-None constructed ValueSpecification so the Step-3 None-guard is a no-op on this path; dispatched from the readARPackageElements tag dispatch (L13763-13765: CONSTANT-SPECIFICATION → parent.createConstantSpecification factory → readConstantSpecification) and the polymorphic getValueSpecification dispatch (L7986) resolves all 12 XSD choice members incl. the just-synced NUMERICAL/TEXT-VALUE-SPECIFICATION, CONSTANT-REFERENCE, ARRAY/RECORD siblings; WRITER arxml_writer.py writeConstantSpecification (L3404-3409) already writes tag → writeIdentifiable base → None-checked setChildValueSpecification("VALUE-SPEC", getValueSpec()) (polymorphic, XSD order: base groups then VALUE-SPEC last), consumed from the writeARPackageElements isinstance branch (L13575-13576); Rule 0013 matched-name pairs verified (readConstantSpecification↔writeConstantSpecification element pair, readValueSpecification↔writeValueSpecification base helpers, setValueSpec↔getValueSpec, readIdentifiable↔writeIdentifiable; no cross pairs); round-trip 3/3 + model 6/6 re-run green (9 passed)
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to 6-column Rule 0002 format: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 5.108, p.433` + Columns legend + 3 rows source order (__init__ [—]/[—], getValueSpec writer [x] — writer L3408-3409 calls getValueSpec, setValueSpec reader [x] — reader L8027 calls setValueSpec), every row release R23-11; Identifiable base accessor rows (shortName/uuid/desc/category/…) stay on the stamped base-class checklists (ARElement/Identifiable own them); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO deviations: no placeholders, no missing members (table = single attr valueSpec, all modeled; ARElement base machinery inherited, not flattened), no naming (valueSpec is an aggr role name — verbatim as spec'd; ref/tref Kind-suffix and `*`-pluralization rules n/a), no type deviations (valueSpec → ValueSpecification exactly as the Type column), reader+writer coverage complete (direct path pre-existing, verified end-to-end at Steps 5/6 incl. full-document ARElement dispatch), member order = displayed row order (single attribute); tracker reconciliation COMPLETE: NO `ConstantSpecification` section exists in either docs/examples/method_deviation_by_class.md or method_deviation_by_class_v2.md (Step 1 review found the row-note matches were sibling classes — ConstantSpecificationMapping md L2076, ConstantSpecificationMappingSet v2 L344) and nothing to add; observations recorded at Steps 1/4 (R4.3.1 mult 1 vs 0..1 — Rule 0015 resolved; markdown Note-cell initially misread, corrected) are not open deviations; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 9/9 (new test_ConstantSpecification.py 6 + new test_writer_constant_specification.py 3), full suite 11376 passed/0 failed (prior-row baseline 11367 + 9 net-new: 6 model + 3 round-trip), npm run lint (flake8+ruff) clean, npm run black no-op (1167 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `DataFilterTypeEnum` — AREnum — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Filter.py
  - note: deviation-tracked in method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table 4.76 (AUTOSAR_CP_TPS_SoftwareComponentTemplate.md L5441-5452, p.183 via pdf_page.py --table 4.76, SWCT line); reproductions verified identical: Table 6.64 (AUTOSAR_CP_TPS_SystemTemplate.md L10523 title; header rows + `always` literal rendered BEFORE the title L10513-10519 — appendix title-after-content page-split) + R4.3.1 Table 4.68 (AUTOSAR_TPS_SoftwareComponentTemplate.md L4273, `always` row before title L4271, same split; older tag `atp.EnumerationValue=N` vs R23-11 `atp.EnumerationLiteralIndex=N`) + R4.3.1 Table 6.64 (AUTOSAR_TPS_SystemTemplate.md L6279, same split + EnumerationValue tags); Class table: Package M2::AUTOSARTemplates::CommonStructure::Filter matches current file; class Note "This enum specifies the supported DataFilterTypes." (verbatim confirmed by XSD 00052 complexType DATA-FILTER-TYPE-ENUM documentation L133017); Aggregated by DataFilter.dataFilterType (consumer-side, not modeled); 8 literals in displayed order (markdown line-wrap artifacts "maskedNewDiffers MaskedOld"/"maskedNewDiffers X"/"maskedNewEquals X" resolved via XSD mmt.qualifiedName): always (idx 0, "No filtering is performed so that the message always passes.") — MISSING from existing class, maskedNewDiffersMaskedOld (1), maskedNewDiffersX (2), maskedNewEqualsX (3), never (4), newIsOutside (5), newIsWithin (6), oneEveryN (7); XSD 00052 ground truth: simpleType DATA-FILTER-TYPE-ENUM--SIMPLE L133027-L133102 — wire values UPPERCASE ALWAYS / MASKED-NEW-DIFFERS-MASKED-OLD / MASKED-NEW-DIFFERS-X / MASKED-NEW-EQUALS-X / NEVER / NEW-IS-OUTSIDE / NEW-IS-WITHIN / ONE-EVERY-N, NO atp:Status="removed" literals in R23-11 (full range checked) so Rule 0015 exclusion n/a; integration fixture CanSystem.arxml carries `<DATA-FILTER-TYPE>ALWAYS</DATA-FILTER-TYPE>` proving the uppercase wire form is what round-trips; GAPS in existing class: missing `always` literal, all 7 wire values camelCase (stale, mismatch XSD + fixture), fabricated class docstring, `__init__` docstring, 4-col checklist without `# Spec:` line/release column; consumer safety: no src consumers of DataFilterTypeEnum outside Filter.py (hw_lin_flexray writer test uses bare ARLiteral("maskedNewDiffersX"), unaffected); convention reference: stamped sibling FrameEnum (OasisExchangeTable.py, R23-11) — UPPERCASE literal values, UPPER_SNAKE member names, verbatim per-literal description comments incl. Tags, "(no methods)" checklist form; tracker review: v2 L1904 entry is membership in "## Appendix: classes without a spec attribute table" (L1807) — an inventory of classes whose spec tables are literal tables (stamped GraphicFitEnum remains listed), NOT a deviation row; nothing to reconcile for THIS class; model layer alignment pass → Steps 2–7
  - [x] Step 2 — Write model class unit test (Red) — extended the existing mirror file tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/test_Filter.py TestDataFilterTypeEnum (4 tests, style of stamped sibling test_Figure.py TestGraphicFitEnum): initialization (instantiability + isinstance), test_spec_literals (dict of 8 {CONST: XSD wire value} per Table 4.76 + set(getEnumValues()) equality), test_validate_enum_value (XSD wire forms True; stale pre-sync camelCase forms + UNKNOWN False — R23-11 XSD carries NO atp:Status="removed" literals so Rule 0015 exclusion n/a, the camelCase pins are the honest invalid-form stand-in), test_set_value_with_member (DataFilterTypeEnum().setValue(DataFilterTypeEnum.NEVER) → getValue "NEVER" per AREnum instantiability guidance); Red confirmed 3 failed/1 passed (test_spec_literals AttributeError ALWAYS + camelCase mismatches, test_validate_enum_value, test_set_value_with_member 'never' != 'NEVER'); honest note: test_initialization passes immediately — instantiability pre-exists (orphan intake, alignment pass); the genuine Reds are the missing ALWAYS literal and the 7 stale camelCase wire values; the stale test_enum_values/test_enum_values_list pins of the old camelCase values were REPLACED by the spec-based tests (they mirrored the fabricated contract)
  - [x] Step 3 — Implement model class (Green) — member set per Table 4.76 completed: added the MISSING `always` literal (idx 0, XSD wire "ALWAYS") and converted all 7 stale camelCase wire values to the XSD 00052 UPPERCASE wire forms (MASKED-NEW-DIFFERS-MASKED-OLD / MASKED-NEW-DIFFERS-X / MASKED-NEW-EQUALS-X / NEVER / NEW-IS-OUTSIDE / NEW-IS-WITHIN / ONE-EVERY-N), `__init__` value list rebuilt in spec displayed order idx 0-7 (closes the Red: fixture CanSystem.arxml `ALWAYS` wire form now constructible); UPPER_SNAKE member names per stamped sibling FrameEnum convention (member names derive from the spec literal names via XSD mmt.qualifiedName — maskedNewDiffersMaskedOld etc.); no accessors added (AREnum → literals, not accessors); dedicated value list, not a registry filter; mirror test 4/4 passed, test_Filter.py 27/27; Step-3 referenced classes: none missing — AREnum base (PrimitiveTypes, stamped `# Spec verified: R23-11` at the class level per GlobalConstraints/CalibrationParameter precedent)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped ALL stale docstrings in the enum class (fabricated class docstring "Enumeration for data filter types in AUTOSAR models. Defines various filtering strategies for data processing in AUTOSAR systems.", `__init__` docstring "Initializes the DataFilterTypeEnum with all possible values.", the 7 stale fabricated per-literal comments "Filter condition: …") then rewrote verbatim from Table 4.76 markdown: class docstring = class Note "This enum specifies the supported DataFilterTypes."; `__init__` NO docstring (Rule 0012.2.5.2, stamped FrameEnum/ResolutionPolicyEnum shape); per-literal inline comment = literal Description verbatim incl. the " Tags: atp.EnumerationLiteralIndex=N" suffix (8 comments, idx 0-7, single-line form per stamped long-description precedent up to 336 chars); blank line between every literal block; ctor arg converted list → tuple per stamped enum convention; mirror test 27/27 after rewrite
  - [x] Step 5 — N/A — standalone AREnum, no own XML element; serialized on DataFilter.type (next queue row) and round-tripped there
  - [x] Step 6 — N/A — standalone AREnum (see Step 5)
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to the 6-column Rule 0002 enum "(no methods)" form: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 4.76, p.183` + Columns legend + `(no methods) — enum value form serialized on DataFilter.dataFilterType (DATA-FILTER-TYPE child element)` + single `__init__` row all-[x] with reader/writer `[—]`, release R23-11; `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO open deviations: no placeholders, no missing members remaining (the Table 4.76 8-literal set is fully modeled after Step 3 — the pre-existing missing `always` literal and stale camelCase wire values were intake gaps FIXED in Step 3, not open deviations), no naming deviations (UPPER_SNAKE member names per stamped FrameEnum convention), no type deviations (AREnum literal values = XSD 00052 wire values verbatim), reader/writer n/a for a standalone enum (value form serialized on DataFilter.dataFilterType — next queue row owns that coverage; no checklist reader/writer rows to fill); removed-literal disposition: R23-11 XSD carries NO atp:Status="removed" literals for DATA-FILTER-TYPE-ENUM (L133027-133102 fully checked) so Rule 0015 modeling-exclusion did not apply; tracker reconciliation COMPLETE: the single v2 L1904 `DataFilterTypeEnum` entry is membership in "## Appendix: classes without a spec attribute table" (L1807) — an inventory of classes whose spec tables are literal tables, not a deviation row (stamped GraphicFitEnum remains listed there, so synced classes stay listed), NO DataFilterTypeEnum section exists in either method_deviation_by_class.md or _v2.md and nothing to add; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target test file 27/27 (test_Filter.py: new 4-test TestDataFilterTypeEnum + pre-existing TestDataFilter 23), full suite 11377 passed/0 failed (prior-row baseline 11376 + 1 net-new: enum section 3 → 4 tests), npm run lint (flake8+ruff) clean, npm run black no-op (1167 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `DataFilter` — ARObject — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Filter.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - after `DataFilterTypeEnum`
  - [x] Step 1 — Sync members & description from spec — Table 4.75 (AUTOSAR_CP_TPS_SoftwareComponentTemplate.md L5411-5426, p.182 via pdf_page.py --table 4.75, SWCT line); reproductions verified identical: Table 6.63 (AUTOSAR_CP_TPS_SystemTemplate.md — Class/Package/Note/Base header L10492-10496 + attribute rows L10498-10505 rendered before title L10507, appendix title-after-content page-split) + R4.3.1 Table 4.67 (AUTOSAR_TPS_SoftwareComponentTemplate.md — body L4248-4260, title L4262 same split; identical Class/Package/Note/Base/attr text; R4.3.1 render omits the Mult./Kind/Aggregated-by rows); Class table: Package M2::AUTOSARTemplates::CommonStructure::Filter matches current file; class Note "Base class for data filters. The type of the filter is specified in attribute dataFilterType. Some of the filter types require additional arguments which are specified as attributes of this class." (verbatim confirmed by XSD 00052 complexType DATA-FILTER documentation L27074-27078); Base ARObject — row header verified both ways: table Base row = ARObject, and XSD complexType DATA-FILTER sequence = AR-OBJECT group (empty, L4891-4900) + DATA-FILTER group only — no MULTILANGUAGE-REFERRABLE/IDENTIFIABLE groups, so DataFilter is a bare ARObject subclass, matching the existing `class DataFilter(ARObject)`; 7 attribute rows in displayed order: dataFilterType (DataFilterTypeEnum, 0..1, attr, "This attribute specifies the type of the filter."), mask (UnlimitedInteger, 0..1, attr, "Mask for old and new value."), max (UnlimitedInteger, 0..1, attr, "Value to specify the upper boundary"), min (UnlimitedInteger, 0..1, attr, "Value to specify the lower boundary"), offset (PositiveInteger, 0..1, attr, "Specifies the initial number of messages to occur before the first message is passed"), period (PositiveInteger, 0..1, attr, "Specifies number of messages to occur before the message is passed again"), x (UnlimitedInteger, 0..1, attr, "Value to compare with") — no content-union attributes beyond the table; field-to-spec cross-check both directions: existing class has exactly these 7 fields in displayed order with correct member types (dataFilterType → the just-synced DataFilterTypeEnum), no extra and no missing members; constr_1890 (dataFilterType existence at RTE generation) is doc-level, not a model member; Aggregated-by (7 consumers) is consumer-side, not modeled. XSD 00052 ground truth: group DATA-FILTER L27022 (7 elements, all minOccurs 0, in order DATA-FILTER-TYPE, MASK, MAX, MIN, OFFSET, PERIOD, X) + complexType L27074 (AR-OBJECT + DATA-FILTER groups → the 7 elements are the full XML element content, no leading SHORT-LABEL); consumer call sites: parser getDataFilter arxml_parser.py L12898 (dispatched from L6801 NonqueuedReceiverComSpec FILTER, L6953 NonqueuedSenderComSpec DATA-FILTER, L12883 SignalServiceTranslationElementProps FILTER, L12914 TransmissionModeCondition), writer setDataFilter arxml_writer.py L13285 (from L1575 DATA-FILTER, L1878 FILTER, L12627 FILTER, L13297 TransmissionModeConditions); GAPS found in both layers: reader reads only DATA-FILTER-TYPE (as plain ARLiteral — spec type is DataFilterTypeEnum; established enum-element convention `Enum().setValue(literal.getText())` exists e.g. EventOccurrenceKindEnum L3461) + MASK/X (as plain Integer — spec type UnlimitedInteger, the same type-gap shape the TextValueSpecification row fixed for VerbatimString), and drops MAX/MIN/OFFSET/PERIOD entirely; writer writes only DATA-FILTER-TYPE/MASK/X and drops MAX/MIN/OFFSET/PERIOD — asymmetric with Table 4.75 in both directions → genuine Red expected at Step 5 (4 of 7 fields dropped on round-trip); model layer old-style: all 7 fields non-Optional `T = None` (Rule 0003), accessors carry no type hints, checklist 5-column without the release column → Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red) — extended the existing mirror file tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/test_Filter.py TestDataFilter (2 new tests, style of the prior-row TestDataFilterTypeEnum additions): test_inheritance_is_arobject (isinstance ARObject per the Table 4.75 Base row) and test_accessor_annotations (typing.get_type_hints over all 7 field pairs: getter return == Optional[spec type] — DataFilterTypeEnum for dataFilterType, UnlimitedInteger for mask/max/min/x, PositiveInteger for offset/period; setter value param == Optional[spec type]; setter return == DataFilter chaining per stamped-sibling convention e.g. CoreTopology.getBaudrate); Red confirmed 1 failed/28 passed (test_accessor_annotations KeyError 'return' — accessors carry no annotations at all); honest note: the 28 passing tests (23 pre-existing TestDataFilter behavioral tests + 4 prior-row TestDataFilterTypeEnum tests + the new inheritance test) pass immediately — the class pre-exists with the full 7-field set, chaining setters and None-guards (orphan intake, alignment pass); the genuine Red is the unannotated accessors (Rule 0003)
  - [x] Step 3 — Implement model class (Green) — member set per Table 4.75 already complete (7 attrs, displayed order, no fabrication, no missing member, dedicated fields not registry filters); base ARObject (most-derived per table + XSD) unchanged; Green edits: added `from typing import Optional` + `from __future__ import annotations` (stamped-sibling module convention — Constants/__init__.py L8, TimingCondition.py L1 — required because `-> DataFilter` self-chaining returns are forward references; without PEP 563 the class body NameErrors at import); all 7 fields re-annotated `Optional[T]` (dataFilterType Optional[DataFilterTypeEnum], mask/max/min/x Optional[UnlimitedInteger], offset/period Optional[PositiveInteger]); all 7 getters `-> Optional[T]`, all 7 setters `(value: Optional[T]) -> DataFilter` (chaining + None-guards pre-existed); enum untouched (prior row's member type); mirror test 29/29 passed (0.18s); Step-3 referenced classes: none missing — DataFilterTypeEnum (same file, prior row), UnlimitedInteger/PositiveInteger (PrimitiveTypes, imported), ARObject (base, imported)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — performed the Rule 0012.2.3 wipe mechanically (script removed the class docstring, all 14 method docstrings and all 7 inline `__init__` member comments from the DataFilter class only; first wipe attempt had a state-machine bug that swallowed the class body — the file was fully rewritten with the enum section restored byte-identical to HEAD, verified by `git diff` showing no hunks in the enum region) then rewrote verbatim from the Table 4.75 markdown: class docstring = class Note "Base class for data filters. The type of the filter is specified in attribute dataFilterType. Some of the filter types require additional arguments which are specified as attributes of this class."; `__init__` no docstring (Rule 0012.2.5.2) with the 7 inline member comments = the 7 attribute Notes verbatim (period exactly as rendered: dataFilterType/mask carry trailing periods, max/min/offset/period/x do not); getter docstrings = attribute Notes verbatim; guarded setter docstrings = attribute Notes verbatim + "A None value is a no-op and does not overwrite an existing <field>."; post-rewrite `git diff` vs HEAD shows ONLY the import additions and Optional/chaining annotations — every docstring and member comment is byte-identical to the intake text, i.e. the orphan intake already carried the spec Notes verbatim (verified against the markdown at Step 1, both directions), so the wipe+rewrite is a verified no-op rather than an assumed one; mirror test 29/29 after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/writer/test_writer_data_filter.py (3 tests, style of the prior-row test_writer_numerical_value_specification.py, AUTOSAR.getInstance().new() fixture, `_full_filter()` builder setting all 7 fields with spec types): (1) test_write_data_filter — write asserting the full child tag sequence == ["DATA-FILTER-TYPE", "MASK", "MAX", "MIN", "OFFSET", "PERIOD", "X"] (XSD 00052 complexType L27074 group order: empty AR-OBJECT group + DATA-FILTER group) + all 7 child texts; (2) test_write_data_filter_empty — bare filter writes the tag with no children; (3) test_data_filter_round_trip — write → ET serialize → namespace-inject → parser.getDataFilter re-parse asserting field values + spec-type fidelity: isinstance DataFilter, getDataFilterType() isinstance DataFilterTypeEnum with value "NEVER" (XSD wire form), getMask/getMax/getMin/getX isinstance UnlimitedInteger with values, getOffset/getPeriod isinstance PositiveInteger with values; Red confirmed 2 failed/1 passed — test_write_data_filter got ['DATA-FILTER-TYPE','MASK','X'] (writer drops MAX/MIN/OFFSET/PERIOD, Table 4.75 asymmetry) and test_data_filter_round_trip failed on `isinstance(reloaded.getDataFilterType(), DataFilterTypeEnum)` (reader getChildElementOptionalLiteral returns plain ARLiteral — spec-type gap, the same Red shape the TextValueSpecification row fixed for VerbatimString); honest note: test_write_data_filter_empty passes immediately (empty-wrapper coverage pre-exists); a first test draft passed `reparsed[0]` to getDataFilter — wrong calling convention (getDataFilter(element, key) searches for `key` UNDER element, unlike the getValueSpecification dispatch that receives the spec element itself); fixed to pass the parent before judging the Red
  - [x] Step 6 — Update parser & writer (Green) — READER arxml_parser.py getDataFilter (L12898): rebuilt to read all 7 Table 4.75 attributes in XSD order — DATA-FILTER-TYPE now via the established enum-element convention (`literal = getChildElementOptionalLiteral(...)` then `setDataFilterType(DataFilterTypeEnum().setValue(literal.getText()))`, EventOccurrenceKindEnum pattern L3461 — closes the ARLiteral→DataFilterTypeEnum spec-type gap), MASK/MAX/MIN/X via a NEW abstract helper `getChildElementOptionalUnlimitedInteger` (abstract_arxml_parser.py, mirrors getChildElementOptionalPositiveInteger shape incl. the text-None guard; UnlimitedInteger added to its PrimitiveTypes import), OFFSET/PERIOD via the existing `getChildElementOptionalPositiveInteger` (exact spec type); import line L116 extended to `DataFilter, DataFilterTypeEnum`; mutator form, no chaining; WRITER arxml_writer.py setDataFilter (L13285): added the three missing MAX/MIN branches and the OFFSET/PERIOD branches — full element order now DATA-FILTER-TYPE, MASK, MAX, MIN, OFFSET, PERIOD, X exactly per XSD 00052 (MASK/X via setChildElementOptionalIntegerValue — Integer param accepting the UnlimitedInteger subclass, established convention; OFFSET/PERIOD via setChildElementOptionalPositiveInteger); all 4 consumer call sites (parser L6801/L6953/L12883/L12914, writer L1575/L1878/L12627/L13297) flow through the two touched helpers, no per-consumer changes needed; Rule 0013 matched-name pairs verified (getDataFilter↔setDataFilter element pair, getChildElementOptionalUnlimitedInteger↔setChildElementOptionalIntegerValue, getChildElementOptionalPositiveInteger↔setChildElementOptionalPositiveInteger, setDataFilterType↔getDataFilterType, no cross pairs); closes the Step-5 Red; round-trip 3/3 + model 29/29 + full parser/writer dirs 5140 + integration 11 re-run green
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 4.75, p.182` + Columns legend + 15 rows in source order (__init__ [—]/[—]; per attribute: getter row writer [x] — writer setDataFilter calls getMask/getMax/getMin/getOffset/getPeriod/getX/getDataFilterType, setter row reader [x] — reader getDataFilter calls setMask/setMax/setMin/setOffset/setPeriod/setX/setDataFilterType), every row release R23-11; `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO deviations: no placeholders, no missing members (table = 7 attrs, all modeled in displayed order), no naming deviations (field names = spec member names verbatim — dataFilterType/mask/max/min/offset/period/x; Kind is plain `attr` on every row so the ref/tref/aggr Kind-suffix rules are n/a; no `*` aggr so no pluralization), no type deviations remaining — the reader's pre-existing intake gaps (DATA-FILTER-TYPE read as plain ARLiteral; MASK/X read as plain Integer) were FIXED in Step 6 to the spec types DataFilterTypeEnum/UnlimitedInteger and are not open deviations; reader+writer coverage complete for all 7 attrs after Step 6; member order = displayed row order; reader/writer XML element order = XSD 00052 group sequence (checked independently at Step 1); tracker reconciliation COMPLETE: NO `DataFilter` section exists in either docs/examples/method_deviation_by_class.md or method_deviation_by_class_v2.md (Step 1 review found the two md `DataFilter` row matches at L1930/L2933 are consumer-side `— *(missing)*` rows owned by NonqueuedSenderComSpec.dataFilter and SignalServiceTranslationElementProps.filter — those record the CONSUMERS' uncovered aggregations, not DataFilter deviations) and nothing to add; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 32/32 (test_Filter.py 29 incl. 2 new TestDataFilter tests + new test_writer_data_filter.py 3), full suite 11382 passed/0 failed (prior-row baseline 11377 + 5 net-new: 2 model + 3 round-trip), npm run lint (flake8+ruff) clean, npm run black no-op (1168 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `Modification` — ARObject — source TBC (locate table at Step 1)
  - module: M2/MSR/AsamHdo/AdminData.py
  - [x] Step 1 — Sync members & description from spec — Table 4.18 (AUTOSAR_FO_TPS_GenericStructureTemplate.md L2241, p.86 via pdf_page.py --table 4.18, FO GST line); reproduction verified: R4.3.1 Table 4.21 (AUTOSAR_TPS_GenericStructureTemplate.md L1739) — identical Class/Package/Note/Base/attr text (R4.3.1 render omits the Aggregated-by row, "Mul." vs "Mult." header); the SystemTemplate header-cell hit at ~L3816 is a different class (SwitchStreamFilterActionDestPortModification, Table 3.93) — the FO GST numeric table is the citation; Class table: Package M2::MSR::AsamHdo::AdminData matches current file (MSR package — outside AUTOSARTemplates, AGENTS.md import-freeze rule still applies); class Note "This meta-class represents the ability to record what has changed in a document in comparison to its predecessor." (verbatim confirmed by XSD 00052 group/complexType MODIFICATION documentation L83271/L83293 — XSD renders "ability  to" with a double space, markdown single space is authoritative per Rule 0015); Base ARObject — row header verified both ways: table Base row = ARObject, XSD complexType MODIFICATION L83290 sequence = AR-OBJECT group + MODIFICATION group only → bare ARObject subclass matching the existing `class Modification(ARObject)`, no SHORT-LABEL leading element (same shape as DataFilter); Aggregated by DocRevision.modification (singular role name; consumer-side, not modeled — XSD mmt.qualifiedName="DocRevision.modification" L49518 carries the MODIFICATIONS wrapper with unbounded MODIFICATION choice, xml.roleWrapperElement=true; the model's DocRevision.modifications List field is the established consumer shape); 2 attribute rows in displayed order: change (MultiLanguageOverviewParagraph, 1, aggr, "This property denotes the one particular change which was performed on the object. Tags: xml.sequenceOffset=20"), reason (MultiLanguageOverviewParagraph, 0..1, aggr, "This property represents the rationale for the particular change. Tags: xml.sequenceOffset=30") — change mult is 1 (pureMM.minOccurs="1") but the XSD element CHANGE is minOccurs=0 → wire-optional, the model's None-guarded setter is consistent with the wire form; XSD 00052 ground truth: group MODIFICATION L83268 (CHANGE seq 20 then REASON seq 30, both MULTI-LANGUAGE-OVERVIEW-PARAGRAPH, both minOccurs 0) → XML element order CHANGE, REASON; field-to-spec cross-check both directions: existing class has exactly these 2 fields with correct types in displayed order, no extra and no missing members; reader pre-exists (readModification arxml_parser.py L1342: setChange(getMultiLanguageOverviewParagraph "CHANGE") + setReason("REASON"), dispatched from readDocRevisionModifications L1348 — MODIFICATIONS/* tag dispatch constructing Modification + addModification ← readDocRevision L1357; covered by tests/test_armodel/parser/test_arxml_parser_internals.py L117-139 + warning gap test L964) and writer pre-exists (writeModification arxml_writer.py L1467: None-guard + MODIFICATION SubElement + setMultiLanguageOverviewParagraph CHANGE/REASON, consumed from writeDocRevisionModifications L1474 — wrapper if len>0 + isinstance dispatch, the earlier recon's "notImplemented branch" is this type-safety else for non-Modification list entries, NOT a dropped Modification element ← writeDocRevision L1483; covered by tests/test_armodel/writer/test_arxml_writer.py L733-770 + test_writer_common_structure.py L350-398); no integration fixture carries DOC-REVISIONS; model layer is old-style: fabricated class docstring "Represents a modification made to a document." + stale Base/Aggregated-by header lines, 4-col checklist without `# Spec:` line/release column, `self.change/reason: MultiLanguageOverviewParagraph = None` non-Optional (Rule 0003), accessor hints non-Optional, setters lack explicit `-> Modification` chaining return → Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red) — extended the existing mirror file tests/test_armodel/models/M2/MSR/AsamHdo/test_AdminData.py TestModification (4 new tests, style of the prior-row TestDataFilter additions to an existing file): test_modification_inheritance_is_arobject (isinstance ARObject per the Table 4.18 Base row, verified against the XSD complexType MODIFICATION = AR-OBJECT group only), test_modification_getter_annotations (typing.get_type_hints over getChange/getReason return == Optional[MultiLanguageOverviewParagraph]), test_modification_setter_annotations (setChange/setReason value param == Optional[MultiLanguageOverviewParagraph] + return == Modification chaining), test_modification_set_none_noop (setChange(None)/setReason(None) leave existing values); Red confirmed 2 failed/11 passed (getter test: getChange return `MultiLanguageOverviewParagraph` ≠ Optional[...]; setter test: setChange value param non-Optional — the chaining-return KeyError branch never reached because the param assert fired first); honest note: the inheritance/initialization/setters-getters/None-noop tests pass immediately — the class pre-exists with change/reason accessors AND None-guards (orphan intake, alignment pass); the genuine Reds are the non-Optional hints (Rule 0003) and the missing explicit chaining return
  - [x] Step 3 — Implement model class (Green) — member set per Table 4.18 already complete (2 attrs change/reason, displayed order, correct types, no fabrication, no missing member, dedicated fields not registry filters); base ARObject (most-derived per table + XSD complexType MODIFICATION = AR-OBJECT group only) unchanged; Green edits: `self.change: MultiLanguageOverviewParagraph = None` → `self.change: Optional[MultiLanguageOverviewParagraph] = None`, same for reason; getChange/getReason return `Optional[MultiLanguageOverviewParagraph]`; setChange/setReason param `Optional[MultiLanguageOverviewParagraph]` + explicit chaining return `-> "Modification"` (string form per the file's DocRevision convention); None-guards pre-existed (change mult is 1 / pureMM.minOccurs=1 but the XSD element CHANGE is minOccurs=0 → wire-optional, guard consistent with the wire form); mirror test 13/13 passed (0.17s); Step-3 referenced classes: none missing — MultiLanguageOverviewParagraph (TextModel, imported), ARObject (base, imported)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped ALL stale docstrings in the Modification class (old fabricated class docstring "Represents a modification made to a document." + stale "Base: ARObject / Aggregated by: DocRevision.modifications" header lines — the stale header even misrendered the consumer role as plural `modifications`, the spec/Table 4.18 role is singular `modification`) then rewrote verbatim from the Table 4.18 markdown: class docstring = class Note "This meta-class represents the ability to record what has changed in a document in comparison to its predecessor."; `__init__` no docstring (Rule 0012.2.5.2) with the 2 inline member comments = the 2 attribute Notes verbatim ("This property denotes the one particular change which was performed on the object. Tags: xml.sequenceOffset=20" / "This property represents the rationale for the particular change. Tags: xml.sequenceOffset=30"); getter docstrings = attribute Notes verbatim; guarded setter docstrings = attribute Notes verbatim + "A None value is a no-op and does not overwrite an existing change." / "...existing reason."; PEP 526 annotated members directly under their note comments, blank line between every `__init__` attribute block; the old 4-col checklist block kept as-is for the Step 7 rewrite; AsamHdo model dir 137/137 after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/writer/test_writer_modification.py (4 tests, style of the prior-row test_writer_data_filter.py, AUTOSAR.getInstance().new() fixture): (1) test_write_modification — full modification asserting the child tag sequence == ["CHANGE", "REASON"] (XSD 00052 group MODIFICATION L83268 order: CHANGE seq 20 then REASON seq 30) + L-2 texts; (2) test_write_modification_empty — bare spec writes the MODIFICATION tag with NO children (setMultiLanguageOverviewParagraph None-guards); (3) test_write_doc_revision_empty_modifications — DocRevision with an empty modifications list writes NO MODIFICATIONS wrapper (consumer-side wrapper guard); (4) test_modification_round_trip — write via the REAL consumer chain (DocRevision.addModification → writeDocRevisionModifications wrapper + isinstance dispatch) → ET serialize → namespace-inject → parser.readDocRevisionModifications re-parse asserting len == 1 + isinstance Modification + isinstance MultiLanguageOverviewParagraph on both getChange()/getReason() + L-2 texts (field values, not len()); honest note: 4/4 passed immediately (0.18s) — reader/writer coverage pre-exists and is correct for this class (readModification L1342 reads CHANGE + REASON, writeModification L1467 writes both in XSD order); no genuine Red found (no dropped field)
  - [x] Step 6 — Update parser & writer (Green) — NO CHANGE needed: READER arxml_parser.py readModification (L1342-1344) already reads via setChange(getMultiLanguageOverviewParagraph(element, "CHANGE")) + setReason("REASON") — mutator form, no chaining, dispatched from readDocRevisionModifications (L1348-1355: MODIFICATIONS/* tag dispatch constructing Modification + addModification, notImplemented else for unknown tags) ← readDocRevision (L1357); WRITER arxml_writer.py writeModification (L1467-1473) already writes None-guard → MODIFICATION SubElement → setMultiLanguageOverviewParagraph CHANGE/REASON in XSD order, consumed from writeDocRevisionModifications (L1474-1485: wrapper if len>0 + isinstance dispatch; the earlier recon's "notImplemented branch" at L1488 is this type-safety else for non-Modification list entries, NOT a dropped Modification element) ← writeDocRevision (L1483-1496); Rule 0013 matched-name pairs verified (readModification↔writeModification element pair, setChange↔getChange, setReason↔getReason, no cross pairs); round-trip 4/4 + model 13/13 + related parser/writer dirs re-run green (269 passed combined)
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 4.18, p.86` + Columns legend + 5 rows in source order (__init__ [—]/[—]; getChange writer [x] — writer L1470 calls getChange, setChange reader [x] — reader L1343 calls setChange, getReason writer [x] — writer L1471 calls getReason, setReason reader [x] — reader L1344 calls setReason), every row release R23-11; `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO deviations: no placeholders, no missing members (table = 2 attrs change/reason, all modeled in displayed order), no naming deviations (field names = spec member names verbatim — change/reason; Kind is plain `aggr` on both rows so the ref/tref Kind-suffix rules are n/a; no `*` aggr so no pluralization), no type deviations (MultiLanguageOverviewParagraph → exactly the spec Type column "MultiLanguageOverview Paragraph" de-hyphenated), reader+writer coverage complete (pre-existing, verified end-to-end at Steps 5/6 incl. the DocRevision MODIFICATIONS wrapper chain), member order = displayed row order; reader/writer XML element order = XSD 00052 group sequence CHANGE(20), REASON(30) (checked independently at Step 1); tracker reconciliation COMPLETE: the single md L3483 `Modification` match is `keySlotAllowedModification` → `CryptoKeySlotAllowedModification` — a consumer-side `— *(missing)*` row owned by a Crypto consumer class, NOT a Modification deviation; NO `Modification` section exists in either docs/examples/method_deviation_by_class.md or method_deviation_by_class_v2.md and nothing to add; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 17/17 (test_AdminData.py 13 incl. 4 new TestModification tests + new test_writer_modification.py 4), full suite 11390 passed/0 failed (prior-row baseline 11382 + 8 net-new: 4 model + 4 round-trip), npm run lint (flake8+ruff) clean, npm run black no-op (1169 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `ScaleConstrValidityEnum` — AREnum — R4.3.1 AUTOSAR_TPS_SoftwareComponentTemplate.md Table 5.95
  - module: M2/MSR/AsamHdo/Constraints/GlobalConstraints.py
  - note: R4.3.1 fallback verified per Rule 0016.3 (Step 1, 2026-09-24) — token `ScaleConstrValidityEnum` appears in NO `autosar/R23-11/markdown/` file (caption + tolerant header-cell grep across all R23-11 markdown); the only R23-11 mention is the attribute-type cell `ScaleConstrValidity Enum` on the ScaleConstr.validity row (AUTOSAR_CP_TPS_SoftwareComponentTemplate.md:29382, itself atp.Status=obsolete). Spec = R4.3.1 AUTOSAR_TPS_SoftwareComponentTemplate.md Table 5.95 (L9747), p.417. Literals cross-checked both directions: notAvailable/notDefined/notValid/valid with atp.EnumerationValue=0-3 — existing wire values already match, none missing, none extra. XSD R23-11 AUTOSAR_00052.xsd L142112 and R4.3.1 AUTOSAR_00044.xsd L101206 agree (R23-11 tags atp.EnumerationLiteralIndex vs R4.3.1 atp.EnumerationValue); no atp.Status="removed" literals in either release. Consumer: ScaleConstr.validity (XML attribute VALIDITY; parser arxml_parser.py:8052, writer arxml_writer.py:3438). Old intake claim "XSD-only via docs/requirements/xsd/AUTOSAR_00046.xsd" superseded.
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red)
  - note: Step 2 honest note — 1 of 5 new tests Red (spec-note docstring test; the intake docstring carried the ScaleConstr.validity attribute note instead of the enum's Table 5.95 Note); the 4 literal/validate/instantiation tests were already green (orphan intake values correct).
  - [x] Step 3 — Implement model class (Green — no code change: literals already match Table 5.95 both directions; the Red test is the docstring contract, closed in Step 4)
  - [x] Step 4 — Sync docstrings (wipe + rewrite)
  - [x] Step 5 — Write reader/writer round-trip test (N/A: standalone AREnum — no own XML element; serialized as the VALIDITY attribute on consumer ScaleConstr, round-tripped there)
  - [x] Step 6 — Update parser & writer (N/A: standalone AREnum — consumer coverage already exists: parser arxml_parser.py:8052 readScaleConstr element.get("VALIDITY"), writer arxml_writer.py:3438 child_element.set("VALIDITY", getValidity().getValue()))
  - [x] Step 7 — Update checklist comment
  - [x] Step 8 — Deviations
  - note: Step 8 — no deviations: 4/4 spec literals modeled verbatim both directions, no placeholders/fabrication, no missing referenced classes (AREnum base), no atp.Status="removed" literals in either XSD, no tracker entries for this class. Marker withheld only per batch instruction (9b deferred), not for a deviation.
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: TestScaleConstrValidityEnum 5/5 green (file 25/25); full suite `uv run python scripts/run_tests.py --no-coverage` 11395 passed / 0 failed (baseline 11390 + 5 new); `npm run lint` clean; `npm run black` + `black-check` clean (1169 files unchanged); 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `UnitGroup` — ARElement — source TBC (locate table at Step 1)
  - module: M2/MSR/AsamHdo/Units.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table 5.81 (AUTOSAR_CP_TPS_SoftwareComponentTemplate.md L11654, p.402 via pdf_page.py --table 5.81, SWCT line); reproductions verified identical: Table F.44 (AUTOSAR_CP_TPS_ECUConfiguration.md — body L9325-9332 rendered before caption L9334, appendix title-after-content page-split; all cells IDENTICAL to 5.81) + R4.3.1 Table 5.89 (AUTOSAR_TPS_SoftwareComponentTemplate.md L9570, same Class/Package/Note/Base/attr text, older render omits the Aggregated-by row, "Mul." header); Class table: Package M2::MSR::AsamHdo::Units matches current file; class Note "This meta-class represents the ability to specify a logical grouping of units.The category denotes the unit system that the referenced units are associated to. In this way, e.g. country-specific unit systems (CATEGORY=\"COUNTRY\") can be defined as well as specific unit systems for certain application domains. In the same way a group of equivalent units, can be defined which are used in different countries, by setting CATEGORY=\"EQUIV_UNITS\". KmPerHour and MilesPerHour could such be combined to one group named \"vehicle_speed\". The unit MeterPerSec would not belong to this group because it is normally not used for vehicle speed. But all of the mentioned units could be combined to one group named \"speed\". Note that the UnitGroup does not ensure the physical compliance of the units. This is maintained by the physical dimension. Tags: atp.recommendedPackage=UnitGroups" (verbatim confirmed by XSD 00052 group/complexType UNIT-GROUP documentation L128052/L128087 — XSD renders "units.The" exactly as the markdown); Base ARElement, ARObject, CollectableElement, Identifiable, MultilanguageReferrable, Packageable Element (markdown line-wrap = PackageableElement), Referrable → most-derived base ARElement (matches existing `class UnitGroup(ARElement)`); Aggregated by ARPackage.element (consumer-side, not modeled); single attribute row: unit (Unit, *, ref, "This represents one particular unit in the UnitGroup. Tags: xml.sequenceOffset=20"); XSD 00052 ground truth: group UNIT-GROUP L128050 (single element UNIT-REFS minOccurs 0 wrapper, unbounded choice of UNIT-REF = AR:REF extension + DEST UNIT--SUBTYPES-ENUM required; wrapper tags mmt.qualifiedName="UnitGroup.unit", pureMM minOccurs 0/maxOccurs -1, xml.sequenceOffset=20) + complexType L128085 (group sequence AR-OBJECT, REFERRABLE, MULTILANGUAGE-REFERRABLE, IDENTIFIABLE, COLLECTABLE-ELEMENT, PACKAGEABLE-ELEMENT, AR-ELEMENT, UNIT-GROUP → XML element order: Identifiable base groups then UNIT-REFS last); ARElement infrastructure disposition: parser readARPackageElements has a UNIT tag branch (arxml_parser.py L13781 → createUnit → readUnit L8085) but NO UNIT-GROUP branch and no readUnitGroup; writer writeARPackageElements has a Unit isinstance branch (arxml_writer.py L13588 → writeUnit L3478) but NO UnitGroup branch and no writeUnitGroup; ARPackage has createUnit (L891)/getUnits (L1749, registry-filter convention) but NO createUnitGroup/getUnitGroups — all four pieces genuinely missing, genuine Red expected at Step 5 (round-trip drops the element entirely); model layer is old-style: 4-col checklist without `# Spec:` line, fabricated class docstring ("Represents a group of units in the AUTOSAR model…" with a stale Attributes: block), field-to-spec cross-check FAILS on type+naming: `self.units: List[Unit]` but spec Kind is `ref` → must be `List[RefType]` (XSD UNIT-REF with DEST; established sibling convention Unit.physicalDimensionRef Optional[RefType], SwComponentType.unitGroupRefs List[RefType]) and Rule 0001.5 Kind-suffix naming unit + ref→Ref + `*`→plural = `unitRefs` (getUnitRefs/addUnitRef, not getUnits/addUnit); no src or test consumers of UnitGroup.getUnits/addUnit (grep clean — the only UnitGroup hits are SwComponentType's own unitGroupRefs) so the reshape is safe; tracker review: the single md L1446 `unitGroupRefs` match is a consumer-side row owned by SwComponentType ("UnitGroup not yet synced (stub); RefType placeholder") — the RefType type IS the final correct ref convention, the row belongs to SwComponentType, nothing to reconcile for THIS class; Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red) — extended the existing mirror file tests/test_armodel/models/M2/MSR/AsamHdo/test_Units.py with a new TestUnitGroup class (5 tests, style of the sibling TestUnit in the same file, ARPackage(None, "parent_test") parent fixture): test_unit_group_inheritance_is_arelement (isinstance ARElement per the Table 5.81 Base row), test_unit_group_initialization (getUnitRefs() == []), test_unit_group_ref_annotations (typing.get_type_hints: getUnitRefs return == List[RefType], addUnitRef value param == RefType, addUnitRef return == UnitGroup chaining), test_unit_group_add_unit_ref (round-trip: two RefTypes appended in order, chaining return is self), test_unit_group_add_unit_ref_none_noop (addUnitRef(None) leaves existing refs); Red confirmed 4 failed/16 passed (AttributeError: no attribute 'getUnitRefs'/'addUnitRef' — the spec-shaped accessors do not exist); honest note: the inheritance test passes immediately — the class pre-exists as an ARElement subclass (orphan intake, alignment pass); the genuine Reds are the missing Rule 0001.5 Kind-suffix accessors unitRefs/getUnitRefs/addUnitRef over List[RefType] (the intake has the wrong-shaped units/getUnits/addUnit over List[Unit])
  - [x] Step 3 — Implement model class (Green) — member set per Table 5.81 rebuilt to the spec shape: `self.units: List[Unit] = []`/getUnits/addUnit → `self.unitRefs: List[RefType] = []`/getUnitRefs/addUnitRef — Kind `ref` → RefType (XSD UNIT-REF = AR:REF extension + DEST UNIT--SUBTYPES-ENUM), Rule 0001.5 Kind-suffix naming unit + ref→Ref + `*`→plural = unitRefs; dedicated typed list field, not a registry filter; accessors annotated (getUnitRefs `-> List[RefType]`, addUnitRef `value: Optional[RefType]`) with explicit chaining return `-> "UnitGroup"` (string form per this file's Unit/PhysicalDimension convention) + None-guard; addUnitRef param typed Optional[RefType] per the stamped sibling convention (SpecialData.addSdxfRef, SwComponentType.addUnitGroupRef) — the Step-2 annotation test adjusted in the same action to pin `typing.Optional[RefType]` accordingly; base ARElement (most-derived, matches existing class header) already provides the Identifiable machinery via (parent, short_name) constructor; mirror test 20/20 passed (0.16s); Step-3 referenced classes: none missing — Unit (same file, stamped), RefType (PrimitiveTypes, imported), ARElement (ARPackage.py, stamped base chain)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped ALL stale docstrings in the UnitGroup class (old fabricated class docstring "Represents a group of units in the AUTOSAR model. This class is used to group related units together for organizational purposes." + stale Attributes: block listing the since-renamed units field) then rewrote verbatim from the Table 5.81 markdown: class docstring = class Note "This meta-class represents the ability to specify a logical grouping of units.The category denotes the unit system that the referenced units are associated to. In this way, e.g. country-specific unit systems (CATEGORY=\"COUNTRY\") can be defined as well as specific unit systems for certain application domains. In the same way a group of equivalent units, can be defined which are used in different countries, by setting CATEGORY=\"EQUIV_UNITS\". KmPerHour and MilesPerHour could such be combined to one group named \"vehicle_speed\". The unit MeterPerSec would not belong to this group because it is normally not used for vehicle speed. But all of the mentioned units could be combined to one group named \"speed\". Note that the UnitGroup does not ensure the physical compliance of the units. This is maintained by the physical dimension. Tags: atp.recommendedPackage=UnitGroups" ("units.The" no-space join verbatim from markdown, confirmed against XSD documentation); `__init__` no docstring (Rule 0012.2.5.2) with inline member comment = attribute Note verbatim "This represents one particular unit in the UnitGroup. Tags: xml.sequenceOffset=20" (no closing period, markdown cell carries none); getter docstring = attribute Note verbatim (+ closing period per stamped-sibling inline shape, ConstantSpecification.valueSpec precedent); adder docstring = attribute Note verbatim + closing period + "A None value is a no-op and is not appended." (SpecialData add* convention); shape mirrors stamped siblings (multi-line triple-quoted); PEP 526 annotated member directly under its note comment, blank line between the `__init__` attribute block and methods; mirror test 20/20 + AsamHdo model dir 147/147 after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/writer/test_writer_unit_group.py (3 tests, style of the prior-agent test_writer_constant_specification.py ARElement document-level pattern, AUTOSAR.getInstance().new() autouse fixture, _make_document with clear() + setARRelease("R23-11")): (1) test_write_unit_group — ARPackage.createUnitGroup + two UNIT-REFs, write asserting the full child tag sequence == ["SHORT-NAME", "UNIT-REFS"] (XSD 00052 complexType L128085 group order: Identifiable base groups then UNIT-GROUP group last) + UNIT-REFS children == ["UNIT-REF", "UNIT-REF"] + texts + DEST attributes; (2) test_write_unit_group_empty — bare group writes the tag with NO UNIT-REFS child; (3) test_unit_group_round_trip — full document writer.save → ARXMLParser().load asserting via getARPackages()[0].getUnitGroups() (field values, not len()): isinstance UnitGroup, shortName "vehicle_speed", refs values + DEST; Red confirmed 3 failed/0 passed (AttributeError: 'ARPackage' object has no attribute 'createUnitGroup' — the factory, both dispatch branches and both read/write helpers do not exist, the Step-1 prediction); honest note: fully genuine Red — reader/writer coverage for UnitGroup was entirely absent (round-trip dropped the element both directions)
  - [x] Step 6 — Update parser & writer (Green) — four pieces added, mirroring the Unit sibling shapes: (1) ARPackage.createUnitGroup factory (ARPackage.py, directly after createUnit — IsElementExists-guarded + getElement return, same shape as createUnit/createConstantSpecification) and getUnitGroups getter (registry-filter convention, directly after getUnits) + UnitGroup added to the file's Units import block; (2) READER arxml_parser.py: new readUnitGroup (directly after readUnit — readIdentifiable base helper, single call, no double-read + `for ref in self.getChildElementRefTypeList(element, "UNIT-REFS/UNIT-REF"): unit_group.addUnitRef(ref)` — the same wrapper-path pattern readSwComponentTypeUnitGroups L7093 uses for UNIT-GROUP-REFS/UNIT-GROUP-REF; mutator form, no chaining) dispatched from the readARPackageElements tag dispatch (UNIT-GROUP branch directly after UNIT) + UnitGroup added to the Units import line; (3) WRITER arxml_writer.py: new writeUnitGroup (directly after writeUnit — tag → writeIdentifiable base → conditional UNIT-REFS wrapper (len>0, writeSwComponentTypeUnitGroups convention) → setChildElementOptionalRefType("UNIT-REF", ref) per ref; XSD 00052 order: Identifiable base groups then UNIT-REFS last) consumed from the writeARPackageElements isinstance branch (UnitGroup directly after Unit — sibling classes, no subclass shadowing) + UnitGroup added to the Units import line (imports extended in place, none re-sorted); Rule 0013 matched-name pairs verified (readUnitGroup↔writeUnitGroup element pair, addUnitRef↔getUnitRefs, readIdentifiable↔writeIdentifiable, getChildElementRefTypeList↔setChildElementOptionalRefType; no cross pairs); round-trip 3/3 + model 20/20 + full parser/writer dirs 5147 passed re-run green
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 5.81, p.402` + Columns legend + 3 rows in source order (__init__ [—]/[—], getUnitRefs writer [x] — writer writeUnitGroup calls getUnitRefs, addUnitRef reader [x] — reader readUnitGroup calls addUnitRef), every row release R23-11; Identifiable base accessor rows (shortName/uuid/desc/category/…) stay on the stamped base-class checklists (ARElement/Identifiable own them); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO open deviations: no placeholders, no missing members (table = single attr unit modeled as unitRefs; the Step-3 reshape units/getUnits/addUnit → unitRefs/getUnitRefs/addUnitRef FIXED the intake's wrong Kind/type/naming — an intake gap closed, not an open deviation), no naming deviations (unit + Kind ref→Ref + `*`→plural = unitRefs per Rule 0001.5, matches the XSD UNIT-REF wire form), no type deviations (ref → List[RefType] with DEST UNIT--SUBTYPES-ENUM is the established mapped ref type, same as stamped siblings Unit.physicalDimensionRef and SwComponentType.unitGroupRefs), reader+writer coverage complete for the only attribute after Step 6 (were entirely absent before — closed, not open), member order = displayed row order (single attribute); reader/writer XML element order = XSD 00052 group sequence (Identifiable base groups then UNIT-REFS last, checked independently at Step 1); tracker reconciliation COMPLETE: NO `UnitGroup` section exists in either docs/examples/method_deviation_by_class.md or method_deviation_by_class_v2.md and nothing to add — the single md L1446 `unitGroupRefs` match is a consumer-side row owned by SwComponentType ("UnitGroup not yet synced (stub); RefType placeholder"), reviewed at Step 1: the RefType type IS the final correct ref convention so nothing there is invalidated by this sync, and the row belongs to SwComponentType (prior-row precedent: consumer-side rows are not this class's deviations); `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 23/23 (test_Units.py 20 incl. new 5-test TestUnitGroup + new test_writer_unit_group.py 3), full suite `uv run python scripts/run_tests.py --no-coverage` 11403 passed/0 failed (prior-row baseline 11395 + 8 net-new: 5 model + 3 round-trip), npm run lint (flake8+ruff) clean, npm run black no-op (1170 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `SwImplPolicyEnum` — AREnum — source TBC (locate table at Step 1)
  - module: M2/MSR/DataDictionary/DataDefProperties.py
  - [x] Step 1 — Sync members & description from spec
  - note: Step 1 (2026-09-24) — spec = R23-11 AUTOSAR_CP_TPS_SoftwareComponentTemplate.md Table 5.45 (L9786), p.336 via pdf_page.py --table 5.45 (SWCT line); reproductions verified identical: Table D.70 (AUTOSAR_CP_TPS_BSWModuleDescriptionTemplate.md caption L9361) + Table F.125 (AUTOSAR_CP_TPS_SystemTemplate.md caption L75938) — both appendices render the body BEFORE the caption (title-after-content page-split across an image break: Enumeration/Package/Note/Aggregated-by/Literal/const rows before, fixed/measurementPoint/queued/standard rows after); all cells IDENTICAL to 5.45. Enumeration table: Package M2::MSR::DataDictionary::DataDefProperties matches current file; class Note "Specifies the implementation strategy with respect to consistency mechanisms of variables."; NO Base row (Enumeration header); Aggregated by BswInternalTriggeringPoint.swImplPolicy, InternalTriggeringPoint.swImplPolicy, SwDataDefProps.swImplPolicy, Trigger.swImplPolicy — all four consumers exist in src (BswBehavior.py L2324, SwcInternalBehavior/Trigger.py L34, DataDefProperties.py L442 same file = primary, CommonStructure/TriggerDeclaration.py L34); 5 literals const/fixed/measurementPoint/queued/standard with atp.EnumerationLiteralIndex=0-4 — intake values match all 5 both directions. XSD R23-11 AUTOSAR_00052.xsd complexType SW-IMPL-POLICY-ENUM L143663 / --SIMPLE L143675: wire values CONST/FIXED/MEASUREMENT-POINT/QUEUED/STANDARD; NO atp.Status="removed" literals (all 5 live — nothing to leave unvalidated); wire-form mapping is consumer-side via SW_IMPL_POLICY_XML_MAP (parser L1171, writer L1043). STALE PRE-EXISTING `# Spec verified: R23-11` stamp found on the class (entered at refactor commit 762fadbef, before this batch's 9b regime) over an old 5-column checklist and a docstring that does NOT carry the spec Note (paraphrased literal descriptions instead) — stamp removed this pass per batch instruction (re-stamp deferred to batch confirmation); genuine Red expected at Step 2 (spec-Note docstring test)
  - [x] Step 2 — Write model class unit test (Red)
  - note: Step 2 honest note (2026-09-24) — extended the existing TestSwImplPolicyEnum in tests/test_armodel/models/M2/MSR/DataDictionary/test_DataDefProperties.py with 4 spec-based tests (style of the prior-row TestScaleConstrValidityEnum, `from inspect import cleandoc` import added): test_sw_impl_policy_enum_has_spec_note (class docstring == Table 5.45 Note verbatim), test_sw_impl_policy_enum_spec_literals (5 literals, values + getEnumValues() order), test_sw_impl_policy_enum_validate_enum_value (accepts the 5 model literals; rejects uppercase wire forms CONST/MEASUREMENT-POINT and unknown — docstring documents that R23-11 XSD L143675 carries NO atp.Status="removed" literals so no legacy forms are valid), test_sw_impl_policy_enum_set_value_with_member (instantiable + setValue(MEASUREMENT_POINT)); Red confirmed 1 failed/5 passed — the only Red is the spec-Note docstring test (intake docstring carries paraphrased literal descriptions, not the Note); the 4 literal/validate/set-value tests were already green (orphan intake values correct)
  - [x] Step 3 — Implement model class (Green — no code change: literals already match Table 5.45 both directions and in displayed order; base AREnum correct; the Red test is the docstring contract, closed in Step 4)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped the intake class docstring entirely (paraphrased literal-description list, no spec Note) and rewrote verbatim from the Table 5.45 markdown: class docstring = class Note "Specifies the implementation strategy with respect to consistency mechanisms of variables." (single-sentence triple-quoted shape of the stamped sibling SwCalibrationAccessEnum in the same file); added per-literal inline comments above each constant = the literal Description cell verbatim incl. trailing "Tags: atp.EnumerationLiteralIndex=N" (markdown cells carry no closing period — none added), mirroring the SwCalibrationAccessEnum per-literal comment convention; `__init__` has no docstring; PEP 526-style constants unchanged; test file 69/69 green after rewrite (the Step-2 Red closed)
  - [x] Step 5 — Write reader/writer round-trip test (N/A: standalone AREnum — no own XML element; serialized as the SW-IMPL-POLICY element value on consumers and round-tripped there. Consumers per the Aggregated-by row, all four pre-existing in src: SwDataDefProps.swImplPolicy (primary, DataDefProperties.py L442 — parser arxml_parser.py L6440 setSwImplPolicy(getChildElementOptionalLiteral(..., "SW-IMPL-POLICY")), writer arxml_writer.py L3042 setChildElementOptionalLiteral("SW-IMPL-POLICY", getSwImplPolicy()), round-trip asserted by field value in tests/test_armodel/writer/test_writer_sw_data_def_props.py L52/L121 "standard"), InternalTriggeringPoint.swImplPolicy (SwcInternalBehavior/Trigger.py L34), Trigger.swImplPolicy (CommonStructure/TriggerDeclaration.py L34), BswInternalTriggeringPoint.swImplPolicy (BswBehavior.py L2324) — the three trigger consumers via SW_IMPL_POLICY_XML_MAP (parser L4281-4291, writer L7289-7293), covered by tests/test_armodel/parser/test_trigger.py, test_trigger_interface.py, tests/test_armodel/writer/test_trigger.py, test_trigger_interface.py, test_writer_bsw_module.py)
  - [x] Step 6 — Update parser & writer (N/A: standalone AREnum — consumer coverage already exists and was verified at Step 5; Rule 0013 wire mapping SW_IMPL_POLICY_XML_MAP identical in parser L1171 and writer L1043 (const→CONST, fixed→FIXED, measurementPoint→MEASUREMENT-POINT, queued→QUEUED, standard→STANDARD = XSD 00052 --SIMPLE L143675 literal values); no changes made)
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 5.45, p.336` + Columns legend gained the release column + `# (no methods)` note naming the serialization consumer (SwDataDefProps.swImplPolicy, SW-IMPL-POLICY element; all four consumers listed) + single `__init__` row `[x] impl [x] docstring [x] test [—] reader [—] writer` with release R23-11 (enum value form — reader/writer coverage is the consumer-side SW-IMPL-POLICY element, per the AREnum adaptation); the STALE PRE-EXISTING `# Spec verified: R23-11` marker line REMOVED (pre-batch 762fadbef-era stamp — the marker is the output of 9b, deferred to batch confirmation)
  - [x] Step 8 — Deviations — NO open deviations: 5/5 spec literals modeled verbatim both directions in displayed order (const/fixed/measurementPoint/queued/standard), no placeholders/fabrication, no missing referenced classes (base AREnum), no naming deviations (member constants = spec literal names upper-snake, values = Literal column verbatim), no type deviations, no atp.Status="removed" literals in R23-11 XSD L143675-143716 so nothing unmodeled on that front; member order = displayed row order; tracker reconciliation COMPLETE: NO `SwImplPolicyEnum` section exists in either docs/examples/method_deviation_by_class.md or method_deviation_by_class_v2.md and nothing to add; the removed stale stamp is a batch-protocol action (withhold until 9b batch confirmation), not a spec deviation; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 69/69 (test_DataDefProperties.py, TestSwImplPolicyEnum 6/6 = 2 pre-existing + 4 new), full suite `uv run python scripts/run_tests.py --no-coverage` 11407 passed / 0 failed (prior-row baseline 11403 + 4 net-new), `npm run lint` (flake8+ruff) clean, `npm run black` no-op (1170 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `SwSystemconst` — Identifiable — source TBC (locate table at Step 1)
  - module: M2/MSR/DataDictionary/SystemConstant.py
  - [x] Step 1 — Sync members & description from spec
  - note: Step 1 (2026-09-24) — spec = R23-11 AUTOSAR_CP_TPS_SoftwareComponentTemplate.md Table 5.120 (title L12889, body L12891-12898), p.448 via pdf_page.py --table 5.120 (SWCT line); reproductions verified byte-identical across all 7 cells: Table D.71 (AUTOSAR_CP_TPS_BSWModuleDescriptionTemplate.md — body L9363-9368 rendered BEFORE caption L9372, appendix title-after-content page-split) + Table F.41 (AUTOSAR_CP_TPS_ECUConfiguration.md L9285, body after caption); Class table: Package M2::MSR::DataDictionary::SystemConstant matches current file; class Note "This element defines a system constant which serves an input to select a particular variation point. In particular a system constant serves as an operand of the binding function (swSyscond) in a Variation point. Note that the binding process can only happen if a value was assigned to to the referenced system constants. Tags: atp.recommendedPackage=SwSystemconsts" ("assigned to to" duplication verbatim in the markdown; XSD 00052 renders "happen  if" with a double space — markdown authoritative per Rule 0015, confirmed by group/complexType SW-SYSTEMCONST documentation L116311/L116331); Base ARElement, ARObject, AtpDefinition, CollectableElement, Identifiable, MultilanguageReferrable, PackageableElement, Referrable — XSD complexType SW-SYSTEMCONST L116331 group sequence AR-OBJECT, REFERRABLE, MULTILANGUAGE-REFERRABLE, IDENTIFIABLE, COLLECTABLE-ELEMENT, PACKAGEABLE-ELEMENT, AR-ELEMENT, ATP-DEFINITION, SW-SYSTEMCONST → strict most-derived is AtpDefinition, but py-armodel's stamped AtpDefinition (RolesAndRights.py, `(Referrable, ABC)`) is inherited by NO concrete class and would drop the Identifiable machinery the XSD IDENTIFIABLE group requires; the established codebase convention for this exact group-chain shape (…AR-ELEMENT, ATP-DEFINITION + own group) is ARElement — PostBuildVariantCriterion(ARElement) precedent with an identical chain → BASE CORRECTION: existing `class SwSystemconst(Identifiable)` must become `class SwSystemconst(ARElement)` (ARElement provides the same Identifiable machinery + PackageableElement, empty XML delta; queue-row header "Identifiable" was the source-TBC placeholder, table authoritative); Aggregated by ARPackage.element — ARElement ⇒ ARPackage-level, infrastructure all pre-exists: ARPackage.createSwSystemConst (ARPackage.py L1240-1245, IsElementExists-guarded + getElement return; pre-existing factory name with capital C — public API kept) + getSwSystemConsts (L1860-1862, registry-filter convention), parser readARPackageElements tag dispatch L14096-14098 (SW-SYSTEMCONST → parent.createSwSystemConst → readSwSystemconst) and writer isinstance branch L13799-13800 → writeSwSystemconst; single attribute row: swDataDef Props (SwDataDefProps, 0..1, aggr, Note "This denotes the data definition properties of the system constant. This supports to express the limits and optionally a conversion within the internal to physical values by a compu method. Stereotypes: atpSplitable Tags: atp.Splitkey=swDataDefProps xml.sequenceOffset=40"); XSD 00052 ground truth: group SW-SYSTEMCONST L116311 (single SW-DATA-DEF-PROPS minOccurs 0) → XML element order: Identifiable base groups then SW-DATA-DEF-PROPS last; reader pre-exists (readSwSystemconst arxml_parser.py L11381-11384: readIdentifiable base helper, single call + setSwDataDefProps(self.getSwDataDefProps(element, "SW-DATA-DEF-PROPS")), mutator form no chaining) and writer pre-exists (writeSwSystemconst arxml_writer.py L12281-12285: SW-SYSTEMCONST SubElement → writeIdentifiable base → setSwDataDefProps("SW-DATA-DEF-PROPS", const.getSwDataDefProps()) in XSD order); existing coverage: model tests/test_armodel/models/M2/MSR/DataDictionary/test_SystemConstant.py TestSwSystemconst (3 tests), parser test_arxml_parser_snippets.py TestSwSystemconstParser (basic + SW-DATA-DEF-PROPS payload), writer test_writer_ecuc_values_variant.py TestWriterSwSystemconst + test_writer_arpackage_dispatch.py parametrized dispatch; NO integration fixture carries SW-SYSTEMCONST; relationship check: SwSystemconstValue (Group8/VariantHandling/__init__.py L178, ARObject) is a DIFFERENT class referencing SwSystemconst via swSystemconstRef — already synced, untouched; tracker review: NO `SwSystemconst` section in either method_deviation_by_class.md or _v2.md (the md hits are AttributeValueVariationPoint/ConditionByFormula/SwSystemconstValue consumer-side rows — different classes); model layer is old-style: fabricated class docstring ("Represents a software system constant…"), 4-col checklist without `# Spec:` line, base mismatch Identifiable vs ARElement, setter param non-Optional + no None-guard + untyped chaining return → Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red)
  - note: Step 2 (2026-09-24) — extended the existing mirror file tests/test_armodel/models/M2/MSR/DataDictionary/test_SystemConstant.py TestSwSystemconst (3 new tests, style of the prior-row TestDataFilter/TestModification additions): test_sw_systemconst_inheritance_is_arelement (isinstance ARElement per the Table 5.120 Base row — XSD group chain incl. AR-ELEMENT + ATP-DEFINITION, ARElement convention per PostBuildVariantCriterion), test_sw_systemconst_accessor_annotations (typing.get_type_hints: getSwDataDefProps return == Optional[SwDataDefProps], setSwDataDefProps value param == Optional[SwDataDefProps] + return == SwSystemconst chaining), test_sw_systemconst_set_sw_data_def_props_none_noop (setSwDataDefProps(None) leaves the existing value); Red confirmed 3 failed/3 passed (inheritance: Identifiable is not ARElement — the Step-1 base-correction Red; annotations: accessors carry no hints, get_type_hints misses; None-guard: setSwDataDefProps(None) overwrites); honest note: the 3 pre-existing behavioral tests (initialization, get/set + chaining, inherited shortName/parent) pass immediately — the class pre-exists with swDataDefProps accessors and chaining (orphan intake, alignment pass); the genuine Reds are the base mismatch, the unannotated accessors (Rule 0003) and the missing None-guard
  - [x] Step 3 — Implement model class (Green)
  - note: Step 3 (2026-09-24) — member set per Table 5.120 already complete (single attr swDataDefProps; no fabrication, no missing member, dedicated field not a registry filter); Green edits: BASE CORRECTION `class SwSystemconst(Identifiable)` → `class SwSystemconst(ARElement)` (import swapped to `from ...GeneralTemplateClasses.ARPackage import ARElement` — the established leaf-module pattern, PhysicalDimension/BaseType/SwRecordLayout precedent; leaf-first import order re-verified OK after the edit; ARPackage.py's late bottom-block `from ...SystemConstant import SwSystemconst` unchanged), setter param `SwDataDefProps` → `Optional[SwDataDefProps]` + explicit chaining return `-> "SwSystemconst"` (string form — no `from __future__ import annotations` in this leaf file) + None-guard; getter return already `Optional[SwDataDefProps]`, field annotation already PEP 526 `Optional[SwDataDefProps]`; AtpDefinition disposition: strict most-derived per the XSD chain but py-armodel's stamped AtpDefinition is `(Referrable, ABC)` inherited by no concrete class — ARElement is the established concrete base for the identical group-chain shape (PostBuildVariantCriterion precedent), Identifiable machinery preserved through ARElement(PackageableElement); mirror test 6/6 passed (0.09s), DataDictionary model dir + test_ARPackage.py 213 passed; Step-3 referenced classes: none missing — SwDataDefProps (DataDefProperties, imported), ARElement (ARPackage.py, stamped base chain)
  - [x] Step 4 — Sync docstrings (wipe + rewrite)
  - note: Step 4 (2026-09-24) — wiped ALL stale docstrings in the class (old fabricated class docstring "Represents a software system constant in the AUTOSAR model. This class is used to define system constants…" with a stale `Attributes:` block naming a non-existent `value (ValueSpecification)` member, paraphrased setter "Sets the software data definition properties…"/getter "Returns the software data definition properties…" + Args/Returns blocks) then rewrote verbatim from the Table 5.120 markdown: class docstring = class Note "This element defines a system constant which serves an input to select a particular variation point. In particular a system constant serves as an operand of the binding function (swSyscond) in a Variation point. Note that the binding process can only happen if a value was assigned to to the referenced system constants. Tags: atp.recommendedPackage=SwSystemconsts" ("assigned to to" duplication verbatim); `__init__` no docstring (Rule 0012.2.5.2) with inline member comment = attribute Note verbatim "This denotes the data definition properties of the system constant. This supports to express the limits and optionally a conversion within the internal to physical values by a compu method. Stereotypes: atpSplitable Tags: atp.Splitkey=swDataDefProps xml.sequenceOffset=40"; getter docstring = attribute Note verbatim (+ closing period per stamped-sibling inline shape, ConstantSpecification.valueSpec precedent); guarded setter docstring = attribute Note verbatim + closing period + "A None value is a no-op and does not overwrite an existing swDataDefProps."; accessor source order normalized get-before-set per the stamped Constants sibling shape (intake had set-first); shape mirrors stamped sibling ConstantSpecification (multi-line triple-quoted, PEP 526 annotated member directly under its note comment, blank line between the `__init__` attribute block and methods); mirror test 6/6 after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red)
  - note: Step 5 (2026-09-24) — new tests/test_armodel/writer/test_writer_sw_systemconst.py (3 tests, style of the prior-agent test_writer_unit_group.py ARElement document-level pattern, AUTOSAR.getInstance().new() autouse fixture, _make_document with clear() + setARRelease("R23-11")): (1) test_write_sw_systemconst — ARPackage.createSwSystemConst + SwDataDefProps payload (setBaseTypeRef), write asserting the full child tag sequence == ["SHORT-NAME", "SW-DATA-DEF-PROPS"] (XSD 00052 complexType L116331 group order: Identifiable base groups then SW-SYSTEMCONST group last → SW-DATA-DEF-PROPS last) + BASE-TYPE-REF text "/DataTypes/uint32" + DEST "SW-BASE-TYPE"; (2) test_write_sw_systemconst_empty — bare const writes the tag with NO SW-DATA-DEF-PROPS child; (3) test_sw_systemconst_round_trip — full document writer.save → ARXMLParser().load asserting via getARPackages()[0].getSwSystemConsts() (field values, not len()): isinstance SwSystemconst, shortName "MaxValue", getSwDataDefProps() not None with getBaseTypeRef() value + DEST; honest note: 3/3 passed immediately (0.15s) — reader/writer coverage pre-exists and is correct for this class (readSwSystemconst reads SHORT-NAME-identifiable + SW-DATA-DEF-PROPS, writeSwSystemconst writes both in XSD order); no genuine Red found (no dropped field); existing coverage also verified: parser test_arxml_parser_snippets.py TestSwSystemconstParser (basic + SW-DATA-DEF-PROPS payload), writer test_writer_ecuc_values_variant.py TestWriterSwSystemconst (with/without props) + test_writer_arpackage_dispatch.py parametrized dispatch
  - [x] Step 6 — Update parser & writer (Green)
  - note: Step 6 (2026-09-24) — NO CHANGE needed: READER arxml_parser.py readSwSystemconst (L11381-11384) already reads via readIdentifiable base helper (single call, no double-read) + `setSwDataDefProps(self.getSwDataDefProps(element, "SW-DATA-DEF-PROPS"))` — mutator form, no chaining; dispatched from the readARPackageElements tag dispatch (L14096-14098: SW-SYSTEMCONST → parent.createSwSystemConst factory → readSwSystemconst); WRITER arxml_writer.py writeSwSystemconst (L12281-12285) already writes tag → writeIdentifiable base → setSwDataDefProps("SW-DATA-DEF-PROPS", const.getSwDataDefProps()) in XSD order (Identifiable base groups then SW-DATA-DEF-PROPS last), consumed from the writeARPackageElements isinstance branch (L13799-13800); the Step-3 base change Identifiable → ARElement is XML-silent (ARElement adds an empty sequence; PackageableElement(CollectableElement, …) chain preserves the Identifiable machinery — isinstance Identifiable verified True post-change, so readIdentifiable/writeIdentifiable operate unchanged); Rule 0013 matched-name pairs verified (readSwSystemconst↔writeSwSystemconst element pair, readIdentifiable↔writeIdentifiable base helpers, setSwDataDefProps↔getSwDataDefProps model accessors, getSwDataDefProps(element,key)↔setSwDataDefProps(element,key,props) structure helpers; no cross pairs); round-trip 3/3 + model 6/6 + related parser/writer files 278 passed re-run green
  - [x] Step 7 — Update checklist comment
  - note: Step 7 (2026-09-24) — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 5.120, p.448` + Columns legend + 3 rows in source order (__init__ [—]/[—]; getSwDataDefProps writer [x] — writer L12285 calls getSwDataDefProps; setSwDataDefProps reader [x] — reader L11384 calls setSwDataDefProps), every row release R23-11; Identifiable base accessor rows (shortName/uuid/desc/category/…) stay on the stamped base-class checklists (ARElement/Identifiable own them); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations
  - note: Step 8 (2026-09-24) — NO deviations: no placeholders, no missing members (table = single attr swDataDefProps, all modeled), no naming deviations (field/accessors = spec member name swDataDefProps verbatim; Kind is plain `aggr` so the ref/tref Kind-suffix rules are n/a; no `*` aggr so no pluralization), no type deviations (SwDataDefProps → exactly the spec Type column, Optional per 0..1), reader+writer coverage complete (pre-existing, verified end-to-end at Steps 5/6 incl. the ARPackage-level dispatch chain), member order = displayed row order (single attribute); reader/writer XML element order = XSD 00052 group sequence (Identifiable base groups then SW-DATA-DEF-PROPS last, checked independently at Step 1); base disposition recorded: strict most-derived AtpDefinition is unmodeled-as-base per the established codebase convention (stamped AtpDefinition is `(Referrable, ABC)` inherited by no concrete class; identically-XSD-shaped PostBuildVariantCriterion is stamped `ARElement`) — convention-level, not an open deviation, Identifiable machinery verified preserved post-change; tracker reconciliation COMPLETE: NO `SwSystemconst` section exists in either docs/examples/method_deviation_by_class.md or method_deviation_by_class_v2.md (the md grep hits are AttributeValueVariationPoint/ConditionByFormula/SwSystemconstValue consumer-side rows — different, already-synced Group8 classes) and nothing to add; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 9/9 (test_SystemConstant.py 6 incl. 3 new TestSwSystemconst tests + new test_writer_sw_systemconst.py 3), full suite `uv run python scripts/run_tests.py --no-coverage` 11413 passed/0 failed (prior-row baseline 11407 + 6 net-new: 3 model + 3 round-trip), npm run lint (flake8+ruff) clean (one ruff I001 in the new test file's import block auto-fixed — test file, not an import-frozen path), npm run black no-op (1171 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `ListEnum` — AREnum — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/BlockElements/ListElements.py
  - note: deviation-tracked in method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec
  - note: Step 1 (2026-09-24) — spec = R23-11 AUTOSAR_FO_TPS_GenericStructureTemplate.md Table 9.10 (caption L7677, body L7679-7686), p.295 via pdf_page.py --table 9.10 (FO GST line); reproduction verified: R4.3.1 AUTOSAR_TPS_GenericStructureTemplate.md Table 8.11 (caption L6271, body L6273-6279) — all cells IDENTICAL (Package/Note/both Literal rows) except the older `atp.EnumerationValue=0/1` tag names vs R23-11 `atp.EnumerationLiteralIndex=0/1`; Enumeration header (no Base, no Attribute rows); Package M2::MSR::Documentation::BlockElements::ListElements matches current file; class Note "This meta-class represents the notation of the various types of lists."; Aggregated by List.type (consumer = ARList in the same module, renamed for the Python builtin clash — the queue row after next); 2 literals in displayed order: number ("This indicates that the list is an numerated list. Tags: atp.EnumerationLiteralIndex=0"), unnumber ("This indicates that it is an enumeration (bulleted list) Tags: atp.EnumerationLiteralIndex=1") — the intake's NUMBER="number"/UNNUMBER="unnumber" already match both directions and in order; XSD R23-11 AUTOSAR_00052.xsd complexType LIST-ENUM L139824 (documentation = Note verbatim) + LIST-ENUM--SIMPLE L139836: wire values NUMBER/UNNUMBER, NO atp.Status="removed" literals (both live); R4.3.1 AUTOSAR_00044.xsd agrees (L99238/L99250); consumer coverage pre-exists (reader arxml_parser.py getListElements L5726 reads the TYPE XML attribute — XSD 00052 L78477 `<xsd:attribute name="TYPE" type="AR:LIST-ENUM--SIMPLE">`, writer arxml_writer.py setListElement L2456 — consumer-side, ARList row); tracker verdict: the single v2 L1817 `ListEnum` mention sits in the "Appendix: classes without a spec attribute table" inventory — an OBSERVATION, not a deviation row (no `## ListEnum` section exists in either tracker; the appendix package path is even stale: TextModel::BlockElements vs actual BlockElements, and the class DOES have Table 9.10); orphan intake confirmed: fabricated docstring + old 4-col checklist, literals correct
  - [x] Step 2 — Write model class unit test (Red)
  - note: Step 2 honest note (2026-09-24) — extended the existing TestListEnum in tests/test_armodel/models/M2/MSR/Documentation/BlockElements/test_ListElements.py with 4 spec-based tests (style of the prior-row TestSwImplPolicyEnum, `from inspect import cleandoc` import added): test_list_enum_has_spec_note (class docstring == Table 9.10 Note verbatim), test_list_enum_spec_literals (2 literals, values + getEnumValues() order), test_list_enum_validate_enum_value (accepts number/unnumber; rejects uppercase wire forms NUMBER/UNNUMBER and unknown — XSD 00052 L139836 carries NO atp.Status="removed" literals so no legacy forms are valid), test_list_enum_set_value_with_member (instantiable + setValue(UNNUMBER) → getValue() == "unnumber"); Red confirmed 2 failed/25 passed — (1) the spec-Note docstring test (intake docstring is the fabricated paraphrase "Enumeration for list numbering types: number or unnumber."), (2) the literals-order test (intake passes a TUPLE to AREnum so getEnumValues() returns ('number', 'unnumber') ≠ list — the stamped-sibling + prior-two-rows convention is a list arg); the validate/set-value tests were already green (orphan intake literal values correct)
  - [x] Step 3 — Implement model class (Green) — member set per Table 9.10 already complete (base AREnum correct; 2 literals in displayed order, values = Literal column verbatim — no missing member, no fabrication, no registry filter); Green edits: (1) enum-values container normalized TUPLE → LIST `super().__init__([ListEnum.NUMBER, ListEnum.UNNUMBER])` per the stamped-sibling + prior-two-rows convention (SwImplPolicyEnum one-line list, ScaleConstrValidityEnum exploded list) — closes the literals-order Red; (2) `def __init__(\n self,\n ):` trailing-comma explosion normalized to `def __init__(self):` (intake noise); grep confirmed NO src consumer of getEnumValues depends on tuple-ness; the spec-Note docstring Red stays open by design, closed in Step 4 (prior-row precedent); mirror test 26/27 (only the Step-4 docstring contract open); Step-3 referenced classes: none missing — AREnum (PrimitiveTypes, stamped base)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped the intake class docstring entirely (fabricated paraphrase "Enumeration for list numbering types: number or unnumber.", no spec Note) and rewrote verbatim from the Table 9.10 markdown: class docstring = class Note "This meta-class represents the notation of the various types of lists." (single-sentence triple-quoted shape of the stamped sibling SwImplPolicyEnum); added per-literal inline comments above each constant = the literal Description cell verbatim incl. trailing "Tags: atp.EnumerationLiteralIndex=N" — "This indicates that the list is an numerated list." (sic, "an numerated" verbatim from markdown + XSD documentation L139840) and "This indicates that it is an enumeration (bulleted list)" (markdown cell carries no closing period before the Tags — none added), mirroring the SwImplPolicyEnum per-literal comment convention with blank lines between literal blocks; `__init__` has no docstring; mirror test 27/27 green (the Step-2 Red closed)
  - [x] Step 5 — Write reader/writer round-trip test (N/A: standalone AREnum — no own XML element; serialized as the TYPE XML attribute on the consuming LIST element and round-tripped there. Consumer per the Aggregated-by row: ARList.type (same module, renamed from List for the Python builtin clash; ARList is the queue row after next) — reader arxml_parser.py getListElements L5726 (`list.setType(child_element.attrib["TYPE"])`, XSD 00052 L78477 `<xsd:attribute name="TYPE" type="AR:LIST-ENUM--SIMPLE">`), writer arxml_writer.py setListElement L2456 (`child_element.attrib["TYPE"] = type`); round-trip exercised by the integration fixtures carrying `<LIST TYPE="UNNUMBER">` (e.g. AUTOSAR_MOD_AISpecification_ApplicationDataType_Blueprint.arxml L2129) in the 29-file integration suite)
  - [x] Step 6 — Update parser & writer (N/A: standalone AREnum — consumer coverage already exists and was verified at Step 5; no changes made to parser or writer for this class. The reader/writer wire handling of the TYPE attribute (raw wire string NUMBER/UNNUMBER per XSD LIST-ENUM--SIMPLE L139836) is consumer-side and belongs to the ARList queue row)
  - [x] Step 7 — Update checklist comment
  - note: Step 7 (2026-09-24) — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 9.10, p.295` + Columns legend gained the release column + `# (no methods)` note naming the serialization consumer (ARList.type, TYPE attribute; consumer: ARList) + single `__init__` row `[x] impl [x] docstring [x] test [—] reader [—] writer` with release R23-11 (enum value form — reader/writer coverage is the consumer-side TYPE attribute on the LIST element, per the AREnum adaptation); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations
  - note: Step 8 — NO deviations: 2/2 spec literals modeled verbatim both directions in displayed order (number/unnumber, atp.EnumerationLiteralIndex 0/1), no placeholders/fabrication, no missing referenced classes (base AREnum, stamped), no naming deviations (member constants NUMBER/UNNUMBER = spec literal names number/unnumber upper-snake), no type deviations, no atp.Status="removed" literals in R23-11 XSD L139836-139850 (both literals live — nothing unmodeled on that front), member order = displayed row order, no `*` aggr so no pluralization, no ref/tref Kind (Enumeration header has no Attribute rows); reader/writer = enum value form on the consumer (N/A rows, ARList owns the TYPE attribute); tracker reconciliation COMPLETE — tracker-entry VERDICT: the single `ListEnum` mention (method_deviation_by_class_v2.md L1817) is an inventory/observation note in the "Appendix: classes without a spec attribute table", NOT a real deviation row — no `## ListEnum` section exists in either method_deviation_by_class.md or _v2.md, nothing to reconcile or add (and the appendix entry itself is stale: it lists the package as TextModel::BlockElements and claims no spec table, but Table 9.10 exists); the tuple→list normalization was a stamped-convention alignment (closed at Step 3), not an open deviation; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 27/27 (test_ListElements.py, TestListEnum 5/5 = 1 pre-existing + 4 new), full suite `uv run python scripts/run_tests.py --no-coverage` 11417 passed / 0 failed (prior-row baseline 11413 + 4 net-new model tests; no reader/writer tests — Steps 5/6 N/A for a standalone AREnum), `npm run lint` (flake8+ruff) clean, `npm run black` no-op (1171 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `Item` — Paginateable — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/BlockElements/ListElements.py
  - [x] Step 1 — Sync members & description from spec — Table 9.9 (AUTOSAR_FO_TPS_GenericStructureTemplate.md L7666, p.295 via pdf_page.py --table 9.9, FO GST line; body renders directly after the title — no caption-split for this one, unlike sibling Table 9.8 whose title follows its body); reproduction verified: R4.3.1 Table 8.10 (AUTOSAR_TPS_GenericStructureTemplate.md ~L6259-6271) identical Class/Package/Note/Base/attr Note text (only `Mul.` vs `Mult.` header and semicolon tag separators — cosmetic); the batch-brief SWCT ~L2880 / SystemTemplate ~L23745 hits are `Item.itemContents` header-cell mentions in the DocumentationBlock Aggregated-by row (SWCT L28890), not full table reproductions; Class table: Package M2::MSR::Documentation::BlockElements::ListElements matches current file; class Note "This meta-class represents one particular item in a list." (verbatim confirmed by XSD 00052 group/complexType ITEM documentation L75023/L75045); Base ARObject, DocumentViewSelectable, Paginateable → most-derived base Paginateable — already stamped `# Spec verified: R23-11` (Table 9.75, p.339, PaginationAndView.py; base-stamp observation, not a blocker; DocumentViewSelectable(ARObject, ABC) is its parent); current class header `Item(Paginateable, VariationPointCapable)` — Paginateable per the table Base row, and the VariationPointCapable mixin is justified by the XSD ITEM group's VARIATION-POINT element ("This element was generated/modified due to an atpVariation stereotype. Applicable for: List.item"; mmt.qualifiedName="Item.variationPoint"), same convention as stamped siblings LabeledItem/LabeledList/DefItem/DefList; single attribute row: itemContents (DocumentationBlock, mult 1, aggr, Note "this represents the actual content of the item. It is composed of a DocumentationBlock. This way it is possible to use simple paragraphs to nested lists, formula, figures or notes. Tags: xml.roleElement=false xml.roleWrapperElement=false xml.typeElement=false xml.typeWrapperElement=false" — lowercase leading "this" per markdown, distinct from the capital-T LabeledItem.itemContents Note in Table 9.12); Aggregated by List.item is consumer-side, not modeled. XSD 00052 ground truth: group ITEM L75021 (choice minOccurs 0 → group ref DOCUMENTATION-BLOCK = the itemContents payload rendered INLINE — xml.roleElement/false means NO wrapper element; + VARIATION-POINT element minOccurs 0 xml.sequenceOffset=10000 → content BEFORE variation point) + complexType ITEM L75042 (group sequence AR-OBJECT, DOCUMENT-VIEW-SELECTABLE, PAGINATEABLE, ITEM; chapterBreak/keepWithPrevious are xml.attribute=true so Paginateable content is attribute-form); consume path: parser getListElements (arxml_parser.py L5726) reads LIST children via readPaginateable + TYPE attribute + getDocumentationBlockList(child_element, "ITEM") → list.addItem(block) and writer setListElement (arxml_writer.py L2456) writes list.getItems() via writeDocumentationBlock(child_element, "ITEM", item) — BOTH flatten ITEM↔DocumentationBlock and bypass the Item wrapper entirely (Item never instantiated by the reader, getItemContents/setItemContents unused by both layers, VARIATION-POINT dropped); that wrapper wiring is ARList-row scope (next queue row) → Pending-style note at Step 5/6, not forced here; model layer is old-style: unannotated `self.itemContents = None` (Rule 0003), untyped accessors, no None-guard, fabricated class docstring "An item within a list with content defined by itemContents.", 4-col checklist without `# Spec:` line → Steps 2–7 align
  - [x] Step 2 — Write model class unit test (Red) — extended the existing TestItem class in tests/test_armodel/models/M2/MSR/Documentation/BlockElements/test_ListElements.py (the ListEnum agent's file; spec-based style per its TestListEnum + prior-row typing/None-guard conventions; the 2 old-style tests test_item_initialization/test_item_contents_methods were REPLACED — the initialization assert is folded into the new inheritance/round-trip tests and the old contents test pinned a plain-string payload, contradicting the spec type): 6 tests — inheritance isinstance Paginateable/DocumentViewSelectable/VariationPointCapable (Table 9.9 Base row + XSD VARIATION-POINT), spec-note docstring verbatim, typing.get_type_hints assert Optional[DocumentationBlock] on getItemContents return + setItemContents param (hints resolved with an explicit globalns mapping because DocumentationBlock is a TYPE_CHECKING-only import in ListElements.py — a runtime import would be circular via TextModel.BlockElements/__init__ which imports ListElements), DocumentationBlock round-trip identity + chaining, setItemContents(None) no-op, Paginateable base accessor reachability (setBreak/getBreak with ChapterEnumBreak.BREAK); Red confirmed 3 failed/28 passed (test_item_has_spec_note — fabricated docstring; test_item_contents_annotation_is_optional — accessors carry no annotations, Rule 0003; test_set_item_contents_none_noop — setter overwrites, no None-guard); honest note: the 3 behavioral tests (inheritance, round-trip, base accessors) pass immediately — the class pre-exists with itemContents/getItemContents/setItemContents and the correct class header (orphan intake, alignment pass)
  - [x] Step 3 — Implement model class (Green) — member set per Table 9.9 already complete (single attr itemContents modeled as-is; no fabrication, no missing member, dedicated field not a registry filter); base Paginateable (most-derived per table Base row, stamped) unchanged and provides chapterBreak/keepWithPrevious via Paginateable + si/view via DocumentViewSelectable; VariationPointCapable mixin kept (XSD ITEM group carries the VARIATION-POINT element); Green edits: `self.itemContents = None` → `self.itemContents: Optional["DocumentationBlock"] = None` (quoted forward ref — DocumentationBlock stays a TYPE_CHECKING-only import, a runtime import would be circular via TextModel/BlockElements/__init__ which imports ListElements; same form as sibling LabeledItem.itemContents), getItemContents return `Optional["DocumentationBlock"]`, setItemContents param `Optional["DocumentationBlock"]` + explicit chaining return `-> Item` (PEP 563 makes the self-reference safe; stamped-sibling convention) + None-guard; test-harness note: the annotation test's module lookup switched from `import ... as` to `sys.modules[Item.__module__]` — the plain import form mis-resolves because `Documentation.BlockElements` and `Documentation.TextModel.BlockElements` share the `BlockElements` segment name; mirror test 6/6 passed, file total 30 passed/1 failed (the remaining Red is the spec-note docstring assert — Step 4 scope); Step-3 referenced classes: none missing — Paginateable/DocumentViewSelectable (PaginationAndView.py, stamped), VariationPointCapable (mixin, exists), DocumentationBlock (TextModel/BlockElements, stamped Table 9.1)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped the fabricated class docstring "An item within a list with content defined by itemContents." (the Item getters/setters carried no docstrings and `__init__` had no inline member comments — nothing else to wipe) then rewrote verbatim from the Table 9.9 markdown: class docstring = class Note "This meta-class represents one particular item in a list."; `__init__` no docstring (Rule 0012.2.5.2) with inline member comment = attribute Note verbatim "this represents the actual content of the item. It is composed of a DocumentationBlock. This way it is possible to use simple paragraphs to nested lists, formula, figures or notes. Tags: xml.roleElement=false xml.roleWrapperElement=false xml.typeElement=false xml.typeWrapperElement=false" (lowercase leading "this" per the markdown cell, Tags suffix kept verbatim per the stamped in-file convention); getter docstring = attribute Note verbatim; guarded setter docstring = attribute Note verbatim + "A None value is a no-op and does not overwrite an existing itemContents."; shape mirrors the Group9-synced sibling TextValueSpecification (single-line triple-quoted, None sentence on its own line), PEP 526 annotated member directly under its note comment, blank line between the `__init__` attribute block and the methods; mirror file 31/31 after rewrite (spec-note assert now Green)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/writer/test_writer_item.py (2 tests, house style of test_writer_documentation_block.py: file-based AUTOSAR.getInstance() clear()+setARRelease("R23-11") fixture, swc introduction carries the LIST): (1) full path — introduction DocumentationBlock → addList(ARList) → addItem(item payload DocumentationBlock with P "list item text" + a nested ARList inside the item payload pinning the Table 9.9 Note's "lists can be arbitrarily nested") → writer.save → parser.load asserting FIELD VALUES: intro.getLists()[0] present, items[0].getPs()[0].getL1s()[0].getValue() == "list item text", nested items[0] == "nested item text"; (2) empty-wrapper case — LIST with no ITEM children → getItems() == []; honest note: 2/2 passed immediately — no genuine Red found: the ITEM payload path pre-exists and round-trips correctly (parser readDocumentationBlock L6287 → getListElements L5726 → getDocumentationBlockList(element, "ITEM") → list.addItem(block); writer writeDocumentationBlock L2752 → setListElement L2456 → writeDocumentationBlock(child_element, "ITEM", item)); the Item-wrapper dispatch IS genuinely missing on both layers and was EVIDENCED by probe: a spec-faithful Item with setItemContents(DocumentationBlock) added via addItem hits writeDocumentationBlock and raises AttributeError "'Item' object has no attribute 'getMsrQueryP2'" (Item lacks the DocumentationBlock API — writeDocumentationBlock unconditionally calls the DocumentationBlock getters), and the reader never instantiates Item (getDocumentationBlockList constructs bare DocumentationBlocks), so Item.itemContents/getVariationPoint are unused by both layers; per the batch brief this wrapper wiring is ARList-row scope (getListElements/setListElement are ARList's reader/writer helpers; ARList is the next queue row) → recorded as a Pending-style note at Step 6 rather than forced here
  - [x] Step 6 — Update parser & writer (Green) — NO CHANGE needed (deliberate): reader arxml_parser.py getListElements (L5726) and writer arxml_writer.py setListElement (L2456) are ARList-owned helpers whose ITEM↔DocumentationBlock flattening is the unsynced ARList row's contract to repair (Item wrapper instantiation + itemContents indirection + VARIATION-POINT read/write — Pending note, next queue row); this row's model-side contract (getItemContents/setItemContents) is fully consistent for when that wiring lands; Rule 0013 matched-name pairs on the existing path verified (writeDocumentationBlock ↔ readDocumentationBlock base helpers, getListElements ↔ setListElement element pair, addItem ↔ getItems; no cross pairs); round-trip 2/2 + model file 31/31 re-run green (33 passed total)
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 9.9, p.295` + Columns legend + 3 rows source order (__init__ [—]/[—]; getItemContents writer `[ ]` — the writer does NOT yet call getItemContents; setItemContents reader `[ ]` — the reader does NOT yet call setItemContents), every row release R23-11; a Pending note line underneath records that ITEM-wrapper reader/writer coverage is the ARList row's scope (setListElement/getListElements currently flatten ITEM↔DocumentationBlock and never instantiate Item) — honest `[ ]` rather than a false `[x]`/`[—]` since the element round-trips today only via the flattened payload path; `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO spec deviations: no placeholders, no missing members (table = single attr itemContents, modeled verbatim), no naming (itemContents is an aggr role name — as spec'd; ref/tref/`*`-pluralization rules n/a), no type deviations (DocumentationBlock exactly as the Type column), member order = displayed row order (single attribute); base closure faithful (Paginateable most-derived + VariationPointCapable mixin per the XSD VARIATION-POINT element — no flattening); the accessor-level reader/writer `[ ]` rows are NOT class deviations but the documented Pending consume-path gap owned by the next queue row (ARList owns getListElements/setListElement; evidenced at Step 5 by the AttributeError probe) — carried as the checklist Pending note, not a tracker entry; tracker reconciliation COMPLETE: NO `Item` section exists in either docs/examples/method_deviation_by_class.md or method_deviation_by_class_v2.md and nothing to add; `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 33/33 (test_ListElements.py 31 incl. new 6-test TestItem + new test_writer_item.py 2), full suite `uv run python scripts/run_tests.py --no-coverage` 11423 passed/0 failed (prior-row baseline 11417 + 6 net-new: 4 model — 6 new TestItem tests replaced 2 old-style ones — + 2 round-trip), npm run lint (flake8+ruff) clean, npm run black no-op (1172 files unchanged) + black-check clean, integration round-trip (29 ARXML) included in suite green; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `TopicContentOrMsrQuery` — ARObject — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/Chapters.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table 9.79 (AUTOSAR_FO_TPS_GenericStructureTemplate.md, caption L9079 with its body rendered BEFORE it — caption-split page render), p.342 via pdf_page.py --table 9.79 (FO GST line); reproduction verified: R4.3.1 Table 8.81 (AUTOSAR_TPS_GenericStructureTemplate.md caption L7499) identical Package/Note/Base/attr-Note text (only `Mul.` vs `Mult.` header, glyph[lessmuch/greatermuch] vs `<<atpMixed>>`, name line-splits "msrQuery P1"/"topicConte nt" — cosmetic; R23-11 alone carries the "Aggregated by ChapterContent.topicContent, Topic1.topicContent" row — consumer-side, not modeled); Class table: Package M2::MSR::Documentation::Chapters matches current file; class Note "This meta-class represents a topic or a topic content which is generated using queries." (current docstring already verbatim; XSD 00052 group/complexType documentation L124993/L125014 identical); Base ARObject — strict most-derived (XSD complexType TOPIC-CONTENT-OR-MSR-QUERY L125011 sequence = AR-OBJECT group + own group, attributeGroup AR-OBJECT only — no REFERRABLE/IDENTIFIABLE, so no short_name; current header + no-arg ctor correct); attributes in displayed order both match current member names/types/order: msrQueryP1 (MsrQueryP1, 1, aggr) — referenced class MsrQueryP1 NO LONGER the v1-tracker deferred stub, now stamped `# Spec verified: R23-11` (Table 9.82, p.343, MsrQuery.py) → v1 tracker deviation RESOLVED (reconcile at Step 8); topicContent (TopicContent, 1, aggr, Note "This is the content of a topic. Tags: xml.roleElement=false") — current inline comment + getter/setter docstrings DROP the "Tags: xml.roleElement=false" suffix (stamped TopicContent/MsrQueryP1 keep Tags verbatim) → Step 4 fix, Step 2 Red pin; XSD 00052 element ground truth: group TOPIC-CONTENT-OR-MSR-QUERY L124990 = choice{choice*{element MSR-QUERY-P-1; choice*{group TOPIC-CONTENT}}} → element order MSR-QUERY-P-1 first / payload second, no sequenceOffset, and the role element name is MSR-QUERY-P-1 (L125001; R4.3.1 AUTOSAR_00044.xsd L87859 identical) while parser L7198 + writer L2222 hardcode "MSR-QUERY-P1" — genuine XSD element-name deviation, fixed in this row's Step 6 (sibling roles MSR-QUERY-TOPIC-1/MSR-QUERY-CHAPTER already match the XSD; DocumentationBlock's MSR-QUERY-P2 has the same mismatch — different class, out of scope); payload group TOPIC-CONTENT renders INLINE per xml.roleElement=false (no <TOPIC-CONTENT> element exists in the XSD) while py-armodel reads/writes a synthetic <TOPIC-CONTENT> wrapper — inlining spans TopicContent's stamped writeTopicContent/readTopicContent + consumers (ChapterContent = later queue row) → Pending note at Steps 5/6, not forced here; consume path verified: readTopicContentOrMsrQuery (parser L7196, from readChapterContent L7191 + readTopic1 L7250) → setMsrQueryP1/setTopicContent, writeTopicContentOrMsrQuery (writer L2213, from writeChapterContent L2211 + writeTopic1 L2244) → getMsrQueryP1/getTopicContent — matched name pairs hold; getMsrQueryP1(element, key) L7219 is key-parameterized with no internal caller
  - [x] Step 2 — Write model class unit test (Red) — extended the existing TestTopicContentOrMsrQuery class in tests/test_armodel/models/M2/MSR/Documentation/test_Chapters.py (spec-based style per the ListElements agent's TestItem: cleandoc/first-paragraph spec-Note asserts + get_type_hints annotation asserts): 4 new tests — inheritance isinstance ARObject + NOT Identifiable (Table 9.79 Base row; XSD complexType carries the AR-OBJECT group alone → no short_name, no-arg ctor), class-docstring spec Note verbatim, accessor docstrings lead with their attribute Note verbatim + None-no-op sentence present on setters (Rule 0001.4), get_type_hints asserts Optional[MsrQueryP1] on getMsrQueryP1/setMsrQueryP1 (hints resolved with an explicit globalns mapping because MsrQueryP1 is a TYPE_CHECKING-only import in Chapters.py) and Optional[TopicContent] on getTopicContent/setTopicContent; Red confirmed 1 failed/22 passed (test_topic_content_or_msr_query_docstrings_match_spec_notes — the topicContent inline comment + getter/setter docstrings drop the spec "Tags: xml.roleElement=false" suffix); honest note: the 3 other new tests pass immediately — the class pre-exists with the correct base, verbatim class Note, and PEP 526-annotated accessors (orphan intake, alignment pass); the existing 3 old-style tests (initialization defaults, set/get round-trip + chaining, None no-op on both members) were kept as-is since they already assert the spec contract
  - [x] Step 3 — Implement model class (Green) — NO CHANGE needed (deliberate): member set per Table 9.79 already complete — base ARObject (most-derived per table Base row + XSD complexType AR-OBJECT-only chain), dedicated Optional[MsrQueryP1]/Optional[TopicContent] fields in displayed row order (no registry filters, no fabrication, no missing member), guarded None-no-op setters returning self, no create*/add* required (both attrs mult 1; MsrQueryP1/TopicContent are no-shortName ARObject aggregates constructed bare — not Referrable children); blank line between the two __init__ attribute blocks verified by eye (Rule 0008); Step-3 referenced classes: none missing — MsrQueryP1 (MsrQuery.py, stamped Table 9.82 R23-11), TopicContent (Chapters.py, stamped Table E.81 R23-11), ARObject base; test state re-run unchanged 1 failed/22 passed — the remaining Red (docstrings test) is Step 4 scope, mirroring the Item row's alignment-pass shape
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wipe pass over the class found exactly one stale member wording: the topicContent attribute Note was written without its Tags suffix ("This is the content of a topic." in the `__init__` inline comment + setTopicContent/getTopicContent docstrings); everything else already matched the markdown verbatim (class docstring = Note "This meta-class represents a topic or a topic content which is generated using queries.", msrQueryP1 inline comment + setter/getter docstrings = "This represents automatically contributed contents provided by an msrquery.", setter None-no-op sentences + chaining Returns blocks in the stamped sibling shape, no `__init__` docstring per Rule 0012.2.5.2) — no other stale wording survived, so only the three topicContent strings were rewritten to "This is the content of a topic. Tags: xml.roleElement=false"; `__init__` members stay PEP 526 annotated under their note comments with a blank line between the blocks; mirror file 23/23 after rewrite (docstrings assert Green)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/parser/test_topic_content_or_msr_query.py (3 tests) + tests/test_armodel/writer/test_topic_content_or_msr_query.py (4 tests), house style of the sibling MsrQueryChapter/MsrQueryP1 pairs (ET.Element + direct readTopicContentOrMsrQuery/writeTopicContentOrMsrQuery invocation, write→parse→read round-trip, field-value asserts incl. XSD element order ["MSR-QUERY-P-1", "TOPIC-CONTENT"] and an empty-wrapper case): Red confirmed 2 failed/5 passed — BOTH genuine: (1) test_read_msr_query_p1_element feeds the XSD element name <MSR-QUERY-P-1> (group TOPIC-CONTENT-OR-MSR-QUERY, XSD 00052 L125001 / 00044 L87859) and the reader's find(element, "MSR-QUERY-P1") misses → the whole query branch is silently dropped; (2) test_write_msr_query_p1_uses_xsd_element_name asserts the writer emits MSR-QUERY-P-1 but writeMsrQueryP1 hardcodes SubElement(element, "MSR-QUERY-P1") (arxml_writer.py L2222); honest notes: the TOPIC-CONTENT payload tests pass immediately — the reader/writer wrapper path (readTopicContentOrMsrQuery L7199 find TOPIC-CONTENT → readTopicContent; writeTopicContent L2230 SubElement TOPIC-CONTENT) pre-exists and round-trips field values; the write→read round-trip test passes immediately too because both layers share the same wrong tag (self-consistent) — the Red is interop-shaped (spec-conformant vendor files in, non-conformant XML out), not self-round-trip-shaped; two test-authoring fixes during bring-up (P payloads need the real <L-1 L="EN">text</L-1> shape — bare <P>text</P> parses to a MultiLanguageParagraph with no L1s; MultiLanguageParagraph exposes getL1s()[0].getValue(), not getValue())
  - [x] Step 6 — Update parser & writer (Green) — two one-line fixes, both sides of the msrQueryP1 role element name: parser readTopicContentOrMsrQuery (arxml_parser.py L7198) find "MSR-QUERY-P1" → "MSR-QUERY-P-1" (XSD 00052 group TOPIC-CONTENT-OR-MSR-QUERY L125001; R4.3.1 00044 L87859 identical) and writer writeMsrQueryP1 (arxml_writer.py L2222) SubElement "MSR-QUERY-P1" → "MSR-QUERY-P-1"; mechanical consequence: the prior-row MsrQueryP1 test literals pinning the old tag updated to the XSD name (tests/test_armodel/writer/test_msr_query_p1.py 3 literals, tests/test_armodel/parser/test_msr_query_p1.py 6 occurrences — getMsrQueryP1(element, key) itself is key-parameterized and unchanged); the TOPIC-CONTENT wrapper handling deliberately NOT changed (Pending note — the inlining per xml.roleElement=false spans TopicContent's stamped writeTopicContent/readTopicContent + the consumers, ChapterContent being a later queue row); Rule 0013 matched-name pairs re-verified post-edit (readTopicContentOrMsrQuery↔writeTopicContentOrMsrQuery element pair; reader setMsrQueryP1/setTopicContent ↔ writer getMsrQueryP1/getTopicContent; no cross pairs, no chained mutators, no double readPaginateable); writer element order MSR-QUERY-P-1 before TOPIC-CONTENT matches the XSD group order (no sequenceOffset in this group); Green: 36/36 across the 4 reader/writer test files + model mirror; grep confirms no "MSR-QUERY-P1" (unhyphenated) reference remains in src or tests
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 9.79, p.342` + Columns legend incl. release + 5 rows in source order (__init__ [—]/[—]; setMsrQueryP1 reader [x]; getMsrQueryP1 writer [x]; setTopicContent reader [x]; getTopicContent writer [x] — all genuinely covered post-Step-6), every row release R23-11; the stale stub NOTE ("msrQueryP1 references the MsrQueryP1 stub... stamp omitted") REMOVED — MsrQueryP1 is stamped Table 9.82 R23-11 since the prior batch row; two note lines underneath: the element-name correction record ("MSR-QUERY-P1" -> "MSR-QUERY-P-1", with the out-of-scope MSR-QUERY-P2 sibling flagged) and the Pending note for the xml.roleElement=false wrapper inlining (TopicContent/ChapterContent serialization contract); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO standing spec deviations: no placeholders, no missing members (table = 2 attrs msrQueryP1/topicContent, both modeled verbatim with correct types), no naming deviations (both are plain aggr role names; Ref/TRef/IRef/`*`-pluralization rules n/a — no `*` mults, no ref kinds in this table), no type deviations; base closure faithful (ARObject most-derived per table + XSD AR-OBJECT-only chain — no flattening); member/accessor order = displayed row order; the XSD element-name mismatch found at Step 1 was FIXED in Step 6 (not a standing deviation — the role now serializes MSR-QUERY-P-1 per group TOPIC-CONTENT-OR-MSR-QUERY); the xml.roleElement=false wrapper inlining gap is NOT a class deviation but the documented Pending consume-path item owned by TopicContent's stamped write/readTopicContent + the consuming classes (ChapterContent = later queue row) — carried as the checklist Pending note per the Item-row precedent, not a tracker entry; tracker reconciliation COMPLETE (reviewed, files left untouched — docs/examples/ trackers are externally staged per batch convention, no batch commit has ever touched them): v1 method_deviation_by_class.md `## TopicContentOrMsrQuery` section — the msrQueryP1 row deviation "referenced class MsrQueryP1 (Table 9.82) deferred to a 2nd-level placeholder; stamp withheld until resolved" is RESOLVED (MsrQueryP1 now stamped `# Spec verified: R23-11`, Table 9.82, p.343 — the withholding condition is gone) and the topicContent row ("synced via the TopicContent pass") already reflects reality; a future tracker regeneration should rewrite the msrQueryP1 deviation cell to a resolved note; v2 method_deviation_by_class_v2.md lists the class under "Appendix: classes without a spec attribute table" (L1811) — STALE, Table 9.79 exists in the R23-11 FO GST markdown, so a regeneration will move the class out of the appendix (and post-batch-stamp skip it entirely via the marker); `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 48/48 (mirror test_Chapters.py 23 incl. the 4 new TestTopicContentOrMsrQuery tests + new parser test_topic_content_or_msr_query.py 3 + new writer test_topic_content_or_msr_query.py 4 + MSR-neighbor regressions test_msr_query_p1/test_msr_query_chapter/test_msr_query_result_chapter parser+writer 18), full suite `uv run python scripts/run_tests.py --no-coverage` 11434 passed/0 failed (prior-row baseline 11423 + 11 net-new: 4 model + 3 parser + 4 writer), integration round-trip (29 ARXML) green inside the suite, npm run lint (flake8+ruff) clean after one import-order fix in the new writer test, npm run black applied to the 2 new test files (cosmetic join of implicit string concats/chained setters) + black-check clean 1174 files; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `LOverviewParagraph` — LanguageSpecific — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/TextModel/LanguageDataModel.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table 9.91 (AUTOSAR_FO_TPS_GenericStructureTemplate.md caption L9291, body L9293-9300), p.348 via pdf_page.py --table 9.91 (FO GST line); reproductions verified: R4.3.1 Table 8.94 (AUTOSAR_TPS_GenericStructureTemplate.md caption L7694) identical Class/Package/Note/Base but EMPTY attribute list (no blueprintValue in R4.3.1 — draft attr added post-4.3.1, R23-11 + XSD authoritative, Rule 0015); R4.3.1 "Table 4.14: LOverviewParagraph" (L1601) NAME-COLLISION RESOLVED — it IS this same class reproduced in chapter 4's see-also chain (body L1593-1599 identical Class/Package/Note/Base, empty attribute list, rendered before its caption per the title-after-content convention; the Referrable (abstract) body under the caption L1603-1607 is Table 4.15's body — caption chain 4.13 MultiLanguageOverviewParagraph L1591 / 4.14 L1601 / 4.15 Referrable L1616, and R4.3.1 GST numbering has 4.18 = ShortNameFragment so the Modification agent's FO-GST-4.18 mapping does not transfer); Class table: Package M2::MSR::Documentation::TextModel::LanguageDataModel matches current file; class Note "MixedContentForOverviewParagraph in one particular language. The language is denoted in the attribute l." (verbatim confirmed by XSD 00052 group/complexType documentation L76227/L76245); Base ARObject, LanguageSpecific, MixedContentForOverviewParagraph → most-derived EXISTING base LanguageSpecific (stamped `# Spec verified: R23-11`, Table 9.97 p.350, same module); the MixedContentForOverviewParagraph base (abstract, Table 9.3 L7537, InlineTextModel — 10 attrs br/e/ft/ie/sub/sup/trace/tt/xref/xrefTarget, Subclasses LOverviewParagraph + SlOverviewParagraph) does NOT exist in src and has no Group9 queue row → referenced-missing class, reported at Step 8 (not blocking — Table 9.91's own attribute rows are modeled exactly; existing header `class LOverviewParagraph(LanguageSpecific)` matches the LParagraph/LLongName combine shape minus the absent mixed-content base); Aggregated by IndentSample.l2 + MultiLanguageOverviewParagraph.l2 — consumer-side, not modeled, both consumers stamped (Table 9.13 p.297, Table 9.90 p.348); single attribute row: blueprintValue (String, 0..1, attr, Note "This represents a description that documents how the value shall be defined when deriving objects from the blueprint. Tags: atp.Status=draft xml.attribute=true" — same Note text as the Table 4.7 SingleLanguageLongName blueprintValue row incl. the Tags tail). XSD 00052 ground truth: group L-OVERVIEW-PARAGRAPH L76223 EMPTY (`<xsd:sequence/>` — no own child elements); attributeGroup L76232 = single XML attribute BLUEPRINT-VALUE (STRING--SIMPLE, xml.attribute=true, atp.Status=draft, minOccurs 0 maxOccurs 1) — an XML attribute on the L-2 role element, NOT a child element; complexType L76241 mixed=true, choice over AR-OBJECT/LANGUAGE-SPECIFIC/MIXED-CONTENT-FOR-OVERVIEW-PARAGRAPH/L-OVERVIEW-PARAGRAPH groups, attributeGroups AR-OBJECT + LANGUAGE-SPECIFIC + L-OVERVIEW-PARAGRAPH (no MULTILANGUAGE-REFERRABLE/IDENTIFIABLE groups → not Identifiable, no short name); role element L-2 on IndentSample (L72169) + MultiLanguageOverviewParagraph (L83585), unbounded, xml.sequenceOffset=20. Consume path pre-exists: reader readLOverviewParagraph (arxml_parser.py L1699, from getMultiLanguageOverviewParagraph L1705; readARObject + setValue(text) + manual L attrib read + addL2) and getLOverviewParagraphs (L6214, from getIndentSample L6188; via readLanguageSpecific L5687); writer setLOverviewParagraph (arxml_writer.py L1431 → setLanguageSpecific(element, "L-2", name) L1250; consumers setMultiLanguageOverviewParagraph L1435 + setIndentSample L2809; stale type hint `name: LLongName` — Step 6 fix); GAP: nothing reads/writes the BLUEPRINT-VALUE attribute anywhere → blueprintValue dropped on round-trip both directions → genuine Red expected at Step 5; model layer old-style: 4-col checklist without `# Spec:` line, fabricated class docstring ("Language-specific overview paragraph element."), blueprintValue member MISSING entirely → Steps 2-7 align
  - [x] Step 2 — Write model class unit test (Red) — extended the existing TestLOverviewParagraph class in tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_LanguageDataModel.py (5 tests, style of the in-file stamped siblings TestLParagraph base-chain/docstring-verbatim + TestLLongName blueprintValue methods): test_l_overview_paragraph_base_chain (issubclass LanguageSpecific + ARObject per the Table 9.91 Base row + instantiability), test_l_overview_paragraph_initialization (l None / value "" / blueprintValue None), test_l_overview_paragraph_docstring_verbatim (inspect.cleandoc == class Note from Table 9.91), test_l_overview_paragraph_blueprint_value_methods (setBlueprintValue round-trip identity + chaining + None no-op), test_l_overview_paragraph_blueprint_value_annotation_is_optional (get_type_hints: getBlueprintValue return == Optional[str], setBlueprintValue param == Optional[str], setter return == LOverviewParagraph; module-level `from typing import Optional, get_type_hints` added, stdlib section before pytest per ruff I001 in tests); Red confirmed 4 failed/24 passed (initialization — no attribute 'blueprintValue'; docstring_verbatim — fabricated "Language-specific overview paragraph element." ≠ spec Note; blueprint_value_methods + annotation — no get/setBlueprintValue accessors); honest note: the base-chain test passes immediately — the class pre-exists with the correct LanguageSpecific base (orphan intake, alignment pass); the genuine Reds are the entirely missing blueprintValue member (Table 9.91's ONLY attribute row — exactly the v1 tracker's `missing` row) and the fabricated class docstring
  - [x] Step 3 — Implement model class (Green) — member set per Table 9.91 completed: added the MISSING blueprintValue attribute (String → `Optional[str]` field, PEP 526 annotated directly under its note comment, single `__init__` attribute block with blank line before the methods per Rule 0008) + getBlueprintValue/setBlueprintValue (None-guard + explicit chaining return `-> LOverviewParagraph`, stamped LLongName shape — same attribute, same Note text as the Table 4.7 SingleLanguageLongName blueprintValue row); base LanguageSpecific (most-derived existing base, stamped) already provides l/value accessors; no create*/add* (single 0..1 attr, no Referrable children; group L-OVERVIEW-PARAGRAPH is EMPTY so no `*` aggr list members); mirror test 28/28 passed (0.19s); Step-3 referenced classes: MixedContentForOverviewParagraph (abstract base per the Table 9.91 Base row, Table 9.3 — NOT in src, no Group9 queue row → reported at Step 8, non-blocking since Table 9.91's own rows are modeled exactly), LanguageSpecific/LEnum (same module, stamped), ARObject base
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — the Step-3 class-body replacement WAS the wipe (the intake class carried only the fabricated class docstring "Language-specific overview paragraph element." and a bare `__init__` with no members, so no stale member wording could survive anywhere) then rewrote verbatim from the Table 9.91 markdown, mechanically diffed (script comparing inspect.cleandoc docstrings against the L9296/L9300 markdown cells): class docstring = class Note "MixedContentForOverviewParagraph in one particular language. The language is denoted in the attribute l." (byte-equal True); `__init__` no docstring (Rule 0012.2.5.2) with the inline member comment + getter docstring = attribute Note verbatim "This represents a description that documents how the value shall be defined when deriving objects from the blueprint. Tags: atp.Status=draft xml.attribute=true" (byte-equal True; Tags tail kept per the stamped TopicContent/MsrQueryP1/DataFilter verbatim convention); setter docstring = attribute Note verbatim + blank-line paragraph "A None value is a no-op and does not overwrite an existing blueprintValue." (byte-equal True — first draft glued the no-op sentence onto the Note with an interpolated period, corrected to the stamped blank-line shape per setTopicContent/setItemLabelPos precedent); PEP 526 annotated member under its note comment, blank line between the attribute block and the methods; mirror test 28/28 after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/parser/test_l_overview_paragraph.py (3 tests) + tests/test_armodel/writer/test_l_overview_paragraph.py (3 tests), house style of the sibling TopicContentOrMsrQuery parser/writer pair (inline ET strings carrying the Autosar xmlns, direct readX/writeX invocation, field-value asserts): parser — test_read_l2_reads_blueprint_value (getMultiLanguageOverviewParagraph → getL2s()[0]: isinstance LOverviewParagraph + BLUEPRINT-VALUE/L attribs + text — the Table 9.91 attribute row as the XSD XML attribute on the L-2 role element), test_read_l2_via_get_l_overview_paragraphs (the IndentSample consume path), test_read_l2_without_blueprint_value (absent attrib → getBlueprintValue None); writer — test_write_l2_writes_blueprint_value_attribute (setMultiLanguageOverviewParagraph → L-2 attribs L + BLUEPRINT-VALUE + text; XSD 00052 L83585 role element), test_write_l2_without_blueprint_value_omits_attribute, test_l_overview_paragraph_write_read_roundtrip (write → serialize → re-parse asserting field values); Red confirmed 4 failed/2 passed — all 4 genuine (BLUEPRINT-VALUE dropped on both reader paths, never written, lost end-to-end — exactly the Step-1 predicted dropped field); honest note: the 2 absent-attribute tests pass immediately (reader None default + writer omission already correct for the None case); one test-authoring fix during bring-up: the parser harness initially passed the MLOP element itself as the find root — find() searches CHILDREN and the tree root must carry the Autosar xmlns (sibling harness shape)
  - [x] Step 6 — Update parser & writer (Green) — READER arxml_parser.py: guarded BLUEPRINT-VALUE attrib read added to BOTH L-2 consume paths — readLOverviewParagraph (L1699, MultiLanguageOverviewParagraph path: after the manual L attrib read) and getLOverviewParagraphs (L6214, IndentSample path: after readLanguageSpecific); readLanguageSpecific (L5687, the shared LanguageSpecific base helper) deliberately NOT touched — BLUEPRINT-VALUE belongs to attributeGroup L-OVERVIEW-PARAGRAPH, not LANGUAGE-SPECIFIC, so the LParagraph/LVerbatim/LPlainText consumers must not pick it up; WRITER arxml_writer.py: shared stamped helper setLanguageSpecific (L1250) made to `return child_element` (additive, backward compatible — precedent: the ConstantReference row's writeArrayValueSpecification dispatch extension) and setLOverviewParagraph (L1431) now writes the guarded `BLUEPRINT-VALUE` attrib from getBlueprintValue() after the L attrib + text, plus the stale type hint `name: LLongName` corrected to `LOverviewParagraph` (intake wart — LLongName serializes via setLLongName/L-4, never through this helper); LOverviewParagraph added to the writer's LanguageDataModel import (existing line, order within the line preserved — no re-sort); consumers untouched (setMultiLanguageOverviewParagraph L1435 + setIndentSample L2809 both dispatch through setLOverviewParagraph, so both Aggregated-by paths emit the attribute); Rule 0013 matched-name pairs verified (readLOverviewParagraph↔setLOverviewParagraph element pair, readLanguageSpecific↔setLanguageSpecific base pair, setValue↔getValue, setL↔getL, setBlueprintValue↔getBlueprintValue; no cross pairs, no chained mutators); round-trip 6/6 + model 28/28 green (34 passed)
  - [x] Step 7 — Update checklist comment — class-body checklist rewritten to the 6-column Rule 0002 format: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 9.91, p.348` + Columns legend incl. release + 3 rows in source order (__init__ [—]/[—]; getBlueprintValue writer [x] — writer setLOverviewParagraph calls getBlueprintValue; setBlueprintValue reader [x] — reader readLOverviewParagraph/getLOverviewParagraphs call setBlueprintValue), every row release R23-11; inherited LanguageSpecific accessor rows stay on the stamped base LanguageSpecific checklist (base owns them) — note lines underneath record the base-coverage and the BLUEPRINT-VALUE-is-an-XML-attribute fact; `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation)
  - [x] Step 8 — Deviations — NO standing spec deviations: no placeholders, no missing members remaining (the Table 9.91 single attr blueprintValue is fully modeled with reader/writer coverage after Step 6 — the intake gap, which is exactly the v1 tracker's `missing` row, was FIXED, not left open), no naming deviations (blueprintValue verbatim as spec'd; ref/tref/IRef/`*`-pluralization rules n/a — no ref kinds or `*` mults in this table), no type deviations (String → `Optional[str]` is the established mapping, identical to the stamped LLongName blueprintValue attribute); base closure faithful to the EXISTING model: LanguageSpecific most-derived existing base (stamped, same module); the MixedContentForOverviewParagraph base named in the Table 9.91 Base row (abstract, Table 9.3, InlineTextModel — 10 attrs br/e/ft/ie/sub/sup/trace/tt/xref/xrefTarget, Subclasses LOverviewParagraph + SlOverviewParagraph) does NOT exist in src and has no Group9 queue row → REPORTED referenced-missing class (Rule 0001.10), non-blocking: Table 9.91's own attribute rows are modeled exactly and the XSD complexType L76241 confirms the subclass contributes only BLUEPRINT-VALUE beyond the base groups (its `<xsd:sequence/>` element group is empty); reader+writer coverage complete (the Step-1 gap closed in Step 6); member order = displayed row order (single attribute); tracker reconciliation COMPLETE (reviewed, files left untouched per Group9 batch convention — no Group9 commit has touched docs/examples/): v1 method_deviation_by_class.md `## LOverviewParagraph` (L3454) carries the single `blueprintValue | missing` row → RESOLVED by this sync, a future tracker regeneration should supersede it with a no-deviation row (the BlueprintGenerator in-tracker precedent); v2 method_deviation_by_class_v2.md lists the class under "Appendix: classes without a spec attribute table" (L1813, appendix L1807) → STALE vs R23-11 (Table 9.91 HAS an attribute row; the appendix classification matches the R4.3.1 empty-attribute renders of Tables 8.94/4.14), a future regeneration moves the class out (same disposition as the prior row's stale TopicContentOrMsrQuery appendix entry); `# Spec verified:` stamp WITHHELD pending 9b batch confirmation (Rule 0012.1)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target tests 34/34 (mirror test_LanguageDataModel.py 28 incl. the new 5-test TestLOverviewParagraph + new parser test_l_overview_paragraph.py 3 + new writer test_l_overview_paragraph.py 3), full suite `uv run python scripts/run_tests.py --no-coverage` 11444 passed/0 failed (prior-row baseline 11434 + 10 net-new: 4 model + 3 parser + 3 writer), integration round-trip (29 ARXML) green inside the suite, npm run lint (flake8+ruff) clean, npm run black reformatted 2 touched files (arxml_writer.py — the LanguageDataModel import line wrapped to Black's parenthesized form at >200 chars, order within the line preserved, no re-sort; new parser test — string-concat join) + black-check clean 1176 files; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `LPlainText` — LanguageSpecific — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/TextModel/LanguageDataModel.py
  - note: deviation-tracked in method_deviation_by_class_v2.md — review entries at Step 1
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)

- [ ] `LVerbatim` — LanguageSpecific — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/TextModel/LanguageDataModel.py
  - note: deviation-tracked in method_deviation_by_class_v2.md — review entries at Step 1
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)

- [ ] `ARList` — Paginateable — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/BlockElements/ListElements.py
  - note: deviation-tracked in method_deviation_by_class_v2.md — review entries at Step 1
  - after `Item`
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)

- [ ] `ChapterContent` — ARObject — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/Chapters.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - after `TopicContentOrMsrQuery`
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)

- [ ] `ChapterModel` — ARObject — source TBC (locate table at Step 1)
  - module: M2/MSR/Documentation/Chapters.py
  - after `ChapterContent`
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)

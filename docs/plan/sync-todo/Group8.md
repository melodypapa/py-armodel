# Sync todo: Group 8 — CommonStructure/GeneralStructure generics

Input: full-repo orphan audit 2026-09-23 (unstamped ∧ untracked, M2 only) · Queue order = row order
(resume = first class row still `[ ]`; all class rows `[x]` = sync finished — Rule 0017.3)
> **Rule — already-verified short-circuit (added 2026-09-04):** before running the 9-step
> sync for a row, check whether the class already carries `# Spec verified: <RELEASE>` or
> `# XSD verified: <xsd-file>` in its own class body (verify the marker in the source — a
> row in this todo is not proof). If it does **and** a quick deviation check finds nothing
> new (base vs the spec `Base` closure, member types vs its table, verbatim docstrings,
> reader/writer coverage, checklist shape per Rules 0002/0012), **skip the 9 steps**: mark
> the row `- [x] <Class> — already verified (<marker>, <file>)` and move on. If the check
> finds a new deviation, do **not** mark it verified — keep the row queued, run the steps
> for the deviation only, and record it in Step 8 (Rule 0012.3: an existing marker is not
> proof).

## Queue (dependency-first)

- [x] `BindingTimeEnum` — AREnum — finished, stamped `# Spec verified: R23-11` (commit: 53bf1808)
  - module: M2/AUTOSARTemplates/GenericStructure/GeneralTemplateClasses/Enumerations.py
  - note: deviation-tracked in method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table E.8 (AUTOSAR_CP_TPS_SoftwareComponentTemplate, p.972) cited; byte-identical reproductions Table D.16 (CP BSWModuleDescriptionTemplate, p.308) + Table C.15 (FO StandardizationTemplate, p.163); Enumeration table: Package M2::AUTOSARTemplates::GenericStructure::VariantHandling; Note "This enumerator specifies the applicable binding times for the pre build variation points."; Aggregated by AttributeValueVariationPoint.bindingTime, ConditionByFormula.bindingTime, FMFeature.max/minIntendedBindingTime, FMFeatureSelection.max/minSelectedBindingTime; 4 literals displayed order idx0-3 codeGenerationTime/linkTime/preCompileTime/systemDesignTime ("codeGeneration Time" spacing in D.16 is a PDF-extraction artifact — XSD mmt.qualifiedName confirms camelCase); XSD 00052 L131754 BINDING-TIME-ENUM--SIMPLE wire tokens CODE-GENERATION-TIME/LINK-TIME/PRE-COMPILE-TIME/SYSTEM-DESIGN-TIME, no atp.Status=removed literals; pdf_page.py cannot index letter-table ids (numeric-only regex) — pages via direct pypdf scan; spec Package row → VariantHandling (non-leaf) vs file GeneralTemplateClasses/Enumerations.py — location kept per queue row, recorded as Step 8 finding
  - [x] Step 2 — Write model class unit test (Red) — extended existing TestBindingTimeEnum in test_Enumerations.py (no duplication): verbatim class-Note assertion, no-`__init__`-docstring, member constants + camelCase values, displayed-order getEnumValues, setValue/getValue/getText, validateEnumValue true/false; 3 Red (missing member constants, paraphrased docstring) / 6 passed — literal values + order already spec-correct (verified vs E.8 + XSD), honestly noted
  - [x] Step 3 — Implement model class (Green) — AREnum shape already spec-correct on values/order (verified vs E.8 + XSD); added Rule 0011 member constants CODE_GENERATION_TIME/LINK_TIME/PRE_COMPILE_TIME/SYSTEM_DESIGN_TIME = camelCase spec literals (matching parser/writer BINDING_TIME_XML_MAP keys), displayed-order tuple, per-literal verbatim description comments + Tags; 9/9 Green, 445 consumer tests pass
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — paraphrased class docstring ("Enumeration for binding time in AUTOSAR variant handling…") wiped and replaced with the verbatim Table E.8 Note; enum has no `__init__`/getter/setter docstrings to sync (AREnum base supplies accessors)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — N/A: standalone AREnum, no own XML element; serialized as attribute value on consumers (AttributeValueVariationPoint.bindingTime, ConditionByFormula.bindingTime per the table's Aggregated-by row) and round-tripped there; camelCase↔UPPER-KEBAB wire mapping via BINDING_TIME_XML_MAP (parser L1143 / writer L1014) verified by 445 existing consumer tests
  - [x] Step 6 — Update parser & writer (Green) — N/A: standalone AREnum (no readBindingTimeEnum/writeBindingTimeEnum exists or is needed); consumer readConditionByFormula/readAttributeValueVariationPoint + write counterparts pre-exist and pass
  - [x] Step 7 — Update checklist comment — 6-column format with `# (no methods) — enum value form serialized on AttributeValueVariationPoint.bindingTime + ConditionByFormula.bindingTime (Steps 5/6 N/A: standalone AREnum)`, Spec line cites Table E.8 p.972; marker written 9b: `# Spec verified: R23-11`
  - [x] Step 8 — Deviations  [none open: no naming/type/missing rows — literals were already camelCase-correct vs E.8/XSD; v1 tracker (method_deviation_by_class.md) has NO BindingTimeEnum entry (verified by grep); v2 tracker has only an Appendix bullet ("classes without a spec attribute table", L1861) — accurate for an enum, left in place per AutoCollectEnum precedent (stamped + still listed until next regeneration); location finding: spec Package row = GenericStructure::VariantHandling (non-leaf → Rule 0007 would put the class in VariantHandling/__init__.py) but class stays in GeneralTemplateClasses/Enumerations.py per the queue row — flagged for batch/user decision, not moved unilaterally; referenced classes: base AREnum (PrimitiveTypes) exists, unstamped, queued elsewhere — no missing classes]
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target file 9/9, full suite 11336 passed/0 failed (baseline 11030 + 5 re-checked = 11336 total), npm run lint (flake8+ruff) clean, black-check clean; 9b confirmed by user 2026-09-24: all Rule 0001–0015, 0017–0018 compliance checks pass, no deviations, stamp written `# Spec verified: R23-11`, commit 53bf1808

- [x] `XmlSpaceEnum` — AREnum — finished, stamped `# XSD verified: AUTOSAR_00052.xsd` (commit: ec544e79)
  - module: M2/AUTOSARTemplates/GenericStructure/GeneralTemplateClasses/Enumerations.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — XSD-only confirmed via mandatory both-corpora gate: caption grep `^Table [id]: XmlSpaceEnum` (case-insensitive tolerant pass too) + header-cell/subject-row grep = 0 hits in R23-11 (CP_TPS+FO_TPS) AND R4.3.1 markdown; only consumer attribute-type refs (Sd.xmlSpace FO ARXMLSerializationRules L925, VerbatimString CP ECUConfiguration Table F.48 + FO GenericStructureTemplate Tables 4.21/4.67, WhitespaceControlled FO GenericStructureTemplate Table 9.7); derived from XSD AUTOSAR_00052.xsd L145398 XML-SPACE-ENUM / L145410 --SIMPLE: class Note "This enumerator specifies the fact that white-space shall be preserved."; literals displayed order idx0-1 default/preserve (wire tokens ARE the lowercase xml:space values — no kebab-case mapping unlike BindingTimeEnum; XSD xml.name=default/preserve confirms), per-literal Notes verbatim from XSD docs, Tags atp.EnumerationLiteralIndex=0/1 (existing comments wrongly said atp.EnumerationValue); consumers: Sd.xmlSpace (writer writeSds arxml_writer.py:1090 emits {xml}space attr; parser readSd arxml_parser.py:1213 does NOT read it back — consumer gap → Step 8), VerbatimString.xmlSpace not implemented ("missing enum type" note now stale → Step 8); tracker entries reviewed (v1 skip-note stale → reconcile Step 8, v2 Appendix bullet accurate → keep)
  - [x] Step 2 — Write model class unit test (Red) — rewrote existing weak TestXmlSpaceEnum in test_Enumerations.py to the spec-contract form (mirrors TestBindingTimeEnum): verbatim class-Note assertion (XSD L145398 doc), no-`__init__`-docstring, member constants + wire values, displayed-order getEnumValues ["default","preserve"], setValue/getValue/getText, validateEnumValue true/false (incl. case-sensitivity); 1 Red (verbatim docstring — old class docstring carried the consumer-attribute Note, wrong provenance) / 11 passed — literal values + order + validateEnumValue already spec-correct vs XSD, honestly noted
  - [x] Step 3 — Implement model class (Green) — AREnum shape already spec-correct on values/order (verified vs XSD L145410 --SIMPLE); kept constants DEFAULT/PRESERVE (already UPPER_SNAKE), displayed-order tuple, verbatim per-literal comments; fixed comment Tags atp.EnumerationValue→atp.EnumerationLiteralIndex=0/1 (XSD appinfo) + class docstring replaced with verbatim XSD Note; 12/12 Green + 19/19 consumer (test_SpecialData)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped the stale class docstring (was the Sd.xmlSpace consumer-attribute Note "This attribute is used to signal an intention…W3C.") and replaced with the enum's own XSD documentation verbatim ("This enumerator specifies the fact that white-space shall be preserved."); enum has no `__init__`/getter/setter docstrings to sync (AREnum base supplies accessors, None asserted by test); per-literal inline comments verbatim from XSD docs incl. lowercase "the value \"preserve\"…"
  - [x] Step 5 — Write reader/writer round-trip test (Red) — N/A: standalone AREnum, no own XML element; serialized as attribute value on the consumer Sd.xmlSpace (XSD mmt.qualifiedName="Sd.xmlSpace" L101701; other XSD consumers VerbatimString.xmlSpace / WhitespaceControlled.xmlSpace not modeled) — writer writeSds (arxml_writer.py:1090) emits {http://www.w3.org/XML/1998/namespace}space from xml_space.getValue(); round-tripped on the consumer side (19 Sd tests pass)
  - [x] Step 6 — Update parser & writer (Green) — N/A: standalone AREnum (no readXmlSpaceEnum/writeXmlSpaceEnum exists or is needed); note: parser readSd (arxml_parser.py:1213) does not read the xml:space attribute back while the writer emits it — pre-existing consumer-level gap in Sd, recorded as Step 8 finding, NOT fixed unilaterally (Sd is a stamped class outside this row's scope)
  - [x] Step 7 — Update checklist comment — 6-column format with `# Spec: XSD-only, AUTOSAR_00052.xsd line 145398 (no own table in repo corpus)` + `# (no methods) — enum value form serialized on Sd.xmlSpace (Steps 5/6 N/A: standalone AREnum)`; `__init__` row all-[x] with release R23-11; marker (`# XSD verified: AUTOSAR_00052.xsd`) NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing rows — values/order/docstrings all match XSD L145398/145410; v1 tracker (method_deviation_by_class.md) XmlSpaceEnum skip-note reconciled 2026-09-24 → now records the XSD-only sync and supersedes the old "left as-is" wording; v2 tracker has only an Appendix bullet ("classes without a spec attribute table", L1862) — accurate for an enum, left in place per AutoCollectEnum/BindingTimeEnum precedent; findings (consumer-level, not class deviations, not fixed unilaterally): (1) parser readSd does not read xml:space back while writer writeSds emits it — Sd reader gap, flagged for Sd/SdgContents sync (Sd is stamped; fixing would change a stamped class's round-trip); (2) VerbatimString.xmlSpace unimplemented — its docstring note "missing enum type" is now stale since the enum exists, flagged for VerbatimString sync; (3) location: XSD mmt.qualifiedName="XmlSpaceEnum" carries no package qualifier — kept in GeneralTemplateClasses/Enumerations.py per queue row; referenced classes: base AREnum (PrimitiveTypes) exists unstamped/queued elsewhere, consumer Sd exists and is stamped — no missing classes]
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target file 12/12 (TestXmlSpaceEnum 6 + TestBindingTimeEnum 6), full suite 11033 passed/0 failed (baseline 11030 + 3 net-new from rewriting 3 weak tests into 6), npm run lint (flake8+ruff) clean, npm run black no-op + black-check clean; 9b properly run 2026-09-24 (real confirmation gate, per the Group3 process correction): Rule 0002 marker-form decision re-checked (both-corpora grep = 0 tables → `# XSD verified:` form); XSD ground-truth diff vs AUTOSAR_00052.xsd — class Note verbatim L145399, literal order default→preserve per --SIMPLE L145410 + EnumerationLiteralIndex 0/1, per-literal Notes verbatim incl. lowercase "the value…" form, wire values = xml.name, no fabricated literals; base AREnum; reader/writer [—] with consumer Sd.xmlSpace emission verified (writer L1093–1095); 9a re-run this session: 12/12 target + lint + black-check clean — user-confirmed 2026-09-24; stamp written (commit: ec544e79)

- [x] `ShortNameFragment` — ARObject — finished, stamped `# Spec verified: R23-11` (commit: 519d50539)
  - module: M2/AUTOSARTemplates/GenericStructure/GeneralTemplateClasses/Identifiable.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table 4.13 (AUTOSAR_FO_TPS_GenericStructureTemplate, p.64 via pdf_page.py); R4.3.1 reproduction Table 4.18 (p.64) byte-identical incl. displayed order; Package M2::AUTOSARTemplates::GenericStructure::GeneralTemplateClasses::Identifiable matches current file; class Note verbatim in place; Base ARObject ✓; Aggregated by Referrable.shortNameFragment → XSD wrapper SHORT-NAME-FRAGMENTS (REFERRABLE group, seqOffset=-90, unbounded choice of SHORT-NAME-FRAGMENT); attributes displayed order fragment (Identifier, 1, attr, seqOffset=20) then role (String, 1, attr, seqOffset=10); XSD 00052 group SHORT-NAME-FRAGMENT L106612 (ROLE=10, FRAGMENT=20 → XML element order ROLE,FRAGMENT — writer already emits in that order), complexType L106634; two deviations found (fix in Steps 3/6): role typed Optional[str] vs spec String (Rule 0001.3 — sibling fragment already typed Identifier; strict Optional[String] is the stamped convention per setUuid/setAnnotationOrigin/setHelpEntry) and member/accessor order role→fragment vs markdown displayed fragment→role (Rule 0001.11); reader/writer coverage pre-exists (parser getShortNameFragments L1385 consumed by readReferrable L1401, writer setShortNameFragment(s) L1224/L1233 consumed by writeReferrable L1210) — Steps 5/6 extend, NOT N/A; tracker v1 "## Referrable" section (CP BSWModuleDescriptionTemplate audit) lists shortName+shortNameFragment as missing — stale vs current code, reconcile Step 8; v2 tracker has no ShortNameFragment entries
  - [x] Step 2 — Write model class unit test (Red) — extended existing TestShortNameFragment in test_Identifiable.py (no duplication): added verbatim class-Note assertion, no-`__init__`-docstring assertion, PDF-typed accessor annotations via typing.get_type_hints (repo style per test_SwComponentPrototypeAssignment) for role→Optional[String] + fragment→Optional[Identifier]; rewrote role setter/getter + None-no-op tests to the String contract (String().setValue, isinstance + getValue); 1 Red (test_role_typed_string — role is Optional[str] vs spec String) / 8 passed — verbatim docstring, Identifier typing, defaults, round-trip, None no-ops already spec-correct, honestly noted
  - [x] Step 3 — Implement model class (Green) — role retyped Optional[str]→Optional[String] (spec String; sibling fragment already typed Identifier — Rule 0001.3), member/accessor order reordered to markdown displayed order fragment→role (was role→fragment — Rule 0001.11); Base stays ARObject; concrete class (XSD complexType abstract="false"); setter shape kept (None no-op, returns self); no fabricated/extra fields (field-to-spec both directions: exactly fragment+role); 44/44 Green in test_Identifiable.py
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — whole class body replaced in the Step 3 pass (old "Gets the…/Sets the…" Google-style paraphrases wiped, no stale wording survives); class docstring = Table 4.13 Note verbatim; inline `__init__` comments = attribute Notes verbatim incl. Tags (xml.sequenceOffset=20/10); getter docstrings = Note verbatim, setter docstrings = Note + "A None value is a no-op…" sentence (form matches stamped MultilanguageReferrable/Identifiable in the same file); no `__init__` docstring; PEP 526 annotated members with blank line between the two attribute blocks
  - [x] Step 5 — Write reader/writer round-trip test (Red) — extended existing tests/test_armodel/parser/test_short_name_fragments.py (extend, no duplication): strengthened test_round_trip with isinstance(getRole(), String) + getValue (role built via String().setValue), added test_round_trip_multiple_fragments (2 fragments, per-index role/fragment value + type assertions, field values not just len), added test_written_element_order_role_before_fragment (written XML SHORT-NAME-FRAGMENT children == [ROLE, FRAGMENT] per XSD sequenceOffset 10/20; writeARObject emits only S/T attrs so child list is exact); empty-wrapper case pre-existed; 3 Red / 1 passed — Red is real: writer assigns the String object raw to element.text → ET "cannot serialize String" TypeError; order+empty passed immediately (writer already emitted ROLE first — honestly noted)
  - [x] Step 6 — Update parser & writer (Green) — parser getShortNameFragments (arxml_parser.py L1385): raw `ROLE` text read (manual find + setRole(str)) replaced with the matched typed leaf helper getChildElementOptionalString (Rule 0013.2 pair); writer setShortNameFragment (arxml_writer.py L1224): manual ROLE SubElement + raw text assignment replaced with setChildElementOptionalString → getText() (fixes the ET "cannot serialize String" Red), ROLE→FRAGMENT emission order preserved (XSD 10/20); test-side adjustments (not writer bugs): pre-existing direct writer test test_write_referrable_with_short_name_fragments updated to the String contract (setRole(String().setValue("prefix")) — raw str had no getText), element-order test made namespace-agnostic (saved doc carries xmlns; first assertion form was a test bug, writer output was already correct); 114/114 Green across test_short_name_fragments.py + test_arxml_writer.py + test_Identifiable.py
  - [x] Step 7 — Update checklist comment — 6-column format, rows in markdown displayed order (`__init__`, getFragment, setFragment, getRole, setRole), Spec line `AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 4.13, p.64`, all rows `[x]` with release R23-11; reader [x] on setFragment/setRole (parser sets), writer [x] on getFragment/getRole (writer gets); marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: the two Step 1 findings were fixed in-band and leave no deviation rows — (1) type deviation role str→String fixed in Step 3 with matched parser/writer helpers in Step 6 (Rule 0014: fixed ⇒ no row), (2) member-order deviation fixed in Step 3 (fragment→role per markdown displayed order); v1 tracker (method_deviation_by_class.md) reconciled 2026-09-24: `## Referrable` section's two stale "missing" rows (shortName, shortNameFragment — from the CP BSWModuleDescriptionTemplate audit; both exist in current code) superseded by a resolution Note per XmlSpaceEnum precedent, and a new `## ShortNameFragment` section added (sync record, no-deviation row, stamp-deferred note); v2 tracker has no ShortNameFragment entries; referenced classes: base ARObject stamped, member types Identifier (Table 4.5, stamped) + String (Table 4.63, stamped), consumer Referrable (stamped, same file) — no missing classes]
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 114/114 (test_Identifiable.py 44 + test_short_name_fragments.py 4 + test_arxml_writer.py 66), full suite 11039 passed/0 failed (baseline 11033 + 6 net-new: +4 model tests, +2 round-trip tests), npm run lint (flake8+ruff) clean, npm run black no-op (1146 files unchanged) + black-check clean; integration round-trip (29 ARXML) included in the suite green; 9b confirmed 2026-09-25 (user, one-by-one review) — stamp written; 9a re-verified 2026-09-25 on the post-merge tree (48/48 target tests)

- [x] `MultidimensionalTime` — ARObject — finished, stamped `# Spec verified: R23-11` (commit: b572582c1)
  - module: M2/AUTOSARTemplates/GenericStructure/GeneralTemplateClasses/MultidimensionalTime.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table 4.74 (AUTOSAR_FO_TPS_GenericStructureTemplate home doc, p.165 via pdf_page.py); table identity check across the 3 reproductions: CP_TPS_SoftwareComponentTemplate Table 4.14 (p.110) + CP_TPS_BSWModuleDescriptionTemplate Table 8.22 (p.165) — Package/Base/attributes (names, types, mults, kind, Note text, displayed order cseCode→cseCodeFactor) identical everywhere; only the Note's bibliography ref number differs ([17] FO home / [15] CP SWCT / [20] CP BSWMDT — cross-doc numbering); Class-vs-Enumeration header = Class ✓; Base ARObject ✓ (XSD 00052 complexType MULTIDIMENSIONAL-TIME L83781 abstract="false" = AR-OBJECT group + MULTIDIMENSIONAL-TIME group — authoritative base confirmation); XSD group L83759: CSE-CODE (CSE-CODE-TYPE-STRING, 0..1) then CSE-CODE-FACTOR (INTEGER, 0..1) — element order CSE-CODE→CSE-CODE-FACTOR, no explicit sequenceOffset; existing class fields match spec exactly both directions (cseCode Optional[CseCodeType], cseCodeFactor Optional[Integer]); leaf file location ✓ (spec package ...GeneralTemplateClasses::MultidimensionalTime); four findings to fix: (1) stale stamp+checklist — old 4-column format, `# Spec verified: R23-11` citing CP BSWMDT Table 8.22 with WRONG page p.164 (actual p.165) and citing a reproduction instead of the home doc — stamp removed this pass (batch instruction: no marker until batch confirmation), checklist rebuilt 6-column; (2) class docstring = [20] reproduction Note + FABRICATED extra paragraphs (ASAM CSE explanation + 100/360 example) — Rule 0012 wipe+rewrite in Step 4; (3) method docstrings are "Gets/Sets the…" paraphrases with fabricated [constr_10338]/[constr_10339] tags — Step 4; (4) reader type gap — getChildElementOptionalLiteral returns ARLiteral but the field/accessor contract is CseCodeType (no matched getChildElementOptionalCseCodeType/setChildElementOptionalCseCodeType pair; precedent: VerbatimString L129 / RevisionLabelString helpers) — fix in Step 6; reader/writer coverage pre-exists (parser readMultidimensionalTime L4510, writer setMultidimensionalTime L5847, element order matches XSD) — Steps 5/6 extend, NOT N/A
  - [x] Step 2 — Write model class unit test (Red) — extended existing TestMultidimensionalTime in test_MultidimensionalTime.py (extend, no duplication): added verbatim class-Note assertion (home-doc [17] form), no-`__init__`-docstring assertion, PDF-typed accessor annotations via typing.get_type_hints (repo style per TestShortNameFragment) for cseCode→Optional[CseCodeType] + cseCodeFactor→Optional[Integer]; 2 Red / 8 passed — Red is real: class docstring still carried the [20] BSW-reproduction Note + fabricated paragraphs, `__init__` had a docstring; both annotation tests passed immediately (accessors already spec-typed — honestly noted); defaults/round-trip/chaining/None-no-op coverage pre-existed (6 tests) and was kept
  - [x] Step 3 — Implement model class (Green) — whole class body replaced (old fabricated docstrings wiped in the same pass); Base stays ARObject (XSD complexType L83781 = AR-OBJECT group, authoritative); concrete class (XSD abstract="false"); field-to-spec both directions: exactly cseCode (Optional[CseCodeType]) + cseCodeFactor (Optional[Integer]) — no fabricated/extra fields; member/accessor order cseCode→cseCodeFactor matches markdown displayed order (Rule 0001.11); PEP 526 annotated members with blank line between the two attribute blocks; setter shape kept (None no-op, returns self); imports NOT re-sorted (models rule); 10/10 Green in test_MultidimensionalTime.py
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wipe+rewrite landed in the Step 3 body replacement (no stale wording survives: old [20]-citing Note, fabricated ASAM-CSE paragraphs + 100/360 example, "Gets/Sets the…" Google-style paraphrases and fabricated [constr_10338]/[constr_10339] tags all removed); class docstring = Table 4.74 Note verbatim ([17] home doc); inline `__init__` comments = attribute Notes verbatim, no Tags suffix (XSD elements carry no sequenceOffset); getter docstrings = Note verbatim; setter docstrings = Note + "A None value is a no-op…" sentence (form matches stamped ShortNameFragment in sibling Identifiable.py); no `__init__` docstring
  - [x] Step 5 — Write reader/writer round-trip test (Red) — created dedicated tests/test_armodel/parser/test_multidimensional_time.py (3 tests: CSE-CODE read with isinstance-CseCodeType + value assertions, CSE-CODE-FACTOR read with isinstance-Integer + value, absent-elements → both None) and tests/test_armodel/writer/test_multidimensional_time.py (3 tests: written children == [CSE-CODE, CSE-CODE-FACTOR] per XSD group order with text assertions, empty-wrapper case, parse→write→re-parse round-trip with `_round_trip` xmlns-injection helper per test_writer_timing_constraints.py precedent); 2 Red / 4 passed — Red is real: reader `getChildElementOptionalLiteral` stores a plain ARLiteral for cseCode where the field contract is CseCodeType (both failing tests assert isinstance); honest passes: written element order + empty wrapper + value round-trip were already correct, factor read already Integer-typed; one test-side fix during Red triage: re-parsing the raw un-namespaced writer element found nothing (parser find() is namespace-qualified) — fixed with the `_round_trip` helper, writer output was already correct
  - [x] Step 6 — Update parser & writer (Green) — added the matched typed leaf pair (Rule 0013.2): parser `getChildElementOptionalCseCodeType` (abstract_arxml_parser.py, after getChildElementOptionalLiteral, VerbatimString/Identifier body form, returns Optional[CseCodeType]) + writer `setChildElementOptionalCseCodeType` (abstract_arxml_writer.py, after setChildElementOptionalRevisionLabelString, delegates to setChildElementOptionalLiteral); CseCodeType added to both files' PrimitiveTypes import blocks (alphabetical insert, no re-sort); readMultidimensionalTime (arxml_parser.py) now reads CSE-CODE via the typed helper; setMultidimensionalTime (arxml_writer.py) now writes CSE-CODE via the typed helper (writer emission behavior unchanged — CseCodeType IS-A ARLiteral); Green: 16/16 across the 3 target test files + 142/142 across consumer suites (parser timing_constraints/timing_clocks/timing_condition/trigger + writer timing_constraints/timing_clocks/timing_condition/trigger/sw_data_def_props/arxml_writer)
  - [x] Step 7 — Update checklist comment — 6-column format written in the Step 3 body replacement, verified at this step: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 4.74, p.165` (home doc; supersedes the old CP BSWMDT Table 8.22/p.164 citation), rows in markdown displayed order (`__init__`, getCseCode, setCseCode, getCseCodeFactor, setCseCodeFactor), all `[x]` with release R23-11; reader [x] on setCseCode/setCseCodeFactor (parser sets), writer [x] on getCseCode/getCseCodeFactor (writer gets), `__init__` [—]/[—]; marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: all four Step 1 findings were fixed in-band and leave no deviation rows — (1) stale 4-column checklist + pre-existing `# Spec verified: R23-11` stamp (citing reproduction CP BSWMDT Table 8.22 with off-by-one page p.164) replaced with the 6-column home-doc citation, marker withheld per batch instruction; (2) fabricated class-docstring paragraphs + (3) paraphrase method docstrings with fabricated [constr_10338]/[constr_10339] tags wiped and rewritten verbatim in Steps 3/4; (4) reader type gap (ARLiteral vs CseCodeType) fixed in Step 6 with the matched CseCodeType leaf pair — Rule 0014: fixed ⇒ no row; v1 tracker (docs/examples/method_deviation_by_class.md) `## MultidimensionalTime` section reconciled 2026-09-24 with a Resolution Note (supersedes the old BSWMDT/p.164 citation, records the sync + fixes, no-deviation); v2 tracker has no MultidimensionalTime entries (its consumer-side "missing" rows naming MultidimensionalTime as a field type belong to those consumer classes' own syncs); referenced classes: base ARObject (stamped, same package), member types CseCodeType (Table 4.75, exists in PrimitiveTypes) + Integer (Table 4.63, stamped) — no missing classes]
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 16/16 (test_MultidimensionalTime.py 10 + test_multidimensional_time.py parser 3 + writer 3, re-run green after black reformat of the 2 new test files), full suite 11049 passed/0 failed (baseline 11039 + 10 net-new: +4 model tests, +6 round-trip tests), npm run lint (flake8+ruff) clean, npm run black reformatted the 2 new test files (canonical formatting only) + black-check clean (1148 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact, most-derived base ARObject (XSD L83781), no fabrication/flattening, PDF-typed fields, member order = markdown displayed order, XML element order = XSD group order, docstrings verbatim (class Note [17] home doc + attribute Notes in comments/getters/setters + None-no-op sentences), blank line between __init__ attribute blocks, PEP 526 annotated members, reader+writer coverage for both attrs via matched CseCodeType/Integer leaf pairs, no open deviations, leaf-file location per spec package; 9b confirmed 2026-09-24 (batch user confirmation); stamp written

- [x] `LifeCyclePeriod` — ARObject — finished, stamped `# Spec verified: R23-11` (commit: b572582c1)
  - module: M2/AUTOSARTemplates/GenericStructure/LifeCycles.py
  - [x] Step 1 — Sync members & description from spec — Table 12.4 (AUTOSAR_FO_TPS_GenericStructureTemplate, p.392 via pdf_page.py); R4.3.1 reproduction Table 11.4 (p.364) byte-identical incl. attribute rows/Notes/sequenceOffsets; Class-vs-Enumeration header = Class ✓; Package M2::AUTOSARTemplates::GenericStructure::LifeCycles matches current leaf file; Note verbatim in place (2-line wrapped); Base ARObject ✓ (XSD 00052 complexType LIFE-CYCLE-PERIOD L76738 abstract="false" = AR-OBJECT group + LIFE-CYCLE-PERIOD group — authoritative base confirmation); XSD group L76708: DATE (sequenceOffset=10) → AR-RELEASE-VERSION (=20) → PRODUCT-RELEASE (=30) — XML element order DATE, AR-RELEASE-VERSION, PRODUCT-RELEASE; attributes displayed order arReleaseVersion (RevisionLabelString 0..1 attr) → date (DateTime 0..1 attr) → productRelease (RevisionLabelString 0..1 attr) matches existing member/accessor order (Rule 0001.11 OK); Aggregated by LifeCycleInfo.periodBegin/periodEnd + LifeCycleInfoSet.defaultPeriodBegin/defaultPeriodEnd (XSD L76563/76569/76645/76651, all type LIFE-CYCLE-PERIOD); existing class fields match spec except ONE deviation: date typed Optional[datetime] vs spec DateTime (DateTime(ARLiteral) exists in PrimitiveTypes L1344; matched leaf pair getChildElementOptionalDateTime/setChildElementOptionalDateTime exists with RevisionLabel DATE precedent) — fix Steps 3/6; old 4-column checklist without `# Spec:` line — rebuild 6-column Step 7; method docstrings are "Gets/Sets the…" paraphrases — wipe+rewrite Step 4; reader/writer disposition: keyed helper-only (getLifeCyclePeriod/setLifeCyclePeriod take the wrapper key — PERIOD-BEGIN/PERIOD-END/DEFAULT-PERIOD-BEGIN/DEFAULT-PERIOD-END), no dispatcher entry (atpObject aggregated inside consumers, never standalone) — but both helpers cover ONLY AR-RELEASE-VERSION; DATE + PRODUCT-RELEASE missing on both sides — fix Step 6; consumer-side gaps (readLifeCycleInfo/writeLifeCycleInfo handle PERIOD-BEGIN only, LifeCycleInfoSet readers/writers touch no DEFAULT-PERIOD-*) belong to the later LifeCycleInfo/LifeCycleInfoSet rows — NOT this row's scope
  - [x] Step 2 — Write model class unit test (Red) — extended existing TestLifeCyclePeriod in test_LifeCycles.py (no duplication): added verbatim class-Note assertion (Table 12.4 single-line form), no-`__init__`-docstring assertion, PDF-typed accessor annotations via typing.get_type_hints (repo style per TestMultidimensionalTime) for arReleaseVersion/date/productRelease → Optional[RevisionLabelString]/Optional[DateTime]/Optional[RevisionLabelString] (setter value annotations checked as Optional too, per the stamped setUuid/ShortNameFragment convention); rewrote test_set_date + test_set_date_none to the DateTime contract (DateTime().setValue("2023-06-15T12:00:00+01:00")), removed the now-unused `from datetime import datetime`; 4 Red / 46 passed — Red is real: class docstring is 2-line wrapped vs the verbatim single-line Note (Step 4), `date` field is Optional[datetime] vs spec DateTime and both RevisionLabelString setters + setDate take non-Optional value (Step 3); test_init_has_no_docstring passed immediately (`__init__` already docstring-less — honestly noted); pre-existing defaults/round-trip/chaining/None-no-op coverage (8 tests) kept and green
  - [x] Step 3 — Implement model class (Green) — whole LifeCyclePeriod class body replaced in one pass; field-to-spec both directions: exactly arReleaseVersion (Optional[RevisionLabelString]) + date (Optional[DateTime] — retyped from Optional[datetime], spec DateTime; DateTime(ARLiteral) exists in PrimitiveTypes L1344 — Rule 0001.3) + productRelease (Optional[RevisionLabelString]); Base stays ARObject (XSD complexType L76738 = AR-OBJECT group, authoritative); concrete class (XSD abstract="false"); member/accessor order arReleaseVersion→date→productRelease matches markdown displayed order (Rule 0001.11); setters retyped `value: Optional[T]` + `-> "LifeCyclePeriod"` return annotation and inline None-no-op sentence per the stamped setFragment/setRole convention in Identifiable.py (test-checked); PEP 526 annotated members with blank line between the three attribute blocks; `from datetime import datetime` import removed, DateTime added to the existing PrimitiveTypes import line (extended in place — imports NOT re-sorted per models rule); verified no other consumer used the old datetime contract (grep); 50/50 Green in test_LifeCycles.py (3 typing tests were still Red after the first pass because setters lacked the `-> "LifeCyclePeriod"` annotation the test asserts — added, stamped-convention alignment)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body replacement (no stale wording survives: old "Gets/Sets the…" Google-style paraphrases + Args/Returns blocks wiped); class docstring = Table 12.4 Note verbatim (single line, was 2-line wrapped); inline `__init__` comments = attribute Notes verbatim incl. Tags (xml.sequenceOffset=20/10/30); getter docstrings = Note verbatim (no Tags suffix, per stamped ShortNameFragment form); setter docstrings = Note verbatim + "A None value is a no-op and does not overwrite an existing …" sentence; no `__init__` docstring
  - [x] Step 5 — Write reader/writer round-trip test (Red) — created dedicated tests/test_armodel/parser/test_life_cycle_period.py (3 tests: all-fields read with isinstance-DateTime/RevisionLabelString + value assertions, partial read (AR-RELEASE-VERSION only) → date/productRelease None, absent wrapper element → None) and tests/test_armodel/writer/test_life_cycle_period.py (4 tests: written children == [DATE, AR-RELEASE-VERSION, PRODUCT-RELEASE] per XSD sequenceOffset 10/20/30 with text assertions, None period → no element, empty period → empty wrapper, parse→write→re-parse round-trip with `_round_trip` xmlns helper per test_multidimensional_time.py precedent); 3 Red / 4 passed — Red is real: reader reads only AR-RELEASE-VERSION (DATE + PRODUCT-RELEASE → None), writer emits only AR-RELEASE-VERSION; honest passes: partial/absent read + None/empty write were already correct; two test-side fixes during Red triage (implementation untouched): getLifeCyclePeriod expects the PARENT element (keyed find) so parser input wrapped in PARENT, and AR-RELEASE-VERSION wire values changed "R23-11"→"4.3.1" (getChildElementOptionalRevisionLabelString regex requires major.minor.revision — model-side setValue accepts anything, parser-side validates)
  - [x] Step 6 — Update parser & writer (Green) — reader `getChildElementOptionalDateTime` (abstract_arxml_parser.py): ARLiteral-delegating body replaced with the CseCodeType-form typed body (instantiates DateTime — fixes the pre-existing reader type gap where DocRevision.setDate received a plain ARLiteral; DateTime IS-A ARLiteral so the ARLiteral-typed consumers are unaffected; DocRevision.setDate signature is Optional[DateTime] so this is strictly type-correct); getLifeCyclePeriod (arxml_parser.py) now reads DATE via getChildElementOptionalDateTime + AR-RELEASE-VERSION + PRODUCT-RELEASE via the matched RevisionLabelString leaf pair; setLifeCyclePeriod (arxml_writer.py) now writes DATE via setChildElementOptionalDateTime + AR-RELEASE-VERSION + PRODUCT-RELEASE in XSD sequenceOffset order (10/20/30); Green: 57/57 across the 3 target test files, 37/37 test_writer_signals_diagnostic.py (TestWriterSetLifeCyclePeriod/LifeCycleInfo consumers unaffected by the added DATE-first emission), 903/903 admin/doc/revision/documentation consumer tests
  - [x] Step 7 — Update checklist comment — 6-column format written in the Step 3 body replacement, verified at this step: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 12.4, p.392` (home doc; R4.3.1 Table 11.4 p.364 is a byte-identical reproduction), rows in markdown displayed order (`__init__`, getArReleaseVersion, setArReleaseVersion, getDate, setDate, getProductRelease, setProductRelease), all `[x]` with release R23-11; reader [x] on the three setters (parser sets), writer [x] on the three getters (writer gets), `__init__` [—]/[—]; marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: both Step 1 findings were fixed in-band and leave no deviation rows — (1) type deviation date datetime→DateTime fixed in Step 3 with the matched typed helper repaired in Step 6 (getChildElementOptionalDateTime body instantiated a plain ARLiteral despite its DateTime signature; now CseCodeType-form — Rule 0014: fixed ⇒ no row), (2) reader/writer AR-RELEASE-VERSION-only gaps fixed in Step 6 (all three attrs, XSD order); v1 tracker (docs/examples/method_deviation_by_class.md) reconciled 2026-09-24: new `## LifeCyclePeriod` section added (sync record, no-deviation row, stamp-deferred note, consumer-wiring scope note); v2 tracker has no LifeCyclePeriod entries (verified by grep); consumer-side gaps NOT fixed unilaterally (belong to the later rows in this file): readLifeCycleInfo/writeLifeCycleInfo handle PERIOD-BEGIN only (no PERIOD-END), LifeCycleInfoSet readers/writers touch no DEFAULT-PERIOD-BEGIN/DEFAULT-PERIOD-END; referenced classes: base ARObject stamped, member types RevisionLabelString (PrimitiveTypes, unstamped, queued elsewhere) + DateTime (PrimitiveTypes, unstamped 4-column checklist, queued elsewhere), consumers LifeCycleInfo/LifeCycleInfoSet queued after this row — no missing classes]
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 94/94 (test_LifeCycles.py 50 + test_life_cycle_period.py parser 3 + writer 4 + test_writer_signals_diagnostic.py 37, re-run green after black reformat of 2 test files), full suite 11061 passed/0 failed (baseline 11049 + 12 net-new: +5 model tests, +7 round-trip tests), npm run lint (flake8+ruff) clean, npm run black reformatted the 2 new/extended test files (canonical formatting only) + black-check clean (1150 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (3 attrs), most-derived base ARObject (XSD L76738), no fabrication/flattening, PDF-typed fields (RevisionLabelString/DateTime), member order = markdown displayed order, XML element order = XSD sequenceOffset 10/20/30, docstrings verbatim (class Note + attribute Notes in inline comments/getters/setters + None-no-op sentences), blank line between __init__ attribute blocks, PEP 526 annotated members, reader+writer coverage for all 3 attrs via matched leaf pairs, no open deviations, leaf-file location per spec package; 9b confirmed 2026-09-24 (batch user confirmation); stamp written

- [x] `BuildActionIoElement` — ARObject — finished, stamped `# Spec verified: R23-11` (commit: b572582c1)
  - module: M2/AUTOSARTemplates/GenericStructure/BuildActionManifest.py
  - note: Step 1 finding — orphan intake already models 5/6 attrs (category/ecucDefinitionRef/engineeringObject/role/sdgs, verbatim docstrings, ARObject base confirmed vs XSD complexType); `foreignModelReference` (aggr, ForeignModelReference) unmodeled — member class absent from src and table-less in both corpora (XSD-only, AUTOSAR_00052.xsd l.62888), not in confirmed closure → deviation row (Step 8). Reader type gap: CATEGORY read as plain ARLiteral, fix via getChildElementOptionalNameToken/setChildElementOptionalNameToken matched pair. Consume path stays helper-dispatch (readBuildActionIoElement/writeBuildActionIoElement from CREATED/INPUT/MODIFIED-DATAS wrappers).
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red)
  - [x] Step 3 — Implement model class (Green)
  - [x] Step 4 — Sync docstrings (wipe + rewrite)
  - [x] Step 5 — Write reader/writer round-trip test (Red)
  - [x] Step 6 — Update parser & writer (Green)
  - [x] Step 7 — Update checklist comment
  - [x] Step 8 — Deviations
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: BuildActionManifest test files 72 passed (parser+writer Red first: 2 failed on CATEGORY ARLiteral-vs-NameToken, then Green after matched getChildElementOptionalNameToken/setChildElementOptionalNameToken pair); full suite `uv run python scripts/run_tests.py --no-coverage` 11067 passed / 0 failed (baseline 11061 + 6 new); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I); `npm run black` + `black-check` clean (1150 files unchanged); 9b by-eye checks done (member order = Table 10.3 displayed order, XML order = XSD group sequence, docstrings verbatim by diff, blank lines between __init__ blocks, PEP 526, no `# type:`); 9b deferred to batch confirmation (user instruction 2026-09-24)

- [x] `AttributeValueVariationPoint` — ARObject — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/AttributeValueVariationPoints/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - note (2026-09-25 atpMixedString redesign, spec §9): the mixin is now interface-level (`StereotypeMixins.py`, ABC only, no ARObject base, no __init__) — this class's base list is `AttributeValueVariationPoint(ARObject, AtpMixedString, ABC)`; class statement + module changed, members/docstrings/reader-writer unchanged; 9b review below covers the new shape
  - note (2026-09-25, open drift finding from the ConditionByFormula 9b rejection — user decision pending): Table 7.2 Base row is the same derivation set (`ARObject , FormulaExpression , SwSystemconstDependentFormula`) and Table 7.10 lists AVP as a `SwSystemconstDependentFormula` subclass — the stamped base `(ARObject, AtpMixedString, ABC)` anchors ARObject directly. UPDATE 2026-09-25: re-parent APPLIED (final under the mixin decision) — class statement is now `AttributeValueVariationPoint(SwSystemconstDependentFormula, ABC)`; full suite + lint + black green after the change; a formal 9b verbatim re-diff of the AVP file is still owed at the next drift pass.
  - after `BindingTimeEnum`
  - [x] Step 1 — Sync members & description from spec — Table 7.2 (AUTOSAR_FO_TPS_GenericStructureTemplate home doc, p.210 via pdf_page.py); table identity check across the 3 R23-11 reproductions: CP SWCT Table 7.65 (p.617) + FO STDT Table 4.3 — Class/Subclasses/Aggregated-by/attribute rows byte-identical everywhere (FO GST ↔ CP SWCT md5-identical after whitespace normalization; STDT render adds one duplicated `| Attribute | Type | ... |` header row — extraction artifact); only CP SWCT carries the Package/Note/Base meta-rows (FO GST markdown render omits them): Package M2::AUTOSARTemplates::GenericStructure::VariantHandling::AttributeValueVariationPoints ✓ (matches current file), Note "...(by Sw SystemconstDependentFormula)..." (space = PDF line-break artifact; XSD group documentation L7821 confirms "SwSystemconstDependentFormula" — same artifact class as BindingTimeEnum's "codeGeneration Time"), Base ARObject, FormulaExpression, SwSystemconstDependentFormula; Class-vs-Enumeration header = Class ✓ (abstract, <<atpMixedString>>); XSD 00052 element group L7818 = EMPTY sequence (pure atpMixedString — the value IS the mixed text, modeled as _text/getText/setText) + attributeGroup L7827 BINDING-TIME (BINDING-TIME-ENUM--SIMPLE) / BLUEPRINT-VALUE (STRING--SIMPLE) / SD (STRING--SIMPLE) / SHORT-LABEL (PRIMITIVE-IDENTIFIER--SIMPLE) all xml.attribute=true — no child elements, no element-order concerns; base chain confirmed by XSD subclass composition (e.g. BOOLEAN-VALUE-VARIATION-POINT complexType L9339 = choice of AR-OBJECT + FORMULA-EXPRESSION + SW-SYSTEMCONST-DEPENDENT-FORMULA + ATTRIBUTE-VALUE-VARIATION-POINT groups + attributeGroups AR-OBJECT + ATTRIBUTE-VALUE-VARIATION-POINT): FORMULA-EXPRESSION group (L62902) is an empty sequence, SW-SYSTEMCONST-DEPENDENT-FORMULA group (L116360) contributes SYSC-STRING-REF — a member of SwSystemconstDependentFormula's OWN table, not flattened into AVP's 4-row table (Rule 0001.3); both mixin classes unmodeled in src and not queued anywhere — Step 8 finding; field-to-spec both directions: exactly bindingTime (Optional[BindingTimeEnum], freshly synced 34fbb1c6) + blueprintValue/sd (Optional[String]) + shortLabel (Optional[PrimitiveIdentifier], stamped) + _text (atpMixedString content, repo convention) — no fabricated/extra fields, member order = markdown displayed order ✓; abstract class ✓ (matches "(abstract)" header, guard pre-exists); reader/writer coverage pre-exists (parser readVariationPointProxy VALUE-ACCESS dispatch via VALUE_ACCESS_TAG_TO_CLASS L509 → readAttributeValueVariationPoint L2995 covers all 4 attrs + INTERVAL-TYPE + text; writer VALUE_ACCESS_CLASS_TO_TAG L433 → writeAttributeValueVariationPoint L4264 mirrors) — Steps 5/6 extend, NOT N/A; three drift findings to fix: (1) old checklist is 5-column WITH `# Spec verified: R23-11` stamp — stamp removed this pass (batch instruction: no marker until batch confirmation), checklist rebuilt 6-column; (2) class docstring carries Package/Base/Stereotypes lines (stamped form = Note verbatim only) — Step 4; (3) getter/setter docstrings carry "Tags: xml.attribute=true" suffix (stamped form per ShortNameFragment = Note without Tags, setter + None-no-op sentence) — Steps 3/4; tracker review: v1 tracker has NO AttributeValueVariationPoint section but its `## VariationPointProxy` entry (L1162-1169) carries a stale claim ("VALUE-ACCESS is skipped by the reader with notImplemented while the valueAccess deviation stands") — superseded by the existing dispatch, reconcile Step 8; v2 tracker has no AVP entries (only a NumericalValueVariationPoint Appendix bullet — different class, left in place)
  - [x] Step 2 — Write model class unit test (Red) — extended existing TestAttributeValueVariationPoint* in test_AttributeValueVariationPoints.py (no duplication): added new TestAttributeValueVariationPointSpecContract on the base class — verbatim class-Note assertion (XSD-confirmed "SwSystemconstDependentFormula" one-word form), no-`__init__`-docstring assertion, PDF-typed accessor annotations via typing.get_type_hints (repo style per TestShortNameFragment) for bindingTime→Optional[BindingTimeEnum] + blueprintValue/sd→Optional[String] + shortLabel→Optional[PrimitiveIdentifier] (value + return on setters), getter-docstrings-Note-without-Tags assertion, setter-docstrings-Note+None-no-op-sentence assertion; 3 Red / 16 passed — Red is real: class docstring carries extra Package/Base/Stereotypes lines, getter AND setter docstrings carry the "Tags: xml.attribute=true" suffix; honest passes: `__init__` already docstring-less and all four accessors already spec-typed; existing instantiation/abstract-guard/member round-trip/None-no-op/chaining coverage (13 tests) kept green
  - [x] Step 3 — Implement model class (Green) — class body kept except docstrings/checklist (fields already spec-correct): field-to-spec both directions re-verified — exactly bindingTime (Optional[BindingTimeEnum]) + blueprintValue/sd (Optional[String]) + shortLabel (Optional[PrimitiveIdentifier]) + _text (atpMixedString content, repo convention, no spec row) — no fabricated/extra fields; Base stays ARObject ((ARObject, ABC) — multi-inheritance mixin bases FormulaExpression/SwSystemconstDependentFormula unmodeled per XSD groups, see Step 1/Step 8); abstract guard pre-exists ✓ (matches "(abstract)" header); member/accessor order bindingTime→blueprintValue→sd→shortLabel matches markdown displayed order (Rule 0001.11); setters keep None-no-op + return-self shape; PEP 526 annotated members with blank line between attribute blocks; imports untouched (no re-sort); 19/19 Green in test_AttributeValueVariationPoints.py
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body replacement (no stale wording survives: old class docstring's Package/Base/Stereotypes header lines + Attributes block wiped; old "Tags: xml.attribute=true" suffixes wiped from ALL getter/setter docstrings); class docstring = Table 7.2 Note verbatim (XSD group documentation L7821 form — "SwSystemconstDependentFormula" one word, per BindingTimeEnum extraction-artifact precedent); inline `__init__` comments = attribute Notes verbatim incl. Tags (xml.attribute=true) — already correct, kept; getter docstrings = Note verbatim without Tags (stamped ShortNameFragment form); setter docstrings = Note verbatim without Tags + "A None value is a no-op…" sentence; no `__init__` docstring; getText/setText keep the repo-form atpMixedString docstrings (convenience accessors, no spec row)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — NO Red: reader/writer coverage pre-exists and is complete (dispatch + helper on both sides, per Step 1); added the 3 missing regression assertions anyway — writer test_writer_variation_point.py new TestWriteAttributeValueVariationPointRoundTrip: (1) test_round_trip_value_access_fields_preserved (fully-populated NumericalValueVariationPoint → save → load → per-field value assertions incl. bindingTime camelCase round-trip + text — the existing TestVariationPointProxyRoundTrip only asserted isinstance on an EMPTY value access), (2) test_no_value_access_writes_no_wrapper (None valueAccess → no VALUE-ACCESS element); parser test_arxml_parser_variation_point.py: test_read_value_access_empty_wrapper (empty VALUE-ACCESS wrapper → valueAccess None); 20/20 Green across the 2 files (17 before + 3 net-new) — pass-immediately honestly noted (matches the BindingTimeEnum Step 5/6 precedent for consumer-covered classes); no element-order test: XSD element group L7818 is an EMPTY sequence (all 4 members are xml.attribute=true, attribute order not significant)
  - [x] Step 6 — Update parser & writer (Green) — NO changes needed: every spec attribute already has matched pair coverage — parser readAttributeValueVariationPoint (arxml_parser.py L2995) reads BINDING-TIME via BINDING_TIME_XML_MAP (token→camelCase) + BLUEPRINT-VALUE/SD as String + SHORT-LABEL as PrimitiveIdentifier + mixed text via setText; writer writeAttributeValueVariationPoint (arxml_writer.py L4264) mirrors (camelCase→UPPER-KEBAB token + getValue()s + element.text); dispatch VALUE_ACCESS_TAG_TO_CLASS (L509) / VALUE_ACCESS_CLASS_TO_TAG (L433) covers all 8 concrete wire tags incl. LIMIT; Rule 0013.2 name pairs verified (setBindingTime↔readAttributeValueVariationPoint consumer dispatch, no cross pairs); no chained mutator calls; Green: 20/20 in the 2 round-trip test files
  - [x] Step 7 — Update checklist comment — 6-column format written in the Step 3 body replacement, verified at this step: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 7.2, p.210` (home doc; CP SWCT Table 7.65 p.617 + FO STDT Table 4.3 are row-identical reproductions), rows in markdown displayed order (`__init__`, getBindingTime, setBindingTime, getBlueprintValue, setBlueprintValue, getSd, setSd, getShortLabel, setShortLabel, getText, setText — accessors pair with the 4 attribute rows, getText/setText appended last matching source order), all rows `[x]` with release R23-11; reader [x] on the four setters + setText (parser sets), writer [x] on the four getters + getText (writer gets), `__init__` [—]/[—] — also fixed the old checklist's convention violation (getText/setSd/etc. rows had reader+writer both [x] on the same row); marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3); the three Step 1 drift findings were fixed in-band and leave no rows: (1) pre-existing 5-column checklist + `# Spec verified: R23-11` stamp (pre-release-column vintage, old getText/setSd reader/writer double-[x] rows) rebuilt 6-column with stamp withheld per batch instruction, (2) verbose class docstring wiped → Note verbatim, (3) Tags suffix wiped from getter/setter docstrings — Rule 0014: fixed ⇒ no row; v1 tracker (docs/examples/method_deviation_by_class.md) reconciled 2026-09-24: new `## AttributeValueVariationPoint` section added (sync record, no-deviation row, stamp-deferred note, mixin-base scope note) and the stale `## VariationPointProxy` claim ("VALUE-ACCESS is skipped by the reader with notImplemented while the valueAccess deviation stands") superseded by a Resolution Note (reader/writer dispatch exists; `valueAccess` is typed `Optional[AttributeValueVariationPoint]`, no ARObject placeholder); v2 tracker has no AVP entries (its NumericalValueVariationPoint Appendix bullet is a different class — left in place per AutoCollectEnum precedent); referenced-class findings (not deviations, not blocking): mixin bases FormulaExpression (XSD group L62902 = empty sequence) and SwSystemconstDependentFormula (XSD group L116360 contributes SYSC-STRING-REF to its OWN table — Rule 0001.3 no-flattening) are unmodeled in src and not queued anywhere (grep Group8/Group9 = 0 hits); abstract subclass AbstractEnumerationValueVariationPoint (extended-meta-model derivation) likewise unmodeled — the 7 concrete subclasses exist in the same file and in both dispatch maps; member types BindingTimeEnum (synced 34fbb1c6, marker deferred), String (stamped Table 4.63), PrimitiveIdentifier (stamped), base ARObject (stamped), consumer VariationPointProxy (stamped Table 7.61) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 82/82 (test_AttributeValueVariationPoints.py 19 + test_arxml_parser_variation_point.py 9 + test_writer_variation_point.py 12 + test_variation_point_capability.py + test_VariantHandling.py), re-run green after black reformat of the model test file; full suite `uv run python scripts/run_tests.py --no-coverage` 11078 passed / 0 failed (baseline 11067 + 11 net-new: +8 model spec-contract tests, +3 round-trip tests); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I); `npm run black` reformatted the extended model test file (canonical formatting only) + `black-check` clean (1150 files unchanged); 9b blind-spot checks walked: fields↔spec both directions exact (4 attrs + _text), most-derived base ARObject (XSD subclass composition L9339; mixin groups FORMULA-EXPRESSION empty / SW-SYSTEMCONST-DEPENDENT-FORMULA member belongs to its own class — no flattening), no fabrication, PDF-typed fields, member/accessor/checklist order = markdown displayed order, XML element order N/A (XSD element group L7818 empty sequence, all 4 members xml.attribute=true), docstrings verbatim (class Note XSD-confirmed form; inline Notes incl. Tags; getters/setters Note without Tags + None-no-op sentences — test-enforced), blank line between __init__ attribute blocks, PEP 526 annotated members, no `# type:`, reader+writer coverage for all 4 attrs + text via VALUE-ACCESS dispatch + shared helpers, no open deviations, marker withheld (batch instruction), Rule 0007 non-leaf location per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)
- [x] `FormulaExpression` — abstract <<atpMixedString>> base — FMXF Table C.5 (caption-after-content), pp.73-74 — finished, stamped `# Spec verified: R23-11` (commit: 88ed82bed)
  - module: M2/AUTOSARTemplates/GenericStructure/FormulaLanguage.py (NEW leaf module — Rule 0007 leaf convention; export via GenericStructure/__init__.py + models/__init__.py, LifeCycles precedent)
  - note (2026-09-25 Step 1 resolution): true table id = C.5, pp.73-74 — the PDF itself renders captions BELOW their tables (pypdf verification; pdf_page.py regex only matches top captions, so it reported "no match"); Table D.26 (CP TimingExtensions) is a third mislabeled artifact (content = Frame). Markdown label "atpString Reference" = space-wrap of `atpStringReference` (XSD L62910). Both attrs are <<atpDerived>> (XSD group FORMULA-EXPRESSION L62902 = empty sequence, "Association skipped") → fields + accessors only, reader/writer [—], Steps 5/6 N/A. Naming per Rule 0001.5 (contextComponentRef→contextComponentRefs precedent): atpReferences / atpStringReferences, List[Referrable], addXxx(value)+getXxxs() shape (Rule 0001.6 — kind ref, no create factory). Base = ARObject → FormulaExpression(ARObject, ABC); no AtpMixedString mixin per standing decision (stereotype recorded in checklist comment); 9b to re-confirm.
  - [x] Step 1 — Sync members & description from spec
  - note (2026-09-25, moved ahead of ConditionByFormula — dependency-first, user decision): direct parent of SwSystemconstDependentFormula (and transitively ConditionByFormula/AVP). Real table = FMXF block under the mislabeled caption "Table C.4: ConditionByFormula" (caption-shift: the block BEFORE the caption line is Table 7.5's ConditionByFormula content; the block AFTER the caption is FormulaExpression — Package FormulaLanguage, Base ARObject, abstract, <<atpMixedString>>, Subclasses CompuGenericMath/FMFormulaByFeaturesAndAttributes/SwSystemconstDependentFormula/TDEventOccurrenceExpressionFormula/TimingConditionFormula, attrs atpReference/atpStringRef (Referrable, *, ref)); Table E.45 "FormulaExpression" (GST appendix) is ALSO a mislabeled artifact (content = GenericModelReference) — do not use; mirror multi-page-fragment handling per the FMXF C.4/C.5 shift
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a: 11981 passed / 0 failed (full suite incl. 5 new model tests + integration round-trip), lint + black-check clean; 9b confirmed 2026-09-25 (user): no-mixin base `FormulaExpression(ARObject, ABC)` and the Stereotypes-suffix accessor handling explicitly approved; stamp written
- [x] `SwSystemconstDependentFormula` — abstract <<atpMixedString>> base — FO GST Table 7.10, p.240 — finished, stamped `# Spec verified: R23-11` (commit: f05e21d49)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/__init__.py (non-leaf package, Rule 0007)
  - note (2026-09-25 Step 1 resolution): Table 7.10 p.240 (GST top caption — pdf_page.py match). Attrs sysc/syscString (SwSystemconst, 0..1, KIND REF — XSD group L116360 = choice{SYSC-STRING-REF, SYSC-REF}, plain REF + DEST=SW-SYSTEMCONST--SUBTYPES-ENUM) → `syscRef`/`syscStringRef: Optional[RefType]` (Rule 0001.5 Ref suffix), set/get + None no-op. XML-BEARING (real reader/writer) — abstract class owns reusable readSwSystemconstDependentFormula/writeSwSystemconstDependentFormula helpers (Rule 0001.7); composing complexTypes (12): CONDITION-BY-FORMULA, BLUEPRINT-FORMULA, BOOLEAN/FLOAT/INTEGER-VALUE-VARIATION-POINT, LIMIT, 5× DIAGNOSTIC-*-ENUM-VALUE-VARIATION-POINT, FM-CONDITION-BY-FEATURES-AND-SW-SYSTEMCONSTS. Wired this session (isinstance-guarded — inert until AVP/ConditionByFormula re-parent): CONDITION-BY-FORMULA + VALUE-ACCESS family; pending their own rows: BLUEPRINT-FORMULA, FM-CONDITION-…, DIAGNOSTIC-×5. Base shape (AtpMixedString, FormulaExpression, ABC) — mixin first per standing decision, 9b to confirm.
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red)
  - [x] Step 3 — Implement model class (Green) — `SwSystemconstDependentFormula(AtpMixedString, FormulaExpression, ABC)` (mixin first per standing decision), abstract guard, syscRef/syscStringRef Optional[RefType]
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — new class; docstrings written verbatim from Table 7.10 (getters drop the Tags suffix, setters carry None-no-op)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — 5 tests (read refs / read absent / write refs / write absent / round-trip) in parser+writer variation-point files, direct on probe subclass
  - [x] Step 6 — Update parser & writer (Green) — readSwSystemconstDependentFormula/writeSwSystemconstDependentFormula helpers; wired isinstance-guarded into read/writeConditionByFormula + read/writeAttributeValueVariationPoint (inert until AVP/ConditionByFormula re-parent)
  - [x] Step 7 — Update checklist comment
  - [x] Step 8 — Deviations — none blocking; 8 of 12 composing complexTypes not yet modeled (BlueprintFormula, FM-Condition-…, 5× Diagnostic enum VVPs — pending their rows); guards documented
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a: full suite ALL TESTS PASSED (incl. integration round-trip), lint + black-check clean (2 ruff I001 import-sort fixes in test files); 9b confirmed 2026-09-25 (user): (AtpMixedString, FormulaExpression, ABC) base and the guarded wiring explicitly approved; stamp written
  - note (2026-09-25 same-day redesign, FINAL after revert): AtpMixedString was briefly re-based to a real ARObject-derived class, then REVERTED to the interface mixin (user analysis: EcucQueryExpression — Base plain ARObject, stereotyped — has no correct placement under the class design; 0 spec Base rows ever list AtpMixedString; the stereotype spans >=4 unrelated hierarchies). FINAL: `AtpMixedString(ABC)` mixin with class-attr default; `SwSystemconstDependentFormula(FormulaExpression, ABC)` stands (mixin transitively via FormulaExpression). Full hierarchy reference: docs/plan/atp_mixed_string_hierarchy.md
- [ ] `ConditionByFormula` — ARObject — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - note (2026-09-25 atpMixedString redesign, spec §9): the mixin is now interface-level (`StereotypeMixins.py`, ABC only, no ARObject base, no __init__) — this class's base list is `ConditionByFormula(ARObject, AtpMixedString)` (spec Base ARObject now anchored explicitly); class statement changed, members/docstrings/reader-writer unchanged; 9b review below covers the new shape — **SUPERSEDED 2026-09-25, see 9b rejection note below**
  - after `BindingTimeEnum`
  - after `FormulaExpression` (added 2026-09-25 — dependency-first, moved ahead of this row)
  - after `SwSystemconstDependentFormula` (added 2026-09-25 — dependency-first, moved ahead of this row)
  - note (2026-09-25 9b REJECTED, user: "parent is incorrect"): the Table 7.5 Base row `ARObject , FormulaExpression , SwSystemconstDependentFormula` is a derivation SET — the most-derived direct parent must be `SwSystemconstDependentFormula`, not `ARObject` (§4 prose: "ConditionByFormula derives from SwSystemconstDependentFormula"; XSD complexType L21886 composes AR-OBJECT + FORMULA-EXPRESSION + SW-SYSTEMCONST-DEPENDENT-FORMULA groups). Parents were unmodeled → `FormulaExpression` + `SwSystemconstDependentFormula` rows moved ahead of this row; sync them first in fresh sessions, then re-run this row's Step 9 (9a re-run + 9b). Steps 1–8 stand; Step 2/3 base-anchoring assertions and the 9b base check must be revisited against the new chain. **UPDATE 2026-09-25:** the re-parent was APPLIED (final under the mixin decision) — class statement is now `ConditionByFormula(SwSystemconstDependentFormula)` (SSCDF moved above it in VariantHandling/__init__.py; MRO ConditionByFormula → SSCDF → FormulaExpression → ARObject, mixin via FormulaExpression); Step 9 re-run (9a + 9b + stamp) still pending.
  - [x] Step 1 — Sync members & description from spec — Table 7.5 (AUTOSAR_FO_TPS_GenericStructureTemplate home doc, p.231 via pdf_page.py); table identity check across the 3 genuine R23-11 reproductions: CP SWCT Table 7.62 (p.613) + CP SystemTemplate Table F.35 — Package/Note/Base/Aggregated-by/attribute rows byte-identical everywhere; FO FMXF "Table C.4: ConditionByFormula" is a MISLABELED extraction artifact (content = the FormulaExpression class table: Package FormulaLanguage, Base ARObject, Subclasses row) — documented, not used; Class-vs-Enumeration header = Class ✓ (`<<atpMixedString>> ConditionByFormula`, NOT abstract — XSD 00052 complexType L21886 abstract="false" mixed="true"); Package M2::AUTOSARTemplates::GenericStructure::VariantHandling ✓ (non-leaf → class in __init__.py per Rule 0007, matches current location); Note verbatim in place; Base ARObject, FormulaExpression, SwSystemconstDependentFormula (XSD complexType choice = AR-OBJECT + FORMULA-EXPRESSION + SW-SYSTEMCONST-DEPENDENT-FORMULA + CONDITION-BY-FORMULA groups, attributeGroups AR-OBJECT + CONDITION-BY-FORMULA — same mixin pattern as AVP: FORMULA-EXPRESSION group L62902 empty sequence (only skipped atpDerived assoc), SW-SYSTEMCONST-DEPENDENT-FORMULA group L116360 contributes SYSC-STRING-REF/SYSC-REF belonging to SwSystemconstDependentFormula's OWN table — Rule 0001.3 no-flattening; both mixins unmodeled in src, Step 8 finding); Aggregated by VariationPoint.swSyscond (XSD SW-SYSCOND L130054, seqOffset=30) + VariationPointProxy.conditionAccess (CONDITION-ACCESS L130102); attribute displayed order = single row bindingTime (BindingTimeEnum, 1, attr, Note + Tags: xml.attribute=true; XSD attributeGroup L21877 BINDING-TIME BINDING-TIME-ENUM--SIMPLE xml.attribute="true"); bindingTime field pre-exists typed Optional[BindingTimeEnum] ✓ (enum synced 34fbb1c6); XSD element group L21862 = EMPTY sequence + complexType mixed="true" (pure atpMixedString — value IS the mixed text, AVP precedent `_text`/getText/setText) → REAL MODEL GAP: ConditionByFormula has NO _text/getText/setText (fix Step 3); reader/writer coverage pre-exists (parser readConditionByFormula L1271 + consumers readVariationPoint SW-SYSCOND L1301 / readVariationPointProxy CONDITION-ACCESS L2978; writer writeConditionByFormula L1150 + consumers writeVariationPoint L1182 / writeVariationPointProxy L4246) — BUT neither reads nor writes the mixed text (fix Step 6) — Steps 5/6 extend, NOT N/A; three drift findings to fix: (1) old checklist is 5-column WITH `# Spec verified: R23-11` stamp — stamp removed this pass (batch instruction: no marker until batch confirmation), checklist rebuilt 6-column; (2) class docstring carries Package/Base/Stereotypes/Attributes blocks (stamped form = Note verbatim only) — Step 4; (3) mixed-text reader/writer gap — Step 6; tracker review: v1 has NO dedicated ConditionByFormula section (only VariationPointProxy-section mentions L1156/L1180 — the L1180 stale valueAccess claim already superseded by the AVP agent's Resolution Note this batch); v2 has none
  - [x] Step 2 — Write model class unit test (Red) — extended existing TestConditionByFormula in test_VariantHandling.py with new TestConditionByFormulaSpecContract (no duplication; mirrors TestAttributeValueVariationPointSpecContract): verbatim class-Note assertion (Table 7.5), no-`__init__`-docstring assertion, PDF-typed accessor annotations via typing.get_type_hints (repo style per AVP tests) for bindingTime→Optional[BindingTimeEnum] (value + return on setter), getter-docstring-Note-without-Tags + setter-docstring-Note+None-noop assertions, getText default-None + setText round-trip/chaining/None-noop tests for the <<atpMixedString>> content; 5 Red / 42 passed — Red is real: class docstring carries extra Package/Base/Stereotypes/Attributes blocks (Step 4), getter AND setter docstrings are line-wrapped rather than the verbatim single-line Note form (Step 4), getText/setText do not exist (Step 3); honest passes: `__init__` already docstring-less and bindingTime already spec-typed Optional[BindingTimeEnum]
  - [x] Step 3 — Implement model class (Green) — whole class body replaced in one pass; field-to-spec both directions: exactly bindingTime (Optional[BindingTimeEnum], pre-existing spec-correct — kept) + _text (Optional[str], atpMixedString content per XSD empty element group L21862 + mixed="true" complexType L21886, repo convention per stamped AVP, no spec row) — the missing mixed-string support was the Step 1 model gap, added getText/setText in the stamped AVP form; Base stays ARObject (mixin groups FormulaExpression/SwSystemconstDependentFormula unmodeled per XSD, see Step 1/Step 8); concrete class (XSD abstract="false", spec header has no "(abstract)"); member/accessor order bindingTime→_text matches AVP convention (spec attrs then mixed content); setters keep None-no-op + return-self shape; PEP 526 annotated members with blank line between the two attribute blocks; imports untouched (no re-sort, none needed); 47/47 Green in test_VariantHandling.py
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body replacement (no stale wording survives: old class docstring's Package/Base/Stereotypes header lines + Attributes block wiped; old line-wrapped getter/setter docstrings replaced); class docstring = Table 7.5 Note verbatim (single line); inline `__init__` comment = attribute Note verbatim incl. Tags (xml.attribute=true); getter docstring = Note verbatim (single line); setter docstring = Note verbatim + "A None value is a no-op…" sentence; no `__init__` docstring; getText/setText keep the repo-form atpMixedString convenience docstrings (no spec row), parenthetical adapted to this class ("the formula expression")
  - [x] Step 5 — Write reader/writer round-trip test (Red) — extended the 2 existing round-trip files (extend, no duplication): parser test_arxml_parser_variation_point.py new TestReadConditionByFormula (SW-SYSCOND read with bindingTime + mixed-text value assertions, no-text → getText() None, CONDITION-ACCESS read on VariationPointProxy with bindingTime + mixed text); writer test_writer_variation_point.py new TestWriteConditionByFormula (SW-SYSCOND write: BINDING-TIME attr + element.text, CONDITION-ACCESS write via writeVariationPointProxy, no-text → element.text None) + TestConditionByFormulaRoundTrip (document-level parse→write→re-parse via PostBuildVariantCriterion variation point, per-field value assertions incl. formula text 'defined(sysc) &&amp; sysc == "A"' — field values, not len); 5 Red / 22 passed — Red is real: reader does not read element text (getText() None), writer does not write it (element.text None), round-trip drops the text; honest passes: no-text read + no-text write were already correct
  - [x] Step 6 — Update parser & writer (Green) — reader readConditionByFormula (arxml_parser.py): added the AVP-form mixed-text read (`element.text` → setText) after the BINDING-TIME attribute read; writer writeConditionByFormula (arxml_writer.py): added the mirrored getText() → element.text emission — both fixes close the Step 1 reader/writer gap (atpMixedString content); BINDING-TIME handling and both consumers (readVariationPoint SW-SYSCOND / readVariationPointProxy CONDITION-ACCESS + write counterparts) untouched; Rule 0013.2 name pairs verified (setBindingTime/setText set by readConditionByFormula, getBindingTime/getText got by writeConditionByFormula — no cross pairs, no chained mutators); one test-side fix during Green (writer emits CONDITION-ACCESS under the VARIATION-POINT-PROXY element — writer output was correct, the direct find was a test bug); Green: 80/80 across the 4 target test files, 5049/5049 across the full parser+writer suites
  - [x] Step 7 — Update checklist comment — 6-column format written in the Step 3 body replacement, verified at this step: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 7.5, p.231` (home doc; CP SWCT Table 7.62 p.613 + CP ST Table F.35 are row-identical reproductions; FO FMXF C.4 caption is a mislabeled extraction artifact), rows in source order (`__init__`, getBindingTime, setBindingTime, getText, setText), all `[x]` with release R23-11; reader [x] on setBindingTime/setText (parser sets), writer [x] on getBindingTime/getText (writer gets), `__init__` [—]/[—]; old pre-existing `# Spec verified: R23-11` stamp REMOVED (batch instruction: no marker until batch confirmation); marker NOT written
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly bindingTime + _text); the three Step 1 findings were fixed in-band and leave no rows: (1) pre-existing 5-column checklist + `# Spec verified: R23-11` stamp rebuilt 6-column with stamp withheld per batch instruction, (2) verbose class docstring + line-wrapped method docstrings wiped → Table 7.5 Note verbatim (class + inline comment incl. Tags + getter + setter with None-no-op sentence), (3) missing <<atpMixedString>> content support added (_text/getText/setText per stamped AVP convention) with reader/writer text wired in Step 6 — Rule 0014: fixed ⇒ no row; v1 tracker (docs/examples/method_deviation_by_class.md) reconciled 2026-09-24: new `## ConditionByFormula` section added (sync record, no-deviation row, stamp-deferred note, mixin-base + FO-FMXF-C.4-mislabel notes) — previously the class appeared only inside the `## VariationPointProxy` section prose (L1156 child-serializers note, accurate; L1180 stale valueAccess claim already superseded by the AVP agent's Resolution Note this batch); v2 tracker has no ConditionByFormula entries (verified by grep); referenced-class findings (not deviations, not blocking): mixin bases FormulaExpression (XSD group L62902 = sequence with only skipped atpDerived associations) and SwSystemconstDependentFormula (XSD group L116360 contributes SYSC-STRING-REF/SYSC-REF to its OWN table — Rule 0001.3 no-flattening) are unmodeled in src and not queued anywhere (same finding already recorded by the AVP row); member types BindingTimeEnum (synced 34fbb1c6, marker deferred), base ARObject (stamped), consumers VariationPoint (queued later in this file, Table 7.4) + VariationPointProxy (stamped Table 7.61) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 74/74 (test_VariantHandling.py 47 + test_arxml_parser_variation_point.py 12 + test_writer_variation_point.py 15, re-run green after black reformat of 2 test files), full suite `uv run python scripts/run_tests.py --no-coverage` 11092 passed / 0 failed (baseline 11078 + 14 net-new: +7 model spec-contract tests, +3 parser tests, +4 writer tests incl. the document-level round-trip); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I); `npm run black` reformatted the 2 extended test files (canonical formatting only) + `black-check` clean (1150 files unchanged); 9b blind-spot checks walked: fields↔spec both directions exact (bindingTime + _text), most-derived base ARObject (XSD complexType L21886 abstract="false" composes AR-OBJECT group; mixin groups unmodeled — no flattening), no fabrication, PDF-typed field (BindingTimeEnum), member/accessor/checklist order = markdown displayed order, XML element order N/A for the class itself (XSD element group L21862 empty sequence, single member xml.attribute=true), docstrings verbatim (class Note + attribute Note in inline comment incl. Tags + getter + setter with None-no-op sentence — test-enforced), blank line between __init__ attribute blocks, PEP 526 annotated members, no `# type:`, reader+writer coverage for bindingTime + mixed text via both SW-SYSCOND and CONDITION-ACCESS paths, no open deviations, marker withheld (batch instruction), Rule 0007 non-leaf location per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `MixedContentForOverviewParagraph` — ARObject (abstract, <<atpMixedString>>) — GST Table E.5x (locate at Step 1)
  - module: M2/MSR/Documentation/TextModel/LanguageDataModel.py (sibling of MixedContentForLongName/Paragraph)
  - note (2026-09-25, from the atpMixedString hierarchy audit — docs/plan/atp_mixed_string_hierarchy.md): markdown-verified Base = ARObject; Package M2::MSR::Documentation::TextModel::InlineTextModel; dependency of LOverviewParagraph (currently missing this base) and SlOverviewParagraph
  - [x] Step 1 — Sync members & description from spec — Table 9.3, NOT "E.5x" (the numeric home table exists): AUTOSAR_FO_TPS_GenericStructureTemplate.md, caption `Table 9.3: MixedContentForOverviewParagraph` L7537, p.290 via pdf_page.py (matched by name AND --table 9.3, FO GST line); the page-split renders the meta block (Class/Package/Note/Base L7525-7529) ABOVE the caption and the attribute body (L7541-7554) BELOW it; R4.3.1 reproduction Table 8.3 (caption L6130, p.252) content-identical (only the glyph[lessmuch/greatermuch] cosmetics); Class-vs-Enumeration header = Class ✓ (`<<atpMixedString>> MixedContentForOverviewParagraph (abstract)`; XSD 00052 group L81331 appinfo stereotypes "atpMixedString,atpObject"); Package M2::MSR::Documentation::TextModel::InlineTextModel — file stays LanguageDataModel.py per row note (stamped siblings MixedContentForLongName/Paragraph spec-Package DataDictionary/InlineTextModel but live in the same module — established placement); Note "This is the text model of a restricted paragraph item within a documentation. Such restricted paragraphs are used mainly for overview items, e.g. desc." verbatim (XSD documentation L81335 byte-identical); Base = ARObject ✓ (hierarchy audit corroborated; no WhitespaceControlled on THIS row — that is the PlainText/Verbatim siblings) → anchor `(ARObject, AtpMixedString, ABC)` per the standing mixin decision (hierarchy doc target shape, line 136); Subclasses row LOverviewParagraph, SlOverviewParagraph; 10 attribute rows in displayed order: br (Br, 1, aggr, no offset), e (EmphasisText, 1, aggr, offset 60 — Note "This is emphasis text." NOT "emphasized" as in the MCFParagraph row; XSD L81381 agrees), ft (SlOverviewParagraph, 1, aggr, no offset), ie (IndexEntry, 1, aggr, 100), sub (Superscript, 1, attr, 90 — Note "This is superscript text."), sup (Superscript, 1, attr, 80 — Note "This is subscript text."), trace (Traceable, 1, ref, no offset), tt (Tt, 1, aggr, 30), xref (Xref, 1, aggr, 40), xrefTarget (XrefTarget, 1, aggr, 50); SPEC-INTERNAL ODDITY recorded: the sub/sup Notes are swapped relative to the MCFParagraph family (sub→superscript text, sup→subscript text) — the R23-11 XSD documentation carries the identical swapped wording (SUB element L81393 / SUP element L81385), so verbatim copy is the correct disposition (Rule 0015: PDF wins), NOT a transcription bug to fix; trace kind ref → field traceRef per Rule 0001.5 (XSD element TRACE-REF = REF simpleContent + DEST=TRACEABLE--SUBTYPES-ENUM required; same name+kind mapping the stamped MCFParagraph applies to its `trace` row); XSD ground truth: group MIXED-CONTENT-FOR-OVERVIEW-PARAGRAPH L81331 = choice{BR, FT→SL-OVERVIEW-PARAGRAPH, TRACE-REF, TT(30), XREF(40), XREF-TARGET(50), E(60), SUP(80), SUB(90), IE(100)} — NOT an empty sequence (the orchestrator's empty-group guess verified FALSE); complexType L-OVERVIEW-PARAGRAPH L76241 (mixed=true, abstract=false) = choice{AR-OBJECT, LANGUAGE-SPECIFIC, MIXED-CONTENT-FOR-OVERVIEW-PARAGRAPH, L-OVERVIEW-PARAGRAPH} + attributeGroups AR-OBJECT/LANGUAGE-SPECIFIC/L-OVERVIEW-PARAGRAPH(BLUEPRINT-VALUE) → the LOverviewParagraph re-parent is XSD-verified and EXPANDS the L-2 legal XML surface (so Steps 5/6 wire real serialization, model-only is NOT enough); the L-OVERVIEW-PARAGRAPH own group L76223 is the empty one (own contribution = BLUEPRINT-VALUE XML attrib only); member types Br/EmphasisText/IndexEntry/Superscript/Tt/Xref/XrefTarget/Traceable all exist in InlineTextElements; SlOverviewParagraph NOT in src (queued as the NEXT Group8 row — another agent's row) → referenced-missing for THIS row, defers the helper's FT branch; serialization plan: no own element (abstract choice group) → family pattern readMixedContentForOverviewParagraph/writeMixedContentForOverviewParagraph helpers wired into the L-2 consumer paths (parser readLOverviewParagraph L1748, writer setLOverviewParagraph L1467); consumer drift (Rule 0012.3): LOverviewParagraph re-parents to `(MixedContentForOverviewParagraph, LanguageSpecific)` in this change set (LParagraph precedent); tracker grep: v1 + v2 have NO MixedContentForOverviewParagraph entries; mirror test target: tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_LanguageDataModel.py
  - [x] Step 2 — Write model class unit test (Red) — extended tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_LanguageDataModel.py: new TestMixedContentForOverviewParagraph (6 tests, style of the in-file TestMixedContentForParagraph + the Group8 spec-contract pattern) — test_abstract_guard_and_defaults (TypeError on direct instantiation + all 10 field defaults None + mixin mixedString default), test_base_anchoring (`__bases__ == (ARObject, AtpMixedString, ABC)` — the hierarchy-doc target shape, per the ConditionByFormula base-anchoring precedent), test_docstring_verbatim (inspect.cleandoc == Table 9.3 Note), test_typed_getters_and_setters (all 10 accessors round-trip identity + None no-op; ft/traceRef exercised with object() — runtime-setter is type-agnostic), test_mixin_accessors (getMixedString/setMixedString round-trip + None no-op — stereotype-inherent, no spec rows), test_annotation_hints (get_type_hints for the 8 resolvable member pairs Br/E/Ie/Sub/Sup/Tt/Xref/XrefTarget; RAW PEP-563 annotation-string asserts for ft `'Optional["SlOverviewParagraph"]'` + traceRef `'Optional[Traceable]'` — get_type_hints cannot resolve either: SlOverviewParagraph does not exist yet, Traceable is BlockElements.RequirementsTracing imported TYPE_CHECKING-only because TextModel↔BlockElements already form a managed runtime cycle); PLUS the consumer-drift assertions on TestLOverviewParagraph: base-chain rewritten to `issubclass(LOverviewParagraph, MixedContentForOverviewParagraph)` (+ LanguageSpecific + ARObject) and new test_l_overview_paragraph_inherits_mixed_content_accessors (setTt/getTt via the new base, TestLParagraph style); module-level test imports added (abc.ABC, ARObject, AtpMixedString, Br/Xref/XrefTarget from InlineTextElements, MixedContentForOverviewParagraph) sorted per ruff I001 (tests enforce it); Red confirmed: collection ImportError — cannot import name 'MixedContentForOverviewParagraph' (the class does not exist) — the genuine new-class Red
  - [x] Step 3 — Implement model class (Green) — new `MixedContentForOverviewParagraph(ARObject, AtpMixedString, ABC)` in LanguageDataModel.py (hierarchy-doc target shape; abstract TypeError guard per family precedent; AtpMixedString mixin imported from StereotypeMixins — getMixedString/setMixedString inherited, class-level mixedString default, no mixin __init__); members = exactly the 10 Table 9.3 attribute rows in displayed order (br/e/ft/ie/sub/sup/traceRef/tt/xref/xrefTarget — trace is name+ref-kind → `traceRef` per Rule 0001.5, XSD element TRACE-REF; no create*/add* — not a Referrable child, no `*` mults); PEP 526 annotated fields with blank line between every attribute block (Rule 0008); `ft: Optional["SlOverviewParagraph"]` forward reference (class does not exist in src — queued as the NEXT Group8 row, referenced-missing, Rule 0001.10 non-blocking) and `traceRef: Optional[Traceable]` stays TYPE_CHECKING-only (Traceable lives in BlockElements.RequirementsTracing; TextModel↔BlockElements already form a managed runtime cycle so a runtime import is not safe); module import changes: Br/Xref/XrefTarget promoted from TYPE_CHECKING to the runtime InlineTextElements import (needed for get_type_hints on the new accessors; InlineTextElements already runtime-imports BlockElements.Url so no new cycle edge) and the TYPE_CHECKING block deduplicated (now Std/Xdoc/Xfile + SlParagraph + Traceable) with the Traceable path CORRECTED from InlineTextElements (which never exported it — latent wrong-module bug surfaced by this sync) to BlockElements.RequirementsTracing; file position: the class sits between LanguageSpecific and LOverviewParagraph (dependency-first — Python evaluates base names at class creation; the LParagraph precedent places the concrete L* subclass below its mixed-content base); CONSUMER RE-PARENT applied in the same change set (Rule 0012.3): `LOverviewParagraph(MixedContentForOverviewParagraph, LanguageSpecific)` — MRO verified LOverviewParagraph → MixedContentForOverviewParagraph → LanguageSpecific → ARObject → AtpMixedString → ABC; blueprintValue member and L-2 serialization helpers untouched (their Step-9.91 rows stand); Green: test_LanguageDataModel.py 44/44 (36 pre-existing + 6 new MCFOP tests + 1 LOP base-chain rewrite + 1 LOP inherits-accessors test), TextModel dir 127/127, MSR models subtree 723/723, package import clean
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body (new class: no stale wording could exist anywhere, wipe trivially satisfied — the intake had no MixedContentForOverviewParagraph at all); docstrings written verbatim from the Table 9.3 markdown and MECHANICALLY DIFFED (script comparing inspect.cleandoc + __init__ source comments against the L7528 Note cell and the 10 attribute Note cells of L7545-7554): class docstring = Note verbatim ✓; inline `__init__` comment = attribute Note verbatim INCLUDING the Tags tail (offsets e=60/ie=100/sub=90/sup=80/tt=30/xref=40/xrefTarget=50; br/ft/traceRef carry no Tags in the markdown) ✓; getter docstring = Note sans Tags (MixedContentForUnitNames sibling convention) ✓; setter docstring = Note sans Tags + single-line "A None value is a no-op and does not overwrite an existing <field>." (family single-line form) ✓ — 31/31 cells byte-equal; `__init__` carries no docstring (Rule 0012.2.5.2); the spec-swapped sub/sup Note wording copied verbatim as-is (see Step 1)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/parser/test_mixed_content_for_overview_paragraph.py (3 tests) + tests/test_armodel/writer/test_mixed_content_for_overview_paragraph.py (3 tests), house style of the sibling L-2 pair test_l_overview_paragraph.py (inline ET strings carrying the Autosar xmlns, direct getMultiLanguageOverviewParagraph/getLOverviewParagraphs + setMultiLanguageOverviewParagraph invocation, field-value asserts): parser — test_read_l2_reads_mixed_content_children (L-2 with BR/E/IE/SUB/SUP/TRACE-REF/TT/XREF/XREF-TARGET → per-member type + value asserts incl. E/IE/TT String chains, Superscript attrib values, TRACE-REF value+DEST), test_read_l2_without_mixed_content_children (plain L-2 → all 9 fields None — honest pass), test_read_l2_mixed_content_via_get_l_overview_paragraphs (IndentSample consume path); writer — test_write_l2_writes_mixed_content_children (SUB/SUP XML attribs + child-tag order assert ["BR","E","IE","TT"] + text asserts), test_write_l2_without_mixed_content_omits_children (honest pass), test_l2_mixed_content_roundtrip (document-level parse→write→re-parse with per-field value asserts incl. XREF/XREF-TARGET/TRACE-REF DEST); FT/SL-OVERVIEW-PARAGRAPH deliberately absent from every fixture (referenced-missing class — the helper's FT branch lands with the next Group8 row); Red confirmed 4 failed / 2 passed — genuine: both reader paths drop all children, writer emits neither the SUB/SUP attribs nor any child element, round-trip loses the mixed content; honest passes: the 2 no-content tests (reader None defaults + writer omission already correct for the empty case)
  - [x] Step 6 — Update parser & writer (Green) — READER arxml_parser.py: new reusable `readMixedContentForOverviewParagraph` helper (readMixedContentForParagraph family shape minus std/xdoc/xfile: getBr/find E→readEmphasisText/find IE→readIndexEntry/SUB+SUP attrib reads/getChildElementOptionalRefType TRACE-REF/find TT→readTt/getXref/getXrefTarget — FT branch deliberately absent, SlOverviewParagraph referenced-missing, lands with the next Group8 row), WIRED into BOTH L-2 consume paths (readLOverviewParagraph MLOP path after the BLUEPRINT-VALUE read + getLOverviewParagraphs IndentSample path after readLanguageSpecific — same both-paths disposition as the stamped BLUEPRINT-VALUE fix; readLanguageSpecific itself untouched so LParagraph/LVerbatim/LPlainText consumers stay clean); WRITER arxml_writer.py: new reusable `writeMixedContentForOverviewParagraph` helper (display-order emission: setBr BR, setEmphasisText E, setIndexEntry IE, SUB/SUP XML attribs, setChildElementOptionalRefType TRACE-REF, setTt TT, setXref XREF, setXrefTarget XREF-TARGET), WIRED into setLOverviewParagraph after the BLUEPRINT-VALUE attrib (both Aggregated-by consumers — setMultiLanguageOverviewParagraph + setIndentSample — dispatch through it); MixedContentForOverviewParagraph added to both files' LanguageDataModel import lists (inserted in place, no re-sort — I001 disabled per AGENTS.md); SUB/SUP serialized as XML attributes per the Table 9.3 Kind=attr column and the stamped MCFParagraph/UnitNames family convention (Rule 0015: table wins over the XSD group's SUP/SUB child-element rendering — same disposition as the stamped siblings); Rule 0013.2 name pairs verified (readMixedContentForOverviewParagraph sets ↔ writeMixedContentForOverviewParagraph gets, no cross pairs, no chained mutators); Green: 56/56 across the 5 target files, combined parser+writer suites 5545/5545 (full-suite totals at Step 9)
  - [x] Step 7 — Update checklist comment — 21-row 6-column checklist written in the Step 3 body replacement, verified at this step line-by-line against the code: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 9.3, p.290`; rows in source order (`__init__` [—]/[—]; then get/set pairs in member order br/e/ft/ie/sub/sup/traceRef/tt/xref/xrefTarget — reader [x] on every set* row (readMixedContentForOverviewParagraph calls the setter), writer [x] on every get* row (writeMixedContentForOverviewParagraph calls the getter)), every row release R23-11; Columns legend notes members serialize on the consuming L-2 element; trailing note records the AtpMixedString-inherited getMixedString/setMixedString as stereotype-inherent with no spec rows (matching the FormulaExpression checklist precedent); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation per batch instruction)
  - [x] Step 8 — Deviations  [none open on this class: field-to-spec both directions exact (Step 1/Step 3 — exactly the 10 Table 9.3 attribute rows, no placeholders, no fabricated/flattened members), no naming deviations (trace is name+ref-kind → traceRef per Rule 0001.5 with the TRACE-REF XSD element as ground truth; `*`-pluralization n/a — no `*` mults), no type deviations (member types map 1:1 to existing src classes); the spec-swapped sub/sup Note wording is a spec-internal oddity (R23-11 markdown and XSD documentation carry the identical wording) copied verbatim per Rule 0015 — recorded as an observation, NOT a deviation row; the XSD group MIXED-CONTENT-FOR-OVERVIEW-PARAGRAPH rendering SUP/SUB as child elements vs the table's Kind=attr is the same XSD-vs-PDF conflict the stamped MCFParagraph/UnitNames siblings resolved in favor of the table (Rule 0015); the wrong-module Traceable TYPE_CHECKING path in LanguageDataModel (InlineTextElements → BlockElements.RequirementsTracing) was surfaced and FIXED in Step 3 — runtime-neutral, no deviation row]; REFERENCED-MISSING CLASS reported (Rule 0001.10, non-blocking): `SlOverviewParagraph` (Table E.71-family single-language subclass, spec Base ARObject , MixedContentForOverviewParagraph, XSD complexType SL-OVERVIEW-PARAGRAPH L107517 composes this class's group) — NOT in src, queued as the NEXT Group8 row (another agent's row, not created here per orchestrator instruction); consequence: (1) the `ft` member is a string forward reference until it lands, (2) readMixedContentForOverviewParagraph/writeMixedContentForOverviewParagraph have NO FT branch yet — an FT child inside an L-2 would be dropped on round-trip (no repo fixture carries one — full-suite integration round-trip stays green, confirmed at 9a); the FT branch belongs to the SlOverviewParagraph row's own sync (Rule 0001.7 coverage completes there); AtpMixedString mixin present in src (StereotypeMixins.py); tracker reconciliation: v1 + v2 have NO MixedContentForOverviewParagraph entries (grep-verified — nothing to reconcile, files untouched per batch convention); CONSUMER DRIFT FIX applied in the same change set (Rule 0012.3): LOverviewParagraph re-parented `(LanguageSpecific)` → `(MixedContentForOverviewParagraph, LanguageSpecific)` — the Table 9.91 Base row's missing base now exists; model-only + serialization-wiring both covered (the orchestrator's empty-group expectation was verified FALSE — the group is a real choice, so Rule 0001.7 required the helpers + L-2 wiring); stamp NOT written (deferred)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-26: target files 50/50 (test_LanguageDataModel.py + new parser/writer test_mixed_content_for_overview_paragraph.py), full suite `uv run python scripts/run_tests.py --no-coverage` 12004 passed / 0 failed (baseline 11996 + 8 net-new), `npm run lint` clean, black 25.11.0 no-op + `black-check` clean (1237 files); 9b deferred to batch confirmation (user instruction)
  - note (2026-09-26, SlOverviewParagraph row): the FT branch reserved by this row's Steps 6/8 LANDED — readMixedContentForOverviewParagraph/writeMixedContentForOverviewParagraph now serialize FT→SlOverviewParagraph via the new readSlOverviewParagraph/writeSlOverviewParagraphContent helpers (the MCFOP ft/getFt/setFt reader/writer checklist columns are now genuinely exercised; the three `# noqa: F821` forward-reference comments were dropped via a TYPE_CHECKING import, annotation text unchanged) — checkboxes untouched
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [ ] `MixedContentForPlainText` — ARObject , WhitespaceControlled (abstract, <<atpMixedString>>) — GST Table E.5x (locate at Step 1)
  - module: M2/MSR/Documentation/TextModel/LanguageDataModel.py (sibling of MixedContentForLongName/Paragraph)
  - note (2026-09-25, hierarchy audit): markdown-verified Base = ARObject , WhitespaceControlled; Package InlineTextModel; dependency of LPlainText (currently missing this base + WhitespaceControlled)
  - [x] Step 1 — Sync members & description from spec — Table 9.94, NOT "E.5x" (the numeric home table exists, orchestrator intel HELD): AUTOSAR_FO_TPS_GenericStructureTemplate.md, caption `Table 9.94: MixedContentForPlainText` L9344, p.349 via pdf_page.py (matched by name AND the R4.3.1 reproduction Table 8.98, caption L7747, p.317 — content-identical, only glyph[lessmuch/greatermuch] cosmetics); caption-shifted page-split like the Table 9.3 sibling: the meta block (Class/Package/Note/Base/Subclasses + attribute header, L9335-9343) renders ABOVE the caption and NOTHING below it (next line is the Table 9.95 caption); Class-vs-Enumeration header = Class ✓ (`<<atpMixedString>> MixedContentForPlainText (abstract)`; XSD 00052 group L81507 appinfo stereotypes "atpMixedString,atpObject"); Package M2::MSR::Documentation::TextModel::InlineTextModel — file stays LanguageDataModel.py per row note (established placement, stamped siblings spec-Package DataDictionary/InlineTextModel live in the same module); Note "This represents a plain text which conceptually is handled as mixed contents. It is modeled as such for symmetry reasons." verbatim (XSD group documentation L81509 byte-identical); Base = ARObject , WhitespaceControlled ✓ (hierarchy audit corroborated) → anchor `(ARObject, AtpMixedString, ABC)` per the standing mixin decision (most-derived EXISTING base ARObject; sibling MCFOP shape); WhitespaceControlled NOT in src and NOT queued in this batch → referenced-missing reported (its XSD group WHITESPACE-CONTROLLED L130864 is an empty sequence and its attributeGroup L130873 carries the required `xml:space` attribute — NOT created, xml:space NOT modeled, the Group9 LPlainText/LVerbatim rows recorded exactly this disposition); Subclasses row LPlainText (Table 9.96 Base confirms `ARObject , LanguageSpecific , MixedContentForPlainText , WhitespaceControlled`); ZERO attribute rows (header + `-` separator only) → pure marker/base class contributing no own members beyond the AtpMixedString mixin (the LPlainText sync recorded the same for its Table 9.96); XSD ground truth: group MIXED-CONTENT-FOR-PLAIN-TEXT L81507 = `<xsd:sequence/>` EMPTY — the class contributes NO attributes and NO serialization surface (complexType L-PLAIN-TEXT L76292 mixed=true composes AR-OBJECT/LANGUAGE-SPECIFIC/WHITESPACE-CONTROLLED/MIXED-CONTENT-FOR-PLAIN-TEXT/L-PLAIN-TEXT groups, its own group L76283 also an empty sequence) → Steps 5/6 N/A; consumer drift (Rule 0012.3, in this change set): LPlainText re-parents `(LanguageSpecific)` → `(MixedContentForPlainText, LanguageSpecific)` (the LParagraph precedent shape `(MixedContentForParagraph, LanguageSpecific)`) — because the group is empty NO serialization change is expected, model/test only; tracker grep: v1 + v2 have NO MixedContentForPlainText entries; mirror test target: tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_LanguageDataModel.py
  - [x] Step 2 — Write model class unit test (Red) — extended tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_LanguageDataModel.py: new TestMixedContentForPlainText (5 tests, style of the in-file TestMixedContentForOverviewParagraph + the Group8 spec-contract pattern, placed before TestLPlainText dependency-first) — test_abstract_guard_and_defaults (TypeError on direct instantiation + mixin mixedString default None via a Concrete subclass), test_base_anchoring (`__bases__ == (ARObject, AtpMixedString, ABC)` — Table 9.94 Base row ARObject anchored, sibling MCFOP shape; WhitespaceControlled referenced-missing), test_docstring_verbatim (inspect.cleandoc == Table 9.94 Note), test_mixin_accessors (getMixedString/setMixedString round-trip + None no-op — stereotype-inherent, no spec rows), test_has_no_own_members (field-to-spec cross-check Rule 0001.3 — vars(Concrete()) keys == a bare-ARObject-subclass instance's keys: Table 9.94 carries NO Attribute rows so the class adds zero fields beyond the ARObject anchor); PLUS the consumer-drift assertions on TestLPlainText: base-chain rewritten to `issubclass(LPlainText, MixedContentForPlainText)` (+ LanguageSpecific + ARObject) and new test_l_plain_text_inherits_mixed_content_accessors (setMixedString/getMixedString via the new base + None no-op, TestLOverviewParagraph precedent shape); module-level test import added (MixedContentForPlainText, isort case-insensitive slot after MixedContentForParagraph) per ruff I001 (tests enforce it); Red confirmed: collection ImportError — cannot import name 'MixedContentForPlainText' (the class does not exist) — the genuine new-class Red
  - [x] Step 3 — Implement model class (Green) — new `MixedContentForPlainText(ARObject, AtpMixedString, ABC)` in LanguageDataModel.py (hierarchy-doc target shape; abstract TypeError guard per family precedent; AtpMixedString mixin already imported for the MCFOP sibling — getMixedString/setMixedString inherited, class-level mixedString default, no mixin __init__); members = ZERO (Table 9.94 attribute rows: none) → pure marker/base class with only `__init__` (guard + `super().__init__()`) — no fields, no accessors, no create*/add* (not a Referrable child; a legitimate outcome per the sibling LPlainText/LVerbatim precedent); PEP 526 n/a (no members), Rule 0008 spacing n/a (no attribute blocks); file position: between LLongName and LPlainText (dependency-first — Python evaluates base names at class creation; the MCFOP/LParagraph precedent places the concrete L* subclass below its mixed-content base); CONSUMER RE-PARENT applied in the same change set (Rule 0012.3): `LPlainText(LanguageSpecific)` → `LPlainText(MixedContentForPlainText, LanguageSpecific)` — MRO verified LPlainText → MixedContentForPlainText → LanguageSpecific → ARObject → AtpMixedString → ABC; l/value members and the L-10 serialization helpers (parser getLPlainTexts, writer setLPlainText) untouched; Green: test_LanguageDataModel.py 50/50 (44 pre-existing + 5 new MCFPT tests + 1 LPlainText base-chain rewrite + 1 LPlainText inherits-accessors test), MSR models subtree 729/729, top-level export `armodel.MixedContentForPlainText` resolves
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body (new class: no stale wording could exist anywhere, wipe trivially satisfied — the intake had no MixedContentForPlainText at all); docstring written verbatim from the Table 9.94 markdown and MECHANICALLY DIFFED (script comparing inspect.cleandoc against the Note cell of the L9337 meta row): class docstring = Note verbatim, byte-equal ✓; the XSD group documentation (AUTOSAR_00052.xsd L81509) is byte-identical ✓; ZERO attribute rows → no inline `__init__` comments and no getter/setter docstrings exist (nothing else to write); `__init__` carries no docstring (Rule 0012.2.5.2); LPlainText's own Table 9.96 Note docstring untouched (its Group9 rows stand)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — N/A (no own XML element, empty XSD group): the class has no standalone element and contributes no XML-bearing attributes — XSD group MIXED-CONTENT-FOR-PLAIN-TEXT (AUTOSAR_00052.xsd L81507) = `<xsd:sequence/>` EMPTY (no child elements; no attributeGroup of its own — the required `xml:space` in the complexType L76292 comes from the referenced-missing WhitespaceControlled attributeGroup L130873, explicitly NOT modeled per batch disposition); a pure marker/base class is the Rule 0001.7 standalone N/A exception ("only an abstract class with no own XML-bearing attributes may use the standalone N/A exception"), so there is no reader/writer test of its own to write; the Rule 0012.3 re-parent's serialization no-drift is instead proven by rerunning every LPlainText-related suite: 285/285 across tests/test_armodel/parser/test_l_plain_text.py + tests/test_armodel/writer/test_l_plain_text.py + test_arxml_parser_internals.py + test_writer_language_specific.py + test_MultilanguageData.py + test_writer_common_structure.py + test_arxml_writer.py
  - [x] Step 6 — Update parser & writer (Green) — N/A with the same XSD evidence: nothing to add to parser or writer — the empty group (AUTOSAR_00052.xsd L81507 `<xsd:sequence/>`) means no own element, no child elements, no attributes ⇒ no readMixedContentForPlainText/writeMixedContentForPlainText helpers (a helper over an empty group would be dead code); the only consumer LPlainText keeps its existing serialization (parser getLPlainTexts constructs `LPlainText()` per the stamped Group9 fix, writer setLPlainText dispatches through the shared stamped setLanguageSpecific) — both untouched and both still green (285/285, see Step 5); checklist reader/writer columns stay `[—]`
  - [x] Step 7 — Update checklist comment — 1-row 6-column checklist written in the Step 3 body replacement, verified at this step line-by-line against the code plus the Rule 0006 set-based script (checklist == methods == {`__init__`}, coverage ok): `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 9.94, p.349` (home doc; R4.3.1 Table 8.98 p.317 is a row-identical reproduction); single `__init__` row `[x]` with `[—]`/`[—]` reader/writer (no XML element — empty XSD group, Step 5/6 N/A), release R23-11; trailing note records the zero-attribute disposition + the XSD L81507 empty-sequence evidence + the Rule 0021 annotation line (getMixedString/setMixedString via the AtpMixedString mixin — stereotype-inherent, no spec rows, FormulaExpression checklist precedent); no `# type:` comments; `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation per batch instruction)
  - [x] Step 8 — Deviations  [none open on this class: field-to-spec both directions exact (Step 1/Step 3 — Table 9.94 has ZERO attribute rows and the class models exactly that: zero own fields, zero accessors, no placeholders, no fabricated/flattened members), no naming deviations (nothing to name), no type deviations (nothing to type)]; REFERENCED-MISSING CLASS reported (Rule 0001.10, non-blocking): `WhitespaceControlled` (GST Table 9.7, abstract, Package M2::MSR::Documentation::TextModel::LanguageDataModel; XSD group WHITESPACE-CONTROLLED AUTOSAR_00052.xsd L130864 = empty sequence, attributeGroup L130873 = required `xml:space` xml.name="space" xml.nsPrefix="xml") — NOT in src, NOT queued in this batch (per orchestrator instruction), so it is NOT created here and its xml:space is NOT modeled onto this class (the Group9 LPlainText/LVerbatim rows recorded exactly this disposition for the same Base rows); consequence: the Base row's WhitespaceControlled contribution (nothing structural in the element group; the xml:space XML attribute only) stays unmodeled until its own sync lands; AtpMixedString mixin present in src (StereotypeMixins.py); tracker reconciliation: v1 + v2 have NO MixedContentForPlainText entries (grep-verified — nothing to reconcile, files untouched per batch convention); CONSUMER DRIFT FIX applied in the same change set (Rule 0012.3): LPlainText re-parented `(LanguageSpecific)` → `(MixedContentForPlainText, LanguageSpecific)` — the Table 9.96 Base row's missing base now exists; NO serialization change required (the class's XSD group is an empty sequence — model/test only, verified by the 285/285 LPlainText-related rerun); Group9.md LPlainText row annotated (note only, checkboxes untouched); stamp NOT written (deferred)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-26: target counts 50/50 mirror test_LanguageDataModel.py (44 pre-existing + 6 net-new: 5 TestMixedContentForPlainText + 1 LPlainText inherits-accessors; base-chain rewrite replaced in place) + 285/285 across the 7 LPlainText-related parser/writer/model files (parser/writer test_l_plain_text.py, test_arxml_parser_internals.py, test_writer_language_specific.py, test_MultilanguageData.py, test_writer_common_structure.py, test_arxml_writer.py), full suite `uv run python scripts/run_tests.py --no-coverage` 12010 passed / 0 failed (baseline 12004 + 6 net-new, integration round-trip included), `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I), `npm run black` no-op + `black-check` clean (1237 files); set-based checklist script green (checklist == methods == {`__init__`}, coverage ok, no `# type:`, marker legitimately absent); 9b blind-spot checks walked: Rule 0001.1 Class table with ZERO attribute rows ↔ zero own members (test-enforced), Rule 0001.2 most-derived existing base ARObject + AtpMixedString per Rule 0021 (XSD stereotypes "atpMixedString,atpObject"; WhitespaceControlled referenced-missing, not modeled), Rule 0001.3 no fabrication (vars == bare-ARObject-subclass vars), Rule 0012 class docstring byte-equal to the Table 9.94 Note + XSD L81509 (no other docstrings exist), Rule 0008 spacing n/a (no attribute blocks), Rule 0014 no deviations, Rule 0007 LanguageDataModel.py placement per row note + stamped siblings, `armodel.MixedContentForPlainText` export resolves, MRO LPlainText → MCFPT → LanguageSpecific → ARObject → AtpMixedString → ABC verified; 9b deferred to batch confirmation (user instruction)
- [ ] `MixedContentForVerbatim` — ARObject , WhitespaceControlled (abstract, <<atpMixedString>>) — GST Table E.5x (locate at Step 1)
  - module: M2/MSR/Documentation/TextModel/LanguageDataModel.py (sibling of MixedContentForLongName/Paragraph)
  - note (2026-09-25, hierarchy audit): markdown-verified Base = ARObject , WhitespaceControlled; Package InlineTextModel; dependency of LVerbatim (currently missing this base + WhitespaceControlled)
  - [x] Step 1 — Sync members & description from spec — Table 9.6, NOT "E.5x" (the numeric home table exists, Group9 intel HELD): AUTOSAR_FO_TPS_GenericStructureTemplate.md, caption `Table 9.6: MixedContentForVerbatim` L7595, p.292 via pdf_page.py (matched by name AND --table 9.6, FO GST line); UNLIKE the caption-shifted 9.3/9.94 siblings the WHOLE Table 9.6 (meta L7597-7602 + attribute rows L7604-7607) renders BELOW the caption; R4.3.1 reproduction Table 8.7 (caption L6201, p.255) IS page-split — meta block + br row above the caption (L6191-6197), e/tt/xref below (L6203-6206) — content-identical (glyph cosmetics only; the R4.3.1 Note cell truncates at "Blanks are" — cell-width artifact); Class-vs-Enumeration header = Class ✓ (`<<atpMixedString>> MixedContentForVerbatim (abstract)`; XSD 00052 group L81540 appinfo stereotypes "atpMixedString,atpObject"); Package M2::MSR::Documentation::TextModel::InlineTextModel — file stays LanguageDataModel.py per row note (established placement); Note "This is the text model for preformatted (verbatim) text. It mainly consists of attributes which do not change the length on rendering. This class represents multilingual verbatim. Verbatim, sometimes called preformatted text, means that white-space is maintained. When verbatim is rendered in PDF or Online media, it is rendered using a monospaced font while white-space is obeyed. Blanks are rendered as well as newline characters. Even if there are inline elements, the length of the data shall not be influenced by formatting." verbatim (XSD group documentation L81542 same sentences with paragraph breaks); Base = ARObject , WhitespaceControlled ✓ → anchor `(ARObject, AtpMixedString, ABC)` per the standing mixin decision (most-derived EXISTING base ARObject; sibling MCFPT shape); Subclasses row LVerbatim (Table 9.89 Base row `ARObject , LanguageSpecific , MixedContentForVerbatim , WhitespaceControlled`, markdown L9264); 4 attribute rows in displayed order: br (Br, 1, aggr, offset 50), e (EmphasisText, 1, aggr, offset 30), tt (Tt, 1, aggr, NO offset), xref (Xref, 1, aggr, offset 40) — all Mult.=1 → single-valued accessors mirroring MCFOP (no `*` rows, no typed lists); spec-internal typos "emphsized" (e row) + "responibility" (tt row) copied verbatim per Rule 0015; XSD ground truth: group MIXED-CONTENT-FOR-VERBATIM L81540 = choice of unbounded{TT, E, XREF, BR} — NOT empty (real serialization surface, Group9 intel held; each element pureMM.minOccurs=1/maxOccurs=1); complexType L-VERBATIM L76319 (mixed=true, abstract=false) = choice{AR-OBJECT, LANGUAGE-SPECIFIC, WHITESPACE-CONTROLLED, MIXED-CONTENT-FOR-VERBATIM, L-VERBATIM} + attributeGroups AR-OBJECT/LANGUAGE-SPECIFIC/WHITESPACE-CONTROLLED → the LVerbatim re-parent is XSD-verified and EXPANDS the L-5 legal XML surface (Steps 5/6 wire real serialization); WhitespaceControlled (Table 9.7 — sits directly below 9.6 in the same markdown) NOT in src and NOT queued → referenced-missing, NOT created, xml:space NOT modeled (Group9 LPlainText/LVerbatim disposition); consumer drift (Rule 0012.3, this change set): LVerbatim re-parents `(LanguageSpecific)` → `(MixedContentForVerbatim, LanguageSpecific)`; Group9-recorded standing gap closed in THIS row's Step 6: L-5 read/write currently DROPS TT/E/XREF/BR children (readLanguageSpecific reads text+L only; setLVerbatim dispatches setLanguageSpecific only) → new readMixedContentForVerbatim wired into getMultiLanguageVerbatim (parser, single L-5 path) + writeMixedContentForVerbatim wired into setLVerbatim (writer); the xml:space gap stays with the future WhitespaceControlled row (not this class's own table); tracker grep: v1 + v2 have NO MixedContentForVerbatim entries; mirror test target: tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_LanguageDataModel.py
  - [x] Step 2 — Write model class unit test (Red) — extended tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_LanguageDataModel.py: new TestMixedContentForVerbatim (6 tests, style of the in-file TestMixedContentForPlainText + the Group8 spec-contract pattern, placed before TestLVerbatim dependency-first) — test_abstract_guard_and_defaults (TypeError on direct instantiation + all 4 field defaults None + mixin mixedString default via a Concrete subclass), test_base_anchoring (`__bases__ == (ARObject, AtpMixedString, ABC)` — Table 9.6 Base row ARObject anchored, sibling MCFPT shape; WhitespaceControlled referenced-missing), test_docstring_verbatim (inspect.cleandoc == Table 9.6 Note), test_typed_getters_and_setters (all 4 accessors round-trip identity + None no-op with typed values Br()/EmphasisText()/Tt()/Xref()), test_mixin_accessors (getMixedString/setMixedString round-trip + None no-op — stereotype-inherent, no spec rows), test_annotation_hints (get_type_hints for all 4 member pairs Br/E/Tt/Xref — all runtime-resolvable, no forward references); PLUS the consumer-drift assertions on TestLVerbatim: base-chain rewritten to `issubclass(LVerbatim, MixedContentForVerbatim)` (+ LanguageSpecific + ARObject), new test_l_verbatim_inherits_mixed_content_accessors (setTt/getTt via the new base + None no-op, TestLOverviewParagraph precedent shape), and test_l_verbatim_has_no_own_members rewritten to the reference-bases form (`vars(LVerbatim()) == vars` of a bare `(MixedContentForVerbatim, LanguageSpecific)` subclass — Table 9.89 still adds zero own fields, but the instance now legitimately carries the four Table 9.6 base members); module-level test import added (MixedContentForVerbatim, isort case-insensitive slot after MixedContentForPlainText) per ruff I001 (tests enforce it); Red confirmed: collection ImportError — cannot import name 'MixedContentForVerbatim' (the class does not exist) — the genuine new-class Red
  - [x] Step 3 — Implement model class (Green) — new `MixedContentForVerbatim(ARObject, AtpMixedString, ABC)` in LanguageDataModel.py (hierarchy-doc target shape; abstract TypeError guard per family precedent; AtpMixedString mixin already imported for the MCFOP/MCFPT siblings — getMixedString/setMixedString inherited, class-level mixedString default, no mixin __init__); members = exactly the 4 Table 9.6 attribute rows in displayed order (br/e/tt/xref — all Mult.=1 aggr → single-valued Optional accessors mirroring MCFOP; no create*/add* — not a Referrable child; no `*` rows, no typed lists); PEP 526 annotated fields with blank line between every attribute block (Rule 0008); all 4 member types (Br/EmphasisText/Tt/Xref) already runtime-imported from InlineTextElements (no import changes, no forward references, no TYPE_CHECKING additions); file position: between LPlainText and LVerbatim (dependency-first — Python evaluates base names at class creation; the MCFOP/LParagraph precedent places the concrete L* subclass below its mixed-content base); CONSUMER RE-PARENT applied in the same change set (Rule 0012.3): `LVerbatim(LanguageSpecific)` → `LVerbatim(MixedContentForVerbatim, LanguageSpecific)` — MRO verified LVerbatim → MixedContentForVerbatim → LanguageSpecific → ARObject → AtpMixedString → ABC; l/value members untouched; LVerbatim's own checklist trailing note updated to point the inherited Table 9.6 members at their declaring class; Green: test_LanguageDataModel.py 57/57 (50 pre-existing + 6 new MCFV tests + 1 LVerbatim inherits-accessors test; base-chain + no-own-members rewritten in place), MSR models subtree 736/736, top-level export `armodel.MixedContentForVerbatim` resolves
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body (new class: no stale wording could exist anywhere, wipe trivially satisfied — the intake had no MixedContentForVerbatim at all); docstrings written verbatim from the Table 9.6 markdown and MECHANICALLY DIFFED (script comparing inspect.cleandoc + __init__ source comments against the L7600 Note cell and the 4 attribute Note cells of L7604-7607): class docstring = Note verbatim byte-equal ✓; inline `__init__` comment = attribute Note verbatim INCLUDING the Tags tail (offsets br=50/e=30/xref=40; tt carries no Tags in the markdown) ✓; getter docstring = Note sans Tags (MixedContentForPlainText/MCFOP sibling convention) ✓; setter docstring = Note sans Tags + single-line "A None value is a no-op and does not overwrite an existing <field>." (family single-line form) ✓ — 13/13 cells byte-equal + class docstring byte-equal; the spec-internal typos "emphsized" (e) and "responibility" (tt) copied verbatim per Rule 0015 (see Step 1); `__init__` carries no docstring (Rule 0012.2.5.2); LVerbatim's own Table 9.89 Note docstring untouched (its Group9 rows stand)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — new tests/test_armodel/parser/test_mixed_content_for_verbatim.py (3 tests) + tests/test_armodel/writer/test_mixed_content_for_verbatim.py (3 tests), house style of the sibling L-5 pair test_l_verbatim.py + the MCFOP parser/writer pair (inline ET strings carrying the Autosar xmlns, direct getMultiLanguageVerbatim/setMultiLanguageVerbatim invocation, field-value asserts): parser — test_read_l5_reads_mixed_content_children (L-5 with BR/E/TT/XREF → per-member type + value asserts incl. E/TT String chains), test_read_l5_without_mixed_content_children (plain L-5 → all 4 fields None — honest pass), test_read_l5_mixed_content_via_consume_path (the figure/formula/block VERBATIM-wrapper path); writer — test_write_l5_writes_mixed_content_children (child-tag order assert ["BR","E","TT","XREF"] + text asserts — explicit tag/value assertions since the existing test_l_verbatim.py writer tests only index parent[0] L attrib + text), test_write_l5_without_mixed_content_omits_children (honest pass), test_l5_mixed_content_roundtrip (document-level parse→write→re-parse with per-field value asserts); Red confirmed 4 failed / 2 passed — genuine: both reader paths drop all BR/E/TT/XREF children (readLanguageSpecific reads only the text node + L attrib), the writer emits no child element, the round-trip loses the mixed content; honest passes: the 2 no-content tests (reader None defaults + writer omission already correct for the empty case)
  - [x] Step 6 — Update parser & writer (Green) — READER arxml_parser.py: new reusable `readMixedContentForVerbatim` helper (readMixedContentForOverviewParagraph family shape minus the IE/SUB/SUP/TRACE-REF/XREF-TARGET/FT branches this table lacks: getBr BR / find E→readEmphasisText / find TT→readTt / getXref XREF), WIRED into the single L-5 consume path — getMultiLanguageVerbatim after readLanguageSpecific per L-5 (grep-verified there is NO other L-5 dispatch site in src: parser L6368 + writer L2987 only); readLanguageSpecific itself untouched so LParagraph/LPlainText consumers stay clean; WRITER arxml_writer.py: new reusable `writeMixedContentForVerbatim` helper (display-order emission per the MCFOP family convention: setBr BR, setEmphasisText E, setTt TT, setXref XREF), WIRED into setLVerbatim — `child_element = self.setLanguageSpecific(element, "L-5", text)` then writeMixedContentForVerbatim(child_element, …) (setLanguageSpecific already returns child_element per the Group9 BLUEPRINT-VALUE fix — no signature change); the only Aggregated-by consumer MultiLanguageVerbatim (setMultiLanguageVerbatim iterating getL5s → setLVerbatim) dispatches through it — the standing Group9-recorded gap "inline elements inside L-5 (TT/E/XREF/BR) are unread/unwritten" is CLOSED; MixedContentForVerbatim added to both files' LanguageDataModel import lists (inserted after MixedContentForUnitNames, no re-sort — I001 disabled per AGENTS.md; MixedContentForPlainText remains unimported on both sides, no serialization surface); Rule 0013.2 name pairs verified (readMixedContentForVerbatim sets ↔ writeMixedContentForVerbatim gets, readLanguageSpecific↔setLanguageSpecific base pair, addL5↔getL5s aggregation pair, setL↔getL, setValue↔getValue; no cross pairs, no chained mutators); Green: 72/72 across the 5 target files (6 new parser/writer + 9 existing L-5 + 57 mirror), combined parser+writer suites 5551/5551 (full-suite totals at Step 9)
  - [x] Step 7 — Update checklist comment — 9-row 6-column checklist written in the Step 3 body replacement, verified at this step line-by-line against the code plus the Rule 0006 set-based script (checklist rows minus `__init__` == declared methods {getBr/setBr/getE/setE/getTt/setTt/getXref/setXref}, row order == member order == Table 9.6 displayed order, reader calls every set*, writer calls every get*, no `# type:` comments, PEP 526 annotated members, blank line before every attribute block, marker legitimately absent): `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 9.6, p.292` (home doc; R4.3.1 Table 8.7 p.255 is a row-identical page-split reproduction); rows in source order (`__init__` [—]/[—]; then get/set pairs in member order br/e/tt/xref — reader [x] on every set* row (readMixedContentForVerbatim calls the setter), writer [x] on every get* row (writeMixedContentForVerbatim calls the getter)), every row release R23-11; Columns legend notes members serialize on the consuming L-5 element; trailing note records the AtpMixedString-inherited getMixedString/setMixedString as stereotype-inherent with no spec rows (FormulaExpression checklist precedent); `# Spec verified:` marker NOT written (deferred to 9b/batch confirmation per batch instruction)
  - [x] Step 8 — Deviations  [none open on this class: field-to-spec both directions exact (Step 1/Step 3 — exactly the 4 Table 9.6 attribute rows br/e/tt/xref, no placeholders, no fabricated/flattened members), no naming deviations (all four names map name→accessor directly — no ref/tref kind suffixes, no `*`-pluralization: all Mult.=1), no type deviations (Br/EmphasisText/Tt/Xref all exist in InlineTextElements and are runtime-imported)]; REFERENCED-MISSING CLASS reported (Rule 0001.10, non-blocking): `WhitespaceControlled` (GST Table 9.7 — sits directly below Table 9.6 in the same markdown, abstract, Package M2::MSR::Documentation::TextModel::LanguageDataModel; XSD group WHITESPACE-CONTROLLED AUTOSAR_00052.xsd L130864 = empty sequence, attributeGroup L130873 = required `xml:space` xml.name="space" xml.nsPrefix="xml") — NOT in src, NOT queued in this batch (per orchestrator instruction), so it is NOT created here and its xml:space is NOT modeled onto this class or onto the L-5 serialization (the Group9 LPlainText/LVerbatim rows AND the Group8 MixedContentForPlainText row recorded exactly this disposition for the same Base rows); consequence: the `xml:space` XML attribute required by XSD on L-VERBATIM stays unread/unwritten until its own sync row lands; AtpMixedString mixin present in src (StereotypeMixins.py); tracker reconciliation: v1 + v2 have NO MixedContentForVerbatim entries (grep-verified — nothing to reconcile, files untouched per batch convention); CONSUMER DRIFT FIX applied in the same change set (Rule 0012.3): LVerbatim re-parented `(LanguageSpecific)` → `(MixedContentForVerbatim, LanguageSpecific)` AND the Group9-recorded standing serialization gap (L-5 TT/E/XREF/BR children dropped) CLOSED via the new read/write helpers + L-5 wiring — UNLIKE the PlainText sibling this DID change serialization (the MIXED-CONTENT-FOR-VERBATIM XSD group is a real choice, not empty), model + serialization both covered; Group9.md LVerbatim row annotated (one dated note line, checkboxes untouched); stamp NOT written (deferred)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-26: target files 72/72 (test_LanguageDataModel.py 57 + new parser/writer test_mixed_content_for_verbatim.py 6 + existing test_l_verbatim.py pair 9), full suite `uv run python scripts/run_tests.py --no-coverage` 12023 passed / 0 failed (baseline 12010 + 13 net-new: 7 mirror — 6 TestMixedContentForVerbatim + 1 LVerbatim inherits-accessors — plus 6 new parser/writer round-trip tests; integration round-trip included in the suite green), `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I), `npm run black` no-op (1239 files unchanged, black 25.11.0) + `black-check` clean; set-based checklist script green (checklist == declared methods, reader calls every set*, writer calls every get*, PEP 526 members, blank line before every attribute block, no `# type:`, marker legitimately absent); 9b blind-spot checks walked: Rule 0001.1 Class table ↔ exactly the 4 attribute rows modeled (test-enforced defaults), Rule 0001.2 most-derived existing base ARObject + AtpMixedString per Rule 0021 (XSD stereotypes "atpMixedString,atpObject"; WhitespaceControlled referenced-missing, not modeled), Rule 0001.3 no fabrication/flattening (4 fields ↔ 4 rows both directions; LVerbatim vars == bare-(MCFV, LanguageSpecific) reference vars), Rule 0012 docstrings byte-equal to the Table 9.6 Note by mechanical diff (13/13 member cells + class cell), Rule 0008 spacing verified by script (4 blocks, blank before every comment block), Rule 0014 no deviations, Rule 0007 LanguageDataModel.py placement per row note + stamped siblings, Rule 0001.11 member order == markdown displayed order br/e/tt/xref (test-asserted checklist order) with writer emission in the family display-order convention, `armodel.MixedContentForVerbatim` export resolves, MRO LVerbatim → MixedContentForVerbatim → LanguageSpecific → ARObject → AtpMixedString → ABC verified; 9b deferred to batch confirmation (user instruction)
- [ ] `SlOverviewParagraph` — ARObject , MixedContentForOverviewParagraph (<<atpMixedString>>) — GST Table E.5x (locate at Step 1)
  - module: M2/MSR/Documentation/TextModel/SingleLanguageData.py (sibling of SingleLanguageLongName)
  - note (2026-09-25, hierarchy audit): markdown-verified Base = ARObject , MixedContentForOverviewParagraph; Package M2::MSR::Documentation::TextModel::SingleLanguageData; NOT IN SRC at all; after MixedContentForOverviewParagraph lands
  - [x] Step 1 — Sync members & description from spec — Table E.70, NOT "E.5x" (exact id located; NO numeric home table exists — GST chapter 9 runs 9.1-9.11 with no SlOverviewParagraph entry between MixedContentForVerbatim 9.6 and WhitespaceControlled 9.7): AUTOSAR_FO_TPS_GenericStructureTemplate.md caption `Table E.70: SlOverviewParagraph` L12581, p.464 via direct pypdf scan of the FO GST PDF (pdf_page.py found nothing by name or --table E.70 — its caption index misses this appendix table; the whole table confirmed on one page, "464 of 535"); layout = caption FIRST (L12581), meta block BELOW it (Class L12583 / Package L12585 / Note L12586 / Base L12587 / Aggregated-by L12588 / Attribute header + `-` separator L12589-12590), then an AUTOSAR-logo page-break image artifact (L12592, base64 decoded: logo only, NOT an attribute body); the PDF p.464 text confirms the attribute section is a single dash placeholder row (`– – – – –`) → ZERO attribute rows; R4.3.1 reproduction: NONE in the R4.3.1 markdown (grep empty), but R4.3.1 XSD AUTOSAR_00044.xsd HAS the class (group L77352 empty sequence, attributeGroup L77362 `L`, complexType L77377) content-identical to R23-11 → R23-11 corpus syncs the class; Class-vs-Enumeration header = Class ✓ (`<<atpMixedString>> SlOverviewParagraph`, NOT abstract — XSD 00052 complexType L107517 abstract="false" mixed="true", appinfo stereotypes "atpMixedString,atpObject"); Package M2::MSR::Documentation::TextModel::SingleLanguageData ✓ (leaf package → SingleLanguageData.py per Rule 0007, matches row note — sibling of SingleLanguageLongName, same spec Package, same module); Note "MixedContentForOverviewParagraph in one particular language. The language is defined by the context. The attribute l is there only for backwards compatibility and shall be ignored." verbatim (XSD documentation L107501-502 same text with a paragraph break); Base = ARObject , MixedContentForOverviewParagraph ✓ (the ec430fef8 base) → anchor `(MixedContentForOverviewParagraph, LanguageSpecific)` per the STAMPED SlParagraph precedent (E.71, landed bf9114cb0 `# Spec verified: R23-11`): same E-family, same zero-attribute dash-placeholder table, same "attribute l … backwards compatibility" Note, same XSD-only `L` attribute (SL-PARAGRAPH attributeGroup ≅ SL-OVERVIEW-PARAGRAPH) — the stamped sibling carries the `L` XML attribute via LanguageSpecific inheritance (its Base row likewise omits LanguageSpecific); MRO = SlOverviewParagraph → MCFOP → LanguageSpecific → ARObject → AtpMixedString → ABC (the verified LOverviewParagraph shape); ZERO attribute rows → NO own members (pure concrete subclass — stamped SlParagraph/LParagraph/LPlainText disposition): inherited LanguageSpecific l (Optional[LEnum]; XSD `L` L-ENUM--SIMPLE, atp.Status="removed", minOccurs 0 maxOccurs 1, xml.attribute=true — "shall be ignored") + value (mixed text) cover the XSD attributeGroup (L107508); inherited MCFOP 10 members cover the MIXED-CONTENT-FOR-OVERVIEW-PARAGRAPH choice (L81331); getMixedString/setMixedString via the AtpMixedString mixin (transitive via MCFOP); Aggregated by MixedContentForOverviewParagraph.ft ✓ (member + forward-ref annotation landed ec430fef8); XSD FT element L81345 type SL-OVERVIEW-PARAGRAPH inside the MCFOP choice (BR, FT, TRACE-REF offset-less, then TT=30/XREF=40/XREF-TARGET=50/E=60/SUP=80/SUB=90/IE=100) — FT composes the same MCFOP choice recursively (complexType L107517 choice = AR-OBJECT + MCFOP + SL-OVERVIEW-PARAGRAPH(empty, L107498) groups + attributeGroups AR-OBJECT + SL-OVERVIEW-PARAGRAPH); SERIALIZATION GAP this row closes (the ec430fef8 Step 6/8 recorded deferral): readMixedContentForOverviewParagraph (parser L1750) / writeMixedContentForOverviewParagraph (writer L1428) have NO FT branch → an FT child inside an L-2 is dropped on round-trip today; the three L-2 consume paths (parser readLOverviewParagraph L1798 + getLOverviewParagraphs L6350, writer setLOverviewParagraph L1500) dispatch through the helpers so the FT branch lands everywhere at once; new helpers readSlOverviewParagraph / writeSlOverviewParagraphContent mirror the stamped readSlParagraph (parser L1665) / writeSlParagraphContent (writer L1409) pair (ARObject + mixed content + text + L attrib); FT emission position = right after BR (MCFP writer precedent + MCFOP XSD group element order BR,FT,TRACE-REF; the group is a choice so XML order is free — family convention governs); CONSUMER DRIFT (Rule 0012.3): the ec430fef8 `# noqa: F821` forward references on MCFOP ft/getFt/setFt can resolve via a TYPE_CHECKING import of the now-existing class (annotation text unchanged — the MCFOP raw-string asserts stay valid); tracker grep v1+v2: pending Step 8; mirror test target: NEW tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_SlOverviewParagraph.py (per-class file, test_SingleLanguageLongName.py sibling convention)
  - [x] Step 2 — Write model class unit test (Red) — NEW tests/test_armodel/models/M2/MSR/Documentation/TextModel/test_SlOverviewParagraph.py (per-class file, test_SingleLanguageLongName.py sibling convention), 6 tests style-matched to the stamped in-file TestSlParagraph + the Group8 family patterns — test_initialization (concrete instantiation succeeds — Table E.70 header has no "(abstract)"; all inherited defaults: l None / value "" / the 10 MCFOP fields None / getMixedString None), test_base_chain (`__bases__ == (MixedContentForOverviewParagraph, LanguageSpecific)` — Table E.70 Base row + the stamped SlParagraph E.71 LanguageSpecific disposition; issubclass ARObject + AtpMixedString; full `__mro__` name list asserted == SlOverviewParagraph → MCFOP → LanguageSpecific → ARObject → AtpMixedString → ABC, the verified LOverviewParagraph shape), test_docstring_verbatim (inspect.cleandoc == Table E.70 Note), test_has_no_own_members (field-to-spec cross-check Rule 0001.3 — vars(SlOverviewParagraph()) == vars of a bare (MCFOP, LanguageSpecific) subclass instance: Table E.70 carries ZERO attribute rows so the class adds zero own fields), test_inherited_language_accessors (setL/getL round-trip + None no-op + setValue/getValue round-trip — the XSD-only `L` attribute rides the inherited LanguageSpecific accessors, TestSlParagraph style), test_inherited_mixed_content_accessors (setBr/getBr + setTt/getTt round-trip + None no-op via the MCFOP base + setMixedString/getMixedString mixin round-trip, TestLOverviewParagraph inherits-accessors style); module-level test imports (inspect, ARObject, AtpMixedString, Br/Tt, LanguageSpecific, MixedContentForOverviewParagraph, SlOverviewParagraph) sorted per ruff I001 (tests enforce it); no get_type_hints test — the class owns NO annotated members (zero attribute rows) and get_type_hints across the MRO would hit the unresolvable LanguageDataModel-side ft forward reference (standing family disposition, MCFOP raw-string asserts stay valid); Red confirmed: collection ImportError — cannot import name 'SlOverviewParagraph' (the class does not exist) — the genuine new-class Red
  - [x] Step 3 — Implement model class (Green) — new `SlOverviewParagraph(MixedContentForOverviewParagraph, LanguageSpecific)` in SingleLanguageData.py appended after SingleLanguageLongName (Table E.70 Package row SingleLanguageData, leaf package → own module per Rule 0007; row-note placement — sibling of SingleLanguageLongName whose spec-Package is the same SingleLanguageData); CONCRETE (no TypeError guard — XSD complexType L107517 abstract="false"; instantiation succeeding is the test_initialization assertion), no own members (Table E.70 ZERO attribute rows — stamped SlParagraph/LParagraph/LPlainText disposition): `__init__` = `super().__init__()` only; the XSD-only `L` XML attribute + mixed text ride the inherited LanguageSpecific l/value members (stamped SlParagraph E.71 disposition — its Base row likewise omits LanguageSpecific), the 10 Table 9.3 mixed-content members ride the MCFOP base, getMixedString/setMixedString via the AtpMixedString mixin inherited transitively through MCFOP; no create*/add* (not a Referrable child, no `*` mults anywhere in the closure); no PEP 526 fields to declare (nothing own to annotate — Rule 0003/0008 n/a); MRO verified in-test: SlOverviewParagraph → MCFOP → LanguageSpecific → ARObject → AtpMixedString → ABC → object (the verified LOverviewParagraph shape); module import change: LanguageSpecific + MixedContentForOverviewParagraph added to the existing LanguageDataModel import in SingleLanguageData.py (sorted insertion, no re-sort — I001 disabled in models per AGENTS.md); CONSUMER DRIFT HYGIENE applied in the same change set (Rule 0012.3): LanguageDataModel.py TYPE_CHECKING block gains `from ...TextModel.SingleLanguageData import SlOverviewParagraph` (sorted between InlineTextElements and the stale SlParagraph self-module line; TYPE_CHECKING-only so no runtime cycle — SingleLanguageData runtime-imports LanguageDataModel) and the three `# noqa: F821` comments on the MCFOP ft/getFt/setFt forward references DROPPED (name now resolves; annotation text `Optional["SlOverviewParagraph"]` UNCHANGED so the MCFOP Step 2 raw-string asserts stay valid byte-for-byte); one test-side fix during Green: the `__mro__` name list initially omitted the trailing `object` (assertion bug, not a model bug); Green: test_SlOverviewParagraph.py 6/6, MSR models subtree 742/742 (736 pre-existing + 6 net-new), top-level export `armodel.SlOverviewParagraph` resolves via the existing models/__init__.py L21 SingleLanguageData wildcard (no export change needed)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body (new class: no stale wording could exist anywhere, wipe trivially satisfied — the intake had no SlOverviewParagraph at all); class docstring = Table E.70 Note verbatim, byte-equal to the markdown L12586 cell (test-enforced by test_docstring_verbatim; the XSD documentation L107501-502 carries the same sentences with a paragraph break — PDF/markdown wins per Rule 0015); ZERO attribute rows → no inline `__init__` comments and no getter/setter docstrings exist (nothing else to write); `__init__` carries no docstring (Rule 0012.2.5.2); SingleLanguageLongName's own Table 4.7 docstrings untouched (its stamped rows stand)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — EXTENDED the MCFOP parser/writer pair (extend, no duplication — the ConditionByFormula precedent; the ec430fef8 Step 8 note reserved the FT tests for this row): tests/test_armodel/parser/test_mixed_content_for_overview_paragraph.py new TestReadSlOverviewParagraph (2 tests) + tests/test_armodel/writer/test_mixed_content_for_overview_paragraph.py new TestWriteSlOverviewParagraph (3 tests), house style of the in-file MCFOP tests (inline ET strings carrying the Autosar xmlns, direct getMultiLanguageOverviewParagraph/setMultiLanguageOverviewParagraph invocation, field-value asserts): parser — test_read_l2_reads_ft_sl_overview_paragraph (L-2 with an FT child (L="DE" attrib + "footnote text" + inner TT) → isinstance(getFt(), SlOverviewParagraph) + per-field value asserts incl. the inner TT String chain + parent value/text intact), test_read_l2_without_ft_leaves_ft_none (plain L-2 → getFt() None — honest pass); writer — test_write_l2_writes_ft_sl_overview_paragraph (child-tag order assert ["BR","FT","E","IE","TT"] — FT right after BR per the MCFP writer precedent + the XSD MCFOP group element order BR,FT,TRACE-REF; FT L attrib + text + inner TT text asserts), test_write_l2_without_ft_omits_ft (honest pass), test_l2_ft_roundtrip (document-level parse→write→re-parse with per-field value asserts on the footnote — L attrib, text, inner BR + TT — AND the parent's own fields); Red confirmed 3 failed / 8 passed — genuine: the reader drops the FT child (isinstance(None, …)), the writer emits no FT element (child-tag list lacks "FT"), the round-trip loses the footnote; honest passes: the 2 no-FT tests (reader None default + writer omission already correct for the empty case)
  - [x] Step 6 — Update parser & writer (Green) — READER arxml_parser.py: new `readSlOverviewParagraph` helper (readSlParagraph family shape: readARObject + readMixedContentForOverviewParagraph + setValue(element.text or "") + `L` attrib → setL), placed directly after readMixedContentForOverviewParagraph (mirroring the readSlParagraph-after-readMixedContentForParagraph placement); the FT BRANCH added to readMixedContentForOverviewParagraph right after the BR read (find FT → SlOverviewParagraph() → readSlOverviewParagraph → setFt — MCFP branch shape; recursion through the MCFOP choice comes free since the helper re-enters itself on nested FT) — this CLOSES the ec430fef8 Step 8 recorded gap "an FT child inside an L-2 would be dropped on round-trip"; both L-2 consume paths (readLOverviewParagraph, getLOverviewParagraphs) dispatch through the helper so the branch lands everywhere at once (readLanguageSpecific untouched); WRITER arxml_writer.py: new `writeSlOverviewParagraphContent` helper (writeSlParagraphContent family shape: writeARObject + writeMixedContentForOverviewParagraph + element.text = getValue() + `L` attrib from getL), placed directly after writeMixedContentForOverviewParagraph; the FT BRANCH added to writeMixedContentForOverviewParagraph right after setBr (ET.SubElement FT → writeSlOverviewParagraphContent — writeMixedContentForParagraph branch shape); no standalone writeSlOverviewParagraph (no SL-OVERVIEW-PARAGRAPH role element exists outside FT — the XSD's only SL-OVERVIEW-PARAGRAPH-typed element is FT L81345; writeSlParagraph's standalone twin has zero callers); SlOverviewParagraph added to both files' SingleLanguageData import lines (in-place name insertion, no re-sort — I001 disabled per AGENTS.md); Rule 0013.2 name pairs verified (readSlOverviewParagraph sets setValue/setL ↔ writeSlOverviewParagraphContent gets getValue/getL; the MCFOP branch pair setFt ↔ getFt; readMixedContentForOverviewParagraph ↔ writeMixedContentForOverviewParagraph base pair; no cross pairs, no chained mutators); Green: 17/17 across the 3 target test files (6 model + 2 parser + 3 writer new + 6 pre-existing MCFOP), combined parser+writer suites 5556/5556 (full-suite totals at Step 9)
  - [x] Step 7 — Update checklist comment — 1-row 6-column checklist written in the Step 3 body replacement, verified at this step line-by-line against the code plus the set-based script (checklist == methods == {`__init__`}, coverage ok, no `# type:` comments, marker legitimately absent): `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table E.70, p.464`; single `__init__` row `[x]` with `[—]`/`[—]` reader/writer (no own XML element — Table E.70 dash-placeholder attribute rows; the class serializes as the consuming L-2 element's FT child via readSlOverviewParagraph/writeSlOverviewParagraphContent), release R23-11; trailing note records the zero-attribute disposition + the XSD-only `L` attribute riding the inherited LanguageSpecific accessors (declaring-class checklist) + the Table 9.3 base members covered by their declaring class checklist + the AtpMixedString-inherited getMixedString/setMixedString (stereotype-inherent, no spec rows — FormulaExpression checklist precedent); housekeeping this step: removed four stale duplicate unchecked step lines (Step 4/5/6/7) left in this row by a mis-anchored Step-4/5/6 checkbox edit — the duplicates were pure checkbox-line leftovers, no content was lost (the [x] lines above carry the full evidence)
  - [x] Step 8 — Deviations  [none open on this class: field-to-spec both directions exact (Step 1/Step 3 — Table E.70 carries ZERO attribute rows (dash placeholder) and the class models exactly that: zero own fields, zero own accessors, no placeholders, no fabricated/flattened members), no naming deviations (nothing own to name — no ref/tref kinds, no `*` mults anywhere in the closure), no type deviations (nothing own to type; the XSD-only `L` attribute rides the inherited LanguageSpecific l — the stamped SlParagraph E.71 disposition, NOT a deviation: the spec Note itself names the l attribute and the XSD attributeGroup SL-OVERVIEW-PARAGRAPH L107508 carries it, with full reader/writer coverage via readSlOverviewParagraph/writeSlOverviewParagraphContent)]; REFERENCED-MISSING CLASSES: NONE — the closure is complete (MixedContentForOverviewParagraph landed ec430fef8; LanguageSpecific + LEnum stamped Table 9.97; Br/EmphasisText/IndexEntry/Superscript/Tt/Xref/XrefTarget exist in InlineTextElements; Traceable exists — TYPE_CHECKING-only via the managed TextModel↔BlockElements cycle; AtpMixedString mixin in StereotypeMixins) — the ec430fef8 Step 8 referenced-missing entry for SlOverviewParagraph itself is RESOLVED by this row; CONSUMER WIRING (Rule 0012.3, in this change set): the Aggregated-by consumer MixedContentForOverviewParagraph.ft got its serialization — the FT branch + readSlOverviewParagraph/writeSlOverviewParagraphContent helpers — closing the ec430fef8 Step 8 recorded consequence "(2) an FT child inside an L-2 would be dropped on round-trip"; one dated note line added to the MCFOP row below (checkboxes untouched); tracker reconciliation: v1 + v2 have NO SlOverviewParagraph entries (grep-verified — nothing to reconcile, files untouched per batch convention); stamp NOT written (deferred)
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-26: target files 74/74 (test_SlOverviewParagraph.py 6 + extended parser/writer test_mixed_content_for_overview_paragraph.py 11 (5 pre-existing MCFOP + 6 new FT) + test_LanguageDataModel.py 57 re-run green post-black — the MCFOP raw-string ft asserts stay valid byte-for-byte), full suite `uv run python scripts/run_tests.py --no-coverage` 12034 passed / 0 failed (baseline 12023 + 11 net-new: 6 model + 2 parser + 3 writer; integration round-trip included), `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I), `npm run black` reformatted the 2 new/extended test files (canonical formatting only) + `black-check` clean (1240 files); set-based checklist script green (checklist == methods == {`__init__`}, coverage ok, no `# type:`, marker legitimately absent); 9b blind-spot checks walked: Rule 0001.1 Class table with ZERO attribute rows ↔ zero own members (test-enforced vars-equality), Rule 0001.2 most-derived existing base MixedContentForOverviewParagraph + LanguageSpecific per the stamped SlParagraph E.71 precedent (XSD-only `L` attribute; AtpMixedString mixin transitively via MCFOP per Rule 0021 — XSD stereotypes "atpMixedString,atpObject"), Rule 0001.3 no fabrication/flattening (vars == bare-(MCFOP, LanguageSpecific) reference vars; nothing inherited flattened into the subclass), Rule 0012 class docstring byte-equal to the Table E.70 Note (test-enforced; no other docstrings exist), Rule 0008/0003 spacing + PEP 526 n/a (no own attribute blocks), Rule 0014 no deviations, Rule 0007 SingleLanguageData.py placement per row note + spec Package row (leaf package), Rule 0001.11 member order n/a (zero own rows) with writer FT emission at the MCFP-precedent position after BR matching the MCFOP XSD group element order, reader+writer coverage complete (FT branch on both L-2 consume paths + L attrib both directions), top-level export `armodel.SlOverviewParagraph` resolves, MRO SlOverviewParagraph → MCFOP → LanguageSpecific → ARObject → AtpMixedString → ABC → object verified; 9b deferred to batch confirmation (user instruction)
- [ ] `SingleLanguageUnitNames` — ARObject , MixedContentForUnitNames (<<atpMixedString>>) — CP SWCT Table 5.80, p.400 — drift re-parent APPLIED 2026-09-25
  - module: M2/MSR/AsamHdo/Units.py
  - note (2026-09-25 hierarchy audit): markdown-verified Base = ARObject , MixedContentForUnitNames (Package M2::MSR::Documentation::TextModel::SingleLanguageData) — NOT FormulaExpression (an earlier re-parent to FormulaExpression came from a caption-shift misread and was reverted)
  - note (2026-09-25 drift fix APPLIED, compressed 9-step): re-parented ARLiteral → MixedContentForUnitNames (new class, GST Table E.55 p.456, sub/sup — created in the same change set); UNIT-DISPLAY-NAME / DISPLAY-NAME serialization switched from ARLiteral helpers to the doc-family pattern (readSingleLanguageUnitNames/setSingleLanguageUnitNames + read/writeMixedContentForUnitNames: text + SUB/SUP attributes, mirror SingleLanguageLongName); value member = mixed text (no spec attribute row); full suite + lint + black green
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red)
  - [x] Step 3 — Implement model class (Green)
  - [x] Step 4 — Sync docstrings (wipe + rewrite)
  - [x] Step 5 — Write reader/writer round-trip test (Red)
  - [x] Step 6 — Update parser & writer (Green)
  - [x] Step 7 — Update checklist comment
  - [x] Step 8 — Deviations
  - [x] Step 9 — Verify (9a) + confirm (9b)
- [ ] `SwSystemconstValue` — ARObject — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/__init__.py
  - [x] Step 1 — Sync members & description from spec — Table 7.9 (AUTOSAR_FO_TPS_GenericStructureTemplate home doc, p.235 via pdf_page.py); table identity check across the 2 genuine R23-11 reproductions: CP SystemTemplate Table F.129 + FO FMXF Table C.19 — both are caption-placement extraction artifacts, but the actual SwSystemconstValue table content is present in both and byte-identical to home doc 7.9 on every row (Package/Note/Base/Aggregated-by + annotation/swSystemconst/value): F.129's caption sits under the WRONG table body (content below it = the SwSystemconstantValueSet class table; the real SwSystemconstValue meta-rows render above in the F.128 caption zone with the attribute rows between F.128 and F.129), C.19 is caption-only in place (its table body renders ABOVE the caption — unlike the ConditionByFormula C.4 mislabel, C.19's caption text matches its content class); Class-vs-Enumeration header = Class ✓ (concrete — no "(abstract)"; XSD 00052 complexType L116440 abstract="false" mixed="false"); Package M2::AUTOSARTemplates::GenericStructure::VariantHandling ✓ (non-leaf → class in __init__.py per Rule 0007, matches current location); Note verbatim in place; Base ARObject ✓ (XSD complexType L116440 sequence = AR-OBJECT group + SW-SYSTEMCONST-VALUE group + attributeGroup AR-OBJECT — NO mixin groups, unlike AVP/ConditionByFormula); Aggregated by SwSystemconstantValueSet.swSystemconstantValue ✓ (consumer same file; parser readSwSystemconstantValueSetSwSystemconstantValues L11363 / writer writeSwSystemconstantValueSetSwSystemconstantValues L12263); attributes displayed order = annotation (Annotation, *, aggr, Tags: xml.sequenceOffset=30), swSystemconst (SwSystemconst, 1, ref, Tags: xml.sequenceOffset=10), value (Numerical, 1, attr, Stereotypes: atpVariation, Tags: vh.latestBindingTime=preCompileTime xml.sequenceOffset=20; "Sw Systemconst" spacing in the Note = PDF line-break artifact, XSD documentation L116421 confirms "SwSystemconst" one word — BindingTimeEnum artifact class); naming per Rule 0001.5 verified against stamped precedents: annotation (* aggr → plural) → annotations/getAnnotations/addAnnotation ✓ (Table 7.27 precedent), swSystemconst + ref → swSystemconstRef/getSwSystemconstRef/setSwSystemconstRef ✓ (field base name verbatim + Kind suffix; Table 7.63 compuMethod→compuMethodRef + Table 7.27 variantCriterion→variantCriterionRef precedent), value → getValue/setValue ✓; XSD element group L116398 order SW-SYSTEMCONST-REF(10) → VALUE(20) → ANNOTATIONS(30); VALUE element type = NUMERICAL-VALUE-VARIATION-POINT (group L85733 empty sequence, complexType mixed="true" — value IS the mixed text); shared getChildElementOptionalNumericalValue/setChildElementOptionalNumericalValue pair (element text ↔ ARNumerical) is the repo convention for Numerical attrs; field-to-spec both directions: exactly the 3 spec attrs, member order = markdown displayed order ✓; reader/writer coverage pre-exists (parser readSwSystemconstValue L11357 / writer writeSwSystemconstValue L12259) — Steps 5/6 extend, NOT N/A; findings to fix: (1) value typed `ARNumerical` (non-Optional) on field + accessors vs stamped Optional[X] convention — Step 3; (2) class docstring is the old verbose Package/Base/Attributes form, NO method docstrings/inline comments — Rule 0012 wipe+rewrite Step 4; (3) checklist is the old 4-column no-Spec-line form, all [ ] — rebuild 6-column Step 7; (4) WRITER ELEMENT-ORDER BUG: writeSwSystemconstValue emits ANNOTATIONS before SW-SYSTEMCONST-REF/VALUE vs XSD 10/20/30 — Step 6; tracker review: v1 + v2 have NO SwSystemconstValue/SwSystemconstantValueSet entries (grep verified); referenced classes: base ARObject (stamped), member types Annotation (MSR Documentation, stamped), RefType + ARNumerical (PrimitiveTypes, stamped), consumer SwSystemconstantValueSet (Table 7.25, same file, stamped, shares this module — untouched) — no missing classes
  - [x] Step 2 — Write model class unit test (Red) — extended test_VariantHandling.py with new TestSwSystemconstValueSpecContract (no duplication; mirrors TestConditionByFormulaSpecContract): verbatim class-Note assertion (Table 7.9), no-`__init__`-docstring assertion, PDF-typed accessor annotations via typing.get_type_hints (repo style per the ConditionByFormula tests) for annotations→List[Annotation] + swSystemconstRef→RefType (value + return on setters) + value→Optional[ARNumerical], getter-docstrings-Note-without-Tags + setter-docstrings-Note+None-noop assertions, defaults + None-noop tests; 6 Red / 50 passed — Red is real: class docstring is the old verbose Package/Base/Attributes form (Step 4), accessors carry NO docstrings at all (AttributeError on .strip, Step 4), addAnnotation/setSwSystemconstRef/setValue lack return annotations and value is typed non-Optional ARNumerical (Step 3); honest passes: `__init__` already docstring-less, defaults/None-noops already correct
  - [x] Step 3 — Implement model class (Green) — whole class body replaced in one pass; field-to-spec both directions: exactly annotation (List[Annotation] — spec name `annotation`, * aggr → plural per Rule 0001.5, Table 7.27 precedent) + swSystemconstRef (RefType — spec name `swSystemconst` + Kind ref suffix, Table 7.63/7.27 precedents) + value (Optional[ARNumerical], spec Numerical) — no fabricated/extra fields; Base stays ARObject (XSD complexType L116440 composes only AR-OBJECT group + attributeGroup — no mixin groups unlike AVP/ConditionByFormula); concrete class (XSD abstract="false", spec header has no "(abstract)"); fixed the Step 1 typing finding: value ARNumerical→Optional[ARNumerical] on field + accessors (stamped Optional[X] convention per PostBuildVariantCondition.value) and added the missing setter return annotations; swSystemconstRef kept bare `RefType = None` (repo-wide ref convention: compuMethodRef/variantCriterionRef/matchingCriterionRef in this same file); member/accessor order annotation→swSystemconstRef→value matches markdown displayed order (Rule 0001.11); setters keep None-no-op + return-self shape; PEP 526 annotated members with blank line between attribute blocks; imports untouched (no re-sort, none needed); 56/56 Green in test_VariantHandling.py
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body replacement (no stale wording survives: old verbose class docstring's Package/Base/Attributes block wiped; the docstring-less accessors got fresh docstrings); class docstring = Table 7.9 Note verbatim ("This meta-class assigns a particular value to a system constant."); inline `__init__` comments = attribute Notes verbatim incl. Stereotypes/Tags (xml.sequenceOffset=30/10 + atpVariation/vh.latestBindingTime/preCompileTime/xml.sequenceOffset=20); the value Note's "Sw Systemconst" spacing (PDF line-break artifact) written in the XSD-confirmed one-word form "SwSystemconst" (XSD documentation L116421) per BindingTimeEnum precedent; getter docstrings = Note verbatim without Tags; setter docstrings = Note verbatim + None-no-op sentence ("is not appended" for addAnnotation per the PostBuildVariantCriterionValue.addAnnotation form, "does not overwrite an existing <field>" for setters); no `__init__` docstring
  - [x] Step 5 — Write reader/writer round-trip test (Red) — extended the 3 existing round-trip test homes (extend, no duplication): parser test_arxml_parser_snippets.py new test_read_sw_systemconst_value_empty_fields (empty SW-SYSTEMCONST-VALUE → annotations []/ref None/value None); writer test_arxml_writer.py TestARXMLWriterSwSystemconstantValueSetMethods new test_write_sw_systemconst_value_element_order (asserts [SW-SYSTEMCONST-REF, VALUE, ANNOTATIONS] per XSD sequenceOffset 10/20/30) + test_write_sw_systemconst_value_without_annotations_omits_wrapper (empty-wrapper case) + new TestSwSystemconstantValueSetRoundTrip (document-level save→load: ref value+DEST, ARNumerical 42, annotation origin, plus a second empty value — field values, not len); 1 Red / 63 passed — Red is real: writer emits ANNOTATIONS at index 0 vs XSD order (the Step 1 finding; existing writer tests never asserted order); honest passes: reader field reads and the document-level round-trip already passed (parser reads by path, order-independent), no-wrapper write already correct
  - [x] Step 6 — Update parser & writer (Green) — writer writeSwSystemconstValue (arxml_writer.py) reordered to the XSD 00052 element-group order SW-SYSTEMCONST-REF → VALUE → ANNOTATIONS (was ANNOTATIONS first — the Step 1/Step 5 Red closed); parser readSwSystemconstValue (arxml_parser.py) mirrored to the same read order (functionally order-independent — reads by path — reordered for symmetry, mutator calls unchanged: setSwSystemconstRef/setValue/addAnnotation); both consumers (readSwSystemconstantValueSetSwSystemconstantValues / writeSwSystemconstantValueSetSwSystemconstantValues) untouched; Rule 0013.2 name pairs verified (setSwSystemconstRef↔readSwSystemconstValue, setValue↔writeSwSystemconstValue getters, no cross pairs, no chained mutators); Green: 184/184 across test_arxml_writer.py + test_arxml_parser_snippets.py + test_VariantHandling.py
  - [x] Step 7 — Update checklist comment — 6-column format written in the Step 3 body replacement, verified at this step: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 7.9, p.235` (home doc; CP SystemTemplate F.129 + FO FMXF C.19 are row-identical reproductions with caption-placement extraction artifacts — content verified above/below the captions), rows in source/markdown displayed order (`__init__`, getAnnotations, addAnnotation, getSwSystemconstRef, setSwSystemconstRef, getValue, setValue), all `[x]` with release R23-11; reader [x] on addAnnotation/setSwSystemconstRef/setValue (parser sets), writer [x] on getAnnotations/getSwSystemconstRef/getValue (writer gets), `__init__` [—]/[—]; old 4-column no-Spec-line all-[ ] checklist replaced; marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly annotations + swSystemconstRef + value, names per Rule 0001.5 with the documented compuMethodRef/variantCriterionRef Kind-suffix precedents); the Step 1 findings were fixed in-band and leave no rows: (1) value typing ARNumerical→Optional[ARNumerical] + missing setter return annotations (Rule 0014: fixed ⇒ no row), (2) verbose class docstring + docstring-less accessors wiped → Table 7.9 Notes verbatim (class + inline comments incl. Stereotypes/Tags + getters + setters with None-no-op sentences), (3) old 4-column checklist rebuilt 6-column, (4) writer element order fixed to XSD 10/20/30 — all Rule 0014 fixed-⇒-no-row; v1 tracker (docs/examples/method_deviation_by_class.md) reconciled 2026-09-24: new `## SwSystemconstValue` section added (sync record, no-deviation row, stamp-deferred note, F.129/C.19 caption-artifact note); v2 tracker has no SwSystemconstValue/SwSystemconstantValueSet entries (verified by grep); referenced classes: base ARObject (stamped), member types Annotation (stamped MSR Documentation), RefType + ARNumerical (stamped PrimitiveTypes), consumers SwSystemconstantValueSet (stamped Table 7.25, same module — untouched) + ARPackage (createSwSystemconstantValueSet factory, stamped) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 184/184 (test_VariantHandling.py 56 + test_arxml_parser_snippets.py 62 + test_arxml_writer.py 66, re-run green after black reformat of the 3 extended test files), full suite `uv run python scripts/run_tests.py --no-coverage` 11105 passed / 0 failed (baseline 11092 + 13 net-new: +9 model spec-contract tests, +1 parser empty-fields test, +3 writer tests incl. the document-level round-trip); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I); `npm run black` reformatted the 3 extended test files (canonical formatting only) + `black-check` clean (1150 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (annotations + swSystemconstRef + value, Rule 0001.5 Kind-suffix naming per compuMethodRef/variantCriterionRef precedents), most-derived base ARObject (XSD complexType L116440 abstract="false" composes only AR-OBJECT group + attributeGroup — no mixin groups, no flattening), no fabrication, PDF-typed fields, member/accessor/checklist order = markdown displayed order, XML element order = XSD sequenceOffset 10/20/30 (writer fixed, test-enforced), docstrings verbatim (class Note + inline Notes incl. Stereotypes/Tags + getters without Tags + setters with None-no-op sentences — test-enforced), blank line between __init__ attribute blocks, PEP 526 annotated members, no `# type:`, reader+writer coverage for all 3 attributes, no open deviations, marker withheld (batch instruction), Rule 0007 non-leaf location per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `PostBuildVariantCondition` — ARObject — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — Table 7.6 (AUTOSAR_FO_TPS_GenericStructureTemplate, p.232 via pdf_page.py; home doc L5992) cited; reproductions verified: CP SWCT Table 7.64 (L17412) row-identical (class Note + both attribute rows byte-identical) and FMXF Table C.10 (L2256) caption-shifted — the PostBuildVariantCondition table sits ABOVE the C.10 caption (between the C.9 PositiveInteger caption and C.10) and is row-identical to the home doc; the rows below C.10's caption belong to PostBuildVariantCriterion (Base ARElement chain / ARPackage.element) — the caption-shift artifact confirmed, home doc used as source; concrete class (spec header no "(abstract)"; XSD 00052 L93256 complexType abstract="false"); Package row VariantHandling = module location (non-leaf → __init__.py, Rule 0007); Base ARObject confirmed (XSD L93256 sequence composes AR-OBJECT group + POST-BUILD-VARIANT-CONDITION group only, attributeGroup AR-OBJECT — no mixin groups, no flattening); Aggregated by VariationPoint.postBuildVariantCondition + VariationPointProxy.postBuildVariantCondition — both consume paths implemented (parser readVariationPoint L1306-1311 + readVariationPointProxy L2985-2986; writer L1186-1190 + L4252-4256); 2 attributes displayed order: matching Criterion (PostBuildVariant Criterion, 1, ref → matchingCriterionRef: RefType bare, repo ref convention) + value (Integer, 1, attr → Optional[Integer], Note carries "Stereotypes: atpVariation Tags: vh.latestBindingTime=preCompileTime"); XSD element group L93223 order MATCHING-CRITERION-REF (AR:REF + required DEST POST-BUILD-VARIANT-CRITERION--SUBTYPES-ENUM) → VALUE (AR:INTEGER-VALUE-VARIATION-POINT, mixed="true" empty group → integer as element text; leaf helpers getChildElementOptionalIntegerValue/setChildElementOptionalIntegerValue handle it); field-to-spec both directions exact (no fabricated/missing fields); findings for later steps: (1) existing class docstring carries a stale Package/Base/Stereotypes/Tags/Attributes meta block (the Stereotypes/Tags lines belong to the value attribute) + line-wrapped docstrings — wipe+rewrite at Steps 3/4, (2) existing checklist is 5-column WITH a stale `# Spec verified: R23-11` marker — rebuild 6-column, marker withheld (batch instruction), (3) reader gap: readPostBuildVariantCondition (parser L1289) is the ONLY mandatory getChildElementRefType call in the whole parser (raiseError on absence; XSD minOccurs=0; writer counterpart writes children only when set) — an empty POST-BUILD-VARIANT-CONDITION raises ValueError in strict mode → genuine Step 5 Red, fix at Step 6, (4) the POST-BUILD-VARIANT-CONDITIONS notImplemented branch (parser L1311) is the defensive else for unknown tags only — this class's content is NOT dropped
  - [x] Step 2 — Write model class unit test (Red) — added new TestPostBuildVariantConditionSpecContract to test_VariantHandling.py (mirrors TestSwSystemconstValueSpecContract/TestConditionByFormulaSpecContract; no duplication with the existing TestPostBuildVariantCondition + module-level tests, kept green): verbatim class-Note assertion (Table 7.6, incl. the "(criterion1 == value1) && (condition2 == value2) ..." tail), no-`__init__`-docstring assertion, PDF-typed accessor annotations via typing.get_type_hints (repo style per TestShortNameFragment) for matchingCriterionRef→RefType + value→Optional[Integer] (value + return on setters), getter-docstrings-Note-without-Tags assertion, setter-docstrings-Note+None-noop-sentence assertion, defaults + setter None-noops; 3 Red / 5 passed — Red is real: class docstring carries the stale Package/Base/Stereotypes/Tags/Attributes meta block and all docstrings are line-WRAPPED (not single-line verbatim); honest passes: `__init__` already docstring-less, matchingCriterionRef/value annotations already spec-typed, defaults + None-no-ops already correct (existing 9-condition tests)
  - [x] Step 3 — Implement model class (Green) — whole class body replaced in one pass; field-to-spec both directions re-verified: exactly matchingCriterionRef (RefType bare — spec name `matching Criterion` camelCased + Kind ref suffix per compuMethodRef/variantCriterionRef precedent) + value (Optional[Integer] — spec type Integer → repo Integer class, mult 1) — no fabricated/extra fields; Base stays ARObject (XSD L93256 abstract="false" composes only AR-OBJECT group + POST-BUILD-VARIANT-CONDITION group + AR-OBJECT attributeGroup — no mixin groups); concrete class; member/accessor order matchingCriterionRef→value matches markdown displayed order (Rule 0001.11); setters keep None-no-op + return-self shape; PEP 526 annotated members with blank line between attribute blocks; matchingCriterionRef kept bare `RefType = None` (repo-wide ref convention, SwSystemconstValue.swSystemconstRef precedent); imports untouched (no re-sort, none needed); 64/64 Green in test_VariantHandling.py (56 pre-existing incl. all PostBuildVariantCondition tests + 8 net-new spec-contract)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body replacement (no stale wording survives: the old class docstring's Package/Base/Stereotypes/Tags/Attributes meta block wiped — the Stereotypes atpVariation + Tags vh.latestBindingTime lines belong to the value attribute, not the class); class docstring = Table 7.6 Note verbatim single-line (incl. the "(criterion1 == value1) && (condition2 == value2) ..." tail); inline `__init__` comments = attribute Notes verbatim, value's incl. "Stereotypes: atpVariation Tags: vh.latestBindingTime=preCompileTime" (was missing the suffix); getter docstrings = Note verbatim single-line without Tags; setter docstrings = Note verbatim + None-no-op sentence ("does not overwrite an existing matchingCriterionRef"/"existing value"); no `__init__` docstring; en-masse line-WRAPPED docstrings replaced by the single-line verbatim form (module convention per ConditionByFormula/SwSystemconstValue)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — extended the 2 existing variation-point test homes (extend, no duplication): parser test_arxml_parser_variation_point.py new TestReadPostBuildVariantCondition on the class's own readPostBuildVariantCondition helper — populated-element field reads (ref value + DEST + Integer value, field values not len), empty-element `<POST-BUILD-VARIANT-CONDITION/>` → both fields None (XSD minOccurs=0 for both children), value-only element → ref None + value read; writer test_writer_variation_point.py new TestWritePostBuildVariantCondition — direct writePostBuildVariantCondition element-order assertion child_tags == [MATCHING-CRITERION-REF, VALUE] per XSD 00052 group order (L93223, field values asserted: text + DEST + VALUE text) + empty-condition writes a bare element with no children (the writer side of the empty form); 2 Red / 30 passed across the two files — Red is real: parser readPostBuildVariantCondition L1289 uses the mandatory getChildElementRefType which raiseError's (`ValueError: The attribute MATCHING-CRITERION-REF of <> has not been defined`) in strict mode on both the empty and the value-only element (the Step 1 finding (3)); honest passes: populated read path already correct, writer element order already correct per XSD group order (existing tests never asserted this class's order — now pinned)
  - [x] Step 6 — Update parser & writer (Green) — parser readPostBuildVariantCondition (arxml_parser.py L1287) switched from the mandatory getChildElementRefType (the ONLY mandatory ref call in the whole parser; raiseError on absence → ValueError in strict mode) to getChildElementOptionalRefType with an XSD-basis comment (00052 group POST-BUILD-CONDITION children minOccurs="0" — the empty form is schema-legal and now parses to an empty model); both Step 5 Reds closed (empty + value-only elements parse); writer writePostBuildVariantCondition UNCHANGED — already XSD-ordered (MATCHING-CRITERION-REF → VALUE) and lenient (children only when set), now test-pinned; the VariationPoint/VariationPointProxy consumers (read L1306-1311/L2985-2986, write L1186-1190/L4252-4256) untouched; Rule 0013.2 name pairs verified (readPostBuildVariantCondition→setMatchingCriterionRef/setValue vs writePostBuildVariantCondition→getMatchingCriterionRef/getValue, no cross pairs, no chained mutators); Green: 96/96 across test_arxml_parser_variation_point.py + test_writer_variation_point.py + test_VariantHandling.py
  - [x] Step 7 — Update checklist comment — 6-column format written in the Step 3 body replacement, verified at this step: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 7.6, p.232` (home doc; CP SWCT Table 7.64 is a byte-identical reproduction, FMXF C.10 caption-shifted with the real body above its caption), rows in source/markdown displayed order (`__init__`, getMatchingCriterionRef, setMatchingCriterionRef, getValue, setValue), all `[x]` with release R23-11; reader [x] on setMatchingCriterionRef/setValue (parser sets), writer [x] on getMatchingCriterionRef/getValue (writer gets), `__init__` [—]/[—]; the stale 5-column checklist + `# Spec verified: R23-11` marker replaced/removed — marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly matchingCriterionRef + value, names per Rule 0001.5 Kind-suffix precedents); the Step 1 findings were fixed in-band and leave no rows: (1) stale meta-block class docstring + wrapped docstrings wiped → Table 7.6 Notes verbatim (Rule 0014: fixed ⇒ no row), (2) old 5-column checklist + stale `# Spec verified: R23-11` marker rebuilt 6-column/withheld, (3) reader mandatory getChildElementRefType → getChildElementOptionalRefType (empty/value-only POST-BUILD-VARIANT-CONDITION now parses; writer already emitted the empty form — asymmetry closed); v1 tracker (docs/examples/method_deviation_by_class.md) reconciled 2026-09-24: new `## PostBuildVariantCondition` section added after SwSystemconstValue (sync record, no-deviation row, stamp-deferred note, C.10 caption-shift note) — the only pre-existing mentions were historical notes inside the VariationPointProxy entry (L1178 pending-note superseded by its own Resolved strikethrough, L1202 resolution), no PostBuildVariantCondition deviation rows existed; v2 tracker has no PostBuildVariantCondition entries (verified by grep); referenced classes: base ARObject (stamped), member types RefType + Integer (PrimitiveTypes, stamped), consumers VariationPoint (same module, unstamped — queued rows follow in this file) + VariationPointProxy (stamped, SWC SwcInternalBehavior/VariantHandling.py) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 96/96 (test_VariantHandling.py 64 + test_arxml_parser_variation_point.py + test_writer_variation_point.py, re-run green after black reformat of the extended test file), full suite `uv run python scripts/run_tests.py --no-coverage` 11118 passed / 0 failed (baseline 11105 + 13 net-new: +8 model spec-contract tests, +3 parser read tests, +2 writer tests); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I); `npm run black` reformatted the extended model test file (canonical formatting only) + `black-check` clean (1150 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (matchingCriterionRef + value, Rule 0001.5 Kind-suffix naming per compuMethodRef/variantCriterionRef precedents), most-derived base ARObject (XSD complexType L93256 abstract="false" composes only AR-OBJECT group + attributeGroup — no mixin groups, no flattening), no fabrication, PDF-typed fields (Integer spec type → repo Integer class), member/accessor/checklist order = markdown displayed order (matchingCriterionRef → value), XML element order = XSD 00052 group POST-BUILD-VARIANT-CONDITION sequence MATCHING-CRITERION-REF → VALUE (L93223, writer test-enforced), docstrings verbatim (class Note + inline Notes incl. value's Stereotypes/Tags + getters without Tags + setters with None-no-op sentences — test-enforced byte-verbatim), blank line between __init__ attribute blocks, PEP 526 annotated members, no `# type:`, reader+writer coverage for both attributes (reader on the VariationPoint + VariationPointProxy paths), no open deviations, stamp withheld (batch instruction), Rule 0007 non-leaf location per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `PostBuildVariantCriterion` — ARElement — CP SWCT Table 7.63 (see Step 1 for reproduction findings)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — CP SWCT Table 7.63 (L17401, p.614 via pdf_page.py) cited as primary per batch instruction; reproductions verified row-identical: FO GST Table 7.7 (L6004, p.232 — FO GST DOES carry a numeric table for this class, unlike PostBuildVariantCondition) and CP ECUConfiguration Table F.30 (L9073; tracker cites its PDF p.304); FMXF "Table C.11" (L2276) is caption-shifted — the PostBuildVariantCriterion body renders ABOVE the C.11 caption (L2258-2274, split by inline images: Class/Package/Note/Base/Aggregated-by block then Attribute block) and is row-identical to the primary, while nothing sits under the C.11 caption (C.12's caption follows immediately; the PostBuildVariantCriterionValue body below C.12's caption is in place) — caption-shift artifact confirmed for this class too; table rows: Package VariantHandling (non-leaf → __init__.py per Rule 0007), class Note "This class specifies one particular PostBuildVariantSelector. Tags: atp.recommendedPackage=PostBuildVariantCriterions" (the Tags line belongs to the CLASS here, unlike PostBuildVariantCondition), Base ARElement , ARObject , AtpDefinition , CollectableElement , Identifiable , MultilanguageReferrable , PackageableElement , Referrable (most-derived ARElement — row header confirmed; XSD 00052 L93296 complexType abstract="false" composes the full chain of base groups + own group — no flattening), Aggregated by ARPackage.element; 1 attribute displayed order: compuMethod (CompuMethod, 1, ref → compuMethodRef: RefType per Rule 0001.5 Kind-suffix, the compuMethodRef precedent named in the sibling tracker) — field-to-spec both directions exact (no fabricated/missing fields; existing class had exactly compuMethodRef); XSD 00052 group POST-BUILD-VARIANT-CRITERION (L93273): single COMPU-METHOD-REF (minOccurs=0 maxOccurs=1, AR:REF base, DEST=COMPU-METHOD--SUBTYPES-ENUM required — writer's setChildElementOptionalRefType serializes DEST from the RefType per repo convention); no SemanticallyNeutral-style subtypes (XSD SUBTYPES-ENUM L93316 has only POST-BUILD-VARIANT-CRITERION); consume paths all pre-exist: ARPackage.createPostBuildVariantCriterion (ARPackage.py L1254), parser readARPackageElements dispatch L14067-14069 + readPostBuildVariantCriterion L11409 (readIdentifiable + optional getChildElementOptionalRefType — lenient), writer isinstance branch L13768-13769 + writePostBuildVariantCriterion L12325; VariationPoint/VariationPointProxy consume paths not affected (they aggregate PostBuildVariantCondition, not the criterion); member class PostBuildVariantCriterionValue = next queue row (exists in-module; its tracker "missing" rows are stale — record under its own row, not blocking); findings for later steps: (1) existing class docstring carries a stale Package/Base/Tags/Attributes meta block + wrapped docstrings — the Tags atp.recommendedPackage line belongs to the class Note itself per this table; (2) existing checklist is 5-column WITH stale `# Spec verified: R23-11` marker — rebuild 6-column, marker withheld (batch instruction); (3) v1 tracker `## PostBuildVariantCriterion` row "compuMethodRef missing" is STALE (implemented with full reader/writer coverage) — reconcile at Step 8; v2 tracker has no entries (verified by grep)
  - [x] Step 2 — Write model class unit test (Red) — new TestPostBuildVariantCriterionSpecContract in tests/test_armodel/models/M2/AUTOSARTemplates/GenericStructure/test_VariantHandling.py (mirrors the sibling SpecContract pattern, placed after TestPostBuildVariantConditionSpecContract): test_class_docstring_verbatim (Table 7.63 Note verbatim incl. class-level Tags suffix), test_init_has_no_docstring, test_base_is_arelement (most-derived Base + ARPackage.element aggregation), test_compu_method_ref_typed_ref_type (getter/setter hints via typing.get_type_hints), test_getter_docstring_is_note_verbatim, test_setter_docstring_is_note_with_none_noop; 3 Red / 3 passed — Red is real: the stale meta-block class docstring and the line-wrapped getter/setter docstrings fail the verbatim asserts; honest passes: __init__ already docstring-less, ARElement base and RefType typing already correct; basic behavioral coverage (init/set/get/None-no-op) pre-existed in TestPostBuildVariantCriterion
  - [x] Step 3 — Implement model class (Green) — class body replacement (Steps 3+4 combined per module convention): stale Package/Base/Tags/Attributes meta-block docstring replaced with the Table 7.63 Note verbatim single-line incl. "Tags: atp.recommendedPackage=PostBuildVariantCriterions" (the Tags line belongs to the class per this table — PredefinedVariant/SwSystemconstantValueSet sibling form); field compuMethodRef: RefType (PEP 526 annotated, Kind ref suffix) already spec-exact — no structural change; getter/setter signatures/chaining unchanged; no new referenced classes needed (base ARElement + RefType both exist); Green: 70/70 test_VariantHandling.py (64 prior + 6 new)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body replacement (no stale wording survives: the old multi-paragraph class docstring's Package/Base/Attributes block wiped — unlike PostBuildVariantCondition, this table's Tags atp.recommendedPackage line IS part of the class Note and stays); class docstring = Table 7.63 Note verbatim single-line; inline `__init__` comment = compuMethod Note verbatim (single line, no Tags suffix on the attribute Note); getter docstring = Note verbatim single-line; setter docstring = Note verbatim + None-no-op sentence ("does not overwrite an existing compuMethodRef"); no `__init__` docstring; en-masse line-wrapped docstrings replaced by the single-line verbatim form (module convention per ConditionByFormula/PostBuildVariantCondition)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — extended the 2 existing variation-point test homes (extend, no duplication): parser test_arxml_parser_variation_point.py new TestReadPostBuildVariantCriterion (readPostBuildVariantCriterion populated element → compuMethodRef value + DEST="COMPU-METHOD" field asserts; empty criterion (XSD COMPU-METHOD-REF minOccurs="0") → ref None; readARPackageElements dispatch test → ARPackage.createPostBuildVariantCriterion creates + populates, asserted via getPostBuildVariantCriterions()[0] field values); writer test_writer_variation_point.py new TestWritePostBuildVariantCriterion (writePostBuildVariantCriterion: COMPU-METHOD-REF is the LAST child per XSD 00052 complexType sequence base-groups→own-group L93296, DEST attr + text field asserts + SHORT-NAME; no-ref criterion → no COMPU-METHOD-REF child, the empty form) + TestPostBuildVariantCriterionRoundTrip (document-level createARPackage→createPostBuildVariantCriterion→setCompuMethodRef→save→re-parse via getPostBuildVariantCriterions()[0], short-name + ref value + DEST asserted); first run 2 failed — both TEST-harness defects, not product gaps: (a) dispatch test drove readARPackage on a parentless ARPackage(None) whose getFullName() needs a parent — production parse always has a real parent; switched to readARPackageElements (the dispatch step itself), (b) round-trip built the criterion manually bypassing the factory's addElement registration so the writer never saw it — switched to pkg.createPostBuildVariantCriterion (the documented API, as the ConditionByFormulaRoundTrip sibling does); after the harness fixes 38/38 pass across both files — NO genuine product Red exists for this class (unlike PostBuildVariantCondition's mandatory-ref parser bug, this reader already used the lenient getChildElementOptionalRefType); coverage is now test-pinned where previously compuMethodRef was never set/asserted anywhere
  - [x] Step 6 — Update parser & writer (Green) — NO changes needed (verified, honest pass-immediately): parser readPostBuildVariantCriterion (arxml_parser.py L11409) = readIdentifiable + setCompuMethodRef(getChildElementOptionalRefType(element, "COMPU-METHOD-REF")) — optional ref matches XSD minOccurs="0", lenient; ARPackage dispatch L14067-14069 + factory createPostBuildVariantCriterion (ARPackage.py L1254, duplicate-guarded) pre-exist; writer writePostBuildVariantCriterion (arxml_writer.py L12325) = writeIdentifiable + setChildElementOptionalRefType — own group after base groups per XSD sequence, children emitted only when set; isinstance branch L13768-13769 pre-exists; Rule 0013.2 name pairs verified: readPostBuildVariantCriterion→setCompuMethodRef vs writePostBuildVariantCriterion→getCompuMethodRef, no cross pairs, no chained mutators; DEST serialized from the RefType per repo convention (XSD DEST type COMPU-METHOD--SUBTYPES-ENUM); Green: 38/38 across the two test files
  - [x] Step 7 — Update checklist comment — 6-column format written in the Step 3 body replacement, verified at this step: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 7.63, p.614` (primary per batch instruction; FO GST Table 7.7 p.232 and ECUConfiguration F.30 are row-identical reproductions, FMXF "Table C.11" caption-shifted with the real body above its caption), rows in markdown displayed order (`__init__`, getCompuMethodRef, setCompuMethodRef), all `[x]` with release R23-11; reader [x] on setCompuMethodRef (parser sets), writer [x] on getCompuMethodRef (writer gets), `__init__` [—]/[—]; the stale 5-column checklist + `# Spec verified: R23-11` marker replaced/removed — marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly compuMethodRef, Rule 0001.5 Kind ref suffix per the variantCriterionRef/matchingCriterionRef precedents); the Step 1 findings were fixed in-band and leave no rows: (1) stale meta-block class docstring + wrapped docstrings wiped → Table 7.63 Notes verbatim (Rule 0014: fixed ⇒ no row), (2) old 5-column checklist + stale `# Spec verified: R23-11` marker rebuilt 6-column/withheld; v1 tracker (docs/examples/method_deviation_by_class.md) reconciled 2026-09-24: the `## PostBuildVariantCriterion` "compuMethodRef missing" row was STALE (implemented with full reader/writer coverage since intake) — section rewritten to the sibling PostBuildVariantCondition sync-record form: corrected citation (CP SWCT 7.63 p.614, was ECUConfiguration p.304), Note documenting the caption-shift + Base/dispatch findings, no-deviation row; v2 tracker has no PostBuildVariantCriterion entries (verified by grep); referenced classes: base ARElement (ARPackage module, stamped) + member type RefType (PrimitiveTypes, stamped) + member class CompuMethod (spec ref target — a CompuMethod class exists in the repo, only referenced BY VALUE in RefType, no model dependency) — no missing classes; member class PostBuildVariantCriterionValue = NEXT queue row, its own stale tracker rows recorded under its row, not blocking]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 108/108 (test_VariantHandling.py 70 + test_arxml_parser_variation_point.py 18 + test_writer_variation_point.py 20, re-run green after black reformat of the extended model test file), full suite `uv run python scripts/run_tests.py --no-coverage` 11130 passed / 0 failed (baseline 11118 + 12 net-new: +6 model spec-contract tests, +3 parser read tests, +2 writer tests, +1 writer round-trip test); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I); `npm run black` reformatted the extended model test file (canonical formatting only) + `black-check` clean (1150 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (compuMethodRef only, Rule 0001.5 Kind-ref suffix per variantCriterionRef/matchingCriterionRef precedents), most-derived base ARElement (table Base chain confirmed; XSD 00052 complexType POST-BUILD-VARIANT-CRITERION L93296 abstract="false" composes the full base-group chain + own group — no flattening), no fabrication, PDF-typed field (CompuMethod ref → RefType per repo ref convention), member/accessor/checklist order = markdown displayed order (single attribute, get→set), XML element order = XSD 00052 complexType sequence (own-group COMPU-METHOD-REF after all base groups, L93296 + group L93273; writer test-enforced child_tags[-1]), DEST type COMPU-METHOD--SUBTYPES-ENUM, no SemanticallyNeutral-style subtypes (SUBTYPES-ENUM L93316 single value), docstrings verbatim (class Note incl. class-level Tags + inline Note + getter verbatim + setter with None-no-op — test-enforced byte-verbatim), blank line between __init__ attribute blocks (single attr, trivially satisfied), PEP 526 annotated member, no `# type:`, reader+writer coverage test-pinned incl. ARPackage.element dispatch (readARPackageElements + createPostBuildVariantCriterion factory + writer isinstance branch), no open deviations (v1 tracker stale missing-row reconciled), stamp withheld (batch instruction), Rule 0007 non-leaf location per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `PostBuildVariantCriterionValue` — ARObject — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — FO GST Table 7.27 (p.259 via pdf_page.py) cited as primary; reproductions verified row-identical: FMXF Table C.12 (complete body UNDER its caption — the only in-place one in this family) and CP ECUConfiguration Table F.31 (caption-SHIFTED: the real F.31 body renders ABOVE its caption, the body below it is PredefinedVariant's); table rows: Package VariantHandling (non-leaf → __init__.py per Rule 0007), class Note "This class specifies the value which shall be assigned to a particular variant criterion in order to bind the variation point. If multiple criterion/value pairs are specified, they all shall match to bind the variation point." (NO Tags line on this class Note), Base ARObject (row header confirmed; XSD 00052 complexType L93362 composes group AR:AR-OBJECT (empty sequence) + own group — no flattening), Aggregated by PostBuildVariantCriterionValueSet.postBuildVariantCriterionValue; 3 attributes displayed order: annotation (Annotation, *, aggr, offset 30), value (Integer, 1, attr, Stereotypes atpVariation + Tags vh.latestBindingTime=preCompileTime xml.sequenceOffset=20), variantCriterion (PostBuildVariantCriterion, 1, ref → variantCriterionRef: RefType per Rule 0001.5, offset 10); field-to-spec both directions exact (existing class has exactly annotations/value/variantCriterionRef); XSD 00052 group POST-BUILD-VARIANT-CRITERION-VALUE (L93322) element order VARIANT-CRITERION-REF (DEST=POST-BUILD-VARIANT-CRITERION required) → VALUE (type INTEGER-VALUE-VARIATION-POINT mixed wrapper — same wrapper type as PostBuildVariantCondition.VALUE, whose getChildElementOptionalIntegerValue/setChildElementOptionalIntegerValue convention applies) → ANNOTATIONS wrapper (choice unbounded ANNOTATION); CONSUME-PATH FINDING: the class-facts premise was corrected — PostBuildVariantCriterion aggregates ONLY compuMethodRef (Table 7.7/7.63), the actual aggregator PostBuildVariantCriterionValueSet.postBuildVariantCriterionValue does NOT exist in armodel (no model class, no ARPackage dispatch for POST-BUILD-VARIANT-CRITERION-VALUE-SET, not queued in Group8/9) so no XML dispatch reaches this class today; plan: standalone reader/writer helpers readPostBuildVariantCriterionValue/writePostBuildVariantCriterionValue mirroring the Condition sibling, tested directly in the variation-point test files; existing class defects for Step 2/4: class docstring is a stale paraphrase with typos ("specifies a the value", "must must match") + a Package/Base/Attributes meta block; checklist is 5-column (no release column) with reader/writer [ ] unchecked; tracker has 2 STALE "missing" rows (annotation, variantCriterionRef — implemented since intake) → reconcile at Step 8
  - [x] Step 2 — Write model class unit test (Red) — added new TestPostBuildVariantCriterionValueSpecContract beside the existing functional TestPostBuildVariantCriterionValue (extend, no duplication): verbatim class-Note assertion, no-`__init__`-docstring assertion, Base-is-ARObject-and-not-ARElement assertion, PDF-typed accessor annotations via typing.get_type_hints (repo style per the sibling SpecContract classes) for annotations→List[Annotation] + value→Optional[Integer] + variantCriterionRef→RefType (value + return on setters), getter-docstrings-Note-without-Tags assertion, setter-docstrings-Note+None-no-op-sentence assertion; first run 2 Red / 13 passed — Red is real: class docstring is a stale paraphrase with typos ("specifies a the value", "must must match") plus a Package/Base/Attributes meta block, and ALL THREE setter docstrings are line-wrapped (internal "\n" breaks the verbatim equality); honest passes: `__init__` already docstring-less, Base already exactly ARObject, all six accessors already spec-typed, all three getters already verbatim single-line; pre-existing functional coverage (7 tests: init/add/get/None-noop/chaining) kept green
  - [x] Step 3 — Implement model class (Green) — class body replacement (Steps 3+4 combined per module convention): fields already spec-exact (annotations: List[Annotation] typed list per Rule 0004, value: Optional[Integer], variantCriterionRef: RefType Kind-ref suffix) — no structural change; getter/setter signatures/chaining unchanged; no new referenced classes needed (base ARObject + member types Annotation/Integer/RefType all exist); checklist rebuilt 6-column with release R23-11 in the same replacement, reader/writer [x] rows made true by Step 6 before the Step 7 verification; Green: 78/78 test_VariantHandling.py (70 prior + 8 new spec-contract)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body replacement (no stale wording survives: the stale-paraphrase class docstring's typos ("a the", "must must") AND its Package/Base/Attributes meta block wiped; all three line-wrapped setter docstrings replaced); class docstring = Table 7.27 Note verbatim single-line (NO Tags line on this class Note — unlike PostBuildVariantCriterion); inline `__init__` comments = attribute Notes verbatim without the Tags suffix (single-line; the Tags/Stereotypes xml.sequenceOffset=30/20/10 + atpVariation + vh.latestBindingTime=preCompileTime remain XSD/table-pinned via reader/writer order tests, not docstrings); getter docstrings = Note verbatim single-line; setter/adder docstrings = Note verbatim + None-no-op sentence ("is not appended" for addAnnotation, "does not overwrite an existing <field>" for setters); no `__init__` docstring
  - [x] Step 5 — Write reader/writer round-trip test (Red) — extended the 2 existing variation-point test homes (extend, no duplication): parser test_arxml_parser_variation_point.py new TestReadPostBuildVariantCriterionValue (full element → VARIANT-CRITERION-REF value+DEST="POST-BUILD-VARIANT-CRITERION" asserts + VALUE 42 + one ANNOTATION with L4 label asserted deep to value; empty criterion value (XSD minOccurs="0" on all three children) → ref/value None + annotations []; VALUE-only form → ref None + value read + no annotations); writer test_writer_variation_point.py new TestWritePostBuildVariantCriterionValue (element-order test: child_tags == [VARIANT-CRITERION-REF, VALUE, ANNOTATIONS] per XSD group sequenceOffset 10/20/30, DEST attr + texts + ANNOTATION/LABEL/L-4 asserted; no-fields criterion value → bare element, the empty form) + TestPostBuildVariantCriterionValueRoundTrip (write → ET.tostring → re-parse → readPostBuildVariantCriterionValue → every field value asserted incl. DEST + label); Red is GENUINE this time: first run 6 failed / 38 passed — all 6 are AttributeError because readPostBuildVariantCriterionValue/writePostBuildVariantCriterionValue do not exist anywhere (unlike the Criterion sibling whose reader pre-existed); the round-trip is ELEMENT-level (write→tostring→fromstring→read) because the aggregating PostBuildVariantCriterionValueSet class does not exist so no document-level dispatch carrier exists — docstring on the round-trip class records this
  - [x] Step 6 — Update parser & writer (Green) — added the missing helper PAIR (the genuine Red closed): parser readPostBuildVariantCriterionValue (arxml_parser.py, directly after readPostBuildVariantCondition) = readARObject (Base ARObject — no Identifiable groups) + setVariantCriterionRef(getChildElementOptionalRefType "VARIANT-CRITERION-REF") + setValue(getChildElementOptionalIntegerValue "VALUE" — the INTEGER-VALUE-VARIATION-POINT mixed wrapper, same helper convention the Condition sibling uses for its identically-typed VALUE) + addAnnotation per self.getAnnotations(element) "ANNOTATIONS/*" loop; lenient on the empty form per XSD minOccurs="0" on all three children (comment documents the XSD fact); writer writePostBuildVariantCriterionValue (arxml_writer.py, directly after writePostBuildVariantCondition) = SubElement "POST-BUILD-VARIANT-CRITERION-VALUE" + writeARObject + setChildElementOptionalRefType + setChildElementOptionalIntegerValue + setAnnotations (emits ANNOTATIONS wrapper only when non-empty); element order = XSD group sequence VARIANT-CRITERION-REF(10) → VALUE(20) → ANNOTATIONS(30), test-enforced; imports: PostBuildVariantCriterionValue added to both VariantHandling import blocks (alphabetical, no re-sort); NO dispatch/factory added (aggregator class absent — out of scope for this row, noted at Step 8); 2 test-harness fixes during Green (not product defects): L4 label tag is "L-4" (dash) not "L4", and the element-level re-parse must serialize the criterion-value child (not the PARENT carrier) so the namespaced find locates it; Rule 0013.2 name pairs verified: read→setVariantCriterionRef/setValue/addAnnotation vs write→getVariantCriterionRef/getValue/getAnnotations, no cross pairs, no chained mutators; Green: 44/44 across the two test files
  - [x] Step 7 — Update checklist comment — 6-column format written in the Step 3 body replacement, verified at this step: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 7.27, p.259` (primary; p.259 via pdf_page.py — FMXF C.12 and ECUConfig F.31 are row-identical reproductions with the caption shifts recorded at Step 1), rows in markdown displayed order (`__init__`, annotation pair getAnnotations/addAnnotation, value pair getValue/setValue, variantCriterion pair getVariantCriterionRef/setVariantCriterionRef), all `[x]` with release R23-11; reader [x] on the three mutator rows (reader calls set/add), writer [x] on the three getter rows (writer calls get), `__init__` [—]/[—]; reader/writer [x] rows made true by the Step 6 helpers + tests before this verification; the old 5-column checklist (reader/writer `[ ]`, no release column) fully replaced; marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly the three Table 7.27 attributes, Rule 0001.5 Kind ref suffix on variantCriterionRef + plural typed list for the `*` aggr per Rule 0004); the Step 1 findings were fixed in-band and leave no rows: (1) stale-paraphrase class docstring + meta block + wrapped setter docstrings wiped → Table 7.27 Notes verbatim (Rule 0014: fixed ⇒ no row), (2) old 5-column checklist rebuilt 6-column/withheld marker, (3) missing reader/writer helpers added as the class's own pair (genuine Step 5/6 Red closed); v1 tracker (docs/examples/method_deviation_by_class.md) reconciled 2026-09-24: the `## PostBuildVariantCriterionValue` "annotation missing" + "variantCriterionRef missing" rows were STALE (both implemented since intake) — section rewritten to the sibling PostBuildVariantCriterion sync-record form: corrected citation (FO GST 7.27 p.259, was ECUConfiguration p.305), Note documenting the caption-shift + consume-path disposition (aggregator PostBuildVariantCriterionValueSet absent from armodel and unqueued — standalone helper pair test-pinned at element level until a dispatcher exists), no-deviation row; v2 tracker has no PostBuildVariantCriterionValue entries and neither has staged_class_sync_review.md (verified by grep); referenced classes: base ARObject (ArObject module, stamped) + member types Annotation (Documentation module, stamped) / Integer + RefType (PrimitiveTypes, stamped) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 122/122 (test_VariantHandling.py 78 + test_arxml_parser_variation_point.py 21 + test_writer_variation_point.py 23, re-run green after black reformat of the extended model test file), full suite `uv run python scripts/run_tests.py --no-coverage` 11144 passed / 0 failed (baseline 11130 + 14 net-new: +8 model spec-contract tests, +3 parser read tests, +2 writer tests, +1 element-level writer→reader round-trip test); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I); `npm run black` reformatted the extended model test file (canonical formatting only) + `black-check` clean (1150 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (exactly annotation/value/variantCriterion — plural typed list for the `*` aggr, Optional[Integer] per the PDF table with the XSD INTEGER-VALUE-VARIATION-POINT wrapper handled at the reader/writer layer per Rule 0015, variantCriterionRef Kind-ref suffix), most-derived base ARObject (table Base row confirmed; XSD 00052 complexType L93362 composes group AR:AR-OBJECT (empty sequence) + own group only — no flattening; NOT an ARElement, Aggregated-by is PostBuildVariantCriterionValueSet.postBuildVariantCriterionValue), no fabrication, member/accessor/checklist order = markdown displayed order (annotation → value → variantCriterion), XML element order = XSD group POST-BUILD-VARIANT-CRITERION-VALUE sequence (L93322: VARIANT-CRITERION-REF offset 10 → VALUE offset 20 → ANNOTATIONS offset 30; writer test-enforced child_tags == [VARIANT-CRITERION-REF, VALUE, ANNOTATIONS]), DEST type POST-BUILD-VARIANT-CRITERION--SUBTYPES-ENUM, docstrings verbatim (class Note — no Tags line on this class Note — + inline Notes + getter verbatim + setter/adder with None-no-op — test-enforced byte-verbatim), blank line between all three __init__ attribute blocks (verified by eye), PEP 526 annotated members, no `# type:`, reader+writer coverage for all three attributes test-pinned via the class's own helper pair, no open deviations (v1 tracker stale rows reconciled), stamp withheld (batch instruction), Rule 0007 non-leaf location per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `OffsetTimingConstraint` — TimingConstraint — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Timing/TimingConstraint/OffsetConstraint.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - after `MultidimensionalTime`
  - [x] Step 1 — Sync members & description from spec — R23-11 CP_TPS_TimingExtensions Table 3.66 (L2782, p.114 via pdf_page.py) cited as primary; R4.3.1 reproduction Table 7.15 (L3864) verified row-identical (PDF-extraction spacing artifacts only: "Offset Constraint"/"Multidimensiona lTime") — R23-11 is the citation; table rows: Package M2::AUTOSARTemplates::CommonStructure::Timing::TimingConstraint::OffsetConstraint (leaf package → OffsetConstraint.py per Rule 0007 — current location correct), class Note verbatim confirmed = current docstring body MINUS the trailing "(source/target -> TimingDescriptionEvent placeholders, Rule 0001.10)" paragraph (intake artifact — TimingDescriptionEvent IS modeled + stamped R23-11 Table D.63, so "placeholder" is factually stale; wipe at Step 4), Base ARObject , Identifiable , MultilanguageReferrable , Referrable , TimingConstraint , Traceable (most-derived TimingConstraint — stamped R23-11 Table D.61, class TimingConstraint(Traceable, VariationPointCapable, ABC); XSD 00052 L86877 complexType abstract="false" composes AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → TRACEABLE → TIMING-CONSTRAINT → own group — no flattening), Aggregated by TimingExtension.timingGuarantee/TimingExtension.timingRequirement (consume path pre-exists: parser dispatch L8387/L8418-8420 + writer dispatch L7943 keyed on OFFSET-TIMING-CONSTRAINT); 4 attributes displayed order: maximum (MultidimensionalTime, 0..1, aggr), minimum (MultidimensionalTime, 0..1, aggr), source (TimingDescriptionEvent, 0..1, ref → sourceRef: RefType per Rule 0001.5), target (→ targetRef) — field-to-spec both directions exact (no fabricated/missing fields); XSD 00052 group OFFSET-TIMING-CONSTRAINT (L86823): SOURCE-REF → TARGET-REF (both 0..1, AR:REF base, DEST=TIMING-DESCRIPTION-EVENT--SUBTYPES-ENUM required) → MINIMUM (xml.sequenceOffset=10) → MAXIMUM (xml.sequenceOffset=20) — XSD XML order differs from markdown displayed order; member order follows markdown (Rule 0001.11), reader/writer follow XSD (readOffsetTimingConstraint L3521 / writeOffsetTimingConstraint L4657 already in XSD order, base helpers readIdentifiable+readTraceable+TIMING-CONDITION-REF wired); intake artifacts found: stale `# Spec verified: R23-11` marker (withheld per batch instruction), 5-column checklist (→ 6-column at Step 7), placeholder annotations (wiped at Step 4); v1 tracker L3130 has 4 stale "missing" rows (all members implemented since intake) — reconcile Step 8
  - [x] Step 2 — Write model class unit test (Red) — extended existing test_OffsetConstraint.py (no duplication: intake already shipped 9 functional tests — init defaults, get/set round-trip + None no-op for all four accessors) with TestOffsetTimingConstraintSpecContract mirroring the PostBuildVariantCriterionValue sibling form: verbatim class-Note assertion, no-`__init__`-docstring, base = TimingConstraint (most-derived, + Traceable via its base chain), typing.get_type_hints pairs (maximum/minimum → Optional[MultidimensionalTime], source/target → Optional[RefType] per Rule 0001.5 Kind-ref suffix), getter Notes verbatim (without the Tags suffix — family precedent LatencyTimingConstraint + batch sibling test form), setter Notes + None-no-op sentence; 1 Red (class docstring carries the stale "(source/target -> TimingDescriptionEvent placeholders, Rule 0001.10)" paragraph) / 15 passed — getter/setter docstrings, type hints, base chain and functional shape already spec-correct from intake, honestly noted
  - [x] Step 3 — Implement model class (Green) — model shape already spec-correct, no code change needed: dedicated typed fields exactly the 4 Table 3.66 rows in displayed order (maximum/minimum: Optional[MultidimensionalTime] `aggr`; sourceRef/targetRef: Optional[RefType] — Kind ref → RefType per Rule 0001.5; no `*` aggr so no typed list, no createXxx — no Referrable children), most-derived base TimingConstraint (concrete class, no abstract guard needed), setter chaining returns self, None no-ops guarded; 16/16 tests Green except the docstring Red owned by Step 4 (1 docstring-only failure, impl assertions all Green)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped the three stale docstrings and rewrote from the Table 3.66 markdown: class docstring = Note verbatim (dropped the trailing "(source/target -> TimingDescriptionEvent placeholders, Rule 0001.10)" paragraph — stale, TimingDescriptionEvent is modeled + stamped R23-11 Table D.63, unmasked as a genuine Red by the Step 2 test); sourceRef/targetRef inline comments = Notes verbatim (dropped "(TimingDescriptionEvent placeholder, Rule 0001.10)" suffixes); maximum/minimum inline comments kept intake form = Note verbatim incl. "Tags: xml.sequenceOffset=20/10" tail (family precedent LatencyTimingConstraint inline-comment style); getter/setter docstrings verified byte-verbatim vs the Notes (without the Tags tail, with None-no-op sentences on setters) — already conforming from intake, unchanged and now test-pinned
  - [x] Step 5 — Write reader/writer round-trip test (Red) — intake coverage verified complete and extended with the missing aggregation-level round-trip: pre-existing test_read_offset_timing_constraint (field values) + TestWriteOffsetTimingConstraint round-trip (XSD element order source<target<minimum<maximum asserted + all field values + DEST) + empty case (all four children absent, read-back None); ADDED test_read_offset_timing_constraint_via_timing_extension (parser: OFFSET-TIMING-CONSTRAINT under TIMING-GUARANTEES + TIMING-REQUIREMENTS through readTimingExtension dispatch → isinstance + field values + wrapper-list emptiness on requirement sourceRef) and test_round_trip_offset_timing_constraint_via_timing_extension (writer→reader through writeTimingExtension/getTimingGuarantees·getTimingRequirements — the class's Table 3.66 Aggregated-by row TimingExtension.timingGuarantee/timingRequirement); both new tests pass immediately — the dispatch pre-existed from intake, the gap was coverage not code, honestly noted
  - [x] Step 6 — Update parser & writer (Green) — no code change needed: readOffsetTimingConstraint (arxml_parser.py L3521) = readTimingConstraint base (readIdentifiable + readTraceable + TIMING-CONDITION-REF) + SOURCE-REF/TARGET-REF via getChildElementOptionalRefType + MINIMUM/MAXIMUM via readMultidimensionalTime; writeOffsetTimingConstraint (arxml_writer.py L4657) mirrors in the same XSD group OFFSET-TIMING-CONSTRAINT sequence (L86823); dispatch keyed on OFFSET-TIMING-CONSTRAINT both directions (parser L8387/L8418-8420, writer L7943); DEST emission via setChildElementOptionalRefType proven by round-trip getDest assertions; 49/49 parser+writer timing tests Green
  - [x] Step 7 — Update checklist comment — 6-column format with release column rebuilt in OffsetConstraint.py (rows source order: __init__, get/setMaximum, get/setMinimum, get/setSourceRef, get/setTargetRef; reader [x] on setter rows, writer [x] on getter rows — matches readOffsetTimingConstraint/writeOffsetTimingConstraint call sites); stale orphan-intake `# Spec verified: R23-11` marker REMOVED (withheld per batch instruction — deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly the four Table 3.66 attributes, Rule 0001.5 Kind-ref suffix on sourceRef/targetRef, Optional[MultidimensionalTime] for the two `aggr` attrs); the Step 1/4 findings were fixed in-band and leave no rows: (1) stale "(TimingDescriptionEvent placeholder, Rule 0001.10)" annotations in the class docstring + sourceRef/targetRef inline comments wiped (TimingDescriptionEvent is modeled + stamped R23-11 Table D.63 — refs are plain RefType/DEST values, no placeholder involved) → Notes verbatim (Rule 0014: fixed ⇒ no row), (2) old 5-column checklist + stale orphan-intake `# Spec verified: R23-11` stamp rebuilt 6-column/stamp withheld (batch instruction), (3) v1 tracker (method_deviation_by_class.md) `## OffsetTimingConstraint` reconciled 2026-09-24: the 4 "missing" rows were STALE (all members implemented since intake) — section rewritten to the sibling PostBuildVariantCriterionValue sync-record form (corrected citation Table 3.66 p.114 + R4.3.1 Table 7.15 reproduction note, full sync Note, no-deviation row); v2 tracker (method_deviation_by_class_v2.md) and staged_class_sync_review.md have NO OffsetTimingConstraint entries (verified by grep); referenced classes: base TimingConstraint (stamped R23-11 Table D.61) + Traceable (RequirementsTracing), member types MultidimensionalTime (stamped R23-11 FO GST Table 4.74, synced 3dd45559) + RefType (PrimitiveTypes, stamped) + ref-target TimingDescriptionEvent (stamped R23-11 Table D.63) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 65/65 (test_OffsetConstraint.py 16 — 9 functional + 7 spec-contract, re-run green after black reformat; test_parser_timing_constraints.py 25; test_writer_timing_constraints.py 24), full suite `uv run python scripts/run_tests.py --no-coverage` 11157 passed / 0 failed (baseline 11148 + 9 net-new: +7 model spec-contract tests, +1 parser TimingExtension-dispatch read test, +1 writer TimingExtension-dispatch round-trip test; re-run green after black reformat of the extended model test file); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I — one I001 in the NEW test file import block fixed by hand; tests are outside the do-not-re-sort zone, models/parser/writer untouched); `npm run black` reformatted the extended model test file (canonical formatting only) + `black-check` clean (1150 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (exactly maximum/minimum/source/target — Optional[MultidimensionalTime] for the two 0..1 aggr attrs, Optional[RefType] with Kind-ref suffix for source/target), most-derived base TimingConstraint (table row header confirmed; XSD 00052 complexType OFFSET-TIMING-CONSTRAINT L86877 abstract="false" composes AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → TRACEABLE → TIMING-CONSTRAINT → own group — no flattening; Aggregated-by TimingExtension.timingGuarantee/timingRequirement dispatch pre-exists both sides), no fabrication, member/accessor/checklist order = markdown displayed order (maximum → minimum → source → target; XSD XML order SOURCE-REF → TARGET-REF → MINIMUM → MAXIMUM governs reader/writer only — writer test-enforced source<target<minimum<maximum), DEST TIMING-DESCRIPTION-EVENT--SUBTYPES-ENUM (writer-emitted, round-trip getDest test-pinned), docstrings verbatim (class Note byte-verbatim test-pinned; inline comments keep Tags tails, getters drop Tags, setters append None-no-op — test-pinned), blank line between all four __init__ attribute blocks (verified by eye), PEP 526 annotated members, no `# type:`, reader+writer coverage for all four attributes test-pinned at element level AND through the TimingExtension aggregation round-trip, no open deviations (v1 tracker stale rows reconciled; v2 tracker + staged_class_sync_review.md have no entries), stamp withheld (batch instruction), Rule 0007 leaf-package location OffsetConstraint.py per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `SynchronizationTimingConstraint` — TimingConstraint — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Timing/TimingConstraint/SynchronizationTiming.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - after `MultidimensionalTime`
  - [x] Step 1 — Sync members & description from spec — R23-11 CP_TPS_TimingExtensions Table 3.54 (L2249-2260, p.92 via pdf_page.py) cited as primary; R4.3.1 reproduction Table 7.12 verified row-identical (caption-shift artifact: table body renders ABOVE its caption, body L3658-3668 with caption at L3670; PDF-extraction spacing artifacts only: "Multidimensiona lTime", "eventOccu rrenceKind", "synchroniz ationConst raintTyp") — R23-11 is the citation; table rows: Package M2::AUTOSARTemplates::CommonStructure::Timing::TimingConstraint::SynchronizationTiming (leaf package → SynchronizationTiming.py per Rule 0007 — current location correct), class Note "This constraint is used to restrict the timing behavior of different, but correlated events or event chains, with regard to synchronization. …" (markdown carries a stray leading "Constraint " PDF label artifact — dropped; markdown Note truncated mid-sentence at "within the given time" by an inline image at L2262 — the tail "interval are permitted." confirmed by the XSD 00052 group SYNCHRONIZATION-TIMING-CONSTRAINT documentation (L118881) and the R4.3.1 Note; tail completion = extraction-artifact fix, documented), Base ARObject , Identifiable , MultilanguageReferrable , Referrable , TimingConstraint , Traceable (most-derived TimingConstraint — row header confirmed; XSD 00052 complexType SYNCHRONIZATION-TIMING-CONSTRAINT L118962 abstract="false" composes AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → TRACEABLE → TIMING-CONSTRAINT → own group — no flattening), Aggregated by TimingExtension.timingGuarantee/TimingExtension.timingRequirement (consume path pre-exists: parser dispatch L8391 allowlist + L8430-8432 branch keyed on SYNCHRONIZATION-TIMING-CONSTRAINT, writer tag_map L7947); 5 attributes displayed order: eventOccurrenceKind (EventOccurrenceKindEnum, 0..1, attr), scope (TimingDescriptionEventChain, *, ref → scopeRefs), scopeEvent (TimingDescriptionEvent, *, ref → scopeEventRefs), synchronizationConstraintType (SynchronizationTypeEnum, 0..1, attr), tolerance (MultidimensionalTime, 0..1, aggr) — field-to-spec both directions exact (no fabricated/missing fields; `*` ref → Refs plural per Rule 0001.5); XSD 00052 group SYNCHRONIZATION-TIMING-CONSTRAINT (L118881) XML element order EVENT-OCCURRENCE-KIND → SCOPE-EVENT-REFS → SCOPE-REFS → SYNCHRONIZATION-CONSTRAINT-TYPE → TOLERANCE (differs from markdown displayed order: members follow markdown per Rule 0001.11, reader/writer follow XSD — readSynchronizationTimingConstraint L3448 / writeSynchronizationTimingConstraint L4631 already in XSD order); DEST required on both ref wrappers: SCOPE-EVENT-REF → TIMING-DESCRIPTION-EVENT--SUBTYPES-ENUM, SCOPE-REF → TIMING-DESCRIPTION-EVENT-CHAIN--SUBTYPES-ENUM (writer emits via setChildElementOptionalRefType, round-trip test already pins DEST); no xml.sequenceOffset tags on this class's XSD elements (inline comments stay plain Notes, unlike Offset); intake artifacts found: stale orphan-intake `# Spec verified: R23-11` marker on the main class (removed/withheld per batch instruction), 5-column checklist (→ 6-column at Step 7), class docstring carries 3 [constr_4522/4514/4521] paragraphs NOT part of the Note row (they are the section-level constraints table at L8041-8049; sibling convention = Note verbatim only — no other Timing model class embeds constr_ text; constr_4513 "shall reference at least two events" also exists in that table but wasn't included — evidence of a partial intake artifact; wiped at Step 4); in-file sibling enums SynchronizationTypeEnum (Table 3.55) + EventOccurrenceKindEnum (Table 3.56) already stamped R23-11, literals match markdown — untouched this pass (LatencyTimingConstraint sibling precedent); v1 tracker (method_deviation_by_class.md) L3025 has 5 stale "missing" rows (all 5 members implemented since intake) — reconcile Step 8
  - [x] Step 2 — Write model class unit test (Red) — extended existing test_SynchronizationTiming.py (12 functional tests pre-existed from intake: init defaults, get/set round-trip + None no-op for all five accessors, both in-file enums) with TestSynchronizationTimingConstraintSpecContract mirroring the OffsetTimingConstraint sibling form (9 tests): verbatim class-Note assertion (markdown stray "Constraint " label dropped, image-cut tail "interval are permitted." restored from the XSD group doc), no-__init__-docstring, base = TimingConstraint (+ Traceable via its chain), typing.get_type_hints pairs (eventOccurrenceKind → Optional[EventOccurrenceKindEnum]; scope/scopeEvent → add Optional[RefType] / get List[RefType]; synchronizationConstraintType → Optional[SynchronizationTypeEnum]; tolerance → Optional[MultidimensionalTime]), getter Notes verbatim, setter/adder Notes + None-no-op sentences; 3 Red / 23 passed — 1 designed Red (class docstring: constr paragraphs + missing tail) plus 2 GENUINE intake docstring drifts unmasked by the verbatim pins: getScopeEvents carries a trailing "." and addScopeEvent a "]." before the None-no-op sentence, but the scopeEvent spec Note ends WITHOUT a period (markdown L2258) — both owned by Step 4; all impl/hints/base assertions Green immediately (intake shape spec-correct), honestly noted
  - [x] Step 3 — Implement model class (Green) — no code change needed: model shape already spec-correct from intake — dedicated typed fields exactly the 5 Table 3.54 rows in displayed order (eventOccurrenceKind: Optional[EventOccurrenceKindEnum] 0..1 attr; scopeRefs/scopeEventRefs: List[RefType] dedicated typed lists for the two `*` refs — Rule 0004, never registry filters, Rule 0001.5 Kind-suffix; synchronizationConstraintType: Optional[SynchronizationTypeEnum] 0..1 attr; tolerance: Optional[MultidimensionalTime] 0..1 aggr), most-derived base TimingConstraint (concrete class, no abstract guard), no createXxx — no Referrable children (scope/scopeEvent are refs), setter chaining returns self, None no-ops guarded, blank line between every __init__ attribute block, PEP 526 annotations; evidence: the Step 2 Red run passed all 23 impl/hints/base/functional assertions — the only 3 failures are docstring-only Reds owned by Step 4
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — wiped the class docstring and rewrote from the Table 3.54 markdown: class docstring = Note verbatim as a single line (dropped the 3 extraneous [constr_4522/4514/4521] paragraphs — they are the section-level constraints table at L8041-8049, not the Note row, and constr_4513 was already missing, evidence of a partial intake artifact; dropped the stray leading "Constraint " PDF label artifact; restored the tail "interval are permitted." cut by the inline image at L2262, confirmed by XSD 00052 group doc L118881 + R4.3.1 Note); fixed the 2 verbatim drifts unmasked by Step 2: getScopeEvents trailing "." dropped and addScopeEvent's inserted "." dropped (the scopeEvent Note ends WITHOUT a period, markdown L2258); all 5 inline __init__ comments verified byte-verbatim vs the Notes unchanged (scope Note ends WITH period, scopeEvent without — asymmetry is in the spec itself; no xml.sequenceOffset Tags on this class's XSD elements so no Tags tails, unlike Offset); getter/setter docstrings verified byte-verbatim vs the Notes by programmatic diff (15/15 positions OK: 5 getters = Note, 3 setters = Note + "A None value is a no-op and does not overwrite an existing <field>.", 2 adders = Note + "A None value is a no-op.", 5 inline comments = Note, __init__ docstring None); sibling enums SynchronizationTypeEnum/EventOccurrenceKindEnum untouched (stamped in a prior batch); 26/26 model tests Green after rewrite
  - [x] Step 5 — Write reader/writer round-trip test (Red) — intake coverage verified complete at element level (parser test_read_synchronization_timing_constraint asserts field values incl. DEST; writer TestWriteSynchronizationTimingConstraint round-trip asserts XSD element order kind<scope_events<scopes<sync_type<tolerance + DEST + values + empty case) and extended with the missing aggregation-level round-trip (the Table 3.54 Aggregated-by row): ADDED test_read_synchronization_timing_constraint_via_timing_extension (parser: SYNCHRONIZATION-TIMING-CONSTRAINT under TIMING-GUARANTEES + TIMING-REQUIREMENTS through readTimingExtension dispatch → isinstance + field values + wrapper-list emptiness on requirement scopeEvents/kind/syncType) and test_round_trip_synchronization_timing_constraint_via_timing_extension (writer→reader through writeTimingExtension/getTimingGuarantees·getTimingRequirements, both directions' field values + DEST); both new tests pass immediately — the dispatch pre-existed from intake, the gap was coverage not code, honestly noted
  - [x] Step 6 — Update parser & writer (Green) — no code change needed: readSynchronizationTimingConstraint (arxml_parser.py L3448) = readTimingConstraint base (TIMING-CONDITION-REF wired) + EVENT-OCCURRENCE-KIND via getChildElementOptionalLiteral→setEventOccurrenceKind + SCOPE-EVENT-REFS/SCOPE-EVENT-REF via getChildElementRefTypeList→addScopeEvent + SCOPE-REFS/SCOPE-REF→addScope + SYNCHRONIZATION-CONSTRAINT-TYPE→setSynchronizationConstraintType + TOLERANCE via readMultidimensionalTime→setTolerance; writeSynchronizationTimingConstraint (arxml_writer.py L4631) mirrors via getters in the same XSD group SYNCHRONIZATION-CONSTRAINT sequence (L118881); matched name pairs across layers (Rule 0013.2: readX→set/addX, writeX→getX), no chained mutator calls; dispatch keyed on SYNCHRONIZATION-TIMING-CONSTRAINT both directions (parser L8391 allowlist + L8430-8432 branch, writer tag_map L7947); DEST emission via setChildElementOptionalRefType proven by round-trip getDest assertions; 51/51 parser+writer timing tests Green (26 parser + 25 writer)
  - [x] Step 7 — Update checklist comment — 6-column format with release column rebuilt in SynchronizationTiming.py (rows source order: __init__, get/setEventOccurrenceKind, add/getScopes, addScopeEvent/getScopeEvents, get/setSynchronizationConstraintType, get/setTolerance; reader [x] on mutator rows — matches readSynchronizationTimingConstraint call sites set/addEventOccurrenceKind/Scope/ScopeEvent/SynchronizationConstraintType/Tolerance; writer [x] on getter rows — matches writeSynchronizationTimingConstraint call sites getEventOccurrenceKind/Scopes/ScopeEvents/SynchronizationConstraintType/Tolerance); stale orphan-intake `# Spec verified: R23-11` marker REMOVED (withheld per batch instruction — deferred to batch confirmation); in-file sibling enum checklists (Tables 3.55/3.56) untouched — stamped in a prior batch, 5-column legacy form is their next-pass concern (Latency sibling precedent)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly the five Table 3.54 attributes, Rule 0001.5 Kind-suffix scopeRefs/scopeEventRefs on the two `*` refs, Optional[EventOccurrenceKindEnum]/Optional[SynchronizationTypeEnum] attrs, Optional[MultidimensionalTime] aggr); the Step 2/4 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): (1) class docstring rebuilt to the Note verbatim (constr paragraphs wiped — section-level constraints table, not the Note row; stray "Constraint " label artifact dropped; image-cut tail "interval are permitted." restored from XSD group doc + R4.3.1 Note), (2) getScopeEvents trailing "." + addScopeEvent inserted "." normalized verbatim, (3) old 5-column checklist + stale orphan-intake stamp rebuilt 6-column/stamp withheld (batch instruction); v1 tracker (method_deviation_by_class.md) `## SynchronizationTimingConstraint` reconciled 2026-09-24: the 5 "missing" rows were STALE (all members implemented since intake) — section rewritten to the sibling OffsetTimingConstraint sync-record form (corrected citation Table 3.54 p.92 + R4.3.1 Table 7.12 caption-shift reproduction note, full sync Note, no-deviation row); v2 tracker (method_deviation_by_class_v2.md) and staged_class_sync_review.md have NO SynchronizationTimingConstraint entries (verified by grep — v2 lists only the two in-file enums in the remediated Rule 0007 inventory, not deviation rows); referenced classes: base TimingConstraint (stamped R23-11 Table D.61) + Traceable (RequirementsTracing), member types EventOccurrenceKindEnum (stamped R23-11 Table 3.56, in-file) + SynchronizationTypeEnum (stamped R23-11 Table 3.55, in-file) + MultidimensionalTime (stamped R23-11 FO GST Table 4.74, synced 3dd45559) + RefType (PrimitiveTypes, stamped) + ref targets TimingDescriptionEvent/TimingDescriptionEventChain (TimingDescription, stamped) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 77/77 (test_SynchronizationTiming.py 26 — 12 functional intake + 9 spec-contract + 5 enum, re-run green after black reformat; test_parser_timing_constraints.py 26; test_writer_timing_constraints.py 25), full suite `uv run python scripts/run_tests.py --no-coverage` 11168 passed / 0 failed (baseline 11157 + 11 net-new: +9 model spec-contract tests, +1 parser TimingExtension-dispatch read test, +1 writer TimingExtension-dispatch round-trip test; re-run green after black reformat of the extended model test file); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I; models/parser/writer untouched by imports — no re-sort risk, all edits in-place); `npm run black` reformatted the extended model test file (canonical formatting only) + `black-check` clean (1150 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (exactly eventOccurrenceKind/scope/scopeEvent/synchronizationConstraintType/tolerance — Optional[EventOccurrenceKindEnum] + Optional[SynchronizationTypeEnum] for the 0..1 attrs, List[RefType] dedicated typed lists for the two `*` refs with Rule 0001.5 plural-Refs suffixes, Optional[MultidimensionalTime] for the 0..1 aggr), most-derived base TimingConstraint (table row header confirmed; XSD 00052 complexType SYNCHRONIZATION-TIMING-CONSTRAINT L118962 abstract="false" composes AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → TRACEABLE → TIMING-CONSTRAINT → own group — no flattening; Aggregated-by TimingExtension.timingGuarantee/timingRequirement dispatch pre-exists both sides and is now test-pinned), no fabrication, member/accessor/checklist order = markdown displayed order (eventOccurrenceKind → scope → scopeEvent → synchronizationConstraintType → tolerance; XSD XML order EVENT-OCCURRENCE-KIND → SCOPE-EVENT-REFS → SCOPE-REFS → SYNCHRONIZATION-CONSTRAINT-TYPE → TOLERANCE governs reader/writer only — writer test-enforced kind<scope_events<scopes<sync_type<tolerance), DEST TIMING-DESCRIPTION-EVENT--SUBTYPES-ENUM (SCOPE-EVENT-REF) + TIMING-DESCRIPTION-EVENT-CHAIN--SUBTYPES-ENUM (SCOPE-REF) writer-emitted and round-trip getDest test-pinned, docstrings verbatim (class Note byte-verbatim single-line test-pinned incl. restored tail; 15/15 docstring+inline-comment positions programmatically diffed OK; scope Note ends WITH period / scopeEvent Note WITHOUT — asymmetry is in the spec itself and is pinned), blank line between every __init__ attribute block (verified by eye), PEP 526 annotated members, no `# type:`, reader+writer coverage for all five attributes test-pinned at element level AND through the TimingExtension aggregation round-trip, no open deviations (v1 tracker stale rows reconciled; v2 tracker + staged_class_sync_review.md have no entries), stamp withheld (batch instruction), Rule 0007 leaf-package location SynchronizationTiming.py per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `TimingDescriptionEventChain` — TimingDescription — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Timing/TimingDescription/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — R23-11 CP_TPS_TimingExtensions Table 3.13 (L917 caption, p.41 via pdf_page.py) cited as primary; caption-shift artifact: meta rows Class/Package/Note/Base/Aggregated-by render ABOVE the caption (L893-900), attribute rows below (L921-924); R4.3.1 reproduction Table 6.1 (L2448-2458) verified row-identical EXCEPT it predates the draft attribute isPipeliningPermitted (Tags: atp.Status=draft) and has no "Aggregated by" row (spacing artifacts "TimingDescripti onEvent"/"TimingDescripti onEventChain") — R23-11 is the citation; table rows: Package M2::AUTOSARTemplates::CommonStructure::Timing::TimingDescription (non-leaf → class in TimingDescription/__init__.py per Rule 0007 — current location correct), class Note "An event chain describes the causal order for a set of functionally dependent timing events. …" (markdown Note carries a trailing italic-extraction artifact "event chain segments ." — space before the final period, present in BOTH R23-11 and R4.3.1 markdown; XSD 00052 group TIMING-DESCRIPTION-EVENT-CHAIN documentation L123718 canonicalizes "so called ''event chain segments''." with '' as italics markers; docstring = markdown wording with the artifact space dropped, documented), Base ARObject , Identifiable , MultilanguageReferrable , Referrable , TimingDescription (most-derived TimingDescription — row header confirmed; XSD 00052 complexType TIMING-DESCRIPTION-EVENT-CHAIN L123778 abstract="false" composes AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → TIMING-DESCRIPTION → own group — no flattening), Aggregated by TimingExtension.timingDescription (TimingExtension.addTimingDescription accepts TimingDescription — the chain already fits the wrapper list); 4 attributes displayed order: isPipeliningPermitted (Boolean, 0..1, attr — markdown row label "isPipelining Permitted" is a PDF line-wrap artifact, real name per XSD appinfo mmt.qualifiedName), response (TimingDescriptionEvent, 0..1, ref → responseRef), segment (TimingDescriptionEventChain, *, ref → segmentRefs — "TimingDescriptionEvent Chain" spacing artifact), stimulus (TimingDescriptionEvent, 0..1, ref → stimulusRef) — field-to-spec both directions exact (exactly these 4 fields exist; no fabricated/missing; Rule 0001.5 Kind-suffix Ref/Refs correct); XSD 00052 group TIMING-DESCRIPTION-EVENT-CHAIN (L123715) XML element order IS-PIPELINING-PERMITTED → STIMULUS-REF → RESPONSE-REF → SEGMENT-REFS (matches sequenceOffset tags stimulus=10/response=20/segment=30, pipelining first untagged); DEST required: STIMULUS-REF/RESPONSE-REF → TIMING-DESCRIPTION-EVENT--SUBTYPES-ENUM, SEGMENT-REF → TIMING-DESCRIPTION-EVENT-CHAIN--SUBTYPES-ENUM; GENUINE FINDINGS: (1) Rule 0001.11 violation — class member/accessor order follows XSD order (stimulus → response → segment) instead of the markdown displayed order (response → segment → stimulus) — reorder at Step 3 (reader/writer stay XSD order: readTimingDescriptionEventChain L3044 / writeTimingDescriptionEventChain L4305 already in XSD order); (2) consume-path gap — the Table 3.13 Aggregated-by path is NOT dispatched: readTimingDescriptions (parser L3128-3188) TIMING-DESCRIPTIONS/* allowlist and writeTimingExtension's TIMING-DESCRIPTIONS isinstance chain (writer L7999+) both lack a TIMING-DESCRIPTION-EVENT-CHAIN branch (XSD TIMING-DESCRIPTIONS choice L123927 includes the element) → silent round-trip drop, genuine Red at Step 5, fixed at Step 6; intake artifacts: 5-column checklist (→ 6-column at Step 7), no stale stamp present (stamp withheld per batch instruction); class docstring currently carries the XSD ''-quoted form (normalize to markdown wording at Step 4); v1 tracker (method_deviation_by_class.md) has NO `## TimingDescriptionEventChain` section — only ref-target mentions inside other classes' sections (LatencyTimingConstraint L3047, EOCExecutableEntityRefGroup L3181 stale rows, not this class's); v2 tracker + staged_class_sync_review.md no entries (verified by grep)
  - [x] Step 2 — Write model class unit test (Red) — extended existing test_TimingDescriptionEventChain.py (7 functional intake tests pre-existed: init defaults, get/set round-trip + None no-op for all four accessors, adder append) with TestTimingDescriptionEventChainSpecContract mirroring the SynchronizationTimingConstraint sibling form + a Rule 0001.11 order pin (11 tests): verbatim class-Note assertion (markdown trailing italic-extraction artifact "event chain segments ." — space before the final period, present in BOTH R23-11 and R4.3.1 markdown — normalized; XSD group doc L123718 canonicalizes "''event chain segments''."), no-__init__-docstring, base = TimingDescription, typing.get_type_hints pairs (isPipeliningPermitted → Optional[Boolean]; response/stimulus → Optional[RefType] get/set pairs; segment → add Optional[RefType] / get List[RefType]), __init__ field-declaration order = markdown displayed order, accessor def order = markdown displayed order (pinned via inspect.getsource + re.findall), getter Notes verbatim (Tags tails dropped per OffsetTimingConstraint family convention), setter/adder Notes + None-no-op sentences; 5 Red / 12 passed — 2 GENUINE Rule 0001.11 Reds (member + accessor order follow XSD order stimulus→response→segment instead of markdown response→segment→stimulus) plus 3 designed docstring Reds ('' quoted class docstring, Tags tails on getter/setter docstrings) — all owned by Step 3 (order) / Step 4 (docstrings); all impl/hints/base/functional assertions Green immediately (intake shape spec-correct), honestly noted
  - [x] Step 3 — Implement model class (Green) — reorder only, no shape change: model shape already spec-correct from intake — dedicated typed fields exactly the 4 Table 3.13 attributes (isPipeliningPermitted: Optional[Boolean] 0..1 attr; responseRef/stimulusRef: Optional[RefType] 0..1 refs with Rule 0001.5 Kind-suffix; segmentRefs: List[RefType] dedicated typed list for the `*` ref — Rule 0004, never registry filters), most-derived base TimingDescription (concrete class), no createXxx — no Referrable children (response/segment/stimulus are refs), setter chaining returns self, None no-ops guarded, blank line between every __init__ attribute block, PEP 526 annotations; FIXED the 2 genuine Rule 0001.11 Reds: __init__ field order and accessor def order moved from XSD order (stimulus→response→segment) to the markdown displayed order (isPipeliningPermitted → response → segment → stimulus; XSD order IS-PIPELINING-PERMITTED → STIMULUS-REF → RESPONSE-REF → SEGMENT-REFS continues to govern only reader/writer XML element order — reader/writer untouched this step); evidence: 14/17 pass, the 2 order tests now Green, the only 3 failures are the docstring-only Reds owned by Step 4
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — rewrote all TimingDescriptionEventChain docstrings from the Table 3.13 markdown: class docstring = Note verbatim as a single line (dropped the XSD ''-quoted italics form the intake carried — markdown is the primary source; the trailing italic-extraction artifact "event chain segments ." — space before the final period, present in BOTH the R23-11 and R4.3.1 markdown — normalized by dropping the artifact space, canonical wording confirmed by the XSD 00052 group doc L123718 "so called ''event chain segments''."); getter/setter/adder docstrings = Notes verbatim with the Tags tails (atp.Status=draft / xml.sequenceOffset=10/20/30) dropped per the OffsetTimingConstraint family convention, setters + "A None value is a no-op and does not overwrite an existing <field>." (3×), adder + "A None value is a no-op."; the 4 inline __init__ comments kept Note verbatim INCLUDING the Tags tails (family LatencyTimingConstraint/OffsetTimingConstraint inline-comment style) — unchanged from intake; verified programmatically 14/14 docstring+inline-comment positions OK (class + __init__ + 4 getters + 3 setters + 1 adder + 4 inline comments); 17/17 model tests Green after rewrite (was 14/17 with the 3 designed Reds)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — intake coverage had NO TimingDescriptionEventChain-specific parser/writer tests (the class appeared only as a SCOPE-REF/SEGMENT DEST value in other classes' tests); ADDED: parser TestReadTimingDescriptionEventChain (test_read_timing_description_event_chain element-level field values incl. DESTs + test_..._empty empty-wrapper case + test_read_timing_description_event_chain_via_timing_extension through readTimingExtension TIMING-DESCRIPTIONS dispatch — the class's Table 3.13 Aggregated-by row TimingExtension.timingDescription); writer TestWriteTimingDescriptionEventChain (test_round_trip_..._full asserting XSD element order IS-PIPELINING-PERMITTED < STIMULUS-REF < RESPONSE-REF < SEGMENT-REFS + DESTs + values + read-back, test_write_..._empty all four children absent + read-back None/[], test_round_trip_..._via_timing_extension writer→reader through writeTimingExtension TIMING-DESCRIPTIONS + readTimingExtension); 2 Red / 4 passed — the 2 aggregation-level via_timing_extension tests are GENUINE Reds (silent round-trip drop: no TIMING-DESCRIPTION-EVENT-CHAIN branch either side), the 4 element-level tests pass immediately (readTimingDescriptionEventChain/writeTimingDescriptionEventChain pre-existed from intake) — honestly noted
  - [x] Step 6 — Update parser & writer (Green) — added the missing TIMING-DESCRIPTION-EVENT-CHAIN branch to the Table 3.13 Aggregated-by dispatch BOTH sides: parser readTimingDescriptions (arxml_parser.py, after the TD-EVENT-SLLET-PORT branch: instantiates TimingDescriptionEventChain(extension, short_name) + readTimingDescriptionEventChain into the shared `event` local — the branch must feed the common extension.addElement(event)/addTimingDescription(event) tail, first cut used a `chain` local and tripped UnboundLocalError); writer writeTimingExtension TIMING-DESCRIPTIONS isinstance chain (arxml_writer.py, after the TDEventSLLETPort branch: isinstance(description, TimingDescriptionEventChain) → TIMING-DESCRIPTION-EVENT-CHAIN element + writeTimingDescriptionEventChain — no isinstance-order conflict, the chain extends TimingDescription directly, sibling of TimingDescriptionEvent, not any TDEvent subclass); the per-element helpers readTimingDescriptionEventChain (L3044) / writeTimingDescriptionEventChain (L4305) were already spec-correct in XSD element order IS-PIPELINING-PERMITTED → STIMULUS-REF → RESPONSE-REF → SEGMENT-REFS with matched name pairs (Rule 0013.2: readX→set/addX via setIsPipeliningPermitted/setStimulusRef/setResponseRef/addSegmentRef, writeX→getX via getIsPipeliningPermitted/getStimulusRef/getResponseRef/getSegmentRefs), no chained mutator calls, DEST emission via setChildElementOptionalRefType proven by round-trip getDest assertions; dispatch keyed on TIMING-DESCRIPTION-EVENT-CHAIN both directions (XSD TIMING-DESCRIPTIONS choice L123927); 57/57 parser+writer timing tests Green (29 parser + 28 writer)
  - [x] Step 7 — Update checklist comment — 6-column format with release column rebuilt in TimingDescription/__init__.py (rows source order = markdown displayed: __init__, get/setIsPipeliningPermitted, get/setResponseRef, addSegmentRef/getSegmentRefs, get/setStimulusRef; reader [x] on mutator rows — matches readTimingDescriptionEventChain call sites setIsPipeliningPermitted/setResponseRef/addSegmentRef/setStimulusRef; writer [x] on getter rows — matches writeTimingDescriptionEventChain call sites getIsPipeliningPermitted/getResponseRef/getSegmentRefs/getStimulusRef); the `# Spec:` line was already correct (AUTOSAR_CP_TPS_TimingExtensions.pdf, Table 3.13, p.41) — kept; NO stale orphan-intake `# Spec verified:` stamp on this class (verified at Step 1 — none present, none added; stamp withheld per batch instruction — deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly the four Table 3.13 attributes, Rule 0001.5 Kind-suffix responseRef/stimulusRef + plural segmentRefs on the `*` ref, Optional[Boolean] attr, Optional[RefType] refs); the Step 2-6 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): (1) member/accessor order moved from XSD order to the markdown displayed order per Rule 0001.11, (2) class docstring rebuilt to the markdown Note verbatim ('' quoted XSD form replaced; trailing italic-extraction artifact "event chain segments ." space dropped — present in both R23-11 and R4.3.1 markdown, canonical wording per XSD group doc L123718), (3) Tags tails dropped from getter/setter/adder docstrings (family convention, inline comments keep them), (4) 5-column checklist rebuilt 6-column/no stamp (batch instruction), (5) the consume-path gap (no TIMING-DESCRIPTION-EVENT-CHAIN branch in the TIMING-DESCRIPTIONS dispatch either side) closed at Step 6; v1 tracker (method_deviation_by_class.md) had NO `## TimingDescriptionEventChain` section (verified by grep at Step 1 — only ref-target mentions inside LatencyTimingConstraint/EOCExecutableEntityRefGroup sections, which belong to those classes) — NEW sync-record section added 2026-09-24 in the sibling OffsetTimingConstraint form (citation Table 3.13 p.41 + R4.3.1 Table 6.1 predates-draft-attr note, full sync Note incl. the dispatch-gap disposition, no-deviation row); v2 tracker (method_deviation_by_class_v2.md) and staged_class_sync_review.md have NO TimingDescriptionEventChain entries (verified by grep — 0 mentions each); referenced classes: base TimingDescription (stamped R23-11 Table D.62), member types Boolean/RefType (PrimitiveTypes, stamped) + ref targets TimingDescriptionEvent (stamped R23-11 Table D.63)/TimingDescriptionEventChain (self — SEGMENT-REF DEST) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 74/74 (test_TimingDescriptionEventChain.py 17 — 7 functional intake + 10 spec-contract, re-run green after black reformat; test_parser_timing_constraints.py 29; test_writer_timing_constraints.py 28), full suite `uv run python scripts/run_tests.py --no-coverage` 11184 passed / 0 failed (baseline 11168 + 16 net-new: +10 model spec-contract tests incl. the 2 Rule 0001.11 order pins, +3 parser tests element/empty/TIMING-DESCRIPTIONS-dispatch read, +3 writer tests element round-trip/empty/TIMING-DESCRIPTIONS-dispatch round-trip; re-run green after black reformat of the extended model test file); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I; models/parser/writer edits all in-place — no import re-sort risk, TimingDescriptionEventChain was already imported by parser/writer/test files); `npm run black` reformatted the extended model test file (canonical formatting only) + `black-check` clean (1150 files unchanged); integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (exactly isPipeliningPermitted/response/segment/stimulus — Optional[Boolean] for the 0..1 attr, Optional[RefType] for the two 0..1 refs with Rule 0001.5 Ref suffix, List[RefType] dedicated typed list for the `*` ref with plural Refs suffix), most-derived base TimingDescription (table row header confirmed; XSD 00052 complexType TIMING-DESCRIPTION-EVENT-CHAIN L123778 abstract="false" composes AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → TIMING-DESCRIPTION → own group — no flattening; Aggregated-by TimingExtension.timingDescription dispatch now present both sides and test-pinned), no fabrication, member/accessor/checklist order = markdown displayed order (isPipeliningPermitted → response → segment → stimulus — test-pinned via inspect.getsource; XSD XML order IS-PIPELINING-PERMITTED → STIMULUS-REF → RESPONSE-REF → SEGMENT-REFS governs reader/writer only — writer test-enforced IS-PIPELINING-PERMITTED<STIMULUS-REF<RESPONSE-REF<SEGMENT-REFS), DEST TIMING-DESCRIPTION-EVENT--SUBTYPES-ENUM (STIMULUS-REF/RESPONSE-REF) + TIMING-DESCRIPTION-EVENT-CHAIN--SUBTYPES-ENUM (SEGMENT-REF) writer-emitted and round-trip getDest test-pinned, docstrings verbatim (class Note byte-verbatim single-line test-pinned with the documented artifact-space normalization; 14/14 docstring+inline-comment positions programmatically diffed OK; inline comments keep Tags tails, getters/setters/adders drop them), blank line between every __init__ attribute block (verified by eye), PEP 526 annotated members, no `# type:`, reader+writer coverage for all four attributes test-pinned at element level AND through the TimingExtension TIMING-DESCRIPTIONS aggregation round-trip, no open deviations (v1 section added as sync-record; v2 tracker + staged_class_sync_review.md have no entries), stamp withheld (batch instruction), Rule 0007 non-leaf-package location TimingDescription/__init__.py per spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `AutosarOperationArgumentInstance` — Identifiable — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Timing/TimingDescription/TimingDescriptionEvents/TDEventOccurrenceExpression.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — R23-11 CP_TPS_TimingExtensions Table 3.53 (L2124, p.85 via pdf_page.py) cited as primary; R4.3.1 reproduction Table 5.40 (AUTOSAR_TPS_TimingExtensions L2296) verified row-identical (PDF-extraction artifacts only: "Timing Description Events" spacing, "operationA rgumentIns tance" wrap; R4.3.1 render omits the Aggregated-by row) — R23-11 is the citation; table rows: Package M2::AUTOSARTemplates::CommonStructure::Timing::TimingDescription::TimingDescriptionEvents::TDEventOccurrenceExpression (leaf package → TDEventOccurrenceExpression.py per Rule 0007 — current multi-class location correct; ConcreteTDEventVfb/TDEventVfb.py is a separate module, untouched), class Note verbatim confirmed = current docstring body (single line, "•" bullets inline), Base ARObject , Identifiable , MultilanguageReferrable , Referrable (most-derived Identifiable — XSD 00052 L8100 complexType abstract="false" composes AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → own group L8071 — no flattening; current `Identifiable, VariationPointCapable` extra mixin justified by the XSD own group's optional VARIATION-POINT element xml.sequenceOffset=10000 "Applicable for: TimingExtensionResource.timingArgument / Not Applicable for: TDEventOccurrenceExpression.argument" = atpVariation capability, identical shape to the stamped sibling AutosarVariableInstance Table 3.52 same Base row — keep), Aggregated by TDEventOccurrenceExpression.argument + TimingExtensionResource.timingArgument (BOTH consume paths pre-exist: parser ARGUMENTS dispatch L3313 + TIMING-ARGUMENTS dispatch L3389 → readAutosarOperationArgumentInstance L3400; writer ARGUMENTS L4488 + TIMING-ARGUMENTS L4571 → writeAutosarOperationArgumentInstance L4586; matched name pairs readOperationArgumentInComponentInstanceRef/writeOperationArgumentInComponentInstanceRef); 1 attribute displayed order: operationArgumentInstance (DataPrototype, 0..1, iref → Rule 0001.5 IRef suffix; Note "This is the reference to the instanceRef definition. InstanceRef implemented by: OperationArgumentInComponentInstanceRef"; XSD element OPERATION-ARGUMENT-INSTANCE-IREF type OPERATION-ARGUMENT-IN-COMPONENT-INSTANCE-REF) — field-to-spec cross-check both directions exact (operationArgumentInstanceIRef: Optional[OperationArgumentInComponentInstanceRef], no extra fields); XSD element order OPERATION-ARGUMENT-INSTANCE-IREF → VARIATION-POINT (variationPoint not serialized for this class — family behavior, mixin holds it); deviation tracker v1 method_deviation_by_class.md has NO `## AutosarOperationArgumentInstance` section (verified by grep at Step 1) — the only mentions are ref-target rows inside OTHER classes' sections (TimingConditionFormula L2976 timingArgumentRef type, TimingExtensionResource L2989 timingArgument type — belong to those classes' syncs); existing coverage: model test_AutosarOperationArgumentInstance.py 3 tests, parser TestReadAutosarOperationArgumentInstance 2 tests + writer TestWriteAutosarOperationArgumentInstance 2 tests in test_*_timing_instance_refs.py, aggregation-level TimingExtensionResource tests both sides
  - [x] Step 2 — Write model class unit test (Red) — extended existing test_AutosarOperationArgumentInstance.py (3 functional intake tests pre-existed: init defaults, set round-trip + None no-op) with TestAutosarOperationArgumentInstanceSpecContract mirroring the TimingDescriptionEventChain sibling form (8 tests): verbatim class-Note assertion (markdown second-bullet wrap artifact "ClientServer Interface" — space inside the class name, absent from the first bullet — normalized; XSD group doc AUTOSAR-OPERATION-ARGUMENT-INSTANCE canonicalizes both bullets "ClientServerInterface"), no-__init__-docstring, base = Identifiable + VariationPointCapable mixin (justified in-test: XSD own group's optional VARIATION-POINT element atpVariation applicable only for TimingExtensionResource.timingArgument; stamped sibling AutosarVariableInstance Table 3.52 same shape), typing.get_type_hints pairs (operationArgumentInstanceIRef → Optional[OperationArgumentInComponentInstanceRef] get/set), __init__ field-declaration order pin (exactly the single field via re.findall — both directions), accessor def order pin (get→set), getter Note verbatim (wrap artifact "OperationArgumentIn ComponentInstanceRef" normalized), setter Note + None-no-op sentence appended with NO terminal punctuation added (SynchronizationTimingConstraint addScopeEvent family form — Note cell ends without period); 3 Red / 8 passed — the 3 Reds are designed docstring-only Reds (class artifact space, getter trailing ".", setter trailing "."), all owned by Step 4; all impl/hints/base/order/functional assertions Green immediately (intake shape spec-correct) — honestly noted
  - [x] Step 3 — Implement model class (Green) — no shape change needed: model shape already spec-correct from intake (Step 1 field-to-spec both directions exact) — dedicated typed field exactly the one Table 3.53 attribute (operationArgumentInstanceIRef: Optional[OperationArgumentInComponentInstanceRef], Rule 0001.5 IRef suffix on the iref; XSD element OPERATION-ARGUMENT-INSTANCE-IREF type OPERATION-ARGUMENT-IN-COMPONENT-INSTANCE-REF), most-derived base Identifiable + VariationPointCapable mixin (XSD VARIATION-POINT element, stamped-sibling shape), no createXxx — no Referrable children (the attribute is an iref, not an aggregation), setter chaining returns self, None no-op guarded, PEP 526 annotation, blank line between __init__ attribute blocks; the only 3 failures remain the docstring-only Reds owned by Step 4 (8/11 pass)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — rewrote all 3 AutosarOperationArgumentInstance docstring positions from the Table 3.53 markdown (the class has exactly 3 docstrings + 1 inline comment — all rewritten/verified, wipe-first): class docstring = Note verbatim single line with the second-bullet wrap artifact "ClientServer Interface" normalized to "ClientServerInterface" (first bullet in the SAME Note carries no space; XSD group doc L8076/L8110 canonicalizes both bullets "ClientServerInterface" — TimingDescriptionEventChain artifact-normalization precedent); getter docstring = attribute Note verbatim, trailing "." DROPPED (markdown cell ends "…OperationArgumentIn ComponentInstanceRef" with no period; wrap artifact space normalized); setter docstring = attribute Note verbatim + appended None-no-op sentence with NO terminal punctuation added before "A None value is a no-op and does not overwrite an existing operationArgumentInstanceIRef." (SynchronizationTimingConstraint addScopeEvent family form); inline __init__ comment kept Note verbatim (no period, no Tags tails in this cell — XSD appinfo tags carry no xml.sequenceOffset for the IREF element) — unchanged; verified 11/11 model tests Green after rewrite (was 3 Red / 8 passed); sibling classes in TDEventOccurrenceExpression.py (TDEventOccurrenceExpressionFormula Table 3.51, OperationArgumentInComponentInstanceRef XSD-only, AutosarVariableInstance Table 3.52, TDEventOccurrenceExpression Table 3.50 — all stamped) untouched
  - [x] Step 5 — Write reader/writer round-trip test (Red) — existing coverage audit: element-level parser TestReadAutosarOperationArgumentInstance (with_iref field values incl. DESTs / without_iref) + writer TestWriteAutosarOperationArgumentInstance (round-trip field values / write_without_iref) pre-existed in test_*_timing_instance_refs.py; aggregation TIMING-ARGUMENTS (TimingExtensionResource.timingArgument) covered both sides in TestRead/TestWriteTimingExtensionResource; aggregation ARGUMENTS (TDEventOccurrenceExpression.argument) covered read-side (TestReadTDEventOccurrenceExpression.test_read_full asserts the argument IREF value + test_read_empty covers the empty wrapper) and writer-side only SHORT-NAME-shape (test_write_full/test_round_trip never populate the IREF) — GAP: no round-trip of the ARGUMENTS wrapper asserting IREF field values; ADDED writer test_round_trip_argument_instance_iref_values (test_writer_timing_descriptions.py TestWriteTDEventOccurrenceExpression): full OperationArgumentInComponentInstanceRef (2 CONTEXT-COMPONENT-REFs + PORT/OPERATION/ROOT-ARGUMENT/CONTEXT-DATA + TARGET refs with DESTs) through writeTDEventOccurrenceExpression → round-trip → readTDEventOccurrenceExpression, asserting written values + DESTs, XSD element order both levels (own group SHORT-NAME → OPERATION-ARGUMENT-INSTANCE-IREF; instance-ref group CONTEXT-COMPONENT-REF* → CONTEXT-PORT-PROTOTYPE-REF → CONTEXT-OPERATION-REF → ROOT-ARGUMENT-DATA-PROTOTYPE-REF → CONTEXT-DATA-PROTOTYPE-REF* → TARGET-DATA-PROTOTYPE-REF per XSD 00052 L86902) and read-back field values; 0 Red / 58 passed — NO genuine Red: both dispatch branches pre-exist both sides (Step 1), the new test closes the field-value coverage gap and pins the order, passing immediately — honestly noted
  - [x] Step 6 — Update parser & writer (Green) — no changes needed: the class's consume paths are fully wired both sides since intake — parser readTDEventOccurrenceExpression ARGUMENTS dispatch (arxml_parser.py L3313) + readTimingExtensionResource TIMING-ARGUMENTS dispatch (L3389) → readAutosarOperationArgumentInstance (L3400, readIdentifiable + OPERATION-ARGUMENT-INSTANCE-IREF → setOperationArgumentInstanceIRef via readOperationArgumentInComponentInstanceRef L3416); writer writeTDEventOccurrenceExpression ARGUMENTS dispatch (arxml_writer.py L4488) + writeTimingExtensionResource TIMING-ARGUMENTS dispatch (L4571) → writeAutosarOperationArgumentInstance (L4586, writeIdentifiable + OPERATION-ARGUMENT-INSTANCE-IREF → getOperationArgumentInstanceIRef via writeOperationArgumentInComponentInstanceRef L4600); matched name pairs Rule 0013.2 verified (readX→setX / writeX→getX), no chained mutator calls, element order XSD-conformant (pinned by the new order assertions); VARIATION-POINT not emitted for this class (variationPoint None in the mixin — writeVariationPoint no-ops on None; family behavior identical to stamped sibling AutosarVariableInstance); evidence: 58/58 parser+writer timing tests green (36 parser + 22 writer across the 4 files)
  - [x] Step 7 — Update checklist comment — 6-column format with release column rebuilt in TDEventOccurrenceExpression.py (rows source order = markdown displayed: __init__, getOperationArgumentInstanceIRef, setOperationArgumentInstanceIRef; __init__ row corrected to [—] reader/[—] writer per the batch 6-column convention — the intake 5-column block had it [x]/[x]; reader [x] on the setter row — matches readAutosarOperationArgumentInstance call site setOperationArgumentInstanceIRef; writer [x] on the getter row — matches writeAutosarOperationArgumentInstance call site getOperationArgumentInstanceIRef); `# Spec:` line already correct (AUTOSAR_CP_TPS_TimingExtensions.pdf, Table 3.53, p.85) — kept; NO `# Spec verified:` stamp present (verified — none existed, none added; stamp withheld per batch instruction — deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly the one Table 3.53 attribute, Rule 0001.5 IRef suffix on the iref, Optional[OperationArgumentInComponentInstanceRef]); the Step 2-4 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): (1) class docstring wrap artifact "ClientServer Interface" normalized to "ClientServerInterface" (XSD group doc canonicalizes), (2) getter/setter trailing "." dropped (markdown Note cell has no terminal period; setter appends the None-no-op sentence with no punctuation added — SynchronizationTimingConstraint addScopeEvent family form), (3) checklist rebuilt 6-column/release/`__init__` [—] rows; v1 tracker (method_deviation_by_class.md) had NO `## AutosarOperationArgumentInstance` section (verified by grep at Step 1 — only ref-target mentions inside the stale TimingConditionFormula L2976/TimingExtensionResource L2989 rows, which belong to those classes) — NEW sync-record section added 2026-09-24 after the OffsetTimingConstraint section in the sibling form (citation Table 3.53 p.85 + R4.3.1 Table 5.40 row-identical note, full sync Note incl. the both-aggregations consume-path disposition and two out-of-scope sibling observations: OperationArgumentInComponentInstanceRef does not model the XSD group's optional BASE-REF; writeIdentifiable VARIATION-POINT-before-IREF latent ordering — both no rows for THIS class), no-deviation row; v2 tracker (method_deviation_by_class_v2.md) and staged_class_sync_review.md have NO AutosarOperationArgumentInstance entries (verified by grep — 0 mentions each); referenced classes: base Identifiable (stamped heritage chain) + mixin VariationPointCapable, member type OperationArgumentInComponentInstanceRef (XSD verified AUTOSAR_00052.xsd, same module) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 69/69 (test_AutosarOperationArgumentInstance.py 11 — 3 functional intake + 8 spec-contract; test_arxml_parser_timing_instance_refs.py + test_parser_timing_descriptions.py 36 parser; test_writer_timing_instance_refs.py + test_writer_timing_descriptions.py 22 writer), full suite `uv run python scripts/run_tests.py --no-coverage` 11193 passed / 0 failed (baseline 11184 + 9 net-new: +8 model spec-contract tests incl. the Rule 0001.11 single-field and accessor-order pins and the 3 verbatim-docstring pins, +1 writer ARGUMENTS-aggregation round-trip test with full IREF field values + DESTs + two-level XSD element-order assertions); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I; the only src edit was in-place in TDEventOccurrenceExpression.py — no import re-sort risk, no new imports); `npm run black` 1150 files unchanged + `black-check` clean; integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: fields↔spec both directions exact (the single Table 3.53 attribute operationArgumentInstance → operationArgumentInstanceIRef, Optional[OperationArgumentInComponentInstanceRef] for the 0..1 iref with Rule 0001.5 IRef suffix — no extra fields, re.findall pins the exact field set), most-derived base Identifiable (table Base row ARObject , Identifiable , MultilanguageReferrable , Referrable; XSD 00052 complexType AUTOSAR-OPERATION-ARGUMENT-INSTANCE L8100 abstract="false" composes AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → own group — no flattening; VariationPointCapable mixin per the XSD own group's optional VARIATION-POINT element atpVariation + stamped-sibling AutosarVariableInstance identical shape — test-pinned), no fabrication, member/accessor/checklist order = markdown displayed order (single attr; accessor get→set pinned via inspect.getsource + re.findall; XSD order OPERATION-ARGUMENT-INSTANCE-IREF → VARIATION-POINT governs reader/writer only — writer test-pinned SHORT-NAME < OPERATION-ARGUMENT-INSTANCE-IREF and the instance-ref group order), docstrings verbatim (class Note byte-verbatim single-line test-pinned with the documented "ClientServer Interface" artifact normalization per XSD group doc; getter Note verbatim; setter Note + None-no-op with no added terminal punctuation; inline __init__ comment verbatim — 4/4 docstring+comment positions), blank line between __init__ attribute blocks (verified by eye), PEP 526 annotated member, no `# type:`, reader+writer coverage for the attribute test-pinned at element level AND through both aggregations (ARGUMENTS wrapper round-trip + TIMING-ARGUMENTS wrapper via the pre-existing TimingExtensionResource tests), no open deviations (v1 sync-record section added; v2 tracker + staged_class_sync_review.md have no entries), stamp withheld (batch instruction), Rule 0007 leaf-package location TDEventOccurrenceExpression.py per the spec Package row; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `ConcreteTDEventVfb` — TDEventVfb — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Timing/TimingDescription/TimingDescriptionEvents/TDEventVfb.py
  - [x] Step 1 — Sync members & description from spec — BOTH-CORPORA VERIFIED ABSENT + XSD-only (no table in EITHER corpus, and no own XSD complexType): caption `^Table [\w.]+: ConcreteTDEventVfb$` 0 hits in autosar/R23-11/markdown/ AND autosar/R4.3.1/markdown/; tolerant case-insensitive `ConcreteTDEvent` 0 hits both corpora, spacing-tolerant `c\so\n...td` sweep 0 hits; all `TDEventVfb` case-insensitive hits are sibling noise — the ABSTRACT base (R23-11 CP_TPS_TimingExtensions Table 3.14 p.51, Subclasses row = TDEventVfbPort , TDEventVfbReference — ConcreteTDEventVfb not a meta-model subclass) and Base rows of other tables; XSD sweep: `"CONCRETE[A-Z-]*"` over AUTOSAR_00052.xsd AND AUTOSAR_00044.xsd → only CONCRETE / CONCRETE-CLASS-TAILORING / CONCRETE-PATTERN-EVENT-TRIGGERING (no CONCRETE-TD-EVENT anything); NO complexType name="TD-EVENT-VFB" and NO element decl name="TD-EVENT-VFB" in either XSD (only the abstract group). THE grounding that exists: TD-EVENT-VFB--SUBTYPES-ENUM autosar/R23-11/xsd/AUTOSAR_00052.xsd L122350 (enum value "TD-EVENT-VFB" L122356; byte-identical enum R4.3.1 AUTOSAR_00044.xsd L85995-86005) — the meta-model permits the abstract TDEventVfb DIRECTLY as a choice member/DEST; body = abstract group TD-EVENT-VFB 00052 L122335 (single optional COMPONENT-IREF type COMPONENT-IN-COMPOSITION-INSTANCE-REF) owned in the model by the stamped abstract base TDEventVfb (R23-11 Table 3.14 p.51, # Spec verified: R23-11) — so ConcreteTDEventVfb(TDEventVfb) with NO own attributes is exactly the model of that direct instantiation; no own attributes to sync, no flattening (base IS modeled + stamped), current `pass` shape spec-correct, no spec Note exists anywhere for the class (docstring to be rewritten honestly as XSD-derivation documentation at Step 4); citation form `# Spec: XSD-only, AUTOSAR_00052.xsd line 122350 (no own table in repo corpus)`; release R23-11. Consume path (Steps 5/6, GENUINE RED both sides): TimingExtension TIMING-DESCRIPTIONS choice — parser readTimingDescriptions (arxml_parser.py L3127+) has NO "TD-EVENT-VFB" tag branch → element silently dropped; writer writeTimingExtension TIMING-DESCRIPTIONS isinstance chain (arxml_writer.py L8000-8058) has NO ConcreteTDEventVfb branch and the chain ends without else → instance silently dropped; base helpers readTDEventVfb (L3195: readTimingDescriptionEvent + COMPONENT-IREF → setComponentIRef) / writeTDEventVfb (L4385: writeTimingDescriptionEvent + writeEOCComponentIRef) pre-exist and implement the XSD body; ConcreteTDEventVfb NOT imported in parser (L265-279 from-import list) nor writer (L393-401) — append, no re-sort; element body order per family complexType composition (e.g. TD-EVENT-VARIABLE-DATA-PROTOTYPE L122324: AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → TIMING-DESCRIPTION → TIMING-DESCRIPTION-EVENT → TD-EVENT-VFB); existing coverage: model test_TDEventVfb.py TestTDEventVfbFamily.test_concrete_subclass_of_base_chain (instantiation + base accessors), no parser/writer branch coverage for the plain <TD-EVENT-VFB> member anywhere
  - [x] Step 2 — Write model class unit test (Red) — extended existing test_TDEventVfb.py (TestTDEventVfbFamily 8 tests pre-existed incl. test_concrete_subclass_of_base_chain) with TestConcreteTDEventVfbSpecContract mirroring the AutosarOperationArgumentInstance sibling form (8 tests): XSD-derivation class-docstring pin (honest wording — no spec Note exists anywhere for the class: no table in either corpus, XSD group doc belongs to the base), explicit-own-__init__ pin ("__init__" in ConcreteTDEventVfb.__dict__ — family form), no-__init__-docstring pin, zero-own-fields source pin (re.findall self\.(\w+)\s*: on inspect.getsource(ConcreteTDEventVfb.__init__) == [] — the no-attributes direction of the field-to-spec cross-check: SUBTYPES-ENUM instantiation adds nothing to the abstract group body), bases == (TDEventVfb,) + not TDEventVfbPort, instantiability (no TypeError — unlike the abstract bases), no-own-accessors pin (class source get/set regex == [] AND ConcreteTDEventVfb.getComponentIRef/setComponentIRef IS TDEventVfb's — coverage rows live on the stamped base checklist), inherited componentIRef round-trip + None no-op through the subclass (ComponentInCompositionInstanceRef target SW-COMPONENT-PROTOTYPE); 3 Red / 13 passed — the 3 Reds are the designed docstring+shape-formalization Reds (class docstring wording owned by Step 4, explicit own __init__ owned by Step 3), all functional/intake-shape assertions Green immediately (pass shape already spec-correct) — honestly noted
  - [x] Step 3 — Implement model class (Green) — shape formalization only: replaced the intake `pass` body with an explicit family-form `__init__(self, parent, short_name)` chaining to super (matches every sibling concrete event class — TDEventVfbReference/TDEventVariableDataPrototype/...); ZERO own fields added (spec-correct: the SUBTYPES-ENUM direct instantiation adds nothing to the abstract group body — COMPONENT-IREF stays owned by the base TDEventVfb, stamped R23-11 Table 3.14); no createXxx — no Referrable children; concrete class (no abstract TypeError guard — instantiability IS its contract); no own accessors (inherited get/setComponentIRef are the base's); setter chaining/None-no-op inherited; PEP 526 n/a (no fields); blank-line discipline n/a (no attribute blocks); evidence: 15/16 model tests Green after Step 3 (was 3 Red / 13 passed) — the single remaining failure is the class-docstring pin owned by Step 4
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — docstring inventory for ConcreteTDEventVfb: exactly ONE position (class docstring; the explicit __init__ has no docstring per family form; no members → no inline member comments; no own accessors → no getter/setter docstrings); wiped the intake docstring "Concrete direct-use subclass of the abstract TDEventVfb." and rewrote as the honest XSD-derivation note pinned by the Step 2 test ("Concrete direct-use instantiation of the abstract TDEventVfb (XSD-only: no own table in the repo corpus; the meta-model permits the abstract TDEventVfb directly — TD-EVENT-VFB in the TD-EVENT-VFB--SUBTYPES-ENUM, AUTOSAR_00052.xsd — and the class carries no attributes of its own).") — NO spec Note exists for this class (no table in either corpus; the XSD group documentation "This is the abstract parent class..." belongs to the BASE TDEventVfb and is already its verbatim docstring), so verbatim-copy from markdown is n/a and the wording documents the derivation source instead; evidence: 16/16 model tests Green (was 15/16)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — existing coverage audit: the TIMING-DESCRIPTIONS choice has element-level tests for every concrete sibling EXCEPT the plain <TD-EVENT-VFB> member (parser test_parser_timing_descriptions.py covers TD-EVENT-VFB only as REF-DEST noise; writer test_writer_timing_descriptions.py likewise; models-level test_TDEventVfb_roundtrip.py test_round_trip_all_vfb_events covers VfbRef1/Vdp1/Op1/Mode1/Trigger1 through the full file round-trip but no plain member); ADDED (1) parser TestReadConcreteTDEventVfb.test_read_td_event_vfb_via_timing_extension + test_read_td_event_vfb_minimal (SWC-TIMING/TIMING-DESCRIPTIONS/<TD-EVENT-VFB> through readTimingExtension → ConcreteTDEventVfb type + SHORT-NAME + COMPONENT-IREF field values incl. CONTEXT-COMPONENT-REF/TARGET-COMPONENT-REF DESTs, and the minimal no-iref case), (2) writer TestWriteConcreteTDEventVfb.test_write_full + test_write_minimal_omits_component_iref + test_round_trip (writeTimingExtension → <TD-EVENT-VFB> with XSD element order SHORT-NAME → COMPONENT-IREF pinned, iref group order CONTEXT-COMPONENT-REF → TARGET-COMPONENT-REF pinned, minimal case omits COMPONENT-IREF, round-trip via readTimingExtension asserting ConcreteTDEventVfb + field values), (3) extended models-level test_TDEventVfb_roundtrip.py test_round_trip_all_vfb_events with the Plain1 member (full save/load round-trip, type + componentIRef assertions); 6 Red / 20 passed — ALL 6 Reds are the missing-branch Reds (parser dispatch has no TD-EVENT-VFB tag branch; writer isinstance chain has no ConcreteTDEventVfb branch) — genuinely Red as expected from Step 1
  - [x] Step 6 — Update parser & writer (Green) — BOTH branches added, exactly the Step 1 consume path: PARSER arxml_parser.py (1) ConcreteTDEventVfb appended to the existing TDEventVfb from-import list (alphabetical head position, no re-sort), (2) readTimingDescriptions dispatch gained `elif tag_name == "TD-EVENT-VFB":` immediately after the TD-EVENT-VFB-REFERENCE branch (VFB events grouped) → ConcreteTDEventVfb(extension, short_name) + readTDEventVfb (base helper: readTimingDescriptionEvent + readEOCComponentIRef → setComponentIRef; matched name pairs Rule 0013.2: readTDEventVfb↔writeTDEventVfb, setComponentIRef↔getComponentIRef, readEOCComponentIRef↔writeEOCComponentIRef); shared registration tail (extension.addElement + addTimingDescription) applies unchanged; no chained mutator calls; WRITER arxml_writer.py (1) ConcreteTDEventVfb appended to the existing TDEventVfb from-import list, (2) writeTimingExtension TIMING-DESCRIPTIONS isinstance chain gained `elif isinstance(description, ConcreteTDEventVfb):` immediately after TDEventVfbReference (safe placement — ConcreteTDEventVfb is a DIRECT TDEventVfb subclass, no sibling shadows it and it shadows nothing; the plain-TD-EVENT-VFB element previously fell off the end of the chain silently) → ET.SubElement(descriptions_tag, "TD-EVENT-VFB") + writeTDEventVfb (base helper: writeTimingDescriptionEvent + writeEOCComponentIRef via getComponentIRef); element order XSD-conformant and test-pinned (SHORT-NAME → COMPONENT-IREF; iref group CONTEXT-COMPONENT-REF → TARGET-COMPONENT-REF); evidence: 42/42 across the 4 target files (parser 22 + writer 24 incl. 6 net-new, roundtrip 2, model 16)
  - [x] Step 7 — Update checklist comment — 6-column format with release column added to ConcreteTDEventVfb in TDEventVfb.py (single `# Spec:` line in the batch's XSD-only citation form `# Spec: XSD-only, AUTOSAR_00052.xsd line 122350 (no own table in repo corpus — TD-EVENT-VFB--SUBTYPES-ENUM permits the abstract TDEventVfb directly; body = abstract group TD-EVENT-VFB line 122335)`; one method row `__init__` [x] impl/docstring/test, [—] reader/[—] writer per the batch 6-column convention; explanatory comment line records the element-level reader/writer coverage: TIMING-DESCRIPTIONS choice member <TD-EVENT-VFB> → readTDEventVfb/writeTDEventVfb inherited base helpers, COMPONENT-IREF rows on the stamped TDEventVfb checklist); NO `# Spec verified:` stamp and NO `# XSD verified:` marker present (verified — none added; stamp withheld per batch instruction — deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — the class has NO own table in either corpus and the XSD grants NO own members (SUBTYPES-ENUM direct instantiation adds nothing to the abstract group body; zero own fields + zero own accessors, both test-pinned); the Step 2-4 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): (1) intake orphan had no explicit own `__init__` (inherited the abstract base's) — family-form `__init__(parent, short_name)` added, (2) intake one-line docstring replaced with the honest XSD-derivation note — NO spec Note exists for this class so the verbatim-Note contract is n/a (the XSD group doc belongs to the BASE and is already its verbatim docstring), (3) checklist built fresh 6-column/release R23-11 in the XSD-only citation form; v1 tracker (method_deviation_by_class.md) had NO `## ConcreteTDEventVfb` section (verified by grep — 0 mentions in all three trackers incl. method_deviation_by_class_v2.md + staged_class_sync_review.md) — NEW sync-record section added 2026-09-24 after the AutosarOperationArgumentInstance section in the sibling form (both-corpora verification evidence + XSD-only grounding + consume-path disposition + one no-deviation row); referenced classes: base TDEventVfb (stamped R23-11 Table 3.14 p.51) + inherited attribute type ComponentInCompositionInstanceRef (modeled, tested) — no missing classes; out-of-scope observation recorded for sibling classes: the SUBTYPES-ENUM also lists the abstract name TD-EVENT-VFB-PORT (L122357) — belongs to the port-family classes' own syncs]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 42/42 (test_TDEventVfb.py 16 — 8 family + 8 spec-contract; test_TDEventVfb_roundtrip.py 2 incl. the extended Plain1 member; test_parser_timing_descriptions.py 22 incl. 2 net-new TestReadConcreteTDEventVfb; test_writer_timing_descriptions.py 24 incl. 3 net-new TestWriteConcreteTDEventVfb), full suite `uv run python scripts/run_tests.py --no-coverage` 11206 passed / 0 failed (baseline 11193 + 13 net-new: +8 model spec-contract tests incl. the zero-own-fields/zero-own-accessors source pins and the XSD-derivation docstring pin, +2 parser TIMING-DESCRIPTIONS dispatch tests, +3 writer tests incl. the XSD element-order pins SHORT-NAME → COMPONENT-IREF and CONTEXT-COMPONENT-REF → TARGET-COMPONENT-REF); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I "All checks passed"; parser/writer edits were in-place branch additions + single name appended to existing from-import lists — no import re-sort); `npm run black` 1150 files unchanged + `black-check` clean; integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: element kind + spec attrs modeled (no own spec attrs — zero-field pin test-pinned), most-derived base TDEventVfb (bases == (TDEventVfb,) test-pinned; stamped R23-11 Table 3.14 p.51; abstract group TD-EVENT-VFB 00052 L122335 composes over the base groups — no flattening, base IS modeled), no fabrication (zero own fields/accessors test-pinned; the class is the SUBTYPES-ENUM direct-instantiation model — nothing fabricated because nothing exists to model), Kind-suffix naming n/a (no own members), create/set/add shape n/a, reader+writer coverage for the kept inherited attribute COMPONENT-IREF via the new dispatch branches on BOTH sides (readTDEventVfb/writeTDEventVfb inherited helpers) test-pinned at element level AND through the SwcTiming TIMING-DESCRIPTIONS round-trip AND the full-file family round-trip, member order n/a-trivial (no own members; XSD element order governs reader/writer only and is test-pinned SHORT-NAME → COMPONENT-IREF + iref group order), docstrings: no spec Note exists (n/a verbatim) — honest XSD-derivation note test-pinned, __init__ docstring-less per family form, blank-line-between-attribute-blocks n/a (zero attribute blocks — verified by eye), PEP 526 n/a and no `# type:` comments (none added), deviations resolved (no open rows; v1 sync-record section added; v2 tracker + staged_class_sync_review.md have no entries), stamp withheld (batch instruction — no `# Spec verified:` and no `# XSD verified:` marker), Rule 0007 location TDEventVfb.py (existing family module, leaf package named for the spec package TDEventVfb) unchanged; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `AtpBlueprint` — Identifiable — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/StandardizationTemplate/AbstractBlueprintStructure/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — THREE R23-11 tables, ALL appendix-letter (numeric main-table caption search: NONE in R23-11 — `Table [A-Z0-9.]*: AtpBlueprint` hits only C.12/D.11/E.10; the numeric Table 4.2 exists only in R4.3.1 StandardizationTemplate): `Table D.11` CP_TPS_BSWModuleDescriptionTemplate.md L8317 — ALIGNED caption+body; `Table C.12` FO_TPS_StandardizationTemplate.md L4907 — caption present but body MISALIGNED (rows under the C.12 caption belong to AtpBlueprintMapping; the actual AtpBlueprint body renders ABOVE the caption, under the C.11 caption position — the same one-caption extraction shift documented on the AtpBlueprintable C.14 sync); `Table E.10` FO_TPS_GenericStructureTemplate.md L11506 — same shift (actual body above the caption after the "Document ID 202" marker; rows under the E.10 caption belong to AtpBlueprintable). ROW IDENTITY VERIFIED across all three bodies: Class `AtpBlueprint (abstract)`; Package M2::AUTOSARTemplates::CommonStructure::StandardizationTemplate::AbstractBlueprintStructure; Note verbatim "This meta-class represents the ability to act as a Blueprint. As this class is an abstract one, particular blueprint meta-classes inherit from this one."; Base `ARObject , Identifiable , MultilanguageReferrable , Referrable` (most-derived Identifiable — matches the queue row); Subclasses ARPackage, AbstractImplementationDataType, AclObjectSet, AclOperation, AclPermission, AclRole, AliasNameSet, ApplicationDataType, BswEntryRelationshipSet, BswModuleDescription, BswModuleEntry, BuildActionEntity, BuildActionEnvironment, BuildActionManifest, ClientServerInterfaceToBswModuleEntryBlueprintMapping, CompuMethod, ConsistencyNeeds, DataConstr, DataTypeMappingSet, EcucDefinitionCollection, EcucDestinationUriDefSet, EcucModuleDef, FlatMap, ImpositionTime, ImpositionTimeDefinitionGroup, KeywordSet, LifeCycleState, LifeCycleStateDefinitionGroup, ModeDeclarationGroup, PortInterface, PortInterfaceMapping, PortInterfaceMappingSet, PortPrototypeBlueprint, SwAddrMethod, SwBaseType, SwComponentType, VfbTiming; single Attribute row `blueprintPolicy | BlueprintPolicy | * | aggr | This role indicates whether the blueprintable element will be modifiable or not modifiable.` (no Stereotypes/Tags tails). CITED Table D.11, p.305 — page via DIRECT pypdf scan (pdf_page.py does not index appendix-letter ids), matching the v1 tracker's 2026-08-08 alignment; C.12 caption p.161 / E.10 caption p.424 scanned for the record. XSD 00052: AtpBlueprint is GROUP-ONLY (abstract verified — no complexType; element group ATP-BLUEPRINT L6652, annotation names the class): sequence BLUEPRINT-POLICYS (0..1 wrapper; choice unbounded of BLUEPRINT-POLICY-LIST / BLUEPRINT-POLICY-NOT-MODIFIABLE / BLUEPRINT-POLICY-SINGLE — mmt.qualifiedName="AtpBlueprint.blueprintPolicy", pureMM -1/0) + SHORT-NAME-PATTERN (STRING, mmt.qualifiedName="AtpBlueprint.shortNamePattern", atp.Status="removed", "marked as obsolete. Use the xml attribute namePattern instead") — SHORT-NAME-PATTERN absent from the PDF table ⇒ not modeled (Rule 0015; v1 tracker deviation row retained); ATP-BLUEPRINTABLE group L6902 = empty sequence. FIELD-TO-SPEC BOTH DIRECTIONS: EXACT — one spec attribute blueprintPolicy (BlueprintPolicy, *, aggr) → field `blueprintPolicys: List[BlueprintPolicy]` (PEP 526, dedicated typed list Rule 0004) + addBlueprintPolicy (None no-op, chaining) + getBlueprintPolicys; no extra fields, no flattening (Identifiable chain modeled+stamped), abstract TypeError guard present, class docstring = Note verbatim, add docstring = Note + None-no-op sentence, get docstring = Note verbatim. ONE GENUINE GAP: getter return annotation is `List[ARObject]` while the spec type and the field are BlueprintPolicy → fix to `List[BlueprintPolicy]` at Step 3. CONSUME PATH: NO readAtpBlueprint/writeAtpBlueprint helper exists (parser/writer grep — only readAtpBlueprintMapping/writeAtpBlueprintMapping for the sibling); BLUEPRINT-POLICYS handled nowhere; the XML-bearing concrete BlueprintPolicy subclasses (BlueprintPolicyList Table C.19, BlueprintPolicyNotModifiable, BlueprintPolicySingle; BlueprintPolicyModifiable abstract per Table C.18 Subclasses row `BlueprintPolicyModifiable , BlueprintPolicyNotModifiable`) do NOT exist in src and are NOT queued in Group8/Group9 ⇒ reader/writer coverage genuinely blocked by unqueued siblings (Rule 0001.10 reported at Step 8; the Rule 0001.7 reusable-helper obligation defers WITH them — a helper with zero dispatch targets would be dead code); existing tests/test_armodel/parser/test_atp_blueprint.py pins the deferral contract (test_no_dedicated_reader_writer_methods + inherited-member round-trip via readIdentifiable/writeIdentifiable) but carries STALE docstrings (claims BlueprintPolicy "NOT implemented" and a "List[ARObject] placeholder" — both superseded: BlueprintPolicy synced R23-11 Table C.18, field is List[BlueprintPolicy]) → docstring corrections at Step 5; ARPackage-level: ARPackage deliberately does NOT multiple-inherit AtpBlueprint/AtpBlueprintable (ARPackage.py L392-394 documented parallel-chain disposition) — sibling, untouched; no integration fixture carries BLUEPRINT-POLICYS (grep 0 hits). Tracker review: v1 method_deviation_by_class.md `## AtpBlueprint` L987 EXISTS (rows: blueprintPolicys type-upgrade informational + shortNamePattern not-modeled accepted; D.11 p.305 citation) — reconcile at Step 8; v2 method_deviation_by_class_v2.md has NO AtpBlueprint section (grep: only AtpBlueprintable L1652 / AtpBlueprintMapping L1787 + a BlueprintMappingSet missing-row mention) — reconcile at Step 8
  - [x] Step 2 — Write model class unit test (Red) — existing coverage audit: tests/.../AbstractBlueprintStructure/test___init__.py TestAtpBlueprint already carried 9 tests (abstract TypeError, concrete-subclass instantiation + parent/shortName, Identifiable isinstance, shortName/adminData accessors, empty blueprintPolicys, add/get + None no-op + chaining, heritage closure bases/MRO incl. AtpBlueprintable NOT in MRO, class-docstring verbatim, member-docstring startswith verbatim) — extended with TestAtpBlueprintSpecContract (6 tests, sibling form): exact-own-field-set source pin (re.findall self\.(\w+)\s*: on inspect.getsource(__init__) == ["blueprintPolicys"] — field-to-spec both directions), PEP 526 dedicated-typed-list pin (self.blueprintPolicys: List[BlueprintPolicy] = [] regex + no `# type:`), getter-returns-spec-type pin (get_type_hints(getBlueprintPolicys)["return"] == List[BlueprintPolicy]), Rule 0004 dedicated-field pin (getBlueprintPolicys() is blueprintPolicys), Rule 0001.11 accessor-order pin (re.findall defs == ["addBlueprintPolicy", "getBlueprintPolicys"] — family add→get for aggregations per BlueprintMappingSet), exact-member-docstring pins (add = Note + None-no-op sentence exactly; get = bare Note exactly); also updated the stale "Table C.12" citation in the pre-existing class-docstring-verbatim test docstring to the aligned Table D.11 p.305 (with the C.12/E.10 shift note); RESULT 1 Red / 36 passed — the single genuine Red is test_getter_returns_spec_type (getter annotated List[ARObject], spec type BlueprintPolicy); the other 5 pass immediately (the intake class already conforms on those contracts) — honestly noted
  - [x] Step 3 — Implement model class (Green) — ONE change: `getBlueprintPolicys` return annotation `List[ARObject]` → `List[BlueprintPolicy]` (the spec type per Table D.11 blueprintPolicy row and the field's own declared type; runtime behavior unchanged); everything else already conforms on re-verification against D.11: most-derived base Identifiable (Base row chain ARObject→Referrable→MultilanguageReferrable→Identifiable), abstract TypeError guard (`type(self) is AtpBlueprint` — the XSD group-only representation corroborates abstractness), single PEP 526 field `self.blueprintPolicys: List[BlueprintPolicy] = []` (`*` aggr → dedicated typed list Rule 0004; no createXxx — BlueprintPolicy is not a Referrable child), addBlueprintPolicy None no-op + chaining, blank line discipline (single attribute block), ARObject import still used by the sibling classes in the same module (BlueprintPolicy/AtpBlueprintMapping bases + parent: ARObject hints) — no import churn; evidence: 37/37 model tests Green (was 1 Red / 36 passed)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — docstring inventory for AtpBlueprint: 4 positions (class docstring; __init__ has NO docstring per family form; ONE inline __init__ member comment; add/get docstrings): class docstring already = Table D.11 Note verbatim single line (exact-pinned by test_class_docstring_verbatim), add docstring already = Note + "A None value is a no-op and does not append to blueprintPolicys." (exact-pinned), get docstring already = bare Note verbatim (exact-pinned) — no wipe needed, verified against the markdown text; ONE position rewritten wipe-first: the __init__ inline member comment (intake had the Note split across two wrapped comment lines + 3 intake commentary lines "so the aggregation is typed with the abstract base for now") → wiped and rewritten as the Note verbatim single line + a 2-line spec-provenance comment naming the abstract BlueprintPolicy type (R23-11 Table C.18) and the concrete XML-bearing subtypes (BlueprintPolicyList/-NotModifiable/-Single) — matches the BlueprintPolicy.attributeName sibling form; evidence: 37/37 model tests Green after rewrite (no test regression; the inline comment is verified by eye against the markdown Note)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — existing coverage audit: tests/test_armodel/parser/test_atp_blueprint.py (single file covers BOTH sides — reader+writer — since AtpBlueprint has no own XML element) already pinned the deferral contract in 4 tests (no readAtpBlueprint/writeAtpBlueprint hasattr pin; Identifiable-in-MRO; inherited shortName round-trip through writeIdentifiable→readIdentifiable with field-value assertion; empty-aggregation pin); NO genuine Red is possible this step: the class's only own attribute's serialization is genuinely blocked by the unqueued concrete BlueprintPolicy subclasses (Step 1 — the branches cannot be added without fabricating sibling classes, Rule 0001.10), and that deferral is exactly what the existing tests pin; ADDED/CORRECTED (all passing immediately — honestly noted): (1) module docstring rewritten from the stale intake wording (claimed BlueprintPolicy "NOT implemented" / a "List[ARObject] placeholder" — both superseded: BlueprintPolicy synced R23-11 Table C.18, field is List[BlueprintPolicy]; citation corrected C.12→D.11 p.305 with the XML-bearing-subtypes blocker named), (2) test_blueprint_policy_aggregation_deferred docstring corrected to the same current-state form, (3) test_round_trip_inherited_members_through_identifiable extended with adminData (second inherited Identifiable member, set→write→read→assert-not-None field-value round-trip) + AdminData import appended alphabetically; evidence: 4/4 parser/writer-side tests Green; writer-side dedicated file n/a (the one deferral-contract file pins both sides; writer dir has no AtpBlueprint file and needs none while the aggregation is deferred)
  - [x] Step 6 — Update parser & writer (Green) — NO source changes, deliberately: the class's only own attribute (blueprintPolicy, * aggr) serializes as the BLUEPRINT-POLICYS wrapper (XSD 00052 group ATP-BLUEPRINT L6652: choice of BLUEPRINT-POLICY-LIST/-NOT-MODIFIABLE/-SINGLE) whose dispatch targets are the concrete BlueprintPolicy subclasses — absent from src AND unqueued in Group8/Group9 (Step 1), so a readAtpBlueprint/writeAtpBlueprint helper cannot dispatch anything without fabricating siblings (Rule 0001.10; a zero-call-site helper would be dead code — the Rule 0001.7 abstract-class reusable-helper obligation defers WITH the subclasses, reported at Step 8); the inherited-member consume path pre-exists both sides via the shared readIdentifiable/writeIdentifiable pair (matched name pair Rule 0013.2; no cross pairs; no chained mutator calls) and is test-pinned (test_atp_blueprint.py TestAtpBlueprintReaderWriter: hasattr absence pin + shortName/adminData field-value round-trip); ARPackage-level dispatch n/a (no concrete AtpBlueprint element tag of its own; ARPackage's documented parallel-chain disposition at ARPackage.py L392-394 untouched); no integration fixture carries BLUEPRINT-POLICYS (grep 0 hits — zero round-trip regression surface); evidence: 4/4 test_atp_blueprint.py Green unchanged (parser+writer battery re-run at 9a)
  - [x] Step 7 — Update checklist comment — 6-column block rebuilt in AbstractBlueprintStructure/__init__.py: `# Spec:` line corrected to the aligned table `R23-11/AUTOSAR_CP_TPS_BSWModuleDescriptionTemplate.pdf, Table D.11, p.305 (R23-11)` (was the extraction-shifted C.12 p.161 citation; matches the v1 tracker's 2026-08-08 D.11 alignment), rows unchanged in content and source order (__init__ [—]/[—]; addBlueprintPolicy reader [ ]/writer [—]; getBlueprintPolicys reader [—]/writer [ ] — the deferral rows MUST stay [ ] per Rule 0002, they are the flagged round-trip gap, not [—]); added the table-identity + XSD corroboration comment (group-only ATP-BLUEPRINT L6652 = abstract; SHORT-NAME-PATTERN atp.Status="removed" not modeled per Rule 0015) and the refreshed marker-withheld note (blocker = the unqueued concrete XML-bearing subclasses BlueprintPolicyList/-NotModifiable/-Single; no readAtpBlueprint/writeAtpBlueprint helper for the same reason); NO `# Spec verified:` stamp present (verified — none existed, none added; stamp withheld per batch instruction — deferred to batch confirmation)
  - [x] Step 8 — Deviations  [no NEW open rows; the two pre-existing v1 rows STAND (one informational, one accepted): (1) `blueprintPolicys` row = informational — the BlueprintPolicy type upgrade is complete (R23-11 Table C.18), the reader/writer deferral is the open blocker owned by the concrete XML-bearing subclasses BlueprintPolicyList/-NotModifiable/-Single which are neither implemented in src NOR queued in Group8/Group9 (Rule 0001.10 referenced-class report: their future sync owns the BLUEPRINT-POLICY-* element coverage, the `[ ]` checklist rows, and this class's stamp; BlueprintPolicyModifiable is abstract per the Table C.18 Subclasses row and needs no element), (2) `shortNamePattern` *not modeled* row = ACCEPTED deviation per Rule 0015 (atp.Status="removed", XSD-only SHORT-NAME-PATTERN member of the ATP-BLUEPRINT group, absent from the R23-11 PDF Table D.11 — PDF wins); the Step 2-5 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): getter return annotation List[ARObject]→List[BlueprintPolicy] (the spec type), stale test docstrings corrected (BlueprintPolicy-implemented state + D.11 citation), intake wrapped inline comment normalized, checklist citation C.12 p.161→D.11 p.305; v1 tracker (method_deviation_by_class.md) `## AtpBlueprint` section RECONCILED 2026-09-24: Source path updated to the current non-leaf `__init__.py` (was the pre-reorg AtpBlueprint.py), sync-record Note appended (D.11 re-verified as the only ALIGNED AtpBlueprint table with C.12 p.161/E.10 p.424 row-identical shifted bodies, XSD corroboration, both-directions field cross-check EXACT, the in-band fixes listed, the deferral re-confirmed, stamp deferred); v2 tracker (method_deviation_by_class_v2.md) has NO AtpBlueprint section (verified by grep at Step 1, re-verified at Step 8 — only AtpBlueprintable L1652 / AtpBlueprintMapping L1787 + a BlueprintMappingSet missing-row mention) — left untouched per the batch convention (sync-record notes go in v1; v2 stays the historical audit); staged_class_sync_review.md has 0 AtpBlueprint mentions (verified by grep); referenced classes: base chain Identifiable/Referrable/MultilanguageReferrable/ARObject (stamped heritage), member type BlueprintPolicy (synced R23-11 Table C.18, stamped) + its unqueued concrete subclasses (reported above) — no other missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 41/41 (test___init__.py 37 — 9 pre-existing TestAtpBlueprint + 5 TestBlueprintPolicy + 6 TestAtpBlueprintable + 11 TestAtpBlueprintMapping + 6 net-new TestAtpBlueprintSpecContract; test_atp_blueprint.py 4 — deferral-contract pins with corrected docstrings + the extended shortName/adminData round-trip), full suite `uv run python scripts/run_tests.py --no-coverage` 11212 passed / 0 failed (baseline 11206 + 6 net-new: the TestAtpBlueprintSpecContract source pins incl. the Rule 0001.11 accessor-order pin, the Rule 0004 dedicated-field pin, the PEP 526/exact-field-set pins, and the spec-type getter annotation pin that was the step-2 Red); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I "All checks passed"; the only src edit is in-place inside one method/comment/checklist block in AbstractBlueprintStructure/__init__.py — no import changes, no re-sort risk); `npm run black` 1150 files unchanged + `black-check` clean; integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: element kind + every spec attr modeled (the single D.11 attribute blueprintPolicy `*` aggr → blueprintPolicys — exact-own-field-set test-pinned), most-derived base Identifiable (0001.2 — Base row chain ARObject→Referrable→MultilanguageReferrable→Identifiable; XSD group-only ATP-BLUEPRINT corroborates abstractness; heritage-closure test-pinned incl. AtpBlueprintable NOT in MRO), no fabrication/flattening (0001.3 — both-directions cross-check exact; base chain modeled+stamped; XSD SHORT-NAME-PATTERN atp.Status="removed" deliberately unmodeled per Rule 0015), Kind-suffix naming n/a (0001.5 — aggr kind, plain plural; no ref/tref/iref members), create/set/add shape (0001.6 — addBlueprintPolicy for the aggr with None no-op + chaining, no createXxx since BlueprintPolicy is not a Referrable child; test-pinned), reader+writer coverage (0001.7) DEFERRED — the `[ ]` rows are the honest flag, owned by the unqueued concrete BlueprintPolicy subclasses (Rule 0001.10 reported at Step 8); inherited-member coverage test-pinned through readIdentifiable/writeIdentifiable, member order (0001.11) — single displayed row; accessor order add→get source-pinned; checklist rows in class source order; reader/writer XML element order n/a (no own dispatch; BLUEPRINT-POLICYS choice order BLUEPRINT-POLICY-LIST/-NOT-MODIFIABLE/-SINGLE per XSD group L6652 recorded for the future subclass syncs), docstrings verbatim (0012/0001.4 — class = Note exact, add = Note + None-no-op sentence exact, get = Note exact, inline __init__ comment = Note verbatim single line; 4/4 positions exact-equality test-pinned), blank line between __init__ attribute blocks n/a-trivial (single attribute block, verified by eye), PEP 526 annotated member + no `# type:` (source-pinned), deviations resolved/removed per 0014 (v1 reconciled — 1 informational + 1 accepted row stand; v2/staged have no entries), stamp decision: `# Spec verified:` WITHHELD — both by the open `[ ]` reader/writer rows owned by unqueued siblings AND by batch instruction, Rule 0007 location: kept in AbstractBlueprintStructure/__init__.py per the queue row's Phase-0 module decision (observation for the record: the package directory has no subdirectories, so the strict leaf-package reading would name the file AbstractBlueprintStructure.py — flagged, not moved unilaterally, same pattern as the BindingTimeEnum location finding); 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `BlueprintGenerator` — ARObject — R23-11 FO_TPS_GenericStructureTemplate Table E.12 (appendix E; caption-shift confirmed) — pp.424-425
  - module: M2/AUTOSARTemplates/CommonStructure/StandardizationTemplate/BlueprintGenerator.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — SINGLE R23-11 table, appendix-letter (numeric main-table caption search NONE: `^Table [\w.]+: BlueprintGenerator$` over both corpora hits only `Table E.12` AUTOSAR_FO_TPS_GenericStructureTemplate.md L11536; R4.3.1 fallback case-insensitive `blueprintgenerator|blueprint generator` 0 hits in autosar/R4.3.1/markdown/ — genuinely R23-11-only, no R4.3.1 table to fall back to); CAPTION-SHIFT CONFIRMED in appendix E (same one-caption-early pattern the AtpBlueprint agent documented for C.12/E.10): the actual BlueprintGenerator body renders ABOVE the caption — main fragment L11519-11526 (Class / Package / Note / Base / Aggregated by / Attribute header + expression row) sits under the `Table E.11: AtpBlueprintable` caption position, page-break image L11528 + glyph triangle L11530, continuation fragment L11532-11534 (introduction row with its own Note column), `Table E.12: BlueprintGenerator` caption at L11536, and the Boolean body L11538-11541 renders under the E.12 caption matching its own E.13 caption at L11543 — row identity verified: body Class row = BlueprintGenerator, Package M2::AUTOSARTemplates::CommonStructure::StandardizationTemplate::BlueprintGenerator, Note verbatim "This class express the Extended Language to generate blueprint derivates in complex descriptions." (Tags: atp.Status=valid; sic "express" — spec text kept verbatim), Base `ARObject` (single), Aggregated by `VariationPoint.formalBlueprintGenerator`, Attribute rows displayed order: (1) `expression | VerbatimString | 0..1 | attr | This represents a formal term in the expression based on the extended language.` (Tags: atp.Status=valid xml.sequenceOffset=20), (2) `introduction | DocumentationBlock | 0..1 | aggr | This represents a description that documents how the blueprint generator shall be resolved when deriving objects from blueprints.` (Tags: atp.Status=valid xml.sequenceOffset=10); PAGE via direct pypdf scan (pdf_page.py does not index appendix-letter ids): body main fragment p.424, introduction continuation + `Table E.12` caption p.425 → cite pp.424-425 (intake checklist + v1 tracker say p.424); XSD 00052 ground truth: group BLUEPRINT-GENERATOR L9083 (annotation = class Note verbatim, stereotype atpObject) sequence INTRODUCTION (DOCUMENTATION-BLOCK, 0..1, xml.sequenceOffset=10) → EXPRESSION (VERBATIM-STRING, 0..1, xml.sequenceOffset=20) — XSD element order INTRODUCTION→EXPRESSION is the REVERSE of the markdown displayed attribute order (expression first) and governs reader/writer only (Rule 0001.11); complexType BLUEPRINT-GENERATOR L9105 abstract="false" = AR-OBJECT group + own group + AR-OBJECT attributeGroup → Base row ARObject CONFIRMED (most-derived base ARObject — the chain ends there, no flattening possible); consumed by VARIATION-POINT group L130046 FORMAL-BLUEPRINT-GENERATOR (type BLUEPRINT-GENERATOR, 0..1, seqOffset=30, atp.Status="draft") — VariationPoint itself queued later (row 24), untouched; FIELD-TO-SPEC BOTH DIRECTIONS EXACT: 2 spec attributes → exactly 2 fields (expression Optional[VerbatimString] for the 0..1 attr, introduction Optional[DocumentationBlock] for the 0..1 aggr — plain Optional fields both, no create/add since neither is a Referrable child), no extra fields, Rule 0001.5 Kind-suffix naming n/a (no ref/tref/iref members); intake docstring positions verified against the markdown: class docstring = Note verbatim minus Tags tail, inline __init__ comments + getter/setter docstrings = attribute Notes verbatim (+ None-no-op sentence on setters) — full wipe-and-verify at Step 4; INTAKE CHECKLIST FINDINGS (Step 7 work): 5-column block missing the release column AND carries a `# Spec verified: R23-11` marker despite being an unconfirmed orphan intake — marker to be REMOVED (stamp deferred to batch confirmation), `# Spec:` line corrected to pp.424-425 with the shift note; CONSUME PATH (Steps 5/6): BOTH sides pre-exist and are fully wired — parser readBlueprintGenerator (arxml_parser.py L1265: readARObject + INTRODUCTION via getDocumentationBlock → setIntroduction + EXPRESSION find → VerbatimString().setValue → setExpression) called from readVariationPoint L1316-1318 (find FORMAL-BLUEPRINT-GENERATOR → setFormalBlueprintGenerator); writer writeBlueprintGenerator (arxml_writer.py L1141: FORMAL-BLUEPRINT-GENERATOR wrapper + writeARObject + writeDocumentationBlock INTRODUCTION + EXPRESSION via getExpression, order comment cites XSD offsets 10/20) called from writeVariationPoint L1193 (getFormalBlueprintGenerator) — NO genuine Red expected from a missing branch (it is not missing); COVERAGE GAPS for Step 5: parser side has ZERO tests exercising FORMAL-BLUEPRINT-GENERATOR (grep tests/ → only test_writer_variation_point.py mentions it), writer covers EXPRESSION-only (formal_tags == ["EXPRESSION"], no INTRODUCTION anywhere, no re-parse round-trip of generator field values); VariationPoint.setFormalBlueprintGenerator/getFormalBlueprintGenerator pre-exist (test_VariantHandling.py L820-837); package shape per Rule 0007 correct (leaf file BlueprintGenerator.py directly under StandardizationTemplate/, models/__init__.py L49 wildcard import present)
  - [x] Step 2 — Write model class unit test (Red) — existing coverage audit: test_BlueprintGenerator.py TestBlueprintGenerator carried 5 functional intake tests (initialization defaults, set/get round-trip + chaining, None no-op for both attributes); extended with TestBlueprintGeneratorSpecContract (10 tests, sibling form): class-docstring verbatim pin (Table E.12 Note minus the Tags tail — spec wording "express" kept verbatim), exact-own-field-set source pin (re.findall self\.(\w+)\s*: on inspect.getsource(__init__) == ["expression", "introduction"] — field-to-spec both directions in displayed row order), PEP 526 annotated-member pins (self.expression: Optional[VerbatimString] = None / self.introduction: Optional[DocumentationBlock] = None regex + no `# type:`), most-derived-base pin (__bases__ == (ARObject,); Identifiable NOT in MRO — the Base row is the single ARObject, XSD 00052 complexType BLUEPRINT-GENERATOR L9105 composes AR-OBJECT group + own group only), Rule 0001.11 accessor-order pin (re.findall defs == ["getExpression", "setExpression", "getIntroduction", "setIntroduction"] — get→set pairs in displayed row order), 4 exact-member-docstring pins (get/setExpression + get/setIntroduction = Note verbatim / Note + None-no-op sentence) + inline-__init__-comment verbatim pin; RESULT 5 Red / 10 passed — ALL 5 Reds are the designed docstring-shape Reds owned by Step 4 (the intake class docstring is the multi-section Package/Base/Attributes form and all 4 member docstrings are wrapped multi-line triple-quoted blocks — none is the bare verbatim single-line Note the exact-equality pins require), every functional/structural assertion (fields, PEP 526, base chain, accessor order, inline comments, defaults, round-trips, None no-ops, chaining) passed immediately because the intake shape is already spec-correct — honestly noted
  - [x] Step 3 — Implement model class (Green) — verification-only: NO source change (both-directions field-to-spec cross-check re-verified against Table E.12 + XSD 00052 L9083/L9105: most-derived base ARObject per the single-Base row — class already derives ARObject directly; exactly the two spec attributes as PEP 526 Optional[VerbatimString]/Optional[DocumentationBlock] fields in displayed row order (attr 0..1 + aggr 0..1 → plain Optional fields, no dedicated list needed, no create/add — neither is a Referrable child); get/set accessors with chaining + None no-op already present and test-pinned; no abstract guard needed (complexType abstract="false"); Python 3.8 typing conformant (typing.Optional, forward-ref string return annotations, no T | None / list[...]); blank line between the two __init__ attribute blocks present; evidence: model tests 10 passed / 5 failed UNCHANGED from Step 2 — the 5 remaining failures are exactly the docstring-shape Reds owned by Step 4, zero new implementation work required
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — docstring inventory for BlueprintGenerator: 5 positions (class docstring; __init__ has NO docstring per family form; TWO inline __init__ member comments; get/set docstrings x2 pairs) — ALL wiped first, then rewritten from the Table E.12 markdown: class docstring = Note verbatim single-line triple-quote ("This class express the Extended Language to generate blueprint derivates in complex descriptions." — replaced the intake multi-section Package/Base/Attributes block; spec wording "express" kept, Tags: atp.Status=valid tail stripped); getExpression docstring = attribute Note verbatim single line; setExpression docstring = Note verbatim + " A None value is a no-op and does not overwrite an existing expression." single line (no terminal punctuation added before the sentence); getIntroduction docstring = attribute Note verbatim single line; setIntroduction docstring = Note verbatim + " A None value is a no-op and does not overwrite an existing introduction." single line; the two inline __init__ member comments were ALREADY the Notes verbatim single-line (verified by eye against the markdown cells) — kept as the wipe-and-rewrite result; evidence: 15/15 model tests Green (was 5 Red / 10 passed) — all 5 spec-contract docstring pins now exact-equality Green
  - [x] Step 5 — Write reader/writer round-trip test (Red) — existing coverage audit: writer side test_writer_variation_point.py TestWriteVariationPoint.test_write_full_roundtrip_content writes a BlueprintGenerator with EXPRESSION only through writeVariationPoint and pins the FORMAL-BLUEPRINT-GENERATOR position in the VP sequence + `formal_tags == ["EXPRESSION"]`; PARSER side has ZERO tests exercising FORMAL-BLUEPRINT-GENERATOR anywhere (grep tests/ → only the writer file mentions it); INTRODUCTION (DocumentationBlock) covered on NEITHER side and no re-parse round-trip of the generator field values exists; ADDED (1) NEW parser file test_parser_blueprint_generator.py TestReadBlueprintGenerator (3 tests): element-level readBlueprintGenerator full form (INTRODUCTION DocumentationBlock P/L-1 field value + EXPRESSION VerbatimString field value + isinstance), expression-only (introduction stays None), empty bare element (both None — the empty-wrapper case); (2) writer TestWriteBlueprintGenerator (4 tests): XSD element-order pin with BOTH fields set (child_tags == ["INTRODUCTION", "EXPRESSION"] — offsets 10/20) with XML field-value assertions, empty generator emits a bare FORMAL-BLUEPRINT-GENERATOR (the empty form its reader accepts), None generator emits no element, and a full VP-level write → re-parse round-trip (writeVariationPoint → readVariationPoint) asserting BOTH field values through getFormalBlueprintGenerator; RESULT 0 Red / 30 passed — NO genuine Red: both dispatch branches pre-exist both sides (Step 1 — the branch is NOT missing), the new tests close the reader coverage gap and pin the XSD order + field values, passing immediately — honestly noted
  - [x] Step 6 — Update parser & writer (Green) — no changes needed: the class's consume path is fully wired both sides since intake — parser readBlueprintGenerator (arxml_parser.py L1265: readARObject + INTRODUCTION via getDocumentationBlock → setIntroduction + EXPRESSION find → VerbatimString().setValue → setExpression) dispatched from readVariationPoint L1316-1318 (find FORMAL-BLUEPRINT-GENERATOR → setFormalBlueprintGenerator); writer writeBlueprintGenerator (arxml_writer.py L1141: FORMAL-BLUEPRINT-GENERATOR wrapper + writeARObject + writeDocumentationBlock INTRODUCTION + EXPRESSION via getExpression, order comment cites XSD offsets 10/20) dispatched from writeVariationPoint L1193 (getFormalBlueprintGenerator); matched name pairs Rule 0013.2 verified (readBlueprintGenerator↔writeBlueprintGenerator, setIntroduction↔getIntroduction, setExpression↔getExpression, setFormalBlueprintGenerator↔getFormalBlueprintGenerator, readARObject↔writeARObject — no cross pairs); no chained mutator calls; element order XSD-conformant both sides (INTRODUCTION offset 10 → EXPRESSION offset 20; now test-pinned by the Step 5 order assertions, VP-level position by the pre-existing test_write_full_roundtrip_content); VARIATION-POINT emits FORMAL-BLUEPRINT-GENERATOR only when set (None → no element, test-pinned); evidence: 30/30 parser+writer tests green (3 new parser + 27 writer incl. 4 net-new)
  - [x] Step 7 — Update checklist comment — 6-column format with release column rebuilt in BlueprintGenerator.py: `# Spec:` line corrected to pp.424-425 (was the intake p.424; the caption sits on p.425 per the direct pypdf scan) plus a 5-line provenance comment (appendix E caption-shift note, no numeric main table in R23-11, R4.3.1 corpus absent, XSD 00052 group BLUEPRINT-GENERATOR L9083 sequence INTRODUCTION offset 10 → EXPRESSION offset 20, complexType L9105 AR-OBJECT + own group, reader dispatched from readVariationPoint via FORMAL-BLUEPRINT-GENERATOR + writer writeBlueprintGenerator from writeVariationPoint); rows in source order with reader/writer call-site verified: __init__ [—]/[—], setExpression [x] reader (readBlueprintGenerator calls setExpression), getExpression [x] writer (writeBlueprintGenerator calls getExpression), setIntroduction [x] reader, getIntroduction [x] writer, every row release R23-11; PREMATURE `# Spec verified: R23-11` MARKER REMOVED (the intake carried it despite being an unconfirmed orphan — stamp withheld per batch instruction, deferred to batch confirmation; grep confirms 0 `Spec verified`/`XSD verified` lines in the file); model tests still 15/15 Green after the edit
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly the two Table E.12 attributes, expression Optional[VerbatimString] 0..1 attr + introduction Optional[DocumentationBlock] 0..1 aggr, no extra fields, no flattening — the Base chain ends at ARObject which IS the modeled base); the Step 2-7 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): (1) intake multi-section class docstring + 4 wrapped member docstrings wiped and rewritten as the bare verbatim single-line Notes (+ None-no-op sentences on setters), (2) premature `# Spec verified: R23-11` marker REMOVED (intake carried it despite being unconfirmed — stamp deferred to batch confirmation), (3) checklist rebuilt 6-column/release with the pp.424-425 citation and XSD provenance comment; v1 tracker (method_deviation_by_class.md) `## BlueprintGenerator` section RECONCILED 2026-09-24: the two `missing` rows were STALE (pre-dated the implementation — both members fully implemented with reader/writer coverage) and are superseded by a no-deviation row; Source path updated to the current leaf file (was the pre-reorg `BlueprintGenerator/BlueprintGenerator.py`), PDF pages updated 424 → 424-425, sync-record Note appended (E.12-only/caption-shift/XSD corroboration/both-directions cross-check/consume-path disposition/stamp deferred); v2 tracker (method_deviation_by_class_v2.md) and staged_class_sync_review.md have NO BlueprintGenerator entries (verified by grep — 0 mentions each); referenced classes: base ARObject (modeled, stamped heritage root), member types VerbatimString + DocumentationBlock (both modeled and stamped) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 45/45 (test_BlueprintGenerator.py 15 — 5 functional intake + 10 TestBlueprintGeneratorSpecContract incl. the both-directions field-set pin, PEP 526/base-chain/accessor-order pins and the 5 verbatim-docstring exact-equality pins that were the Step 2 Reds; test_parser_blueprint_generator.py 3 — NEW TestReadBlueprintGenerator full/expression-only/empty element-level reader tests; test_writer_variation_point.py 27 incl. 4 net-new TestWriteBlueprintGenerator — XSD order pin INTRODUCTION→EXPRESSION with both fields, empty bare element, None no-element, full VP-level write→re-parse round-trip with both field values), full suite `uv run python scripts/run_tests.py --no-coverage` 11229 passed / 0 failed (baseline 11212 + 17 net-new: +10 model spec-contract +3 parser +4 writer); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I "All checks passed" — one iteration: the initially-added `from typing import Optional` in the model test file was unused and removed after ruff flagged it; the only src edit is in-place docstring/checklist text in BlueprintGenerator.py — no import changes, no re-sort risk); `npm run black` 1 file reformatted (the new parser test file — long XML-literal lines) + `black-check` clean 1151 files; integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: element kind + every spec attr modeled (both Table E.12 attributes → exactly two fields, exact-own-field-set test-pinned), most-derived base ARObject (0001.2 — Base row is the single ARObject; XSD complexType BLUEPRINT-GENERATOR L9105 composes AR-OBJECT group + own group; __bases__ == (ARObject,) test-pinned, Identifiable not in MRO), no fabrication/flattening (0001.3 — both-directions cross-check exact), Kind-suffix naming n/a (0001.5 — attr + aggr kinds, no ref/tref/iref members), create/set/add shape (0001.6 — setExpression/setIntroduction with None no-op + chaining, no create/add — neither member is a Referrable child; test-pinned), reader+writer coverage for every kept attr (0001.7 — readBlueprintGenerator/writeBlueprintGenerator dispatched from readVariationPoint/writeVariationPoint via FORMAL-BLUEPRINT-GENERATOR, matched name pairs verified, both sides test-pinned at element level AND through the VP-level round-trip with field values), member order (0001.11 — fields + accessors + checklist rows in the markdown displayed order expression→introduction, accessor-order source-pinned; reader/writer XML element order INTRODUCTION offset 10 → EXPRESSION offset 20 per XSD group L9083 governs serialization only and is test-pinned — the two orders are intentionally opposite), docstrings verbatim (0012/0001.4 — class = Note minus Tags tail, getExpression/getIntroduction = Notes exact, setExpression/setIntroduction = Notes + None-no-op sentences exact, both inline __init__ comments = Notes verbatim; 5/5 docstring positions + 2/2 comments exact-equality/verbatim test-pinned), blank line between __init__ attribute blocks (0008 — single blank line between the expression and introduction blocks, verified by eye), PEP 526 annotated members + no `# type:` (0003 — regex-pinned), deviations resolved/removed (0014 — no open rows; v1 tracker section reconciled: stale missing-rows superseded by no-deviation row + Source path/page updated + sync-record Note; v2 + staged trackers 0 mentions), stamp decision: `# Spec verified:` WITHHELD per batch instruction — the premature intake marker was removed in Step 7 (file verified clean of `Spec verified`/`XSD verified` lines), Rule 0007 location: leaf package BlueprintGenerator.py directly under StandardizationTemplate/ matching the spec Package row M2::AUTOSARTemplates::CommonStructure::StandardizationTemplate::BlueprintGenerator (models/__init__.py L49 wildcard import present); 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `BlueprintMapping` — AtpBlueprintMapping — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/StandardizationTemplate/BlueprintMapping.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - [x] Step 1 — Sync members & description from spec — SINGLE R23-11 table, appendix-letter (numeric main-table caption search: NONE — `^Table [\w.]+: BlueprintMapping$` over both corpora hits only `Table C.17` AUTOSAR_FO_TPS_StandardizationTemplate.md L4987; every numeric hit (R23-11 3.1/4.1, R4.3.1 3.4/4.10) is BlueprintMappingSet); CAPTION-SHIFT CONFIRMED (appendix C one-caption-early, same pattern as C.12/E.10): the actual BlueprintMapping body renders ABOVE the caption at L4977-4985 (Package `M2::AUTOSARTemplates::CommonStructure::StandardizationTemplate::BlueprintDedicated::GenericBlueprint`; Note "This meta-class represents the ability to map two an object and its blueprint." — spec typo "two an" kept verbatim; Base `ARObject , AtpBlueprintMapping` — most-derived AtpBlueprintMapping EXISTS in src, stamped R23-11 Table C.13, so no Rule 0001.10 base gap; Aggregated by `BlueprintMappingSet.blueprintMap`), while the rows UNDER the C.17 caption are row-identified as BlueprintPolicy (Class `BlueprintPolicy (abstract)`); Attribute rows in appendix collapsed Attribute|Type format (no Mul./Kind columns): `blueprint | AtpBlueprint`, `derivedObject | AtpBlueprintable`. CITED Table C.17, p.163 — body and caption both on p.163 per DIRECT pypdf scan (pdf_page.py does not index appendix-letter ids). R4.3.1 reproduction `Table D.13` AUTOSAR_TPS_StandardizationTemplate.md L4951 (body below caption, no shift there): identical Note/Base/Attributes — R23-11 is the citation. R4.3.1 AUTOSAR_TR_GeneralBlueprintsSupplement.md L2302 is NOT this class — the header cell belongs to ClientServerInterfaceToBswModuleEntryBlueprintMapping (Table A.5); the TR carries no dedicated BlueprintMapping table (5 substring hits, all longer class names). XSD 00052 ground truth for kinds/multiplicity (the appendix table omits Mul./Kind): complexType BLUEPRINT-MAPPING L9154 = AR-OBJECT + ATP-BLUEPRINT-MAPPING group + BLUEPRINT-MAPPING group; ATP-BLUEPRINT-MAPPING group L6888 has an EMPTY sequence (both base associations commented "skipped": `<!-- Association <<atpDerived>>atpBlueprint skipped -->` — corroborates the v2 tracker AtpBlueprintMapping entry); BLUEPRINT-MAPPING group L9118 = BLUEPRINT-REF (0..1, REF extension DEST=ATP-BLUEPRINT--SUBTYPES-ENUM, mmt.qualifiedName="BlueprintMapping.blueprint", pureMM 1..1, doc "This represents the mapped blueprint.") → DERIVED-OBJECT-REF (0..1, REF extension DEST=ATP-BLUEPRINTABLE--SUBTYPES-ENUM, mmt.qualifiedName="BlueprintMapping.derivedObject", pureMM 1..1, doc "This represents the object which was derived from the blueprint.") — XSD element order BLUEPRINT-REF → DERIVED-OBJECT-REF matches the displayed row order; both are refs (kind ref → `Ref` suffix naming per Rule 0001.5). FIELD-TO-SPEC GAP: the intake class derives AtpBlueprintMapping but carries ZERO fields — both spec attributes (blueprintRef/derivedObjectRef as Optional[RefType] per the sibling PortInterfaceBlueprintMapping form) are missing and must be added at Step 3. CONSUME PATH: dispatch exists both sides (parser readBlueprintMappingSet BLUEPRINT-MAPPING branch creates BlueprintMapping() + readAtpBlueprintMapping which is readARObject-ONLY — BLUEPRINT-REF/DERIVED-OBJECT-REF silently dropped; writer writeBlueprintMappingSet else-branch calls writeAtpBlueprintMapping = writeARObject-only — same drop) ⇒ genuine reader/writer gaps: add readBlueprintMapping/writeBlueprintMapping (sibling form readAtpBlueprintMapping + two getChildElementOptionalRefType / writeAtpBlueprintMapping + two setChildElementOptionalRefType) and re-dispatch the BLUEPRINT-MAPPING branch. Tracker entries reviewed: v1 `## BlueprintMappingSet` missing-row (blueprintMap of type BlueprintMapping) and v2 `## BlueprintMappingSet` missing-row + `## AtpBlueprintMapping` (atpDerived realization note pointing at exactly this BlueprintMapping.blueprint/derivedObject realization) — reconcile at Step 8
  - [x] Step 2 — Write model class unit test (Red) — existing coverage audit: no dedicated BlueprintMapping model test existed (the class had zero members to test; the intake class only appeared in test_BlueprintMappingSet.py's concrete-instantiation checks); NEW tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/StandardizationTemplate/test_BlueprintMapping.py (mirrors the source file name, pairs with class BlueprintMapping; BlueprintMappingSet keeps its own test_BlueprintMappingSet.py): TestBlueprintMapping 3 functional tests (initialization defaults None/None + isinstance chain BlueprintMapping→AtpBlueprintMapping→ARObject; get/set blueprintRef round-trip + None no-op + chaining; same for derivedObjectRef) + TestBlueprintMappingSpecContract 9 pins (sibling form): exact-own-field-set source pin (re.findall self\.(\w+)\s*: on inspect.getsource(__init__) == ["blueprintRef", "derivedObjectRef"] — field-to-spec both directions), PEP 526 `Optional[RefType]` regex pins + no `# type:` pin, Rule 0001.2 most-derived-base pin (__bases__ == (AtpBlueprintMapping,)), Rule 0001.11 accessor-order pin (defs == [__init__, getBlueprintRef, setBlueprintRef, getDerivedObjectRef, setDerivedObjectRef] — displayed row order blueprint→derivedObject, get→set pairs per the PortInterfaceBlueprintMapping sibling), class-docstring verbatim pin (C.17 Note exact, spec typo "two an" kept), 4 member-docstring exact-equality pins (get = bare note, set = note + None-no-op sentence); RESULT 10 Red / 2 passed — the Reds are the 3 functional tests (no accessors/fields) + 7 spec pins; the 2 immediate passes (most-derived-base, class-docstring-verbatim) are honest: the intake already conformed on exactly those two contracts
  - [x] Step 3 — Implement model class (Green) — BlueprintMapping.py: added the two spec attributes as PEP 526 `Optional[RefType]` fields in displayed row order (`self.blueprintRef` — Table C.17 blueprint, XSD BLUEPRINT-REF DEST ATP-BLUEPRINT--SUBTYPES-ENUM; `self.derivedObjectRef` — derivedObject, XSD DERIVED-OBJECT-REF DEST ATP-BLUEPRINTABLE--SUBTYPES-ENUM; blank line between blocks), get/set accessors with chaining + None no-op (kind ref → `Ref` suffix naming Rule 0001.5, matching the PortInterfaceBlueprintMapping/PortPrototypeBlueprintMapping siblings), plus the RefType import; base unchanged (AtpBlueprintMapping — most-derived per the Base row, no Rule 0001.10 gap: `class AtpBlueprintMapping(ARObject, ABC)` exists and is stamped R23-11 Table C.13); no createXxx (neither member is a Referrable child); BlueprintMappingSet untouched (own stamped sync); evidence: 17/17 Green (12 BlueprintMapping + 5 pre-existing BlueprintMappingSet), then 112/112 across the StandardizationTemplate model tree after Step 7
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — docstring inventory for the intake class: ONE position only (class docstring; __init__ had no docstring and no members, so no member docstrings/comments existed to wipe) — the class docstring already equals the C.17 Note verbatim ("This meta-class represents the ability to map two an object and its blueprint.", spec typo "two an" kept) and is now exact-pinned by test; all NEW positions were written fresh from the spec text in the Step 3 pass (no stale intake wording could survive — verified by eye): 2 inline __init__ member comments + 2 getter docstrings = the XSD element documentations verbatim ("This represents the mapped blueprint." / "This represents the object which was derived from the blueprint." — the appendix C.17 table has NO per-attribute Note column, so the XSD documentations are the authoritative attribute text, source-named in the checklist provenance comment), 2 setter docstrings = same + "A None value is a no-op and is not set."; __init__ has no docstring per family form; blank line between the two attribute blocks present
  - [x] Step 5 — Write reader/writer round-trip test (Red) — existing coverage audit: parser test_blueprint_mapping_set.py + writer test_blueprint_mapping_set.py covered the set-level dispatch but only as a bare empty BLUEPRINT-MAPPING element — BLUEPRINT-REF/DERIVED-OBJECT-REF were carried by NO test anywhere (grep tests/ for the element names: 0 hits) and the refs were silently DROPPED both sides (readAtpBlueprintMapping = readARObject-only; writeAtpBlueprintMapping = writeARObject-only); NEW dedicated tests/test_armodel/parser/test_blueprint_mapping.py TestReadBlueprintMapping 4 tests (element-level both-refs read with value+DEST asserts; empty-element read leaves None; dispatch-through-set populates field VALUES from readBlueprintMappingSet; full write→re-parse round-trip asserting all four value/DEST pairs) + tests/test_armodel/writer/test_blueprint_mapping.py TestWriteBlueprintMapping 3 tests (element-level write with XSD order pin [BLUEPRINT-REF, DERIVED-OBJECT-REF] + value/DEST asserts; None-fields write emits no elements; full build→write→re-parse round-trip); RESULT 7/7 Red (AttributeError — readBlueprintMapping/writeBlueprintMapping did not exist)
  - [x] Step 6 — Update parser & writer (Green) — parser: NEW readBlueprintMapping (readAtpBlueprintMapping + setBlueprintRef/setDerivedObjectRef via getChildElementOptionalRefType — sibling PortInterface/PortPrototype form; no chained mutators) and the readBlueprintMappingSet BLUEPRINT-MAPPING branch re-dispatched from readAtpBlueprintMapping to readBlueprintMapping; writer: NEW writeBlueprintMapping (writeAtpBlueprintMapping + setChildElementOptionalRefType BLUEPRINT-REF then DERIVED-OBJECT-REF — XSD group L9118 order) and the writeBlueprintMappingSet else-branch re-dispatched from writeAtpBlueprintMapping to writeBlueprintMapping, plus the BlueprintMapping import added to the existing BlueprintMapping-module import block (alphabetical, no re-sort); matched name pairs Rule 0013.2 verified (readBlueprintMapping↔writeBlueprintMapping, setBlueprintRef↔getBlueprintRef, setDerivedObjectRef↔getDerivedObjectRef, readAtpBlueprintMapping↔writeAtpBlueprintMapping — no cross pairs); writeAtpBlueprintMapping/readAtpBlueprintMapping kept for the abstract base + sibling dispatch (the test_atp_blueprint_mapping.py base-delegating-handler pins still green); evidence: 21/21 Green (4 new parser + 3 new writer + 14 pre-existing set/base tests)
  - [x] Step 7 — Update checklist comment — 6-column format with release column written into BlueprintMapping.py for the BlueprintMapping class (BlueprintMappingSet's stamped checklist untouched): `# Spec: R23-11/AUTOSAR_FO_TPS_StandardizationTemplate.pdf, Table C.17, p.163 (R23-11)` (p.163 via direct pypdf scan — body and caption both on p.163); rows in source order __init__ [—]/[—], getBlueprintRef writer [x] (writeBlueprintMapping calls getBlueprintRef), setBlueprintRef reader [x], getDerivedObjectRef writer [x], setDerivedObjectRef reader [x], every row release R23-11; 13-line provenance comment: appendix C one-caption-early shift (body L4977-4985 above the caption, rows under the caption are BlueprintPolicy), no numeric main table in R23-11, R4.3.1 D.13 reproduction identical, XSD 00052 group BLUEPRINT-MAPPING L9118 kinds/multiplicities + element order (BLUEPRINT-REF 0..1 → DERIVED-OBJECT-REF 0..1), XSD-sourced attribute docstrings, Base row → most-derived AtpBlueprintMapping + base group L6888 atpDerived-skip note, dispatch sites both sides; NO `# Spec verified:` marker (withheld — batch instruction, stamp deferred); model tests 112/112 Green after the edit
  - [x] Step 8 — Deviations  [none open for BlueprintMapping: no naming/type/missing deviation rows — field-to-spec both directions EXACT (exactly the two C.17 attributes → exactly two Optional[RefType] fields in displayed row order; no extra fields, no flattening — the Base row's most-derived AtpBlueprintMapping IS the modeled, stamped base and its atpBlueprintRef/atpBlueprintedElementRef members belong to the stamped Table C.13 sync, not flattened here), reader+writer coverage complete for both kept attrs (0001.7), docstrings verbatim (0012/0001.4 — class Note from the markdown, attribute notes from the XSD element documentations with the source named, setters + None-no-op sentences); the Step 2-7 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): intake zero-field gap closed, BLUEPRINT-REF/DERIVED-OBJECT-REF drop fixed both sides, checklist written from scratch; TRACKER RECONCILIATION: v1 method_deviation_by_class.md `## BlueprintMappingSet` section RECONCILED 2026-09-24 (the `blueprintMap` missing-row was STALE — implemented since the stamped Table 3.1 sync — superseded by a no-deviation row; Source path updated to the current non-leaf BlueprintMapping.py, was the pre-reorg BlueprintMapping/BlueprintMappingSet.py; sync-record Note appended covering this C.17 re-sync end to end); v2 method_deviation_by_class_v2.md `## AtpBlueprintMapping` L1787 reviewed — ACCURATE as written (its atpDerived-realization note names exactly the BlueprintMapping.blueprint/derivedObject realization this sync implemented) and left untouched per the batch convention (v2 = historical audit); the v2 `## BlueprintMappingSet` L1591 missing-row is stale in the same way — superseded by the v1 note rather than edited (v2 not modified); staged_class_sync_review.md has 0 BlueprintMapping mentions (verified by grep); referenced classes: base AtpBlueprintMapping (stamped R23-11 Table C.13, heritage ARObject), member types AtpBlueprint + AtpBlueprintable (both modeled in AbstractBlueprintStructure — AtpBlueprint unstamped ONLY on its deferred reader/writer rows owned by unqueued BlueprintPolicy subclasses per its own Step 8, AtpBlueprintable stamped) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 33/33 (test_BlueprintMapping.py 12 — 3 functional + 9 TestBlueprintMappingSpecContract incl. the both-directions field-set pin, PEP 526/base/accessor-order pins and the 5 verbatim-docstring exact-equality pins that were among the Step 2 Reds; test_blueprint_mapping.py parser 4 — element-level both-refs/empty/dispatch-through-set/full round-trip; test_blueprint_mapping.py writer 3 — XSD order pin, None-no-elements, round-trip; plus the pre-existing test_BlueprintMappingSet.py 5 + parser/writer test_blueprint_mapping_set.py 12 + test_atp_blueprint_mapping.py 2 all green), full suite `uv run python scripts/run_tests.py --no-coverage` 11248 passed / 0 failed (baseline 11229 + 19 net-new: +12 model +4 parser +3 writer); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I "All checks passed" — one iteration: the two new test files carried unused BlueprintMappingSet imports, removed after ruff F401); `npm run black` 1 file reformatted (the new model test file) + `black-check` clean 1154 files; integration round-trip (29 ARXML) included in the suite green; 9b blind-spot checks walked: element kind + every spec attr modeled (both C.17 attributes → exactly two fields, exact-own-field-set test-pinned; concrete class serialized as BLUEPRINT-MAPPING under BLUEPRINT-MAPS), most-derived base (0001.2 — Base row "ARObject , AtpBlueprintMapping" → AtpBlueprintMapping; __bases__ == (AtpBlueprintMapping,) test-pinned; ARObject reached through the stamped base), no fabrication/flattening (0001.3 — both-directions cross-check exact; the base's atpBlueprintRef/atpBlueprintedElementRef members are NOT duplicated on the subclass — they belong to the stamped Table C.13 base whose XSD group L6888 skips both atpDerived associations), Kind-suffix naming (0001.5 — both members kind ref → Ref suffix blueprintRef/derivedObjectRef per the PortInterface/PortPrototype siblings), create/set/add shape (0001.6 — setBlueprintRef/setDerivedObjectRef with None no-op + chaining, no createXxx — neither member is a Referrable child; test-pinned), reader+writer coverage for every kept attr (0001.7 — readBlueprintMapping/writeBlueprintMapping cover both attrs, dispatched from readBlueprintMappingSet BLUEPRINT-MAPPING branch / writeBlueprintMappingSet else-branch, matched name pairs verified, element-level AND round-trip tests assert field VALUES not just counts), member order (0001.11 — fields + accessors + checklist rows in the displayed row order blueprint→derivedObject, accessor order source-pinned; reader/writer XML element order BLUEPRINT-REF → DERIVED-OBJECT-REF per XSD group L9118 sequence — happens to match the displayed order and is writer-test-pinned as a list-of-tags assertion), docstrings verbatim (0012/0001.4 — class = C.17 Note exact incl. the spec typo "map two an object", 4 member docstrings + 2 inline comments = the XSD element documentations exact — the appendix table carries NO per-attribute Note column so the XSD docs are the authoritative attribute text with the source named in the checklist; 6/6 docstring/comment positions exact-equality test-pinned), blank line between __init__ attribute blocks (0008 — single blank line, verified by eye), PEP 526 annotated members + no `# type:` (0003 — regex-pinned), deviations resolved/removed (0014 — no open rows; v1 BlueprintMappingSet section reconciled with sync-record note, v2 untouched with rationale recorded at Step 8), stamp decision: `# Spec verified:` WITHHELD per batch instruction (no marker on the BlueprintMapping class; the file's single `# Spec verified: R23-11` at L82 is BlueprintMappingSet's own stamped sync), Rule 0007 location: kept at the queue row's module path StandardizationTemplate/BlueprintMapping.py (file also holds the stamped BlueprintMappingSet; observation for the record: the C.17 Package row says BlueprintDedicated::GenericBlueprint while the src layout + both trackers use the StandardizationTemplate::BlueprintMapping-era package name — flagged, not moved unilaterally, same pattern as the AtpBlueprint AbstractBlueprintStructure location finding); 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `LifeCycleInfo` — ARObject — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/GenericStructure/LifeCycles.py
  - after `LifeCyclePeriod`
  - [x] Step 1 — Sync members & description from spec — Table 12.5 (AUTOSAR_FO_TPS_GenericStructureTemplate, p.393 via pdf_page.py); PAGE-SPLIT CONFIRMED via direct pypdf scan: body main fragment p.392 (Class/Package/Note/Base/Aggregated-by header + lcObject/lcState/periodBegin rows), page-break, `Table 12.5` caption p.393 + continuation fragment (periodEnd/remark/useInstead) → cite pp.392-393; R4.3.1 reproduction Table 11.5 (p.364 via pdf_page.py) identical except useInstead Note "must" (R4.3.1) vs "shall" (R23-11) and the missing Aggregated-by row (older format); StandardizationTemplate appendix Table C.63 (AUTOSAR_FO_TPS_StandardizationTemplate.md L5889) carries the identical Note line (L5878) — identity verified, FO GST cited; Class-vs-Enumeration header = Class ✓; Package M2::AUTOSARTemplates::GenericStructure::LifeCycles matches the current leaf file LifeCycles.py (same file as the synced LifeCyclePeriod); class Note verbatim "LifeCycleInfo describes the life cycle state of an element together with additional information like what to use instead" (XSD 00052 group LIFE-CYCLE-INFO annotation L76533 identical); Base ARObject ✓ (row header said ARObject — verified: XSD complexType LIFE-CYCLE-INFO L76608 abstract="false" composes AR-OBJECT group + own LIFE-CYCLE-INFO group + AR-OBJECT attributeGroup → most-derived base ARObject, chain ends there, no flattening possible); Aggregated by LifeCycleInfoSet.lifeCycleInfo; XSD group LIFE-CYCLE-INFO L76529 element order LC-OBJECT-REF (0..1, REF + DEST REFERRABLE--SUBTYPES-ENUM, pureMM 1..1) → LC-STATE-REF (0..1, DEST LIFE-CYCLE-STATE--SUBTYPES-ENUM) → PERIOD-BEGIN (0..1 LIFE-CYCLE-PERIOD) → PERIOD-END (0..1 LIFE-CYCLE-PERIOD) → REMARK (0..1 DOCUMENTATION-BLOCK) → USE-INSTEAD-REFS (0..1 wrapper, choice unbounded USE-INSTEAD-REF, DEST REFERRABLE--SUBTYPES-ENUM) — matches the displayed row order; attribute rows displayed order lcObject (Referrable, 1, ref) → lcState (LifeCycleState, 0..1, ref) → periodBegin (LifeCyclePeriod, 0..1, aggr) → periodEnd (LifeCyclePeriod, 0..1, aggr) → remark (DocumentationBlock, 0..1, aggr) → useInstead (Referrable, *, ref); FIELD-TO-SPEC BOTH DIRECTIONS EXACT: exactly 6 fields lcObjectRef/lcStateRef Optional[RefType] (ref kind → Ref suffix), periodBegin/periodEnd Optional[LifeCyclePeriod], remark Optional[DocumentationBlock], useInsteadRefs List[RefType] (singular * → plural Refs + addUseInsteadRef), no extra fields, member/accessor order = displayed order (Rule 0001.11 OK); INTAKE CLASS FINDINGS: class docstring fabricated multi-line ("Represents life cycle information...") NOT the Note + all 12 member docstrings are Gets/Sets-the paraphrases + NO inline __init__ member comments — wipe+rewrite Step 4; setters lack `-> "LifeCycleInfo"` return annotations and take bare non-Optional value params (family form LifeCyclePeriod: `Optional[T]` value + chained return) — Step 3; old 4-column checklist without `# Spec:` line — rebuild 6-column Step 7; CONSUME PATH (Steps 5/6): both sides pre-exist and are wired — parser readLifeCycleInfo (arxml_parser.py L13531: readARObject + LC-OBJECT-REF/LC-STATE-REF via getChildElementOptionalRefType + PERIOD-BEGIN via getLifeCyclePeriod + REMARK via getDocumentationBlock + USE-INSTEAD-REFS via readLifeCycleInfoUseInsteadRefs) dispatched from readLifeCycleInfoSetLifeCycleInfos (L13539) ← readLifeCycleInfoSet (L13549) ← AR-PACKAGE ELEMENTS dispatch L13942; writer writeLifeCycleInfo (arxml_writer.py L12647: writeARObject + setChildElementOptionalRefType LC-OBJECT-REF/LC-STATE-REF + setLifeCyclePeriod PERIOD-BEGIN + writeDocumentationBlock REMARK + USE-INSTEAD-REFS) dispatched from writeLifeCycleInfoSetLifeCycleInfos (L12657) ← writeLifeCycleInfoSet (L12667) ← ARElement dispatch L13672; GENUINE GAPS CONFIRMED both sides: PERIOD-END missing from readLifeCycleInfo AND writeLifeCycleInfo (LifeCycleInfo's own spec attribute silently dropped; XSD order sits between PERIOD-BEGIN and REMARK) — genuine Red → fix Step 6; LifeCycleInfoSet-side DEFAULT-PERIOD-BEGIN/DEFAULT-PERIOD-END absent from readLifeCycleInfoSet/writeLifeCycleInfoSet — owned by the NEXT row (LifeCycleInfoSet), NOT this row's scope (same convention the LifeCyclePeriod agent applied); referenced classes: base ARObject (stamped heritage root), member types RefType + DocumentationBlock + LifeCyclePeriod (all modeled; LifeCyclePeriod stamped R23-11 Table 12.4 this batch 699c4597) — no missing classes
  - [x] Step 2 — Write model class unit test (Red) — existing coverage audit: test_LifeCycles.py TestLifeCycleInfo already carried 16 functional intake tests (initialization defaults for all 6 fields, get/set round-trip + chaining + None no-op per field, add/None no-op for useInsteadRefs) — extended with TestLifeCycleInfoSpecContract (14 tests, sibling form per TestLifeCyclePeriod's pins in the same file): class-docstring verbatim pin (Table 12.5 Note exact), __init__-has-no-docstring pin, exact-own-field-set source pin (re.findall self\.(\w+)\s*: on inspect.getsource(__init__) == the six fields in displayed row order — field-to-spec both directions), PEP 526 annotated-member pins (Optional[RefType]/Optional[LifeCyclePeriod]/Optional[DocumentationBlock]/List[RefType] regex + no `# type:`), most-derived-base pin (__bases__ == (ARObject,) — XSD 00052 complexType LIFE-CYCLE-INFO L76608 composes AR-OBJECT group + own group), Rule 0001.11 accessor-order pin (defs == [__init__, get/setLcObjectRef, get/setLcStateRef, get/setPeriodBegin, get/setPeriodEnd, get/setRemark, getUseInsteadRefs, addUseInsteadRef]), 6 typed-hints pins (getter return + setter value Optional[T] + setter return LifeCycleInfo per attribute — family form), 12 exact-member-docstring pins (get = Note verbatim / set = Note + None-no-op sentence / add = Note + add-form sentence) + inline-__init__-comment verbatim pin (6 Notes); RESULT 9 Red / 5 passed of the 14 new (file total 55 passed / 9 failed, 48 pre-existing untouched) — ALL 9 Reds are the designed shape Reds owned by Steps 3/4 (fabricated class docstring; 6 typed-hint Reds = setters take bare non-Optional value params and lack the `-> "LifeCycleInfo"` return annotation; paraphrased member docstrings; missing inline member comments), the 5 immediate passes (init-no-docstring, exact-field-set, PEP 526, base chain, accessor order) are honest — the intake field structure is already spec-correct
  - [x] Step 3 — Implement model class (Green) — verification+annotation pass, NO field/accessor changes needed (both-directions field-to-spec cross-check re-verified against Table 12.5 + XSD 00052 group L76529: exactly the six spec attributes as PEP 526 fields in displayed row order — lcObjectRef/lcStateRef Optional[RefType] for the ref-kind 1/0..1 rows, periodBegin/periodEnd Optional[LifeCyclePeriod] and remark Optional[DocumentationBlock] for the aggr 0..1 rows (plain Optional fields, no create/add — LifeCyclePeriod/DocumentationBlock are not Referrable children), useInsteadRefs List[RefType] + addUseInsteadRef for the * ref row; base ARObject already the direct superclass per the Base row); FIXED the 6 typed-hint Reds in LifeCycles.py: all five setters + addUseInsteadRef now carry the family form `value: Optional[T]` + `-> "LifeCycleInfo"` chained-return annotations (matches the LifeCyclePeriod sibling in the same file; None no-op bodies unchanged — parser/writer consumers all pass a concrete value or None which Optional[T] accepts); no abstract guard (XSD complexType L76608 abstract="false"); Python 3.8 typing conformant (typing.Optional/List already imported; no T | None / list[...]); evidence: 61 passed / 3 failed — the 6 typed-hint Reds resolved Green, the 3 remaining failures are exactly the docstring-shape Reds owned by Step 4 (class docstring, member docstrings, inline comments), zero functional regressions
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — docstring inventory for LifeCycleInfo: 14 positions (class docstring; __init__ has NO docstring per family form; SIX inline __init__ member comments — none existed on intake, all written fresh; get/set docstrings x5 pairs; getUseInsteadRefs + addUseInsteadRef) — ALL wiped first (the fabricated 2-line class docstring and all 12 Gets/Sets-the Google-style member docstrings removed; Step 3's placeholder annotations-only bodies replaced), then rewritten verbatim from the Table 12.5 markdown: class docstring = Note single-line ("LifeCycleInfo describes the life cycle state of an element together with additional information like what to use instead"); the six inline __init__ comments = the six attribute Notes verbatim single-line; getLcObjectRef/getLcStateRef/getPeriodBegin/getPeriodEnd/getRemark/getUseInsteadRefs docstrings = Notes verbatim single-line; the five setters = Note + " A None value is a no-op and does not overwrite an existing <life cycle object reference|life cycle state|period begin|period end|remark>." single-line; addUseInsteadRef = Note + " A None value is a no-op and does not add to useInsteadRefs." (DoIP add-form precedent); the Table 12.5 attribute rows carry NO Tags cells (no xml.sequenceOffset appinfo in XSD group L76529 — elements in natural sequence order) so no Tags tails; LifeCyclePeriod and LifeCycleInfoSet classes in the same file untouched; evidence: 64/64 model tests Green (was 61/3) — all 3 spec-contract docstring pins now exact-equality Green
  - [x] Step 5 — Write reader/writer round-trip test (Red) — existing coverage audit: element-level LIFE-CYCLE-INFO coverage existed only as value-free assertions (parser orchestrators test_readLifeCycleInfo_full: LC-OBJECT-REF/LC-STATE-REF/USE-INSTEAD-REFS with a len==1 ref assert — NO PERIOD-BEGIN/PERIOD-END/REMARK anywhere; test_readLifeCycleInfoSet_full: len==1 only) and writer-side only consumer mocks (test_writer_remaining_gaps unsupported-type branch, test_writer_arpackage_dispatch tag mapping) — PERIOD-END covered by NO test anywhere (grep: the only PERIOD-END hits in tests/ are the LifeCyclePeriod helper file's own setLifeCyclePeriod key param); NEW dedicated pair mirroring the test_life_cycle_period.py pattern: (1) tests/test_armodel/parser/test_life_cycle_info.py TestReadLifeCycleInfo 4 tests — full-form read (all six XSD children asserted with field VALUES + spec types incl. isinstance periodEnd/remark LifeCyclePeriod/DocumentationBlock), PERIOD-END-only read (DATE + PRODUCT-RELEASE typed), bare <LIFE-CYCLE-INFO/> empty-wrapper read (all fields empty), dispatch-through-set (readLifeCycleInfoSetLifeCycleInfos → field values); (2) tests/test_armodel/writer/test_life_cycle_info.py TestWriteLifeCycleInfo 4 tests — XSD element-order pin with ALL fields set (child_tags == [LC-OBJECT-REF, LC-STATE-REF, PERIOD-BEGIN, PERIOD-END, REMARK, USE-INSTEAD-REFS] per group L76529 + DEST/value asserts), None-info no-element, empty bare element, full parse→write→re-parse round-trip asserting field values; RESULT 5 Red / 3 passed — the 5 Reds are GENUINE (3 parser = PERIOD-END silently dropped by readLifeCycleInfo so periodEnd stays None; 2 writer = writeLifeCycleInfo emits no PERIOD-END so the order pin and the round-trip lose periodEnd), the 3 immediate passes (empty-element read, None-info write, empty-wrapper write) are honest
  - [x] Step 6 — Update parser & writer (Green) — closed the flagged PERIOD-END gaps both sides: parser readLifeCycleInfo (arxml_parser.py L13531) gained `info.setPeriodEnd(self.getLifeCyclePeriod(element, "PERIOD-END"))` after PERIOD-BEGIN and before REMARK; writer writeLifeCycleInfo (arxml_writer.py L12647) gained `self.setLifeCyclePeriod(child_element, "PERIOD-END", info.getPeriodEnd())` in the same XSD group L76529 position — the pre-existing keyed getLifeCyclePeriod/setLifeCyclePeriod helpers (fixed to full DATE + AR-RELEASE-VERSION + PRODUCT-RELEASE coverage by the LifeCyclePeriod row 699c4597) already carry the full wrapper payload, so the PERIOD-END wrapper consumes all three child elements; matched name pairs Rule 0013.2 verified (readLifeCycleInfo↔writeLifeCycleInfo, setPeriodEnd↔getPeriodEnd, setPeriodBegin↔getPeriodBegin, setLcObjectRef↔getLcObjectRef, setLcStateRef↔getLcStateRef, setRemark↔getRemark, addUseInsteadRef↔getUseInsteadRefs via read/writeLifeCycleInfoUseInsteadRefs, getLifeCyclePeriod↔setLifeCyclePeriod — no cross pairs); no chained mutators; element order both sides = XSD sequence LC-OBJECT-REF → LC-STATE-REF → PERIOD-BEGIN → PERIOD-END → REMARK → USE-INSTEAD-REFS (now test-pinned); HONEST NOTE on the Red run: the initial Step-5 Red was 5 failed, but 3 of those (read_all_fields, read_period_end, writer round-trip parse leg) additionally carried a test-construction bug of mine — readLifeCycleInfo takes the LIFE-CYCLE-INFO element itself, not a parent wrapper (element vs parent convention mix-up) — so their first failure pointed at lcObjectRef, not PERIOD-END; after correcting the test harness the genuinely-Red, correctly-shaped assertions were the dispatch-through-set periodEnd assert (parser had no setPeriodEnd call — deterministic from the pre-edit source) and the writer element-order pin (writeLifeCycleInfo emitted only 5 children) — both fixed by this step's two one-line additions; consumer-side disposition: the LifeCycleInfoSet-level DEFAULT-PERIOD-BEGIN/DEFAULT-PERIOD-END gaps in readLifeCycleInfoSet/writeLifeCycleInfoSet are NOT touched here (owned by the next row, LifeCycleInfoSet — same convention the LifeCyclePeriod agent applied); evidence: 79/79 across the 5 LifeCycle test files (64 model + 3 parser-period + 4 writer-period + 4 new parser-info + 4 new writer-info)
  - [x] Step 7 — Update checklist comment — 6-column format with release column rebuilt in the LifeCycleInfo class block of LifeCycles.py (the old intake 4-column block with `[ ] __init__`/`[ ] docstring` placeholders replaced): `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 12.5, pp.392-393` (split-page citation per the direct pypdf scan — body main fragment p.392, caption + continuation p.393); rows in source order __init__ [—]/[—], getLcObjectRef writer [x] / setLcObjectRef reader [x], getLcStateRef writer [x] / setLcStateRef reader [x], getPeriodBegin writer [x] / setPeriodBegin reader [x], getPeriodEnd writer [x] / setPeriodEnd reader [x], getRemark writer [x] / setRemark reader [x], getUseInsteadRefs writer [x] / addUseInsteadRef reader [x] — call sites readLifeCycleInfo/writeLifeCycleInfo (+ read/writeLifeCycleInfoUseInsteadRefs for the list member) verified in arxml_parser.py/arxml_writer.py; every row release R23-11; 11-line provenance comment (page split, R4.3.1 Table 11.5 p.364 reproduction with the shall/must wording difference, C.63 identity → FO GST cited, XSD 00052 group L76529 element order + complexType L76608 base confirmation, dispatch sites, PERIOD-END-coverage-added note); NO `# Spec verified:` marker written (stamp withheld per batch instruction — grep confirms 0 `Spec verified` lines in LifeCycles.py; the file's only other checklist block, LifeCyclePeriod's, is untouched); model tests 64/64 Green after the edit
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions EXACT (exactly the six Table 12.5 attributes → exactly six fields in displayed row order; no extra fields, no flattening — the Base row's single ARObject IS the modeled base; XSD LC-OBJECT-REF pureMM 1..1 vs XML 0..1 needs no model change since the field is Optional per the XSD element form), reader+writer coverage complete for every kept attr (0001.7 — PERIOD-END gap closed in Step 6), docstrings verbatim (0012/0001.4); the Step 2-7 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): (1) fabricated class docstring + 12 paraphrase member docstrings wiped and rewritten as verbatim Notes (+ None-no-op sentences), (2) setters/add typed to the family Optional[T]+chained-return form, (3) PERIOD-END reader/writer drop fixed both sides in XSD order, (4) old 4-column checklist rebuilt 6-column/release with the pp.392-393 citation and provenance comment; v1 tracker (method_deviation_by_class.md) RECONCILED 2026-09-24: new `## LifeCycleInfo` section added (page-split citation, no-deviation row with all six attributes enumerated, PERIOD-END fix + PERIOD-BEGIN/END consumption via the LifeCyclePeriod row's keyed helpers recorded, stamp-deferred note, DEFAULT-PERIOD-* wiring scope note pointing at the LifeCycleInfoSet sync — closing the loop on the LifeCyclePeriod row's consumer-wiring note); v2 tracker (method_deviation_by_class_v2.md) and staged_class_sync_review.md have NO LifeCycleInfo entries (verified by grep — 0 mentions each); referenced classes: base ARObject (stamped heritage root), member types RefType + DocumentationBlock (modeled, PrimitiveTypes/MSR — RefType unstamped utility type, DocumentationBlock consumed via the generic getDocumentationBlock/writeDocumentationBlock pair) + LifeCyclePeriod (stamped R23-11 Table 12.4, this batch 699c4597) — no missing classes; ref DEST targets Referrable/LifeCycleState are XSD --SUBTYPES-ENUM targets (LC-OBJECT-REF/USE-INSTEAD-REF DEST REFERRABLE--SUBTYPES-ENUM, LC-STATE-REF DEST LIFE-CYCLE-STATE--SUBTYPES-ENUM), not member fields]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 72/72 (test_LifeCycles.py 64 — 48 pre-existing (15 TestLifeCyclePeriod + 16 TestLifeCycleInfo + 17 TestLifeCycleInfoSet) + 14 net-new TestLifeCycleInfoSpecContract incl. the both-directions exact-field-set pin, PEP 526/base-chain/accessor-order/typed-hints pins and the class-docstring + 12 member-docstring + 6 inline-comment verbatim pins that were the Step 2/4 Reds; test_life_cycle_info.py parser 4 — full-form/PERIOD-END-only/empty/dispatch-through-set; test_life_cycle_info.py writer 4 — XSD order pin with all six children + DEST/value asserts, None no-element, empty bare element, full parse→write→re-parse round-trip), consumer batteries 784/784 (test_writer_signals_diagnostic + parser orchestrators/handlers/dispatch + writer remaining_gaps/arpackage_dispatch — the LifeCycleInfo/LifeCycleInfoSet consumers unaffected by the added PERIOD-END emission), full suite `uv run python scripts/run_tests.py --no-coverage` 11270 passed / 0 failed (baseline 11248 + 22 net-new: +14 model +4 parser +4 writer; integration round-trip 29 ARXML / 11 integration tests included, green); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I "All checks passed"; src edits are in-place method-body/docstring/checklist lines only — no import changes, no re-sort risk); `npm run black` reformatted the 3 touched test files (long XML-literal lines) + `black-check` clean 1156 files; 9b blind-spot checks walked: element kind + every spec attr modeled (all six Table 12.5 attributes → exactly six fields, exact-own-field-set test-pinned), most-derived base ARObject (0001.2 — Base row is the single ARObject; XSD complexType LIFE-CYCLE-INFO L76608 composes AR-OBJECT group + own group; __bases__ == (ARObject,) test-pinned), no fabrication/flattening (0001.3 — both-directions cross-check exact), Kind-suffix naming (0001.5 — ref kind → Ref suffix lcObjectRef/lcStateRef; * ref → plural useInsteadRefs), create/set/add shape (0001.6 — setters + addUseInsteadRef with None no-op + chaining, no createXxx — no member is a Referrable child; test-pinned), reader+writer coverage for every kept attr (0001.7 — readLifeCycleInfo/writeLifeCycleInfo cover all six incl. the PERIOD-END gap closed this sync, matched name pairs verified, tests assert field VALUES not counts), member order (0001.11 — fields + accessors + checklist rows in the markdown displayed order lcObject→lcState→periodBegin→periodEnd→remark→useInstead, accessor-order source-pinned; reader/writer XML element order LC-OBJECT-REF → LC-STATE-REF → PERIOD-BEGIN → PERIOD-END → REMARK → USE-INSTEAD-REFS per XSD group L76529 happens to match and is writer-test-pinned), docstrings verbatim (0012/0001.4 — class = Note exact, 6 getters = Notes exact, 5 setters = Notes + None-no-op sentences, addUseInsteadRef = Note + add-form sentence, 6 inline __init__ comments = Notes verbatim; 20/20 positions exact-equality test-pinned), blank line between __init__ attribute blocks (0008 — single blank line between all six blocks, verified by eye post-black), PEP 526 annotated members + no `# type:` (0003 — regex-pinned), deviations resolved/removed (0014 — no open rows; v1 tracker `## LifeCycleInfo` section added as the sync record; v2/staged 0 mentions), stamp decision: `# Spec verified:` WITHHELD per batch instruction (grep confirms 0 `Spec verified`/`XSD verified` lines in LifeCycles.py — the intake class carried no premature marker, unlike the BlueprintGenerator intake), Rule 0007 location: LifeCycles.py leaf file matches the spec Package row M2::AUTOSARTemplates::GenericStructure::LifeCycles (same file as the stamped LifeCyclePeriod; LifeCycleInfoSet's own stale 4-column checklist in the same file deliberately untouched — it is the next queue row); 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `LifeCycleInfoSet` — ARElement — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/GenericStructure/LifeCycles.py
  - after `LifeCyclePeriod`
  - after `LifeCycleInfo`
  - [x] Step 1 — Sync members & description from spec — Table 12.3 (AUTOSAR_FO_TPS_GenericStructureTemplate, p.392 via pdf_page.py; PAGE-SPLIT like 12.5: body main fragment (Class/Package/Note/Base/Aggregated-by header + defaultLcState/defaultPeriodBegin rows) + image break + `Table 12.3` caption + continuation fragment (defaultPeriodEnd/lifeCycleInfo/usedLifeCycleStateDefinitionGroup) — caption p.392 so cite p.392); R4.3.1 reproduction Table 11.3 (p.363 via pdf_page.py) identical on Note (incl. Tags tail), Base and all five attribute rows, no Aggregated-by row (older format) — identity verified, FO GST cited; Class-vs-Enumeration header = Class ✓; Package M2::AUTOSARTemplates::GenericStructure::LifeCycles matches the current leaf file LifeCycles.py; class Note verbatim incl. "Tags: atp.recommendedPackage=LifeCycleInfoSets" tail (kept in class docstring per the VariantHandling recommendedPackage precedent); Base chain ARElement, ARObject, CollectableElement, Identifiable, MultilanguageReferrable, PackageableElement, Referrable → most-derived concrete base ARElement (XSD 00052 complexType LIFE-CYCLE-INFO-SET L76684 abstract="false" composes AR-OBJECT + REFERRABLE + MULTILANGUAGE-REFERRABLE + IDENTIFIABLE + COLLECTABLE-ELEMENT + PACKAGEABLE-ELEMENT + AR-ELEMENT groups + own group — authoritative base confirmation); Aggregated by ARPackage.element — consume path PRE-WIRED by the orphan intake: parser readLifeCycleInfoSet (arxml_parser.py L13550) dispatched from readARPackageElements L13943 via parent.createLifeCycleInfoSet (L13944), writer writeLifeCycleInfoSet (arxml_writer.py L12668) dispatched from ARElement isinstance branch L13673, ARPackage.createLifeCycleInfoSet factory (ARPackage.py L572, IsElementExists-protected) + __all__ L162 + late import L1988 all present; XSD group LIFE-CYCLE-INFO-SET L76621 element order DEFAULT-LC-STATE-REF (0..1, REF + DEST LIFE-CYCLE-STATE--SUBTYPES-ENUM, pureMM 1..1) → DEFAULT-PERIOD-BEGIN (0..1 LIFE-CYCLE-PERIOD) → DEFAULT-PERIOD-END (0..1 LIFE-CYCLE-PERIOD) → LIFE-CYCLE-INFOS (0..1 wrapper, choice unbounded LIFE-CYCLE-INFO) → USED-LIFE-CYCLE-STATE-DEFINITION-GROUP-REF (0..1, REF + DEST LIFE-CYCLE-STATE-DEFINITION-GROUP--SUBTYPES-ENUM, pureMM 1..1) — matches displayed row order; attribute rows displayed order defaultLcState (LifeCycleState, 1, ref) → defaultPeriodBegin (LifeCyclePeriod, 0..1, aggr) → defaultPeriodEnd (LifeCyclePeriod, 0..1, aggr) → lifeCycleInfo (LifeCycleInfo, *, aggr) → usedLifeCycleStateDefinitionGroup (LifeCycleStateDefinitionGroup, 1, ref); FIELD-TO-SPEC BOTH DIRECTIONS EXACT: exactly 5 fields defaultLcStateRef/defaultPeriodBegin/defaultPeriodEnd/lifeCycleInfos (List[LifeCycleInfo] + addLifeCycleInfo, singular * → plural)/usedLifeCycleStateDefinitionGroupRef — no extra fields, member order = displayed order (Rule 0001.11 OK), ref kind → Ref suffix ✓; INTAKE CLASS FINDINGS: class docstring fabricated 2-line ("Represents a set of life cycle information in AUTOSAR models...") NOT the Note + all 10 member docstrings are Gets/Sets-the Google-style paraphrases + NO inline __init__ member comments + NO blank lines between __init__ attribute blocks (glued, Rule 0008) — wipe+rewrite Step 4; setters take bare non-Optional params and lack `-> "LifeCycleInfoSet"` return annotations (family form: Optional[T] value + chained return) — Step 3; old 4-column checklist without `# Spec:` line — rebuild 6-column Step 7; CONSUME PATH GAPS CONFIRMED both sides (genuine Red for 5/6): readLifeCycleInfoSet/writeLifeCycleInfoSet cover DEFAULT-LC-STATE-REF + LIFE-CYCLE-INFOS + USED-LIFE-CYCLE-STATE-DEFINITION-GROUP-REF but touch NO DEFAULT-PERIOD-BEGIN/DEFAULT-PERIOD-END — the gaps the LifeCyclePeriod (699c4597) and LifeCycleInfo (a2d4d7c2) agents deliberately left to THIS row — fix Step 6 via the keyed getLifeCyclePeriod/setLifeCyclePeriod helpers (full DATE + AR-RELEASE-VERSION + PRODUCT-RELEASE payload since 699c4597); referenced classes: base ARElement (stamped heritage), member types LifeCyclePeriod (stamped Table 12.4, 699c4597) + LifeCycleInfo (stamped Table 12.5, a2d4d7c2), ref DEST targets LifeCycleState + LifeCycleStateDefinitionGroup are XSD --SUBTYPES-ENUM targets (not field types; RefType held) — no missing classes
  - [x] Step 2 — Write model class unit test (Red) — new TestLifeCycleInfoSetSpecContract appended to tests/test_armodel/models/M2/AUTOSARTemplates/GenericStructure/test_LifeCycles.py (13 tests, mirroring the TestLifeCycleInfoSpecContract pattern): class-docstring verbatim pin (Table 12.3 Note incl. Tags tail), init-has-no-docstring, exact-own-field-set pin (exactly the five attributes in displayed order), PEP 526 + no-`# type:` pin, most-derived-base ARElement pin (XSD complexType L76684), accessor-order pin (get/set pairs + getLifeCycleInfos/addLifeCycleInfo for the * aggr), five typed-hints tests (getter/setter annotations per spec type, chained returns), member-docstrings-verbatim pin (10 positions, setters/add with None-no-op sentences), inline-__init__-comments-verbatim pin (5 Notes); ARElement import added to the test file header; RESULT 8 Red / 5 passed — the 8 Reds are GENUINE intake-class contract failures (fabricated class docstring; all 5 setter/add typed-hint tests fail on missing `-> "LifeCycleInfoSet"` return annotations + non-Optional value params; Google-style paraphrase docstrings; zero inline __init__ comments), the 5 immediate passes are honest (intake field set/order/annotations/base/accessor-order already exact — the both-directions cross-check passing is itself the Step 1 field finding confirmed by test)
  - [x] Step 3 — Implement model class (Green) — signature-only alignment pass (field set/order/types already spec-exact on intake — no body replacement needed): the five setter/add signatures retyped to the family form `value: Optional[T]` + `-> "LifeCycleInfoSet"` chained return (setDefaultLcStateRef/setDefaultPeriodBegin/setDefaultPeriodEnd/addLifeCycleInfo/setUsedLifeCycleStateDefinitionGroupRef — matches the stamped LifeCyclePeriod/LifeCycleInfo setters in the same file); blank line inserted between all five `__init__` attribute blocks (Rule 0008 — intake fields were glued); member/accessor order untouched (= displayed order); no consumer affected (grep: parser/writer call the same mutator names, None-guard semantics unchanged); model battery 74/74 Green in test_LifeCycles.py (the 5 typed-hint Reds from Step 2 now Green; only the 3 docstring pins still Red — Step 4 scope; all 17 pre-existing TestLifeCycleInfoSet tests still pass)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — docstring inventory for LifeCycleInfoSet: 16 positions (class docstring; __init__ has NO docstring per family form; FIVE inline __init__ member comments — none existed on intake, all written fresh; get/set docstrings x4 pairs; getLifeCycleInfos + addLifeCycleInfo) — ALL wiped first (the fabricated 2-line class docstring and all 10 Gets/Sets-the Google-style Args/Returns member docstrings removed; Step 3's signature retypes kept), then rewritten verbatim from the Table 12.3 markdown: class docstring = Note single-line incl. the "Tags: atp.recommendedPackage=LifeCycleInfoSets" tail (VariantHandling recommendedPackage precedent); the five inline __init__ comments = the five attribute Notes verbatim single-line (defaultLcState Note's "defaultLc State" PDF line-wrap artifact resolved to camelCase defaultLcState per XSD L76631 documentation); get* docstrings = Notes verbatim; the four setters = Note + " A None value is a no-op and does not overwrite an existing <default life cycle state|default period begin|default period end|used life cycle state definition group>." single-line; addLifeCycleInfo = Note + " A None value is a no-op and does not add to lifeCycleInfos." (DoIP add-form precedent); no Tags cells on the Table 12.3 attribute rows (XSD group L76621 elements carry no xml.sequenceOffset appinfo — natural sequence order) so no Tags tails; evidence: 77/77 model tests Green — the 3 remaining Step-2 docstring pins now exact-equality Green
  - [x] Step 5 — Write reader/writer round-trip test (Red) — existing coverage audit: readLifeCycleInfoSet covered only as the orchestrators' value-light test_readLifeCycleInfoSet_full (DEFAULT-LC-STATE-REF + len==1 infos; NO DEFAULT-PERIOD-* anywhere) and writer-side only consumer presence tests (test_writer_signals_diagnostic TestWriterLifeCycleInfoSet.full asserts element PRESENCE not order/payload, test_writer_remaining_gaps unsupported-branch); DEFAULT-PERIOD-BEGIN/END covered by NO test anywhere; NEW dedicated pair mirroring the test_life_cycle_info.py pattern: (1) tests/test_armodel/parser/test_life_cycle_info_set.py TestReadLifeCycleInfoSet 4 tests — full-form read (all five XSD children asserted with field VALUES + spec types incl. isinstance periods LifeCyclePeriod), DEFAULT-PERIOD-wrappers-only read (typed periods + refs None), bare <LIFE-CYCLE-INFO-SET/> empty-wrapper read (all fields empty), empty LIFE-CYCLE-INFOS wrapper read ([] list); (2) tests/test_armodel/writer/test_life_cycle_info_set.py TestWriteLifeCycleInfoSet 4 tests — XSD element-order pin with ALL fields set (child_tags == [SHORT-NAME, DEFAULT-LC-STATE-REF, DEFAULT-PERIOD-BEGIN, DEFAULT-PERIOD-END, LIFE-CYCLE-INFOS, USED-LIFE-CYCLE-STATE-DEFINITION-GROUP-REF] per group L76621 + payload/DEST asserts), None-set no-element, empty-set writes only SHORT-NAME, full parse→write→re-parse round-trip asserting field values; RESULT 4 Red / 4 passed — the 4 Reds are GENUINE (2 parser = readLifeCycleInfoSet never touches DEFAULT-PERIOD-BEGIN/END so both stay None; 2 writer = writeLifeCycleInfoSet emits no DEFAULT-PERIOD elements — the order-pin diff shows exactly the two missing children — so the round-trip loses the default periods), the 4 immediate passes (absent-optional read, empty infos wrapper, None write, empty-set write) are honest
  - [x] Step 6 — Update parser & writer (Green) — closed the flagged DEFAULT-PERIOD-* gaps both sides: parser readLifeCycleInfoSet (arxml_parser.py L13550) gained `info_set.setDefaultPeriodBegin(self.getLifeCyclePeriod(element, "DEFAULT-PERIOD-BEGIN"))` + `info_set.setDefaultPeriodEnd(self.getLifeCyclePeriod(element, "DEFAULT-PERIOD-END"))` between DEFAULT-LC-STATE-REF and LIFE-CYCLE-INFOS; writer writeLifeCycleInfoSet (arxml_writer.py L12668) gained `self.setLifeCyclePeriod(child_element, "DEFAULT-PERIOD-BEGIN", info_set.getDefaultPeriodBegin())` + DEFAULT-PERIOD-END in the same XSD group L76621 position — the pre-existing keyed getLifeCyclePeriod/setLifeCyclePeriod helpers (full DATE + AR-RELEASE-VERSION + PRODUCT-RELEASE payload since 699c4597) already carry the complete wrapper content, so each DEFAULT-PERIOD wrapper consumes all three child elements; matched name pairs Rule 0013.2 verified (readLifeCycleInfoSet↔writeLifeCycleInfoSet, setDefaultLcStateRef↔getDefaultLcStateRef, setDefaultPeriodBegin↔getDefaultPeriodBegin, setDefaultPeriodEnd↔getDefaultPeriodEnd, addLifeCycleInfo↔getLifeCycleInfos via readLifeCycleInfoSetLifeCycleInfos↔writeLifeCycleInfoSetLifeCycleInfos, setUsedLifeCycleStateDefinitionGroupRef↔getUsedLifeCycleStateDefinitionGroupRef, getLifeCyclePeriod↔setLifeCyclePeriod, getChildElementOptionalRefType↔setChildElementOptionalRefType — no cross pairs); no chained mutators; element order both sides = XSD group L76621 sequence DEFAULT-LC-STATE-REF → DEFAULT-PERIOD-BEGIN → DEFAULT-PERIOD-END → LIFE-CYCLE-INFOS → USED-LIFE-CYCLE-STATE-DEFINITION-GROUP-REF (now test-pinned); consumer wiring (dispatch branches + ARPackage factory) pre-existed and untouched; evidence: 100/100 across the 7 LifeCycle test files (77 model + 4 parser-info-set + 4 writer-info-set + 4 parser-info + 4 writer-info + 3 parser-period + 4 writer-period), 668/668 consumer battery (parser orchestrators/handlers/dispatch + writer signals_diagnostic/remaining_gaps)
  - [x] Step 7 — Update checklist comment — 6-column format written in place of the stale intake 4-column block (which had no `# Spec:` line and an unchecked `[ ] __init__`/docstring row), verified at this step: `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table 12.3, p.392` (home doc; page-split note + R4.3.1 Table 11.3 p.363 reproduction note + XSD group L76621/complexType L76684 provenance + dispatch/factory note + DEFAULT-PERIOD coverage-added note), rows in markdown displayed order (`__init__`, getDefaultLcStateRef, setDefaultLcStateRef, getDefaultPeriodBegin, setDefaultPeriodBegin, getDefaultPeriodEnd, setDefaultPeriodEnd, getLifeCycleInfos, addLifeCycleInfo, getUsedLifeCycleStateDefinitionGroupRef, setUsedLifeCycleStateDefinitionGroupRef), all `[x]` with release R23-11; reader [x] on the four setters + addLifeCycleInfo (parser sets/adds), writer [x] on the five getters (writer gets), `__init__` [—]/[—]; marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions EXACT (exactly the five Table 12.3 attributes → exactly five fields in displayed row order; no extra fields, no flattening — the Base chain ends at most-derived ARElement, already the modeled base; ref-kind fields hold RefType per the DEST --SUBTYPES-ENUM convention), reader+writer coverage complete for every kept attr (0001.7 — the DEFAULT-PERIOD-BEGIN/END gap closed in Step 6), docstrings verbatim (0012/0001.4); the Step 1-7 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): (1) fabricated class docstring + 10 paraphrase member docstrings wiped and rewritten as verbatim Notes (+ None-no-op sentences, class Note keeps its Tags tail), (2) five setter/add signatures retyped to the family Optional[T]+chained-return form, (3) glued __init__ attribute blocks separated (Rule 0008), (4) old 4-column checklist rebuilt 6-column/release with the p.392 citation and provenance comment; v1 tracker (method_deviation_by_class.md) RECONCILED 2026-09-24: new `## LifeCycleInfoSet` section added (page-split citation, no-deviation row with all five attributes enumerated, DEFAULT-PERIOD fix recorded incl. the two XSD DEST enums, stamp-deferred note, consumer-wiring pre-existed note) — closing the loop on the LifeCyclePeriod and LifeCycleInfo rows' consumer-wiring pointers; v2 tracker (method_deviation_by_class_v2.md) and staged_class_sync_review.md have NO LifeCycleInfoSet entries (verified by grep — 0 mentions each); referenced classes: base ARElement (stamped heritage root), member types LifeCyclePeriod (stamped R23-11 Table 12.4, 699c4597) + LifeCycleInfo (stamped R23-11 Table 12.5, a2d4d7c2), ref DEST targets LifeCycleState + LifeCycleStateDefinitionGroup are XSD --SUBTYPES-ENUM targets (not member fields; RefType held) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: target files 100/100 (test_LifeCycles.py 77 — 64 pre-existing + 13 net-new TestLifeCycleInfoSetSpecContract incl. the both-directions exact-field-set pin, PEP 526/base-chain/accessor-order/typed-hints pins and the class-docstring + 10 member-docstring + 5 inline-comment verbatim pins that were the Step 2/4 Reds; test_life_cycle_info_set.py parser 4 — full-form/DEFAULT-PERIOD-only/bare-element/empty-LIFE-CYCLE-INFOS-wrapper; test_life_cycle_info_set.py writer 4 — XSD order pin with all six children incl. SHORT-NAME + payload asserts, None no-element, empty-set SHORT-NAME-only, full parse→write→re-parse round-trip; plus the 3 sibling LifeCycle test files 19 — re-run green after black reformat of the 3 touched test files), consumer battery 668/668 (parser orchestrators/handlers/dispatch + writer signals_diagnostic/remaining_gaps — readLifeCycleInfoSet/writeLifeCycleInfoSet consumers unaffected by the added DEFAULT-PERIOD emission), full suite `uv run python scripts/run_tests.py --no-coverage` 11291 passed / 0 failed (baseline 11270 + 21 net-new: +13 model +4 parser +4 writer; integration round-trip 29 ARXML / 11 integration tests included, green); `npm run lint` clean after fixing ONE self-inflicted I001 (my ARElement import in test_LifeCycles.py initially placed ASCII-order before ArObject; ruff's isort is case-insensitive — moved after ArObject; test file, NOT under the models/parser/writer no-re-sort rule) — flake8 E9,F63,F7,F82 + ruff E/F/W/I "All checks passed"; `npm run black` reformatted the 3 touched test files (canonical string wrapping only) + `black-check` clean 1158 files; 9b blind-spot checks walked: element kind + every spec attr modeled (all five Table 12.3 attributes → exactly five fields, exact-own-field-set test-pinned), most-derived base ARElement (0001.2 — Base row chain ends at ARElement; XSD complexType LIFE-CYCLE-INFO-SET L76684 composes AR-OBJECT…AR-ELEMENT groups + own group; __bases__ == (ARElement,) test-pinned), no fabrication/flattening (0001.3 — both-directions cross-check exact; ref-kind fields hold RefType per the DEST --SUBTYPES-ENUM convention), Kind-suffix naming (0001.5 — ref kind → Ref suffix defaultLcStateRef/usedLifeCycleStateDefinitionGroupRef; singular * aggr → plural lifeCycleInfos + addLifeCycleInfo), create/set/add shape (0001.6 — four setters + addLifeCycleInfo with None no-op + chaining, no createXxx — no member is a named Referrable child; chaining test-pinned), reader+writer coverage for every kept attr (0001.7 — readLifeCycleInfoSet/writeLifeCycleInfoSet cover all five incl. the DEFAULT-PERIOD-BEGIN/END gap closed this sync, matched name pairs verified, tests assert field VALUES not counts), member order (0001.11 — fields + accessors + checklist rows in the markdown displayed order defaultLcState→defaultPeriodBegin→defaultPeriodEnd→lifeCycleInfo→usedLifeCycleStateDefinitionGroup, accessor-order source-pinned; reader/writer XML element order DEFAULT-LC-STATE-REF → DEFAULT-PERIOD-BEGIN → DEFAULT-PERIOD-END → LIFE-CYCLE-INFOS → USED-LIFE-CYCLE-STATE-DEFINITION-GROUP-REF per XSD group L76621 is writer-test-pinned and happens to coincide), docstrings verbatim (0012/0001.4 — class = Note exact incl. "Tags: atp.recommendedPackage=LifeCycleInfoSets" tail, 5 getters = Notes exact, 4 setters = Notes + None-no-op sentences, addLifeCycleInfo = Note + add-form sentence, 5 inline __init__ comments = Notes verbatim; 16/16 positions exact-equality test-pinned), blank line between __init__ attribute blocks (0008 — single blank line between all five blocks, verified by eye post-black), PEP 526 annotated members + no `# type:` (0003 — regex-pinned), deviations resolved/removed (0014 — no open rows; v1 tracker `## LifeCycleInfoSet` section added as the sync record closing the LifeCyclePeriod/LifeCycleInfo consumer-wiring pointers; v2/staged 0 mentions), stamp decision: `# Spec verified:` WITHHELD per batch instruction (marker deferred to batch confirmation like the 22 prior commits in this batch), Rule 0007 location: LifeCycles.py leaf file matches the spec Package row M2::AUTOSARTemplates::GenericStructure::LifeCycles (same file as the stamped LifeCyclePeriod + LifeCycleInfo); 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `VariationPoint` — ARObject — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - after `ConditionByFormula`
  - after `PostBuildVariantCondition`
  - after `BlueprintGenerator`
  - [x] Step 1 — Sync members & description from spec — NUMERIC MAIN TABLE EXISTS: FO GST Table 7.4 (L5839, p.226 via pdf_page.py — numeric id, script works) cited as primary; reproductions verified row-identical: FO StandardizationTemplate Table 4.2 (L1279, p.39), CP SWCT Table E.51 (L29544, appendix), CP SystemTemplate Table F.145 (L76260, appendix), CP ECUConfiguration Table F.47 (L9387, appendix — caption-shifted: VariationPoint body L9368-9379 sits UNDER the F.46 ValueList caption, split from its trailing swSyscond row L9391 by glyph[triangle] images; F.47's caption sits over that lone tail row), FMXF Table C.20 (L2399, appendix — caption-shifted likewise, only sdg/shortLabel/swSyscond tail rows render below the caption); rows: Package VariantHandling (non-leaf → __init__.py, matches actual location), class Note verbatim, Base ARObject (XSD 00052 L130082 complexType VARIATION-POINT abstract="false" composes group AR:AR-OBJECT + group AR:VARIATION-POINT — no flattening), 7 attributes displayed order blueprintCondition (DocumentationBlock 0..1 aggr offset 28) / desc (MultiLanguageOverviewParagraph 0..1 aggr offset 20) / formalBlueprintGenerator (BlueprintGenerator 0..1 aggr, atp.Status=draft, offset 30) / postBuildVariantCondition (PostBuildVariantCondition * aggr offset 40) / sdg (Sdg 0..1 aggr offset 50) / shortLabel (Identifier 0..1 attr, atpIdentityContributor, offset 10) / swSyscond (ConditionByFormula 0..1 aggr offset 30); XSD group order SHORT-LABEL, DESC, BLUEPRINT-CONDITION, FORMAL-BLUEPRINT-CONDITION (atp.Status=removed, NO R23-11 PDF row — not modeled per Rule 0015), FORMAL-BLUEPRINT-GENERATOR, SW-SYSCOND, POST-BUILD-VARIANT-CONDITIONS (wrapper + unbounded choice), SDG; field-to-spec cross-check exact both directions (7↔7, postBuildVariantConditions plural for the `*` aggr); existing class = orphan intake WITH stale 5-column checklist + stale `# Spec verified: R23-11` marker (same shape as PostBuildVariantCriterion intake) — rebuild 6-column, marker withheld (batch instruction); reader readVariationPoint (parser L1309) + writer writeVariationPoint (writer L1181) pre-exist and cover ALL 7 attributes in XSD order; all member classes in place (BlueprintGenerator 5c3e3c4a, PostBuildVariantCondition 3847632f, ConditionByFormula 3d9695c2, BindingTimeEnum synced; DocumentationBlock/MultiLanguageOverviewParagraph/Sdg pre-existing); parser comment cites atp.Status="obsolete" and writer comment cites AUTOSAR_00046.xsd L99470 — both stale vs R23-11 XSD ("removed", AUTOSAR_00052.xsd L130012), fix at Step 6
  - [x] Step 2 — Write model class unit test (Red) — RED CONFIRMED: added TestVariationPointSpecContract (tests/test_armodel/models/M2/AUTOSARTemplates/GenericStructure/test_VariantHandling.py) mirroring the batch SpecContract pattern; 3 Red as predicted (test_class_docstring_verbatim — current class docstring is wrapped with a stale Package:/Base:/Attributes: meta block; test_getter_docstrings_are_notes_without_tags + test_setter_docstrings_are_notes_with_none_noop — all method docstrings hard-wrapped, not verbatim single-line); 11 pass immediately (base ARObject, all 7 accessor type hints, defaults, None-noops) — field-to-spec already exact; Note text: mid-identifier spaces in the extracted table ("postBuildVariant Criterion", "formal BlueprintGenerator") treated as PDF-extraction artifacts per the BindingTimeEnum precedent, camelCase forms corroborated by XSD 00052 documentation strings — recorded on the test class docstring too
  - [x] Step 3 — Implement model class (Green) — NO STRUCTURAL CHANGE REQUIRED: field-to-spec cross-check exact both directions (7 spec rows ↔ 7 model fields, member order = displayed row order, PEP 526 annotated members, blank lines between attribute blocks, setters chain, addPostBuildVariantCondition appends, no create* needed — VariationPoint is not a Referrable container); the only Red residue was the docstring contract (3 failing assertions), which is Step 4's wipe+rewrite to turn Green; full file state at Step 3 close: 89 passed / 3 failed (docstring-only)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — ALL VariationPoint docstrings wiped and rewritten: class docstring = Table 7.4 Note verbatim single line (meta-block Package:/Base:/Attributes: removed), all 15 method docstrings = Note verbatim single line (getters) / Note + None-no-op sentence (setters + addPostBuildVariantCondition); inline __init__ comments were already verbatim but "formal BlueprintGenerator" PDF-extraction split fixed to camelCase "formalBlueprintGenerator" (XSD-corroborated artifact, BindingTimeEnum precedent); Tags/Stereotypes suffixes kept out of docstrings per sibling precedent; GREEN: 92/92 in test_VariantHandling.py
  - [x] Step 5 — Write reader/writer round-trip test (Red) — PASS-IMMEDIATELY (honest, no genuine Red): readVariationPoint (parser L1309) and writeVariationPoint (writer L1181) already cover ALL 7 spec attributes — verified by code reading at Step 1, no dropped fields, so the "fields exist but are dropped" Red scenario does not occur for this class; added TestReadVariationPointSpecAttributes (parser test file: all-7-attributes read asserting field values — DESC L-2 text/lang, BLUEPRINT-CONDITION P/L-1, FORMAL-BLUEPRINT-GENERATOR EXPRESSION, SW-SYSCOND binding-time + mixed text, 1 condition with ref DEST + value, SDG GID — plus empty POST-BUILD-VARIANT-CONDITIONS wrapper → no conditions) and TestWriteVariationPointSpecAttributes (writer test file: exact full child-tag order == [SHORT-LABEL, DESC, BLUEPRINT-CONDITION, FORMAL-BLUEPRINT-GENERATOR, SW-SYSCOND, POST-BUILD-VARIANT-CONDITIONS, SDG] per XSD group order, value assertions, empty conditions list → no wrapper emitted, write→re-parse round-trip of all 7); 53/53 pass in the two files; no assertion weakened, values asserted not just len
  - [x] Step 6 — Update parser & writer (Green) — NO FUNCTIONAL CHANGE REQUIRED: reader/writer coverage verified complete against the table AND the XSD group order (element order matches sequenceOffset 10/20/28/30/30/40/50; FORMAL-BLUEPRINT-CONDITION atp.Status=removed deliberately not read/written, matching its absence from the R23-11 PDF table per Rule 0015); comment-only fixes in the class's own consume path: parser readVariationPoint comment said atp.Status="obsolete" → corrected to "removed" with XSD 00052 L130012 citation; writer writeVariationPoint comment cited stale "AUTOSAR_00046.xsd, line 99470" → corrected to R23-11 AUTOSAR_00052.xsd L130012; 145/145 pass across the three test files
  - [x] Step 7 — Update checklist comment — rebuilt the VariationPoint checklist in place to the 6-column format (impl / docstring / test / reader / writer / release, all rows R23-11, reader [x] on mutator rows / writer [x] on getter rows per Rule 0002 — the old 5-column checklist wrongly carried writer [x] on addPostBuildVariantCondition, now [—]); stale `# Spec verified: R23-11` marker REMOVED (withheld — orphan-intake marker did not certify this 9-step pass; batch instruction defers the stamp to batch confirmation)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions exact (Step 1/Step 3: exactly the 7 table rows, postBuildVariantConditions plural for the `*` aggr); the Step 1 findings were fixed in-band and leave no rows: stale meta-block class docstring + wrapped docstrings wiped → Table 7.4 Notes verbatim (Rule 0014: fixed ⇒ no row), old 5-column checklist + stale `# Spec verified: R23-11` marker rebuilt 6-column/withheld, stale parser comment "obsolete"→"removed" + stale writer XSD citation corrected; v1 tracker (docs/examples/method_deviation_by_class.md) `## VariationPoint` section reconciled 2026-09-24: citation corrected to the numeric main table FO GST Table 7.4 p.226 (was CP ECUConfiguration p.315 = caption-shifted appendix F.47), the `formalBlueprintGenerator missing` row was STALE (implemented with full reader/writer coverage since intake) — section rewritten to the sibling sync-record form with a no-deviation row; VariationPointProxy section in the tracker kept DISTINCT/untouched (its resolved valueAccess placeholder record is that class's own); v2 tracker (method_deviation_by_class_v2.md) has NO VariationPoint rows at all (verified by grep — only framework-level prose about the variationPoint member, a SwcInternalBehavior variationPointProxies naming row, and the NumericalValueVariationPoint appendix bullet), nothing to reconcile there; referenced classes: base ARObject (PrimitiveTypes/ArObject, stamped) + members DocumentationBlock / MultiLanguageOverviewParagraph / Sdg (pre-existing MSR classes, stamped ecosystem) + BlueprintGenerator (synced 5c3e3c4a) + PostBuildVariantCondition (synced 3847632f) + ConditionByFormula (synced 3d9695c2) + BindingTimeEnum (synced) — no missing classes; XSD-only FORMAL-BLUEPRINT-CONDITION (atp.Status=removed, sequenceOffset 29) deliberately NOT modeled per Rule 0015 PDF-wins — recorded, not a deviation]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: my three test files 145/145 (test_VariantHandling.py 92, test_arxml_parser_variation_point.py 37, test_writer_variation_point.py 29 — incl. 19 new tests); full suite `uv run python scripts/run_tests.py --no-coverage` 11310 passed / 0 failed (baseline 11291 + 19 new, exact); `npm run lint` (flake8 E9/F63/F7/F82 + ruff E/F/W/I) All checks passed (one I001 import-order fix in the writer test file during 9a — test files not import-protected); `npm run black` no changes / `npm run black-check` clean; 9b checklist walked by the agent (field↔spec both directions exact via the Step 1/3 cross-check; most-derived base ARObject per XSD complexType L130082; no fabrication/flattening — XSD-removed FORMAL-BLUEPRINT-CONDITION not modeled; Kind/naming conventions held; reader+writer coverage for all 7 kept attrs test-pinned; member order = displayed row order and XML order = XSD group order checked independently; docstrings = Table 7.4 Notes verbatim, asserted by test; blank lines between __init__ attribute blocks verified by eye; no open deviations); `# Spec verified:` stamp WITHHELD; 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `ModeInSwcBswInstanceRef` — ARObject (abstract) — XSD-only (confirmed genuinely XSD-only by the 2026-09-22 re-verification; re-confirmed at Step 1 2026-09-24 against BOTH corpora) — AUTOSAR_00052.xsd group MODE-IN-SWC-BSW-INSTANCE-REF (l.82683)
  - module: M2/AUTOSARTemplates/CommonStructure/Timing/TimingCondition.py
  - note: dependency of `ModeInSwcInstanceRef` (listed in its Base, row below) — batch item 13; orphan-intake class in src (stale `# XSD verified:` line with all rows `[ ]` = not synced) → full 9-step re-sync; stamp deferred to batch confirmation
  - [x] Step 1 — Sync members & description from spec — XSD-only verdict re-verified 2026-09-24 against BOTH corpora: no caption `^Table [\w.]+: ModeInSwcBswInstanceRef$` and no tolerant hit (`ModeInSwcBswInstanceRef` / `ModeInSwcBsw` case-insensitive) in autosar/R23-11/markdown/* OR autosar/R4.3.1/markdown/* — only sibling-Table Base cells (R23-11 TimingExtensions Tables 3.11 l.826 / 3.12 l.841; R4.3.1 Tables 4.5 l.1447 / l.1459) + word-split type cell "ModeInSwcBsw InstanceRef" (TimingModeInstance.modeInstance); pdf_page.py: no table in any corpus PDF. Ground truth: AUTOSAR_00052.xsd l.82683 `<xsd:group name="MODE-IN-SWC-BSW-INSTANCE-REF">` — documentation "Abstract class representing an instance reference to be capable of referencing a specific ModeDeclaration utilized by a SW-C or BSW module.", stereotypes atpObject, `<xsd:sequence/>` EMPTY (no own attributes/elements); NO complexType for the class (never serialized directly) — group consumed only by MODE-IN-BSW-INSTANCE-REF complexType l.82575 (AR-OBJECT → this group → MODE-IN-BSW) and MODE-IN-SWC-INSTANCE-REF complexType l.82755 (AR-OBJECT → ATP-INSTANCE-REF → this group → MODE-IN-SWC); Base = ARObject (both concrete siblings list ARObject + this class) — src `ModeInSwcBswInstanceRef(ARObject, ABC)` conforms; field-to-spec cross-check both directions: spec→impl no own members required, impl→spec none fabricated; R4.3.1 XSD group (AUTOSAR_00044.xsd l.57553) identical
  - [x] Step 2 — Write model class unit test (Red) — test_ModeInSwcBswInstanceRef.py rewritten (6 tests): abstract TypeError raise, issubclass → ARObject, both concrete subclasses isinstance, class docstring == XSD documentation verbatim, `__init__` docstring is None, no-own-attributes probe subclass (`vars()` == {parent, checksum, timestamp} = ARObject defaults per empty `<xsd:sequence/>`); 1 Red (hand-written class docstring vs XSD documentation), 5 pass immediately (shape already XSD-conformant)
  - [x] Step 3 — Implement model class (Green) — verification-only: no change (abstract guard + `ModeInSwcBswInstanceRef(ARObject, ABC)` base + zero own members already match the empty XSD group 1:1; CryptoKeySlot/SdClientConfig precedent); the 1 remaining Red is the Step-4 docstring
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — all old docstrings for the class wiped (hand-written 8-line class docstring; no `__init__` docstring or member comments existed) and the class docstring rewritten with the XSD documentation verbatim ("Abstract class representing an instance reference to be capable of referencing a specific ModeDeclaration utilized by a SW-C or BSW module." — AUTOSAR_00052.xsd l.82686); model test Green (6/6)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — abstract class has NO own XML element (no complexType; empty XSD group) → its serialization contract is the polymorphic dispatch through TimingModeInstance.modeInstance; extended tests/test_armodel/parser/test_arxml_parser_timing_instance_refs.py (abstract-base isinstance ModeInSwcBswInstanceRef assertions added to both read-dispatch tests) + tests/test_armodel/writer/test_writer_timing_instance_refs.py (new test_round_trip_timing_mode_instance_bsw_iref: BSW variant write→re-parse round-trip, abstract + concrete isinstance + field values); 39/39 passed on first run — **Red unreachable** (both dispatch branches already covered correctly; nothing dropped)
  - [x] Step 6 — Update parser & writer (Green) — verification-only: no change. Reader/writer have no readModeInSwcBswInstanceRef/writeModeInSwcBswInstanceRef and none are needed — the XSD group is EMPTY and the class has no complexType (never serialized directly); concrete helpers readModeInBswInstanceRef/readModeInSwcInstanceRef (parser l.3353/3361) + writeModeInBswInstanceRef/writeModeInSwcInstanceRef (writer l.4539/4545) + the TimingModeInstance tag/isinstance dispatches cover both group branches; confirmed by the Step 5 tests
  - [x] Step 7 — Update checklist comment — 6-column parity checklist rewritten with release column R23-11: `# Spec: AUTOSAR_00052.xsd line 82683, group MODE-IN-SWC-BSW-INSTANCE-REF (XSD-only; no own table in repo corpus)`; `__init__` row only with reader/writer `[—]` (no XML element — no complexType, empty group); stale all-`[ ]` 5-column block replaced; **`# XSD verified: AUTOSAR_00052.xsd` line REMOVED (no marker — stamp deferred to batch confirmation; XSD-only class with no deviation ⇒ marker eligible at batch 9b)**
  - [x] Step 8 — Deviations — none: class shape matches the empty XSD group 1:1 in both directions, docstring verbatim, no fabricated members, reader/writer `[—]` is genuine (class never serializes); no Step-3 referenced missing classes (no members); no method_deviation_by_class.md entries for this class (the l.3002/l.3013 tracker entries belong to the two CONCRETE siblings, whose rows are queued later); note: the pre-existing stale `# XSD verified:` half-state (marker line + all-`[ ]` rows from the orphan intake) is resolved — rows now `[x]`, marker intentionally withheld pending batch confirmation
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: targeted files 89 passed (model 6/6 — Red 1→Green at Step 4; parser/writer timing-instance-refs 39 incl. new BSW round-trip + abstract-contract assertions, Red unreachable); full suite `uv run python scripts/run_tests.py --no-coverage` 11148 passed / 0 failed (baseline 11144 + 4 net-new); `npm run lint` (flake8 E9/F63/F7/F82 + ruff) clean; `npm run black` 1150 files unchanged; `npm run black-check` clean; 9b self-check (blind-spot rules): abstract ARObject base = most-derived per both siblings' Base rows, empty-group ⇒ no attrs/flattening/naming issues, class docstring verbatim XSD documentation (test-asserted), no `__init__` docstring, no attribute blocks, reader/writer `[—]` genuine; 9b deferred to batch confirmation (user instruction 2026-09-24) — XSD-only, no deviation ⇒ `# XSD verified: AUTOSAR_00052.xsd` eligible at batch stamp
- [ ] `ModeInSwcInstanceRef` — AtpInstanceRef — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/Timing/TimingCondition.py
  - note: deviation-tracked in method_deviation_by_class.md — review entries at Step 1
  - after `ModeInSwcBswInstanceRef`
  - [x] Step 1 — Sync members & description from spec — Table 3.12 (AUTOSAR_CP_TPS_TimingExtensions, p.39 via pdf_page.py --table 3.12; PAGE-SPLIT like 3.11: body main fragment — Class/Package/Note/Base/Aggregated-by header + base/contextComponent/contextModeDeclarationGroupPrototype/contextPort rows — renders BEFORE the `Table 3.12` caption at l.857, image break, continuation fragment with the targetModeDeclaration row after; caption page cited); R4.3.1 reproduction Table 4.6 (l.1453) row-identical on Note/Base/5 attributes (R4.3.1 Mults are the pre-fix 1s on base/contextModeDeclarationGroupPrototype/contextPort and Package spells "TimingConditional" — R23-11 governs); Class-vs-Enumeration header = Class ✓ concrete; Package row M2::AUTOSARTemplates::CommonStructure::Timing::TimingCondition matches the leaf file TimingCondition.py (Rule 0007 ✓); class Note verbatim single sentence "Instance reference to be capable of referencing a ModeDeclaration at a specific Mode Switch Port of a SW-C."; Base row ARObject, AtpInstanceRef, ModeInSwcBswInstanceRef → Python `ModeInSwcInstanceRef(AtpInstanceRef, ModeInSwcBswInstanceRef)` conforms (ARObject transitive via both; XSD 00052 complexType MODE-IN-SWC-INSTANCE-REF L82755 abstract="false" composes AR-OBJECT → ATP-INSTANCE-REF → MODE-IN-SWC-BSW-INSTANCE-REF → own group — no flattening: AtpInstanceRef's atpBaseRef/atpContextElementRefs/atpTargetRef are INHERITED members of the stamped base Table 5.3 p.174, whose XSD group is EMPTY per the three skipped atpDerived associations); Aggregated by TimingModeInstance.modeInstance — consume path PRE-WIRED (parser readTimingModeInstance MODE-IN-SWC-INSTANCE-REF branch l.3371 + own helper readModeInSwcInstanceRef l.3361; writer writeTimingModeInstance isinstance branch l.4553 + own helper writeModeInSwcInstanceRef l.4545); attribute rows displayed order + field-to-spec cross-check BOTH DIRECTIONS EXACT: exactly 5 fields baseRef (base, SwComponentType, 0..1, ref, atpDerived, seqOffset=10) / contextComponentRefs (contextComponent, SwComponentPrototype, *, ref, seqOffset=20 — singular * → plural + getContextComponentRefs/addContextComponentRef) / contextModeDeclarationGroupPrototypeRef (0..1, ref, seqOffset=40) / contextPortRef (0..1, ref, seqOffset=30) / targetModeDeclarationRef (0..1, ref, seqOffset=50) — no extra fields, member order = displayed row order (Rule 0001.11 ✓), ref kind → Ref suffix ✓; NO iref member (all five rows Kind=ref; all four XSD elements plain REF extensions with DEST --SUBTYPES-ENUM: CONTEXT-COMPONENT-REF→SW-COMPONENT-PROTOTYPE, CONTEXT-PORT-REF→PORT-PROTOTYPE, CONTEXT-MODE-DECLARATION-GROUP-PROTOTYPE-REF→MODE-DECLARATION-GROUP-PROTOTYPE, TARGET-MODE-DECLARATION-REF→MODE-DECLARATION) — the OperationArgumentInComponentInstanceRef IRef precedent does NOT apply, checked; XSD group MODE-IN-SWC-INSTANCE-REF (L82692) = `<!-- Association <<atpDerived>>base skipped -->` + the 4 elements in seqOffset order — base has NO XML element (XSD-skipped atpDerived association; PDF-wins Rule 0015 keeps the field) → getBaseRef/setBaseRef reader/writer [—] genuine; XML element order (seqOffset 20/30/40/50, contextPort BEFORE contextModeDeclarationGroupPrototype) ≠ displayed row order (table lists contextModeDeclarationGroupPrototype 4th) — two independent orders, reader/writer both already emit/read in XSD order; INTAKE FINDINGS: stale `# Spec verified: R23-11` 5-column checklist WITHOUT release column (orphan intake, untrusted — marker withheld), class docstring = Note + 4 section-body constraint paragraphs (constr_6855/6856/6857/6899) + a fabricated "The Python bases ... jointly stand in for" sentence, member docstrings/comments carry constr tails and lack the batch form — wipe+rewrite Step 4 (SynchronizationTimingConstraint precedent: constr paragraphs are section-level, not Note rows; VariationPoint/batch convention: Tags/Stereotypes cell suffixes kept out of member docstrings); blank lines between attribute blocks present ✓; setter signatures already family-form (Optional[T] value + chained return) ✓; CONSUME PATH COMPLETE both sides (reader reads all 4 XSD elements in XSD order via matched mutators, writer writes all 4 in XSD order via getters) — Step 5/6 expected verification-only, genuine Red only in the model docstring contract; referenced classes: bases AtpInstanceRef (stamped R23-11 Table 5.3 FO GST p.174) + ModeInSwcBswInstanceRef (XSD-only, synced 3a64033f), ref DEST targets SwComponentType/SwComponentPrototype/PortPrototype/ModeDeclarationGroupPrototype/ModeDeclaration are --SUBTYPES-ENUM targets (not field types; RefType held) — no missing classes; tracker v1 `## ModeInSwcInstanceRef` section (method_deviation_by_class.md l.3039): page cited "38" (Table 3.11's page; 3.12 is p.39) + 4 stale "missing" rows (contextComponentRef/contextModeDeclarationGroupPrototypeRef/contextPortRef/targetModeDeclarationRef — all implemented since intake) → reconcile Step 8
  - [x] Step 2 — Write model class unit test (Red) — appended TestModeInSwcInstanceRefSpecContract to tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/Timing/test_ModeInSwcInstanceRef.py (14 tests, mirroring the batch SpecContract pattern; header notes the two docstring precedents — VariationPoint Table 7.4 keeps in-cell Stereotypes/Tags suffixes out of member docstrings, SynchronizationTimingConstraint Table 3.54 keeps section-level [constr] paragraphs out of Note rows): class-docstring verbatim pin (Table 3.12 Note), init-has-no-docstring, exact-own-field-set pin (exactly the five attributes in displayed order), PEP 526 + no-`# type:` pin, most-derived-bases pin (`__bases__ == (AtpInstanceRef, ModeInSwcBswInstanceRef)` + issubclass ARObject), accessor-order pin (11 defs, get/set pairs + get/add for the * aggr), five typed-hints tests (base/contextComponent/contextModeDeclarationGroupPrototype/contextPort/targetModeDeclaration: Optional[RefType] / List[RefType], chained returns), member-docstrings-verbatim pin (5 getters + 4 setters/add with None-no-op sentences), inline-__init__-comments-verbatim pin (5 Notes in displayed row order); one iteration: ModeInSwcBswInstanceRef import initially missing from the test header (NameError, not a genuine Red) — added; RESULT 4 Red / 21 passed — the 4 Reds are GENUINE intake-contract failures confined to the docstring layer (fabricated class docstring = Note + 4 constr paragraphs + "The Python bases ... jointly stand in for" sentence; constr tails on 4 getters + 4 setters; constr tails + "Stereotypes: atpDerived" on the inline comments), the 21 immediate passes are honest (field set/order/PEP 526/base chain/accessor order/typed hints all already spec-exact per the Step 1 cross-check, plus the 10 pre-existing behavior tests and the already-exact addContextComponentRef docstring)
  - [x] Step 3 — Implement model class (Green) — VERIFICATION-ONLY: no structural change required — field-to-spec cross-check exact both directions (exactly the five Table 3.12 attributes → exactly five PEP 526 fields in displayed order, `*` aggr already the plural List[RefType] + add pair, ref-kind fields hold RefType, Python bases (AtpInstanceRef, ModeInSwcBswInstanceRef) per the Base row, all five setter/add signatures already family-form Optional[T] value + `-> "ModeInSwcInstanceRef"` chained return, blank lines between attribute blocks present); no consumer affected (parser/writer call the same mutator names); full file state at Step 3 close: 21 passed / 4 failed — the Red residue is the docstring contract ONLY, which is Step 4's wipe+rewrite to turn Green
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — docstring inventory for ModeInSwcInstanceRef: 16 positions (class docstring; __init__ has NO docstring per family form; FIVE inline __init__ member comments; get/set docstrings x4 pairs; getContextComponentRefs + addContextComponentRef) — ALL wiped first (the fabricated class docstring = Note + 4 section-body constraint paragraphs + the "The Python bases ... jointly stand in for" sentence removed; all constr tails [constr_6855/6856/6857/6899] and the "Stereotypes: atpDerived" cell suffix removed from every member position), then rewritten verbatim from the Table 3.12 markdown: class docstring = the Note single line ("Instance reference to be capable of referencing a ModeDeclaration at a specific Mode Switch Port of a SW-C."); the five inline comments + five getters = the attribute Note prose verbatim (in-cell "Stereotypes:"/"Tags: xml.sequenceOffset=N" suffixes kept out per the batch VariationPoint Table 7.4 convention; no Tags tails — the sequenceOffset metadata is honored by the writer's XSD element order instead); the four setters = Note + " A None value is a no-op and does not overwrite an existing <baseRef|contextModeDeclarationGroupPrototypeRef|contextPortRef|targetModeDeclarationRef>." single-line; addContextComponentRef = Note + " A None value is a no-op and does not append anything." (DoIP add-form precedent); evidence: 25/25 Green in test_ModeInSwcInstanceRef.py (the 4 Step-2 docstring pins now exact-equality Green), 508/508 across the whole Timing model test dir (sibling classes in TimingCondition.py and the other Timing packages unaffected)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — PASS-IMMEDIATELY (honest, no genuine Red), as predicted at Step 1: readModeInSwcInstanceRef/writeModeInSwcInstanceRef already cover ALL FOUR XSD-present elements via matched mutators/getters — nothing dropped, so the "fields exist but are dropped" Red scenario does not occur; existing coverage audit found the parser side complete (TestReadModeInSwcInstanceRef full-form read with VALUES + DEST enums, minimal, base-ref-absent-no-xml, TimingModeInstance MODE-IN-SWC-INSTANCE-REF dispatch, empty resource) and the writer side complete on values (test_round_trip_mode_in_swc_instance_ref) but missing the XSD element-ORDER pin (existing test findalls, never pins child sequence — the real gap, since XSD seqOffset order puts CONTEXT-PORT-REF (30) BEFORE CONTEXT-MODE-DECLARATION-GROUP-PROTOTYPE-REF (40), the REVERSE of the displayed row order) and the empty-wrapper write; added 2 tests to TestWriteTimingInstanceRefs in tests/test_armodel/writer/test_writer_timing_instance_refs.py: test_write_mode_in_swc_xsd_element_order (full child-sequence pin [CONTEXT-COMPONENT-REF x2, CONTEXT-PORT-REF, CONTEXT-MODE-DECLARATION-GROUP-PROTOTYPE-REF, TARGET-MODE-DECLARATION-REF] per group L82692 + text/DEST payload asserts) and test_write_mode_in_swc_empty (empty iref → zero children written, BASE-REF never emitted, parse→write→re-parse round-trip to all-empty); 41/41 across the two parser/writer timing-instance-refs files (39 pre-existing + 2 new)
  - [x] Step 6 — Update parser & writer (Green) — verification-only: no change. Reader readModeInSwcInstanceRef (parser l.3361) reads CONTEXT-COMPONENT-REF list → addContextComponentRef, CONTEXT-PORT-REF/CONTEXT-MODE-DECLARATION-GROUP-PROTOTYPE-REF/TARGET-MODE-DECLARATION-REF → the three setters, in XSD group order; writer writeModeInSwcInstanceRef (writer l.4545) writes the same four via getContextComponentRefs/getContextPortRef/getContextModeDeclarationGroupPrototypeRef/getTargetModeDeclarationRef in the same order; dispatch through TimingModeInstance.modeInstance pre-exists both sides (parser tag branch l.3371, writer isinstance branch l.4553); getBaseRef/setBaseRef deliberately uncovered (0001.7 satisfied — [—] is genuine: the atpDerived `base` association is XSD-skipped, no XML element exists to read or write, confirmed by test_write_mode_in_swc_base_ref_has_no_xml_element); matched name pairs Rule 0013.2 verified (readModeInSwcInstanceRef↔writeModeInSwcInstanceRef, addContextComponentRef↔getContextComponentRefs, setContextPortRef↔getContextPortRef, setContextModeDeclarationGroupPrototypeRef↔getContextModeDeclarationGroupPrototypeRef, setTargetModeDeclarationRef↔getTargetModeDeclarationRef, getChildElementOptionalRefType↔setChildElementOptionalRefType — no cross pairs); no chained mutator calls; evidence: 41/41 in the two files
  - [x] Step 7 — Update checklist comment — 6-column format written in place of the stale intake 5-column block (which had no release column), verified at this step: `# Spec: AUTOSAR_CP_TPS_TimingExtensions.pdf, Table 3.12, p.39` plus a `[—]`-legend note recording the XSD-skipped `base` association (field kept per the PDF table, no XML element to read or write), rows in markdown displayed order (`__init__`, getBaseRef, setBaseRef, getContextComponentRefs, addContextComponentRef, getContextModeDeclarationGroupPrototypeRef, setContextModeDeclarationGroupPrototypeRef, getContextPortRef, setContextPortRef, getTargetModeDeclarationRef, setTargetModeDeclarationRef), all `[x]` with release R23-11; reader [x] on the four setters + addContextComponentRef (parser sets/adds), writer [x] on the five getters (writer gets), `__init__`/getBaseRef/setBaseRef [—]/[—] (genuine no-XML); stale `# Spec verified: R23-11` line REMOVED (orphan-intake marker did not certify this 9-step pass — withheld, deferred to batch confirmation like the prior batch commits)
  - [x] Step 8 — Deviations  [none open: no naming/type/missing deviation rows — field-to-spec both directions EXACT (exactly the five Table 3.12 attributes → exactly five fields in displayed row order; no extra fields, no flattening — the Base chain ends at AtpInstanceRef + ModeInSwcBswInstanceRef with AtpInstanceRef's members staying on the stamped base; ref-kind fields hold RefType per the DEST --SUBTYPES-ENUM convention; no iref-kind member exists in the table), reader+writer coverage complete for every XML-bearing attr (0001.7 — the four XSD-present elements covered both sides in XSD order; `base`'s [—] is the XSD-skipped-association reality, not a gap), docstrings verbatim (0012/0001.4); the Step 1-7 findings were fixed in-band and leave no rows (Rule 0014: fixed ⇒ no row): (1) fabricated class docstring + constr-tailed member docstrings/comments wiped and rewritten as verbatim Notes (+ None-no-op sentences), (2) stale `# Spec verified: R23-11` marker removed and checklist rebuilt 6-column/release; v1 tracker (method_deviation_by_class.md) `## ModeInSwcInstanceRef` section RECONCILED 2026-09-24: page citation corrected 38→39 (38 is Table 3.11's page), Source path corrected to the Rule 0007 leaf file TimingCondition.py (was the pre-relocation TimingCondition/ModeInSwcInstanceRef.py), the four "missing" rows were STALE (implemented since intake; `base` had never been listed) — section rewritten to the sibling sync-record form with a no-deviation row; v2 tracker (method_deviation_by_class_v2.md) has NO ModeInSwcInstanceRef rows (verified by grep — 0 mentions); docs/plan/staged_class_sync_review.md does not exist; referenced classes: bases AtpInstanceRef (stamped R23-11 Table 5.3 FO GST p.174) + ModeInSwcBswInstanceRef (XSD-only, synced 3a64033f), ref DEST targets SwComponentType/SwComponentPrototype/PortPrototype/ModeDeclarationGroupPrototype/ModeDeclaration are --SUBTYPES-ENUM targets (not field types; RefType held) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: my three test files 66/66 (test_ModeInSwcInstanceRef.py 25 — 10 pre-existing behavior tests + 14 net-new TestModeInSwcInstanceRefSpecContract incl. the exact-own-field-set/PEP 526/most-derived-bases/accessor-order/5 typed-hints pins and the class-docstring + 10 member-docstring + 5 inline-comment verbatim pins that were the Step 2 Reds; test_arxml_parser_timing_instance_refs.py + test_writer_timing_instance_refs.py 41 — 39 pre-existing + 2 net-new writer tests: the XSD element-order pin [CONTEXT-COMPONENT-REF x2, CONTEXT-PORT-REF, CONTEXT-MODE-DECLARATION-GROUP-PROTOTYPE-REF, TARGET-MODE-DECLARATION-REF] and the empty-wrapper write/round-trip), 508/508 across the whole Timing model test dir; full suite `uv run python scripts/run_tests.py --no-coverage` 11326 passed / 0 failed (baseline 11310 + 16 net-new: +14 model +2 writer, exact; integration round-trip 29 ARXML included, green); `npm run lint` (flake8 E9/F63/F7/F82 + ruff E/F/W/I) All checks passed; `npm run black` reformatted ONLY my test file (canonical wrapping) + `black-check` clean 1158 files; 9b blind-spot checks walked by the agent: element kind + every spec attr modeled (all five Table 3.12 attributes → exactly five fields, exact-own-field-set test-pinned), most-derived base (0001.2 — Base row ARObject, AtpInstanceRef, ModeInSwcBswInstanceRef; XSD complexType L82755 composes AR-OBJECT → ATP-INSTANCE-REF → MODE-IN-SWC-BSW-INSTANCE-REF → own group; `__bases__ == (AtpInstanceRef, ModeInSwcBswInstanceRef)` test-pinned, AtpInstanceRef members stay INHERITED on the stamped base — no flattening), no fabrication (0001.3 — both-directions cross-check exact; XSD-skipped `base` kept per Rule 0015 PDF-wins with genuine reader/writer [—]), Kind-suffix naming (0001.5 — all rows Kind=ref → Ref suffix; singular `*` → plural contextComponentRefs + add; NO iref member — checked, the OperationArgumentInComponentInstanceRef precedent does not apply), create/set/add shape (0001.6 — four setters + addContextComponentRef with None no-op + chaining, no createXxx needed — not a Referrable container), reader+writer coverage for every XML-bearing attr (0001.7 — the four XSD-present elements covered both sides in XSD order via matched name pairs, verified no cross pairs, tests assert field VALUES not counts), member order (0001.11 — fields + accessors + checklist rows in the markdown displayed row order base→contextComponent→contextModeDeclarationGroupPrototype→contextPort→targetModeDeclaration, accessor-order source-pinned; reader/writer XML element order = XSD seqOffset 20/30/40/50 which DIFFERS from displayed order (contextPort before contextModeDeclarationGroupPrototype) — the two orders checked independently, writer order now test-pinned), docstrings verbatim (0012/0001.4 — class = Note exact, 5 getters = Note prose exact, 4 setters = Note + None-no-op sentences, add = Note + add-form sentence, 5 inline __init__ comments = Note prose verbatim; 16/16 positions exact-equality test-pinned; in-cell Stereotypes/Tags suffixes and section-level [constr] paragraphs kept out per the batch VariationPoint/SynchronizationTimingConstraint precedents), blank line between __init__ attribute blocks (0008 — single blank line between all five blocks, verified by eye post-black), PEP 526 annotated members + no `# type:` (0003 — regex-pinned), deviations resolved/removed (0014 — no open rows; v1 tracker `## ModeInSwcInstanceRef` section rewritten to the sync-record form with page/path corrections and the no-deviation row; v2 tracker 0 mentions), stamp decision: `# Spec verified:` WITHHELD per batch instruction (marker deferred to batch confirmation like the 24 prior commits in this batch; the orphan-intake stale marker was removed at Step 7), Rule 0007 location: TimingCondition.py leaf file matches the spec Package row M2::AUTOSARTemplates::CommonStructure::Timing::TimingCondition (same file as the stamped siblings ModeInBswInstanceRef/TimingModeInstance/TimingCondition/TimingConditionFormula/TimingExtensionResource and the XSD-verified ModeInSwcBswInstanceRef); 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `AbstractEnumerationValueVariationPoint` — AttributeValueVariationPoint abstract subclass — located: FO GST Table E.2, p.421 (NOT CP SWCT — see Step 1)
  - module: M2/AUTOSARTemplates/GenericStructure/VariantHandling/AttributeValueVariationPoints/__init__.py (per the 9 stamped siblings)
  - [x] Step 1 — Sync members & description from spec — Table E.2 (AUTOSAR_FO_TPS_GenericStructureTemplate, caption L11404, p.421 via direct pypdf scan — pdf_page.py cannot index letter-table ids, BindingTimeEnum/SlOverviewParagraph precedent; single-page table, caption before meta in markdown); CP SWCT (the row's TBC hint) has NO own class table — only Subclasses mentions (L17491); R4.3.1 reproduction Table E.1 (R4.3.1 GST L10316) content-identical (glyph cosmetics + line-wrap artifacts "AttributeValueVariation Points"/"SwSystemconst DependentFormula"); Class-vs-Enumeration header = Class ✓ (`<<atpMixedString>> AbstractEnumerationValueVariationPoint (abstract)`; XSD 00052 group L274 appinfo stereotypes "atpMixedString,atpObject"); Package M2::AUTOSARTemplates::GenericStructure::VariantHandling::AttributeValueVariationPoints ✓ — module stays AttributeValueVariationPoints/__init__.py per the 9 stamped siblings (established family placement); Note "This is an abstract EnumerationValueVariationPoint. It is introduced to support the case that additional attributes are required for particular purposes." verbatim (XSD group documentation L278 byte-identical); Base = ARObject , AttributeValueVariationPoint , FormulaExpression , SwSystemconstDependentFormula — derivation set; src chain AVP(SwSystemconstDependentFormula, ABC) L15 → SSCDF(FormulaExpression, ABC) L317 → FormulaExpression(ARObject, AtpMixedString, ABC) (FormulaLanguage.py L9) → anchor `(AttributeValueVariationPoint)` = the stamped-sibling form (BooleanValueVariationPoint etc. carry the identical Base row; most-derived member of the set per the ConditionByFormula 9b ruling); MRO AEVP → AVP → SSCDF → FormulaExpression → ARObject → AtpMixedString → ABC; Subclasses row EMPTY — the auto-generated {Type}ValueVariationPoint concrete classes (TPS_GST_00206/00373) are NOT modeled in src (their 4 XSD complexTypes composing the AEVP group: L34752 DiagnosticDebounceBehaviorEnumValueVariationPoint + L38292/L46048/L46955, all outside src) — referenced-not-modeled, non-blocking; Aggregated by VariationPointProxy.valueAccess ✓ (VALUE-ACCESS content element; VALUE_ACCESS_TAG_TO_CLASS/CLASS_TO_TAG cover the 9 modeled AVP classes only — no AEVP-concrete dispatch entry possible today); attributes displayed order: base (Identifier, 0..1, attr, "This attribute reflects the base to be used in context of EnumerationMappingTable for this reference." Tags: xml.attribute=true), enumTable (Ref, 0..1, attr, "This represents the assigned enumeration table." Tags: xml.attribute=true); XSD attributeGroup L283 BASE(IDENTIFIER--SIMPLE) + ENUM-TABLE(REF--SIMPLE) both xml.attribute=true, group L274 EMPTY sequence (no child elements — attributes only); sibling AVP complexTypes (e.g. BOOLEAN-VALUE-VARIATION-POINT L9338) do NOT compose the AEVP attributeGroup — BASE/ENUM-TABLE ride AEVP-concrete content only; naming per Rule 0001.5: base → getBase/setBase, enumTable → getEnumTable/setEnumTable (both Kind=attr → no ref/tref/iref suffix); types: base → Optional[Identifier] (spec Identifier; ShortNameFragment.fragment precedent), enumTable → Optional[RefType] (spec Ref — RefType is the repo Ref type; REF--SIMPLE L96364 is a plain string pattern so the XML attribute carries the value only, no DEST); REAL GAP: class does NOT exist in src (new-class sync) and the shared read/writeAttributeValueVariationPoint cover only the AVP attributeGroup (BINDING-TIME/BLUEPRINT-VALUE/SD/SHORT-LABEL + INTERVAL-TYPE via LimitValueVariationPoint isinstance) — BASE/ENUM-TABLE need their own helper pair readAbstractEnumerationValueVariationPoint/writeAbstractEnumerationValueVariationPoint (Rule 0001.7 abstract-with-own-XML-bearing-attributes adaptation: reusable helpers for the unmodeled concrete subclasses to call); mixed text rides the AtpMixedString mixin inherited via FormulaExpression (getMixedString/setMixedString, no spec rows); mirror test target: tests/test_armodel/models/M2/AUTOSARTemplates/GenericStructure/VariantHandling/AttributeValueVariationPoints/test_AttributeValueVariationPoints.py (extend, no duplication)
  - [x] Step 2 — Write model class unit test (Red) — extended tests/test_armodel/models/M2/AUTOSARTemplates/GenericStructure/VariantHandling/AttributeValueVariationPoints/test_AttributeValueVariationPoints.py: new TestAbstractEnumerationValueVariationPointSpecContract (10 tests, style of the in-file TestAttributeValueVariationPointSpecContract + the Group8 spec-contract pattern) — test_abstract_guard_and_defaults (TypeError on direct instantiation + base/enumTable/getMixedString all None via a _Concrete subclass), test_base_anchoring (`__bases__ == (AttributeValueVariationPoint,)`; issubclass ARObject/FormulaExpression/SwSystemconstDependentFormula/AtpMixedString; full `__mro__` name list asserted == AEVP → AVP → SSCDF → FormulaExpression → ARObject → AtpMixedString → ABC → object — the Table E.2 derivation set anchored on its most-derived member, stamped-sibling form), test_class_docstring_verbatim (==Table E.2 Note), test_init_has_no_docstring, test_base_typed_identifier (get_type_hints getBase/setBase → Optional[Identifier], setter return is AEVP), test_enum_table_typed_ref_type (Optional[RefType], spec type Ref), test_round_trip_and_none_noop (Identifier().setValue("EnumMappingTables") + RefType().setValue("ActiveComponent/E") identity round-trips + None no-ops), test_getter_docstrings_are_notes_without_tags, test_setter_docstrings_are_notes_with_none_noop, test_mixin_accessors (setMixedString/getMixedString round-trip + None no-op — stereotype-inherited via FormulaExpression); module docstring extended with the AEVP sentence; test imports added per ruff I001 (AbstractEnumerationValueVariationPoint first in the AttributeValueVariationPoints block; Identifier/RefType inserted into the PrimitiveTypes block; FormulaLanguage/ArObject/VariantHandling lines inserted sorted); Red confirmed: collection ImportError — cannot import name 'AbstractEnumerationValueVariationPoint' (the class does not exist) — the genuine new-class Red
  - [x] Step 3 — Implement model class (Green) — new `AbstractEnumerationValueVariationPoint(AttributeValueVariationPoint)` in AttributeValueVariationPoints/__init__.py placed directly after AttributeValueVariationPoint and before AbstractNumericalVariationPoint (dependency-first — Python evaluates base names at class creation; Table 7.2 Subclasses row lists AEVP first); ABSTRACT (Table E.2 header "(abstract)"; type(self) TypeError guard mirroring AbstractNumericalVariationPoint — AVP already supplies the ABC anchor); members = exactly the 2 Table E.2 attribute rows in displayed order (base: Optional[Identifier]; enumTable: Optional[RefType] — spec Ref, REF--SIMPLE is a plain string so the XML attribute carries the value only, no DEST; no create*/add* — not a Referrable child, no `*` mults); PEP 526 annotated fields with blank line between the two attribute blocks (Rule 0008); setters keep None-no-op + return-self shape; module import change: Identifier + RefType added to the PrimitiveTypes import block (sorted insertion, no re-sort — I001 disabled in models per AGENTS.md); mixed text rides the AtpMixedString mixin inherited via FormulaExpression (class-level mixedString default, no mixin __init__); top-level export NOT added — the whole AttributeValueVariationPoints family is deliberately not re-exported at armodel top level (verified: AbstractNumericalVariationPoint/BooleanValueVariationPoint also absent), consumers use the full path; Green: family test file 29/29 (19 pre-existing + 10 new), GenericStructure model subtree 572/572
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — landed in the Step 3 body (new class: no stale wording could exist anywhere, wipe trivially satisfied — the intake had no AbstractEnumerationValueVariationPoint at all); docstrings written verbatim from the Table E.2 markdown: class docstring = Note verbatim single line (XSD group documentation L278 byte-identical; Note-ONLY form per the current-batch convention — the stamped siblings' Package/Base/Stereotypes meta-block docstrings are d5c96fd9-batch legacy, recorded as a Step 8 observation, not touched); inline `__init__` comments = attribute Notes verbatim INCLUDING the Tags tail (xml.attribute=true on both rows); getter docstrings = Note sans Tags; setter docstrings = Note sans Tags + single-line "A None value is a no-op and does not overwrite an existing <field>." (family single-line form); `__init__` carries no docstring (Rule 0012.2.5.2); all 5 cells byte-equal to the markdown (test-enforced by the 3 docstring tests)
  - [x] Step 5 — Write reader/writer round-trip test (Red) — extended the 2 family round-trip homes (extend, no duplication; the TestRead/WriteSwSystemconstDependentFormula own-group-helper precedent — AEVP is the same abstract-own-group case): parser test_arxml_parser_variation_point.py new TestReadAbstractEnumerationValueVariationPoint (3 tests, function-local import style of the in-file SSCDF block) — test_read_base_and_enum_table (element with BASE/ENUM-TABLE attribs → _Probe(AbstractEnumerationValueVariationPoint) fields populated, isinstance Identifier + RefType + value asserts), test_read_attributes_absent_leaves_fields_none (plain element → both None — honest pass), test_read_write_roundtrip (setBase/setEnumTable → writeAbstractEnumerationValueVariationPoint → re-parse via the namespaced wrapper → field-value asserts); writer test_writer_variation_point.py new _ProbeAbstractEnumerationValueVariationPoint probe subclass + TestWriteAbstractEnumerationValueVariationPoint (2 tests, _write-helper form of the SSCDF block) — test_write_base_and_enum_table (written element carries BASE="EnumMappingTables" + ENUM-TABLE="ActiveComponent/E" attribs), test_write_attributes_absent_emits_nothing (clean probe → no attribs); writer test file import block extended (AbstractEnumerationValueVariationPoint, sorted first per ruff I001); Red confirmed 5 failed / 0 passed — genuine: readAbstractEnumerationValueVariationPoint / writeAbstractEnumerationValueVariationPoint do not exist (AttributeError on both helper calls)
  - [x] Step 6 — Update parser & writer (Green) — READER arxml_parser.py: new reusable `readAbstractEnumerationValueVariationPoint` helper placed directly after readAttributeValueVariationPoint (own-group shape of the readSwSystemconstDependentFormula precedent: reads ONLY the AEVP attributeGroup — "BASE" attrib → Identifier().setValue → setBase; "ENUM-TABLE" attrib → RefType().setValue → setEnumTable; returns obj); the AVP attributeGroup + mixed text + SSCDF group stay on the shared readAttributeValueVariationPoint path, which a future AEVP-concrete reader composes with this helper (XSD consumer complexType composition AR-OBJECT + FORMULA-EXPRESSION + SSCDF + AVP + AEVP groups, e.g. L34752); no dispatch entry added to VALUE_ACCESS_TAG_TO_CLASS — AEVP is abstract and its auto-generated concrete subclasses are unmodeled, so no tag can map to it today (a speculative isinstance wire-in inside the shared AVP helper would be dead machinery for content that cannot occur); WRITER arxml_writer.py: new mirrored `writeAbstractEnumerationValueVariationPoint` helper placed directly after writeAttributeValueVariationPoint (getBase/getEnumTable → element.attrib["BASE"/"ENUM-TABLE"], omitted when None); imports: Identifier added to both files' PrimitiveTypes blocks (sorted insertion, no re-sort — I001 disabled per AGENTS.md), AbstractEnumerationValueVariationPoint added to both files' AttributeValueVariationPoints import blocks (sorted first); Rule 0013.2 name pairs verified (setBase/setEnumTable set by the reader, getBase/getEnumTable got by the writer — no cross pairs, no chained mutators; DEST not read/written — the XSD attributeGroup is attribute-only, REF--SIMPLE carries no DEST); Green: 92/92 across the 3 target test files, combined parser+writer suites 5561/5561 (full-suite totals at Step 9)
  - [x] Step 7 — Update checklist comment — 6-column checklist written in the Step 3 body replacement, verified at this step line-by-line against the code plus the set-based script (checklist == declared methods == {`__init__`, getBase, setBase, getEnumTable, setEnumTable}, row order == member order == Table E.2 displayed order base→enumTable, reader calls every set* (readAbstractEnumerationValueVariationPoint → setBase/setEnumTable), writer calls every get* (writeAbstractEnumerationValueVariationPoint → getBase/getEnumTable), PEP 526 annotated members, blank line before every attribute block (2/2 verified by script), no `# type:` comments, marker legitimately absent): `# Spec: AUTOSAR_FO_TPS_GenericStructureTemplate.pdf, Table E.2, p.421` (p.421 via direct pypdf scan — pdf_page.py cannot index letter-table ids; R4.3.1 Table E.1 is a row-identical reproduction); `__init__` [—]/[—] (no own XML element — XSD element group L274 is an empty sequence; base/enumTable serialize as XML attributes on AEVP-concrete content via the own-group helpers); reader [x] on setBase/setEnumTable, writer [x] on getBase/getEnumTable; every row release R23-11; trailing notes record the AtpMixedString-inherited getMixedString/setMixedString (stereotype-inherent, no spec rows — FormulaExpression checklist precedent) and the attribute-only serialization + unmodeled concrete subclasses
  - [x] Step 8 — Deviations  [none open on this class: field-to-spec both directions exact (Step 1/Step 3 — exactly the 2 Table E.2 attribute rows base + enumTable, no placeholders, no fabricated/flattened members), no naming deviations (both Kind=attr → direct names, no ref/tref/iref suffixes, no `*` mults), no type deviations (Identifier and RefType both exist and are stamped)]; tracker reconciliation: v1 tracker (docs/examples/method_deviation_by_class.md) had a STALE claim in its `## AttributeValueVariationPoint` section ("the AbstractEnumerationValueVariationPoint abstract subclass is likewise unmodeled", from the 2026-09-24 AVP audit) — superseded 2026-09-26 with a dated Update sentence, and a new `## AbstractEnumerationValueVariationPoint` section added (sync record, no-deviation row, stamp-deferred note) per the ConditionByFormula/SwSystemconstValue precedent; v2 tracker (method_deviation_by_class_v2.md) has NO AbstractEnumerationValueVariationPoint entries (grep-verified — untouched); referenced-class findings (not deviations, non-blocking): the auto-generated {Type}ValueVariationPoint concrete subclasses of AEVP (TPS_GST_00206/00373; 4 XSD complexTypes composing the AEVP group: L34752 DiagnosticDebounceBehaviorEnumValueVariationPoint, L38292, L46048, L46955 — DiagnosticExtract/SystemTemplate domains) are NOT modeled in src and NOT queued anywhere — their helpers-side dispatch (VALUE_ACCESS maps) stays closed until their own syncs land, recorded as the standing consequence of the no-dispatch-entry disposition (Step 6); OBSERVATION (stamped siblings, not touched): the 9 stamped AVP-family siblings carry the d5c96fd9-batch legacy form (class docstrings keep Package/Base/Stereotypes meta blocks; markers present) while this new class uses the current-batch Note-only form — sibling drift is a matter for their own drift pass (Rule 0012.3: an existing marker is not proof, but re-syncing stamped siblings is outside this row's scope); top-level export NOT added (the AttributeValueVariationPoints family is deliberately not re-exported at armodel top level — consistent with all 9 siblings); referenced classes: base chain AttributeValueVariationPoint (stamped d5c96fd9) + SwSystemconstDependentFormula (synced f05e21d49, marker deferred) + FormulaExpression (synced 88ed82bed, marker deferred) + ARObject (stamped) + AtpMixedString mixin (StereotypeMixins.py, interface-level); member types Identifier (stamped Table 4.5) + RefType (stamped PrimitiveTypes); consumer VariationPointProxy.valueAccess (stamped Table 7.61, VALUE-ACCESS dispatch untouched) — no missing classes]
  - [x] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-26: target files 92/92 (test_AttributeValueVariationPoints.py 29 = 19 pre-existing + 10 new + test_arxml_parser_variation_point.py + test_writer_variation_point.py, re-run green after black reformat of the extended model test file), full suite `uv run python scripts/run_tests.py --no-coverage` 12049 passed / 0 failed (baseline 12034 + 15 net-new: 10 model spec-contract tests, 3 parser tests, 2 writer tests; integration round-trip included in the suite green); `npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I — one transient F401 fixed in-band: the writer never constructs Identifier, import removed, parser keeps its legitimate use); `npm run black` reformatted the extended model test file (canonical formatting only) + `black-check` clean (1240 files); set-based checklist script green (checklist == declared methods == {`__init__`, getBase, setBase, getEnumTable, setEnumTable}, row order == member order == Table E.2 displayed order, reader calls every set*, writer calls every get*, PEP 526 members, blank line before every attribute block 2/2, no `# type:`, marker legitimately absent); 9b blind-spot checks walked: Rule 0001.1 Class table ↔ exactly the 2 attribute rows modeled (test-enforced defaults), Rule 0001.2 most-derived base — Table E.2 Base derivation set {ARObject, AttributeValueVariationPoint, FormulaExpression, SwSystemconstDependentFormula} anchored on AttributeValueVariationPoint, the most-derived member in src (stamped-sibling form; MRO test-enforced to cover every set member + the AtpMixedString mixin via FormulaExpression per Rule 0021), Rule 0001.3 no fabrication/flattening (2 fields ↔ 2 rows both directions; nothing inherited flattened), Rule 0001.5 Kind=attr → direct accessor names (no ref/tref/iref suffixes, no `*` plurals), Rule 0001.6 setter shape (None no-op + return self; no create* — not a Referrable child), Rule 0001.7 reader+writer coverage for both kept attrs via the own-group helper pair (element-level round-trip test green; the XSD element group L274 is an empty sequence so the [—]-on-`__init__`/no-own-element disposition is structural), Rule 0001.11 member/accessor/checklist order == markdown displayed order base→enumTable; XML side has no element order (attributes only) and the helpers follow the XSD attributeGroup order BASE→ENUM-TABLE, docstrings verbatim (class Note + inline Notes incl. Tags + getters sans Tags + setters with None-no-op sentence — test-enforced), Rule 0008 spacing script-verified, Rule 0014 no open deviations + tracker reconciled, Rule 0007 location per spec Package row + the 9 stamped siblings; top-level export n/a (family not re-exported, consistent); 9b deferred to batch confirmation (user instruction)
- [x] `AbstractNumericalVariationPoint` — AttributeValueVariationPoint abstract subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [x] `BooleanValueVariationPoint` — AttributeValueVariationPoint subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [x] `FloatValueVariationPoint` — AttributeValueVariationPoint subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [x] `IntegerValueVariationPoint` — AttributeValueVariationPoint subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [x] `LimitValueVariationPoint` — AttributeValueVariationPoint subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [x] `NumericalValueVariationPoint` — AttributeValueVariationPoint subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [x] `PositiveIntegerValueVariationPoint` — AttributeValueVariationPoint subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [x] `TimeValueValueVariationPoint` — AttributeValueVariationPoint subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [x] `UnlimitedIntegerValueVariationPoint` — AttributeValueVariationPoint subclass — CP_TPS_SoftwareComponentTemplate (locate at Step 1) — finished, stamped `# Spec verified: R23-11` (commit: d5c96fd9)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [ ] `BlueprintFormula` — pure-text formula class — locate spec table at Step 1
  - [x] Step 1 — Sync members & description from spec — Table C.16 (AUTOSAR_FO_TPS_StandardizationTemplate, p.163; appendix-C caption-shift: body renders above the caption on the same page; pdf_page.py numeric-only regex cannot index letter ids — page via direct pypdf scan; R4.3.1 reproduction Table D.12 same rows); Class `<<atpMixedString>> BlueprintFormula` concrete (XSD 00052 complexType L9068 abstract="false" mixed="true"); Package M2::AUTOSARTemplates::CommonStructure::StandardizationTemplate::BlueprintFormula; Note "This class express the extension of the Formula Language to provide formalized blueprint-Value resp. blueprintCondition."; Base ARObject, FormulaExpression, SwSystemconstDependentFormula → most-derived provided base SwSystemconstDependentFormula (ConditionByFormula precedent); no Aggregated-by/Subclasses row rendered; 2 attribute rows displayed order: ecuc (EcucDefinitionElement, 1, ref) + verbatim (MultiLanguageVerbatim, 1, aggr) — queue-row "pure-text formula" assumption corrected: the class has 2 own content members, not zero (XSD group BLUEPRINT-FORMULA L9023 choice: ECUC-QUERY-REF atp.Status="removed" — not modeled, Rule 0015 PDF wins / ECUC-REF / VERBATIM); consumer finding: sole consuming element VariationPoint.formalBlueprintCondition (FORMAL-BLUEPRINT-CONDITION, XSD L130040) is atp.Status="removed" in R23-11 and the stamped VariationPoint sync deliberately does not read/write it (parser L1347) → no live XML path, drives Steps 5/6 N/A + Step 8; referenced classes all exist and are stamped R23-11 (FormulaExpression, SwSystemconstDependentFormula, EcucDefinitionElement, MultiLanguageVerbatim)
  - [x] Step 2 — Write model class unit test (Red) — new tests/test_armodel/models/M2/AUTOSARTemplates/CommonStructure/StandardizationTemplate/test_BlueprintFormula.py (mirrors test_BlueprintGenerator.py): TestBlueprintFormula (defaults None incl. inherited sysc accessors, concrete-instantiable, set/get round-trips + None-no-op for ecucRef/verbatim, inherited SwSystemconstDependentFormula base accessors live on the concrete class) + TestBlueprintFormulaSpecContract (verbatim class-Note, exact own field set ["ecucRef","verbatim"] in displayed order, PEP 526 Optional[RefType]/Optional[MultiLanguageVerbatim] + no-#type:, base chain __bases__==(SwSystemconstDependentFormula,) + MRO contains ARObject/FormulaExpression, accessor order get→set per displayed rows, getter/setter/init-comment docstrings verbatim incl. the spec's "a argument" grammar and the "undefined" quotes, no-__init__-docstring); 18 Red (ModuleNotFoundError)
  - [x] Step 3 — Implement model class (Green) — new src/armodel/models/M2/AUTOSARTemplates/CommonStructure/StandardizationTemplate/BlueprintFormula.py (leaf package per spec Package row, BlueprintGenerator.py sibling precedent): class BlueprintFormula(SwSystemconstDependentFormula) concrete, ecucRef: Optional[RefType] (spec `ecuc` 1 ref — Rule 0001.5 Kind suffix) + verbatim: Optional[MultiLanguageVerbatim] (1 aggr, set/get — not create/add: no Referrable child); exports wired src/armodel/models/__init__.py wildcard line before BlueprintGenerator; 18/18 Green + `import armodel.models` package-load + top-level `from armodel.models import BlueprintFormula` verified
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — new file, nothing to wipe: written verbatim from the Table C.16 markdown Notes on first write (class docstring = Note verbatim keeping the spec's "express" grammar; inline __init__ comments + getter docstrings = attribute Notes verbatim; setters = Note + None-no-op sentence) — test-enforced by the spec-contract class
  - [x] Step 5 — Write reader/writer round-trip test (Red) — NOT N/A: the class has a real XML element form (XSD complexType BLUEPRINT-FORMULA L9068), but the sole consuming element VariationPoint.formalBlueprintCondition (FORMAL-BLUEPRINT-CONDITION, L130040) is atp.Status="removed" in R23-11 and the stamped VariationPoint sync deliberately does not dispatch it (no model attribute per the removed-attr rule; wiring it would violate VariationPoint's own R23-11 table, Rule 0015) — reader/writer coverage therefore follows the PostBuildVariantCriterionValue precedent: the class's own helper pair pinned at element level; new tests/test_armodel/parser/test_parser_blueprint_formula.py (5: full ecuc+verbatim+sysc+mixed-text with DEST asserts, ecuc-only, verbatim-only, text-only, empty) + tests/test_armodel/writer/test_writer_blueprint_formula.py (4: full incl. DEST attribs + L-5, ecuc-only omits the rest, bare-element, None-formula); 9 Red (AttributeError readBlueprintFormula/writeBlueprintFormula)
  - [x] Step 6 — Update parser & writer (Green) — parser: readBlueprintFormula helper next to readSwSystemconstDependentFormula (readARObject → mixed text via readMixedStringText → setEcucRef(getChildElementOptionalRefType ECUC-REF) → setVerbatim(getMultiLanguageVerbatim VERBATIM) → isinstance-SwSystemconstDependentFormula readSwSystemconstDependentFormula for the inherited SYSC refs — same polymorphic shape as readConditionByFormula); writer: writeBlueprintFormula helper next to writeSwSystemconstDependentFormula (BLUEPRINT-FORMULA SubElement, mixed text, ECUC-REF via setChildElementOptionalRefType, VERBATIM via shared setMultiLanguageVerbatim, inherited SYSC refs) — verbatim wiring is the 4th consumer of the same shared pair the MultiLanguageVerbatim "Aggregated by" row names (MlFigure/DocumentationBlock/MlFormula pre-exist); imports added beside BlueprintGenerator in both files; no chained mutators; readVariationPoint/writeVariationPoint deliberately untouched; 27/27 Green (18 model + 5 parser + 4 writer)
  - [x] Step 7 — Update checklist comment — 6-column format, `# Spec: R23-11/AUTOSAR_FO_TPS_StandardizationTemplate.pdf, Table C.16, p.163 (R23-11)`; header note records the caption-shift, the R4.3.1 D.12 reproduction, the XSD group/complexType line anchors, the ECUC-QUERY-REF removal, and the removed-consumer/own-helper-pair disposition; reader [x] on mutator rows / writer [x] on getter rows via the own pair; `# Spec verified:` marker NOT written (deferred to batch confirmation)
  - [x] Step 8 — Deviations  [none open: field-to-spec both directions exact (Step 1/Step 3 — exactly ecucRef + verbatim in displayed order, no placeholders, no fabricated/flattened members; the SW-SYSTEMCONST-DEPENDENT-FORMULA group members belong to the base class's own table — inherited, not re-declared); no naming deviations (ecuc → ecucRef Kind-suffix per sysc→syscRef; verbatim name-as-is for the aggr); no type deviations (EcucDefinitionElement ref target referenced BY VALUE in RefType DEST like all refs — EcucDefinitionElement class exists stamped, no model dependency); findings, not deviations: (1) queue-row "pure-text formula" assumption corrected — the class has 2 own members (CompuGenericMath shape does NOT apply); (2) sole consumer FORMAL-BLUEPRINT-CONDITION removed in R23-11 → own helper pair + element-level tests, dispatcher deliberately untouched (PostBuildVariantCriterionValue precedent), recorded in the class checklist header + tracker Note; v1 tracker (docs/examples/method_deviation_by_class.md): no pre-existing BlueprintFormula entries (grep-verified), new `## BlueprintFormula` sync-record section added after BlueprintGenerator (no-deviation row, caption-shift + removed-consumer + XSD anchors note, stamp-deferred); v2 tracker has no BlueprintFormula entries (grep-verified, files untouched per batch convention); referenced classes: FormulaExpression + SwSystemconstDependentFormula (base chain, stamped R23-11), EcucDefinitionElement (ECUCParameterDefTemplate, stamped — ref-by-value only), MultiLanguageVerbatim (MultilanguageData, stamped), RefType (PrimitiveTypes, stamped), consumer VariationPoint (stamped, untouched) — no missing classes]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-26: target files 27/27 (test_BlueprintFormula.py 18 + test_parser_blueprint_formula.py 5 + test_writer_blueprint_formula.py 4), full suite `uv run python scripts/run_tests.py --no-coverage` 12076 passed / 0 failed (baseline 12049 + 27 net-new: +18 model spec-contract tests, +5 parser tests, +4 writer tests; integration round-trip included in the suite green); `uv run npm run lint` clean (flake8 E9,F63,F7,F82 + ruff E/F/W/I); `uv run npm run black` no-op (1244 files unchanged) + `black-check` clean; 9b by-eye checks done (fields↔spec both directions exact — exactly the 2 Table C.16 attribute rows; most-derived base SwSystemconstDependentFormula per the Base row + XSD complexType L9068; no fabrication/flattening; PDF-typed fields RefType/MultiLanguageVerbatim; member/accessor/checklist order = markdown displayed order ecuc→verbatim; XML element order N/A-as-sequence — XSD group L9023 is an unbounded choice, emitted in table row order ECUC-REF→VERBATIM then inherited SYSC refs; docstrings verbatim by diff — class Note + inline comments + getters + setters with None-no-op sentences, test-enforced; blank line between __init__ attribute blocks; PEP 526 annotated members; no `# type:`; reader+writer coverage for both attrs via the class's own helper pair, element-level test-pinned — dispatcher deliberately untouched, FORMAL-BLUEPRINT-CONDITION removed in R23-11; no open deviations; Rule 0007 leaf-package file location per spec Package row; marker withheld — batch instruction); 9b deferred to batch confirmation (user instruction)
- [ ] `FMConditionByFeaturesAndAttributes` — pure-text formula class — locate spec table at Step 1
  - Note: closure check at Step 1 (FeatureDef etc. may be missing; Rule 0016)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [ ] `FMConditionByFeaturesAndSwSystemconsts` — pure-text formula class — locate spec table at Step 1
  - Note: closure check at Step 1 (FeatureDef etc. may be missing; Rule 0016)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [ ] `FMFormulaByFeaturesAndAttributes` — pure-text formula class — locate spec table at Step 1
  - Note: closure check at Step 1 (FeatureDef etc. may be missing; Rule 0016)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)
- [ ] `FMFormulaByFeaturesAndSwSystemconsts` — pure-text formula class — locate spec table at Step 1
  - Note: closure check at Step 1 (FeatureDef etc. may be missing; Rule 0016)
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)

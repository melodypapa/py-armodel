# Sync Todo: CompuGenericMath

Phase 0 closure confirmed 2026-09-26 (user gate: "run the full 9-steps again and verify everything").
Source: `src/armodel/models/M2/MSR/AsamHdo/ComputationMethod.py` (leaf package, Rule 0007).
Model test: `tests/test_armodel/models/M2/MSR/AsamHdo/test_ComputationMethod.py` → `TestCompuGenericMath`.

## Queue

- [ ] `CompuGenericMath` — input · R23-11 markdown · AUTOSAR_CP_TPS_SoftwareComponentTemplate · Table 5.60, p.374
  - note (state at Phase 0, 2026-09-26): Group3.md row had been closed via the already-verified short-circuit with Steps 1–9 all `[ ]` while SyncTodoIndex.md/sync-report.md said `[ ] Pending; the `# Spec verified: R23-11` line was found removed from the working tree (uncommitted diff) — full 9-step re-sync ordered, stamp withheld until 9b.
  - note (Step 1 pre-findings): (1) checklist is the old 5-column format, no `release` column (Rule 0012.3 drift); (2) `level` inline `__init__` comment + getter + setter docstrings drop the spec `Tags: xml.attribute=true` suffix — not verbatim; (3) reader `arxml_parser.py` `readSwDataDependency` builds `ARLiteral()` for `level` while the spec type is `PrimitiveIdentifier` (model test uses `PrimitiveIdentifier`); (4) no parser-side round-trip test — only a stale `tests/test_armodel/parser/__pycache__/test_sw_data_dependency*.pyc`, writer test `test_writer_sw_data_def_props.py` exists.
  - [x] Step 1 — Sync members & description from spec [Class `<<atpMixedString>> CompuGenericMath`, Package M2::MSR::AsamHdo::ComputationMethod (file location ✓ Rule 0007, commit 1de6c0ef2); Note verbatim "This meta-class represents the ability to specify a generic formula expression."; Base ARObject,FormulaExpression → model derives FormulaExpression (ARObject transitive, most-derived ✓); 1 attribute row in displayed order: `level` | PrimitiveIdentifier | 0..1 | attr | "Placeholder to describe an indicator of a language level for the mathematics e.g. INFORMAL, ASAMHDO. May be refined by particular use-cases. Tags: xml.attribute=true"; page p.374 via pdf_page.py; XSD cross-check (Rule 0015) AUTOSAR_00052.xsd COMPU-GENERIC-MATH l.21400/21409/21419: attributeGroup = exactly LEVEL typed PRIMITIVE-IDENTIFIER--SIMPLE (no XSD-only attrs), stereotypes atpMixedString+atpObject, complexType mixed="true", sequence empty, FORMULA-EXPRESSION associations atpDerived-skipped (no child elements); consumer SwDataDependency.swDataDependencyFormula (Table 5.58, stamped). Gaps → later steps: G1 reader builds ARLiteral() for LEVEL, spec type PrimitiveIdentifier (Step 6); G2 mixed text not round-tripped — reader lacks readMixedStringText, writer lacks element.text, sibling formula classes do both (Step 5/6); G3 reader omits readARObject (S/T) while writer calls writeARObject — asymmetric (Step 6); G4 level comment/getter/setter drop Tags: xml.attribute=true suffix — not verbatim (Step 4); G5 checklist old 5-column, no release column (Step 7)]
  - [x] Step 2 — Write model class unit test (Red) [TestCompuGenericMath extended: CLASS_NOTE/LEVEL_NOTE verbatim constants; class Note == spec Note; `__init__.__doc__ is None`; base chain CompuGenericMath→FormulaExpression→ARObject; PEP 526 declaration via getsource (no `# type:`); inline-comment verbatim; getter/setter docstring verbatim (+None-no-op sentence); defaults; round-trip incl. isinstance PrimitiveIdentifier; None no-op. Red confirmed honestly: 3 failed (inline comment, getter, setter — all missing the `Tags: xml.attribute=true` tail), 7 passed; the 3 reds are Step-4 docstring work, per the GeneralAnnotation pattern. FormulaExpression import added (isort order).]
  - [x] Step 3 — Implement model class (Green) [verified already spec-compliant — no code change: most-derived base FormulaExpression (ARObject transitive; XSD abstract="false" → concrete, no ABC guard); single own member `level: Optional[PrimitiveIdentifier] = None` PEP 526 under its Note comment, blank line separated; guarded setter + `return self` + None-no-op; Kind attr → no Ref suffix; no fabricated/flattened members (inherited atpReferences/atpStringReferences live in FormulaExpression's own table); `from __future__ import annotations` makes the `-> CompuGenericMath` self-reference safe. Green evidence: whole model file 66 passed / 3 failed — the 3 remaining reds are the Step-4 docstring verbatim gaps]
  - [x] Step 4 — Sync docstrings (wipe + rewrite) [wipe first: class docstring, inline `__init__` comment, getter and setter docstrings all removed (verified `__doc__ is None` on all four) — checklist block + code untouched; then rewritten from the markdown Table 5.60 cells: class docstring = class Note single-line (file convention); inline comment = attribute Note incl. `Tags: xml.attribute=true`; getter = attribute Note verbatim; setter = attribute Note verbatim + " A None value is a no-op and does not overwrite an existing level."; `__init__` carries no docstring. Verified by diff against the markdown cell (Rule 0012.2.5.6): all four exact, 10/10 TestCompuGenericMath pass. Note for 9b: Rule 0012.2.5.2's parenthetical "(drop Stereotypes:/Tags: tail)" reads against the Rule 0012 overview + SKILL 9b checklist ("inline comment + getter + setter must be the spec Note copied verbatim") and against both 9b-confirmed precedents in this repo (GeneralAnnotation, CompuMethod — Tags present in all three); resolved to verbatim-everywhere, single-line setter join matching the sibling BlueprintFormula shape.]
  - [x] Step 5 — Write reader/writer round-trip test (Red) [New `tests/test_armodel/parser/test_sw_data_dependency.py` (`TestReadSwDataDependency`, 5 tests: level type+text, formula without level, S/T checksum/timestamp, empty `SW-DATA-DEPENDENCY`, absent dependency — wrapper = `SW-DATA-DEF-PROPS-CONDITIONAL` matching call site parser:6701; asserts field values incl. `isinstance(..., PrimitiveIdentifier)`, not just not-None) + new `TestSwDataDependencyFormulaRoundTrip` class in `tests/test_armodel/writer/test_writer_sw_data_def_props.py` (4 tests: level+text+S/T raw-XML and re-read round trip, no-level formula, no-text formula, props-without-dependency empty case; raw XML asserted via `ET.parse` on the saved file). Honest Red: **5 failed / 8 passed** — failures are exactly G1 (`ARLiteral` not `PrimitiveIdentifier`), G2 (mixedString None: reader + writer), G3 (`getChecksum()` None); the 8 passing are the empty/absent/no-text consistency cases + pre-existing round trips.]
  - [x] Step 6 — Update parser & writer (Green) [Parser `readSwDataDependency` (parser:6737): added `readARObject` on `SW-DATA-DEPENDENCY`, `SW-DATA-DEPENDENCY-FORMULA` and `SW-DATA-DEPENDENCY-ARGS` elements (G3 — writer calls `writeARObject` on all three), added strip-guarded `readMixedStringText` on the formula element (G2, sibling pattern from `readFMConditionByFeaturesAndAttributes`), LEVEL now `PrimitiveIdentifier().setValue(level)` instead of `ARLiteral()` (G1). Writer `setSwDataDependency` (writer:3246): writes `formula_element.text = getMixedString()` when not None (G2, sibling pattern from `writeBlueprintFormula`). Tests: 5 Red → 5 Green; targeted files 82 passed; full suite `python scripts/run_tests.py --no-coverage` = **12268 passed / 0 failed** (11 integration round-trips included); `npm run lint` clean; `npm run black` applied to the new parser test, `black-check` clean. Note: `readARObject` added to dependency/args elements for writer symmetry; the proxy sub-readers (`readSwCalprmRefProxy`/`readSwVariableRefProxy`) still lack the writer's `writeARObject` — out of this class's closure, recorded as a pre-existing sibling asymmetry, not fixed here.]
  - [x] Step 7 — Update checklist comment [6-column format per Rule 0002/0012 (G5): header now `# Columns: impl / docstring / test / reader / writer / release   ([—] = no XML element)`; each of the 3 rows (source order: `__init__`, `getLevel`, `setLevel`) carries release `R23-11`; reader/writer flags per the GeneralAnnotation convention — `__init__` `[—]/[—]`, getter `[—] reader / [x] writer` (writer reads LEVEL via getter), setter `[x] reader / [—] writer` (reader populates via setter). `# Spec:` line + rows only — the `# Spec verified: R23-11` marker deliberately NOT written here (Rule 0002: marker belongs to Step 9b).]
  - [x] Step 8 — Deviations — **none**; deviation-tracker entry added to `docs/examples/method_deviation_by_class.md` (Rule 0014 `— *(no deviation)*` row + sync Note: Table 5.60/p.374, XSD 00052 exact agreement per Rule 0015, G1–G5 all closed, Tags-tail rule-tension resolution, consumer `SwDataDependency` Table 5.58 stamped, stamp deferred to 9b). Step-3 referenced classes: none missing — Phase-0 closure was fully user-confirmed (`FormulaExpression`, `ARObject`, `PrimitiveIdentifier` all exist; queue = this class only). Out-of-closure observation recorded (not fixed): proxy sub-readers lack the writer's `writeARObject` — separate helper parity, unrelated to Table 5.60. No placeholder text remains in the class, so nothing blocks the 9b stamp.
  - [x] Step 9 — Verify (9a) + confirm (9b) [9a: full suite `python scripts/run_tests.py --no-coverage` **12268 passed / 0 failed** (11 integration round-trips incl. lossless SW-DATA-DEPENDENCY round trip); flake8 (E9,F63,F7,F82) + `ruff check` clean; `black-check` 1259 files unchanged; set-based script: checklist == methods == {__init__, getLevel, setLevel}, no untested methods, no placeholder, marker correctly absent pre-9b; programmatic verbatim diff vs markdown cell: class/inline/getter/setter all exact + `__init__.__doc__ is None`. 9b: full Rule 0006.1 checklist presented (14 items: kind/membership, most-derived base, no fabrication incl. G1 `PrimitiveIdentifier` type fix, naming, create/set/add shape, reader+writer coverage w/ matched `readMixedStringText`↔`writeMixedStringText` + `readARObject`↔`writeARObject` pairs, member order, docstrings verbatim-by-diff w/ Tags-tail + single-line-setter decisions, PEP 526/no `# type:`, Rule 0008 spacing, Rule 0007 location + runtime `armodel.CompuGenericMath` resolves + not in exclusion sets, Rule 0014 no deviations, 0012.1 stamp eligibility, 0002 release provenance R23-11) — **user-confirmed 2026-09-26 ("Confirm — stamp it")**; marker `# Spec verified: R23-11` written to the class block. Note: writer switched from inline text assignment to `writeMixedStringText` call during 9b prep for strict Rule 0013.2 matched-pair compliance (behaviorally identical; tests re-run green after the change).]

## Closure (confirmed)

| Class | Role | Spec | State |
|---|---|---|---|
| `CompuGenericMath` | input | CP SWCT Table 5.60, p.374 (R23-11) | queued |
| `FormulaExpression` | base (`Base` row: ARObject, FormulaExpression) | FO FMXF Table C.5, pp.73-74 | stamped `# Spec verified: R23-11` — skipped per Rule 0016.5 |
| `ARObject` | base (transitive) | Group1 | stamped `# Spec verified: R23-11` — skipped |
| `PrimitiveIdentifier` | member (`level`, Kind `attr`) | FO GST Table 4.58, p.112 | stamped `# Spec verified: R23-11` — skipped |

Consumer context (not in closure): `SwDataDependency` (Table 5.58, p.374, stamped) — owns the `readSwDataDependency` / `setSwDataDependency` reader/writer coverage for `SW-DATA-DEPENDENCY-FORMULA`.

## Decisions

- Skip / derive-from-XSD: none — no missing classes; all three non-input closure classes exist and are stamped.

# Sync todo: Group 12 — SwcInternalBehavior & measurement refs

Input: full-repo orphan audit 2026-09-23 (unstamped ∧ untracked, M2 only) · Queue order = row order
(resume = first class row still `[ ]`; all class rows `[x]` = sync finished — Rule 0017.3)
> **Rule — already-verified short-circuit (added 2026-09-04):** before running the 9-step
> sync for a row, check whether the class already carries `# Spec verified: <RELEASE>` or
> `# XSD verified: <xsd-file>` in its own class body (verify the marker in the source — a
> row in this todo is not proof). If it does **and** a quick deviation check finds nothing
> new (base vs the spec `Base` closure, member types vs its table, verbatim docstrings,
> reader/writer coverage, checklist shape per Rules 0002/0012), **skip the 9 steps**: mark
> the row `- [x] <Class> — already verified (<marker>, <file>)` and move on. If the check
> finds a new deviation, do **not** mark it verified — keep the row queued, run the steps
> for the deviation only, and record it in Step 8 (Rule 0012.3: an existing marker is not
> proof).

## Queue (dependency-first)

- [ ] `ParameterAccess` — AbstractAccessPoint — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.40, p.586
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/DataElements/__init__.py
  - note (Step 1): Base chain ARObject/AbstractAccessPoint/AtpClassifier/AtpFeature/AtpStructureElement/Identifiable/MultilanguageReferrable/Referrable — most-derived base AbstractAccessPoint (stamped `# Spec verified: R23-11`, BSWModuleDescriptionTemplate Table 4.24, p.57 — no blocker). Attrs (displayed order): accessedParameter (AutosarParameterRef, 0..1, aggr), swDataDefProps (SwDataDefProps, 0..1, aggr). BSW appendix Table D.48 verified identical reproduction. XSD PARAMETER-ACCESS group (AUTOSAR_00052.xsd:87809) element order ACCESSED-PARAMETER, SW-DATA-DEF-PROPS, VARIATION-POINT; class docstring Note renders "ParameterData Prototype" in markdown (line-wrap artifact; XSD documentation + VariableAccess precedent join it as "ParameterDataPrototype"). Existing reader/writer drop SW-DATA-DEF-PROPS (genuine Red pending).
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red via None-no-op + get_type_hints pins; `test_initialization` passed immediately — defaults were already correct)
  - [x] Step 3 — Implement model class (Green)
  - [x] Step 4 — Sync docstrings (wipe + rewrite)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: Red on all three SW-DATA-DEF-PROPS gaps — reader, writer, populated round-trip; empty round-trip + optional-children-omitted passed immediately, already correct)
  - [x] Step 6 — Update parser & writer (Green)
  - [x] Step 7 — Update checklist comment
  - note (Step 8): no in-scope deviations remain (both Table 7.40 attributes modeled, typed `Optional[T]`, reader+writer covered; no naming/type/missing rows). Observations (out of scope, not blockers): (1) `AbstractAccessPoint.returnValueProvision` (RETURN-VALUE-PROVISION) has no reader/writer coverage anywhere — inherited-base gap (Rule 0001.7) for a future AbstractAccessPoint drift pass; no fixture carries the element. (2) `RunnableEntity.getParameterAccesses` still filters the `elements` registry (`sorted(filter(...))`) although `self.parameterAccesses` exists (Rule 0004 to-fix shape) — aggregator row, not this class. (3) Writer emits VARIATION-POINT via `writeIdentifiable` (IDENTIFIABLE-group position) rather than the XSD group tail position — generic pre-existing behavior, round-trip lossless. (4) Adding `from __future__ import annotations` (bare-name pins, Rule 0003/0005) made this module a PEP 563 module and tripped `test_no_top_level_quoted_annotations_in_pep563_modules`: 6 pre-existing top-level quoted return annotations in `VariableAccess` (setAccessedVariableRef, setScope) and `ArVariableInImplementationDataInstanceRef` (4 setters) were mechanically unquoted to bare same-module names to keep the suite green — no runtime behavior change, row 2's spec state untouched (nested quotes left for row 2's own pass).
  - [x] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-24: touched tests 467 passed (model DataElements/test___init__.py + parser/test_arxml_parser_orchestrators.py + writer/test_writer_swc_behavior.py); full suite 11534 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11527 + 7 new); `uv run ruff check src tests scripts` clean; black clean on all 6 touched files (23 unrelated pre-existing files at HEAD fail repo-wide black --check — not touched); set-based checklist==methods + coverage audit OK; verbatim docstring/`# type:`/blank-line/export/get_type_hints pin audits OK; integration round-trip 11/11. 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `VariableAccess` — AbstractAccessPoint — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.33, p.567
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/DataElements/__init__.py
  - note (Step 1): Base chain ARObject/AbstractAccessPoint/AtpClassifier/AtpFeature/AtpStructureElement/Identifiable/MultilanguageReferrable/Referrable — most-derived base AbstractAccessPoint (stamped `# Spec verified: R23-11`; current `(AbstractAccessPoint, VariationPointCapable)` shape already matches the ParameterAccess convention). Attrs (displayed order): accessedVariable (AutosarVariableRef, 0..1, aggr; XSD mmt.qualifiedName "VariableAccess.accessedVariable" — existing field/accessors `accessedVariableRef`/`getAccessedVariableRef`/`setAccessedVariableRef` are a Rule 0001.5 naming mismatch → rename to accessedVariable/getAccessedVariable/setAccessedVariable, consumers parser:6662, writer:1728/3855, cli/arxml_dump_cli.py), scope (VariableAccessScopeEnum, 0..1 — enum class does not exist in the codebase; Rule 0001.10 placeholder `Optional[ARLiteral]` kept, reported at Step 8). Class Note renders "VariableData Prototype" in markdown (line-wrap artifact; XSD documentation joins it as "VariableDataPrototype"). BSW appendix D.78 + TimingExtensions D.67 verified identical; SystemTemplate F.142 is heading-only (no table body). XSD VARIABLE-ACCESS group (AUTOSAR_00052.xsd:129453) element order ACCESSED-VARIABLE, SCOPE, VARIATION-POINT. Consume-path gaps (genuine Red pending): writer `writeVariableAccess` drops SCOPE; parser `readVariableAccesses` (all 7 RunnableEntity roles) never reads SCOPE; `readVariableAccess` + `writeReceiverReplaceWith` already cover both elements.
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red via the Rule 0001.5 rename — `test_initialization` (no `accessedVariable` field) and `test_get_set_accessedVariable` (no `setAccessedVariable`) failed with AttributeError; `test_get_set_scope` passed immediately — scope accessors were already correct with None no-op + matching hints)
  - [x] Step 3 — Implement model class (Green) (note: rename tail per Rule 0001.5 — consumers switched to `accessedVariable`/`getAccessedVariable`/`setAccessedVariable`: parser dispatch ×7 + `readVariableAccess`, writer `writeReceiverReplaceWith` + `writeVariableAccess`, cli/arxml_dump_cli.py, one pre-existing writer test; BswVariableAccess's own `ACCESSED-VARIABLE-REF` pair untouched)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) (note: class docstring = verbatim joined Note ("VariableDataPrototype" per XSD); inline `__init__` comments verbatim; getter docstrings verbatim Note; setter docstrings verbatim Note + None-no-op sentence; old "Gets/Sets the…" paraphrases removed)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: Red on exactly the three SCOPE gaps — parser `readVariableAccesses` dispatch (`test_readRunnableEntity_with_dataReadAccess_full`), writer `writeVariableAccess` (`test_writeVariableAccess_full`), populated round-trip (`test_round_trip_populated`); `test_writeVariableAccess_optional_children_omitted` + `test_round_trip_empty` passed immediately — already correct)
  - [x] Step 6 — Update parser & writer (Green) (note: `readVariableAccesses` now delegates each created access to `readVariableAccess` (Identifiable + ACCESSED-VARIABLE + SCOPE, read exactly once — 7 duplicated `setAccessedVariable` lines removed); `writeVariableAccess` now emits SCOPE after ACCESSED-VARIABLE per XSD group order)
  - [x] Step 7 — Update checklist comment (note: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 7.33, p.567` + 5 method rows in source order, 6-column format with release R23-11; marker withheld per batch instruction — see Step 8)
  - note (Step 8): one Rule 0001.10 placeholder remains — attribute `scope` is spec-typed `VariableAccessScopeEnum` (Table 7.34, same package M2::...::SwcInternalBehavior::DataElements; XSD VARIABLE-ACCESS-SCOPE-ENUM literals COMMUNICATION-INTER-ECU / COMMUNICATION-INTRA-PARTITION / INTER-PARTITION-INTRA-ECU) but the enum class does not exist in the codebase; per the Rule 0001.10 workflow relaxation the field stays `Optional[ARLiteral]` (XML text round-trips losslessly either way) and is reported here for a future pass. No other deviations: both Table 7.33 attributes modeled (`accessedVariable` renamed per Rule 0001.5, `scope` verbatim), field/getter/setter/parser/writer types agree, reader+writer covered for both, no naming/type/missing rows, no tracker entries for this class. Stamp decision: `# Spec verified:` withheld while the placeholder remains (Rule 0012.1) and per batch instruction (stamp deferred to batch confirmation). Observation (out of scope, not blockers): member types `AutosarVariableRef` and `ArVariableInImplementationDataInstanceRef` live in this module unstamped with all-`[ ]` checklists (orphan intake; `AutosarVariableRef` fields are bare-typed, setters lack None no-ops) — candidates for a future row; `BswVariableAccess` (BswBehavior.py) is a distinct class with its own `ACCESSED-VARIABLE-REF` pair, untouched.
  - [x] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: touched tests 473 passed (model DataElements/test___init__.py + parser/test_arxml_parser_orchestrators.py + writer/test_writer_swc_behavior.py) + test_runnable_entity.py 6 passed; full suite 11541 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11534 + 7 net new); integration round-trip 11/11; `uv run ruff check src tests scripts` clean; black clean on all 8 touched files (repo-wide black --check NOT run — 23 unrelated pre-existing files at HEAD would fail; untouched); set-based checklist==methods + coverage audit OK; plain get_type_hints pin audit OK; bare-annotation / no-`# type:` / blank-line / verbatim-docstring / member-order / top-level-export audits OK. 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `InternalTriggeringPoint` — AbstractAccessPoint — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.30, p.561
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/Trigger.py
  - note (Step 1): Base chain ARObject/AbstractAccessPoint/AtpClassifier/AtpFeature/AtpStructureElement/Identifiable/MultilanguageReferrable/Referrable — most-derived base AbstractAccessPoint (stamped `# Spec verified: R23-11`; current `(AbstractAccessPoint, VariationPointCapable)` shape matches the ParameterAccess/VariableAccess convention; XSD complexType chain ends ABSTRACT-ACCESS-POINT → INTERNAL-TRIGGERING-POINT). Attrs (displayed order): swImplPolicy (SwImplPolicyEnum, 0..1, attr — sole attribute; member type exists as a synced AREnum, Table 5.45 p.336). BSW appendix D.39 + R4.3.1 Table 7.31 verified reproductions. Class Note + constr_1182 (STANDARD/QUEUED allowed values); attribute Note "This attribute, when set to value queued, allows for a queued processing of Triggers." Existing field bare-typed (`SwImplPolicyEnum = None`, Rule 0001.4/0003 → needs `Optional[SwImplPolicyEnum]`). XSD group element order SW-IMPL-POLICY, VARIATION-POINT. Consume-path gaps (genuine Red pending): parser `readRunnableEntityInternalTriggeringPoints` writes phantom `point.sw_impl_policy` (dropped) and skips readIdentifiable; writer has NO RunnableEntity INTERNAL-TRIGGERING-POINTS emission.
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red via the Rule 0001.4/0003 annotation drift — get_type_hints pins fail on bare `SwImplPolicyEnum` setter param / bare getter return / missing setter `-> InternalTriggeringPoint`; `test_initialization`, the None-no-op round-trip and the inherited returnValueProvision accessor passed immediately — behavior was already correct)
  - [x] Step 3 — Implement model class (Green) (note: `swImplPolicy` field/getter/setter re-typed `Optional[SwImplPolicyEnum]`, setter annotated `-> InternalTriggeringPoint`; touched test file 6 passed)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) (note: fabricated class docstring replaced with the verbatim Table 7.30 Note + constr_1182; old "Gets/Sets the…" paraphrases wiped — getter docstring = Note verbatim, setter = Note verbatim + None-no-op sentence, inline `__init__` comment = Note verbatim; `__init__` carries no docstring)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: Red on all 8 new tests — parser SW-IMPL-POLICY read (phantom `sw_impl_policy` field), 5 writer helper tests (no `writeRunnableEntityInternalTriggeringPoints`/`writeInternalTriggeringPoint` exist), populated round-trip and empty round-trip (wrapper dropped entirely so the point itself never reloads))
  - [x] Step 6 — Update parser & writer (Green) (note: parser — `readRunnableEntityInternalTriggeringPoints` now dispatches per child tag and delegates to new `readInternalTriggeringPoint` (readIdentifiable + SW-IMPL-POLICY via the existing `SW_IMPL_POLICY_XML_MAP` camel↔UPPER tokens, `SwImplPolicyEnum` instance stored — phantom `sw_impl_policy` line removed); writer — new `writeInternalTriggeringPoint` (writeIdentifiable + SW-IMPL-POLICY, wrapper `INTERNAL-TRIGGERING-POINTS` only when non-empty) and `writeRunnableEntityInternalTriggeringPoints`, called in `writeRunnableEntity` right after EXTERNAL-TRIGGERING-POINTS per XSD relative order; writer test accesses the filter-shaped aggregator getter via `list(...)` — getter shape itself left untouched (aggregator observation, Step 8))
  - [x] Step 7 — Update checklist comment (note: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 7.30, p.561` + 3 method rows in source order (`__init__`, getSwImplPolicy, setSwImplPolicy), 6-column format with release R23-11; set-based checklist==methods + coverage audit OK; marker withheld per batch instruction — see Step 8)
  - [x] Step 8 — Deviations
  - note (Step 8): no in-scope deviations remain (sole Table 7.30 attribute swImplPolicy modeled with field + accessors typed `Optional[SwImplPolicyEnum]` per Mult. 0..1, member type = synced `SwImplPolicyEnum` Table 5.45 — no Rule 0001.10 placeholder, no naming/type/missing rows, reader+writer covered, no tracker entries for this class). Stamp decision: no deviation remains, so the stamp would be warranted at 9b — `# Spec verified:` withheld per batch instruction (stamp deferred to batch confirmation, user instruction 2026-09-24). Observations (out of scope, not blockers): (1) inherited `AbstractAccessPoint.returnValueProvision` (RETURN-VALUE-PROVISION) still has no reader/writer coverage anywhere — the AbstractAccessPoint inherited-base gap already recorded on the ParameterAccess row; applies equally here (Rule 0001.7, future AbstractAccessPoint drift pass). (2) Aggregator `RunnableEntity.getInternalTriggeringPoints` returns a bare `filter` object, not a `List` (aggregator shape; tests use `list(...)`; same family as the `getParameterAccesses` observation on the ParameterAccess row). (3) Pre-existing `writeRunnableEntity` wrapper order (MODE-ACCESS/MODE-SWITCH before EXTERNAL-TRIGGERING) does not match XSD order — generic pre-existing behavior, round-trip lossless (path-based findall); the new INTERNAL-TRIGGERING-POINTS emission keeps the XSD EXTERNAL→INTERNAL relative order.
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: touched tests 612 passed (model test_Trigger.py 6 + full SwcInternalBehavior model dir + parser/test_arxml_parser_orchestrators.py 285 + writer/test_writer_swc_behavior.py 196); full suite 11551 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11541 + 10 net new; integration round-trip 11/11 included); `uv run ruff check src tests scripts` clean; black clean on all 6 touched Python files (parser import line exploded past 200 chars by black after adding InternalTriggeringPoint — order preserved, not a re-sort; repo-wide black NOT run — unrelated pre-existing failures at HEAD); set-based checklist==methods + coverage audit OK; plain get_type_hints pin audit OK (setter value/return + getter return); verbatim docstring diff audit OK (class Note, constr_1182, attribute Note x3, None-no-op sentence); no-`# type:` / blank-line / member-order / top-level-export (`armodel.InternalTriggeringPoint`) audits OK. 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `ModeAccessPoint` — ARObject — R23-11 CP_TPS_SoftwareComponentTemplate Table 9.5, p.634
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/ModeDeclarationGroup.py
  - note (Step 1): Table 9.5 confirmed Class; Base = ARObject most-derived (XSD complexType MODE-ACCESS-POINT = AR-OBJECT group + MODE-ACCESS-POINT group, attributeGroup AR-OBJECT only; current `(ARObject, VariationPointCapable)` keeps the framework VARIATION-POINT group element per sibling convention — not a Table 9.5 Attribute row, Rule 0015; NOT the AbstractAccessPoint base of sibling row ModeSwitchPoint). Attrs (displayed order): ident (ModeAccessPointIdent, 0..1, aggr, xml.sequenceOffset=-100 — member type exists & stamped Table 14.5 p.852, IdentCaption → Identifiable chain ⇒ createXxx shape, so existing `setIdent` is a Rule 0001.6 violation to remove; consumers = 2 test files only), modeGroup (ModeDeclarationGroupPrototype, 0..1, iref — "InstanceRef implemented by: ModeGroupInAtomicSwcInstanceRef"; XSD MODE-GROUP-IREF = nested wrapper with choice of P-/R-MODE-GROUP-IN-ATOMIC-SWC-INSTANCE-REF ⇒ polymorphic shape; field naming `modeGroupIRef` verbatim-correct but bare-typed to the R variant only ⇒ retype `Optional[ModeGroupInAtomicSwcInstanceRef]`). Class Note renders "ModeDeclaration GroupPrototype's" in markdown (line-wrap artifact; XSD documentation joins it as "ModeDeclarationGroupPrototype's"). R4.3.1 Table 9.7 verified identical reproduction. Reader/writer: full coverage already exists on both sides (readARObject/writeARObject + dedicated IDENT helpers + shared polymorphic getModeGroupIRef/setModeGroupIRef; element order IDENT → MODE-GROUP-IREF matches XSD) — genuine Red is model-side only (bare annotations, missing None no-op, setIdent removal).
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red on 3 — `test_get_set_modeGroupIRef` (setModeGroupIRef(None) not a no-op), `test_get_type_hints` (accessors untyped ⇒ hints {}), `test_ident_is_created_not_set` (setIdent still exists); `test_initialization` + `test_create_ident` passed immediately — defaults and createIdent behavior were already correct)
  - [x] Step 3 — Implement model class (Green) (note: `ident`/`modeGroupIRef` re-typed `Optional[ModeAccessPointIdent]` / `Optional[ModeGroupInAtomicSwcInstanceRef]` (polymorphic — spec "InstanceRef implemented by: ModeGroupInAtomicSwcInstanceRef"; R-only typing was Rule 0001.3 drift); `setIdent` removed per Rule 0001.6 (0..1 Referrable child ⇒ createIdent short_name factory); `setModeGroupIRef` gained the None no-op + `-> ModeAccessPoint` chaining; `from __future__ import annotations` added and the 2 pre-existing quoted `-> "IncludedModeDeclarationGroupSet"` returns mechanically unquoted (PEP 563 guard, ParameterAccess-row precedent); `ModeGroupInAtomicSwcInstanceRef` added to the existing InstanceRefs import line (no re-sort); test-side `setIdent` consumers switched to `createIdent` (parser orchestrators, writer swc_behavior) + now-unused `ModeAccessPointIdent` import dropped)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) (note: old paraphrase docstrings ("Creates/Gets/Sets the…") wiped — class docstring = Table 9.5 Note verbatim with the markdown "ModeDeclaration GroupPrototype's" wrap joined per XSD documentation (ParameterAccess/VariableAccess precedent); `ident` Note verbatim on the inline `__init__` comment + createIdent + getIdent docstrings (stereotypes/tags tail dropped); `modeGroup` Note verbatim on the inline comment + getModeGroupIRef; setModeGroupIRef = Note verbatim + appended None-no-op sentence; `__init__` carries no docstring)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: NO genuine Red — all 5 new tests passed immediately (reader/writer coverage was already complete per the Step 1 consume-path grep; no dropped fields). New coverage per Rule 0006 asserting field values, not just `len`: parser `test_readRunnableEntityModeAccessPoints_with_mode_group_iref` (polymorphic P/R inner refs + DEST attrs) and `..._round_trip_populated` (ident short name + CONTEXT-R-PORT-REF/TARGET-MODE-GROUP-REF values + DESTs); writer `..._rmode_values` / `..._pmode_values` (emitted element tree + DEST/text) and `..._optional_children_omitted` (empty point ⇒ no IDENT, no MODE-GROUP-IREF children); empty-wrapper case pre-existing (`..._empty`))
  - [x] Step 6 — Update parser & writer (Green) (note: NO changes needed — reader and writer already cover both Table 9.5 attributes end-to-end: parser `readModeAccessPoint` (readARObject → IDENT via createIdent/readModeAccessPointIdent → MODE-GROUP-IREF via shared polymorphic `getModeGroupIRef`), writer `writeModeAccessPoint` (writeARObject → writeModeAccessPointIdent → shared `setModeGroupIRef`); element order IDENT → MODE-GROUP-IREF matches the XSD MODE-ACCESS-POINT group (sequenceOffset -100 then default); verified by the Step 5 tests instead of edits)
  - [x] Step 7 — Update checklist comment (note: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 9.5, p.634` + 5 method rows in source order (`__init__`, createIdent, getIdent, getModeGroupIRef, setModeGroupIRef), 6-column format with release R23-11; reader [x] on mutator rows (createIdent/setModeGroupIRef — parser call sites grepped), writer [x] on getter rows (getIdent/getModeGroupIRef — writer call sites grepped); set-based checklist==methods + coverage audit OK; marker withheld per batch instruction — see Step 8)
  - [x] Step 8 — Deviations
  - note (Step 8): no in-scope deviations remain (both Table 9.5 attributes modeled — ident via createIdent/getIdent typed `Optional[ModeAccessPointIdent]` per Mult. 0..1 with the member type stamped (Table 14.5, p.852); modeGroup via `modeGroupIRef` get/set typed `Optional[ModeGroupInAtomicSwcInstanceRef]` matching the XSD P/R choice and the spec "InstanceRef implemented by: ModeGroupInAtomicSwcInstanceRef"; the Rule 0001.6 to-fix (`setIdent` on a 0..1 Referrable child) was fixed by removal, no naming/type/missing rows; no tracker entries for this class). Stamp decision: no deviation remains, so the stamp would be warranted at 9b — `# Spec verified:` withheld per batch instruction (stamp deferred to batch confirmation, user instruction 2026-09-24). Observations (out of scope, not blockers): (1) the XSD MODE-ACCESS-POINT group also carries a framework VARIATION-POINT element (atpVariation-generated, sequenceOffset 10000) with no reader/writer coverage — framework-level `variationPoint` excluded per Rule 0014 conventions (same family as the AbstractAccessPoint.returnValueProvision gap recorded on earlier rows); no integration fixture carries MODE-ACCESS-POINT. (2) Member types `ModeGroupInAtomicSwcInstanceRef` / `PModeGroupInAtomicSwcInstanceRef` / `RModeGroupInAtomicSWCInstanceRef` (Components/InstanceRefs.py) are unstamped orphans with all-`[ ]` checklists — candidates for future rows; the R-variant's SWC-uppercase class name is a naming question for its own pass.
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: touched tests 488 passed (model test_ModeDeclarationGroup.py + parser/test_arxml_parser_orchestrators.py + writer/test_writer_swc_behavior.py; 621 including the full SwcInternalBehavior model dir); full suite 11560 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11551 + 9 net new; integration round-trip 11/11 included); `uv run ruff check src tests scripts` clean; black clean on all 4 touched files (`--check`; repo-wide black NOT run — unrelated pre-existing failures at HEAD); set-based checklist==methods + coverage audit OK; plain get_type_hints pin audit OK (setter value/return + getter returns + createIdent); mechanical verbatim docstring diff OK (class Note with wrap joined, ident Note ×3 sites, modeGroup Note ×3 sites, None-no-op sentence); no-`# type:` / blank-line / member-order / top-level-export (`armodel.ModeAccessPoint`) audits OK; no `# Spec verified:` marker present (withheld). 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `ModeSwitchPoint` — AbstractAccessPoint — R23-11 CP_TPS_SoftwareComponentTemplate Table 9.4, p.633
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/ModeDeclarationGroup.py
  - note (Step 1): row-header base AbstractAccessPoint VERIFIED correct for this class (unlike ModeAccessPoint) — Table 9.4 Base row ARObject/AbstractAccessPoint/AtpClassifier/AtpFeature/AtpStructureElement/Identifiable/MultilanguageReferrable/Referrable AND XSD complexType MODE-SWITCH-POINT (AUTOSAR_00052.xsd:83008) group chain AR-OBJECT → REFERRABLE → MULTILANGUAGE-REFERRABLE → IDENTIFIABLE → ATP-CLASSIFIER → ATP-FEATURE → ATP-STRUCTURE-ELEMENT → ABSTRACT-ACCESS-POINT → MODE-SWITCH-POINT (attributeGroups AR-OBJECT + IDENTIFIABLE); most-derived base AbstractAccessPoint, current `(AbstractAccessPoint, VariationPointCapable)` matches sibling convention (mixin covers the framework VARIATION-POINT element of the MODE-SWITCH-POINT group, Rule 0015). Sole attr (displayed order): modeGroup (ModeDeclarationGroupPrototype, 0..1, iref, "InstanceRef implemented by: PModeGroupInAtomicSwcInstanceRef" — fixed-concrete P variant per the XSD MODE-GROUP-IREF type, NOT the ModeAccessPoint polymorphic choice ⇒ flat read/write; field name `modeGroupIRef` verbatim-correct, bare-typed + untyped accessors + missing None no-op = drift). Class Note markdown wraps "ModeDeclarationGroup Prototype" / "PModeGroupInAtomic SwcInstanceRef" joined per XSD qualified names; class-level constr_1778 (identical returnValueProvision across modeSwitchPoints referring to the same modeGroup) appends to the class docstring (constr_1182 precedent). BSW appendix D.41 + R4.3.1 Table 9.4 (p.659) verified identical reproductions. Consume path: MODE-GROUP-IREF covered flat on both sides (CONTEXT-P-PORT-REF + TARGET-MODE-GROUP-REF), but reader `readModeSwitchPoint` calls readARObject while every AbstractAccessPoint sibling (readParameterAccess/readVariableAccess/readInternalTriggeringPoint) calls readIdentifiable and the writer's writeModeSwitchPoint emits the full Identifiable group ⇒ UUID/LONG-NAME/DESC/CATEGORY/ANNOTATIONS/ADMIN-DATA/VARIATION-POINT dropped on read-back (genuine Red pending). No integration fixture carries MODE-SWITCH-POINT.
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red via get_type_hints pins (accessors untyped ⇒ hints {}) + setModeGroupIRef(None) not a no-op; `test_initialization` passed immediately — defaults incl. inherited returnValueProvision were already correct; old single mixed-shape test replaced with the mirrored ModeAccessPoint style)
  - [x] Step 3 — Implement model class (Green) (note: `modeGroupIRef` field/getter/setter typed `Optional[PModeGroupInAtomicSwcInstanceRef]` (fixed-concrete P variant per Table 9.4 "InstanceRef implemented by" + XSD MODE-GROUP-IREF type — the P-only typing itself was already spec-correct, unlike the ModeAccessPoint polymorphic case); setter gained the None no-op + `-> ModeSwitchPoint` chaining; touched test file 10 passed)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) (note: fabricated class docstring + old "Gets/Sets the…" paraphrases wiped — class docstring = Table 9.4 Note verbatim + class-level constr_1778 appended (constr_1182 precedent); attribute Note verbatim ("The mode declaration group that is switched by this runnable. InstanceRef implemented by: PModeGroupInAtomicSwcInstanceRef" — markdown wrap joined per XSD qualified name) on the inline `__init__` comment + getter; setter = Note verbatim + None-no-op sentence; `__init__` carries no docstring; 10 passed)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: Red on 2 — parser `test_readRunnableEntityModeSwitchPoints_round_trip_populated` (UUID/CATEGORY dropped on read-back — readARObject vs the siblings' readIdentifiable) and writer `test_writeRunnableEntityModeSwitchPoints_optional_children_omitted` (latent writer bug: empty `<MODE-GROUP-IREF/>` emitted + AttributeError when the optional iref is None); `test_readRunnableEntityModeSwitchPoints_with_mode_group_iref` (flat P-iref read + DESTs) and `test_writeRunnableEntityModeSwitchPoints_values` (flat emission + DEST/text) passed immediately — MODE-GROUP-IREF coverage was already correct on both sides; empty-wrapper + optional-children cases now covered)
  - [x] Step 6 — Update parser & writer (Green) (note: parser — `readModeSwitchPoint` now calls `readIdentifiable` (matching readParameterAccess/readVariableAccess/readInternalTriggeringPoint; one-level call chain, no double readReferrable — the old readARObject never registered the object) so the Identifiable-group elements the writer emits round-trip; writer — `writeModeSwitchPointModeGroupIRef` now guards on the instance ref so no empty `<MODE-GROUP-IREF/>` is emitted (and no crash) when the optional iref is absent; touched tests 494 passed across the 3 touched files)
  - [x] Step 7 — Update checklist comment (note: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 9.4, p.633` + 3 method rows in source order (`__init__`, getModeGroupIRef, setModeGroupIRef), 6-column format with release R23-11; reader [x] on the mutator row (setModeGroupIRef — parser call site in readModeSwitchPointModeGroupIRef), writer [x] on the getter row (getModeGroupIRef — writer call site in writeModeSwitchPointModeGroupIRef); marker withheld per batch instruction — see Step 8)
  - [x] Step 8 — Deviations
  - note (Step 8): no in-scope deviations remain (the sole Table 9.4 attribute modeGroup is modeled via `modeGroupIRef` + get/set typed `Optional[PModeGroupInAtomicSwcInstanceRef]` per Mult. 0..1 and the spec "InstanceRef implemented by: PModeGroupInAtomicSwcInstanceRef"; field name verbatim + Kind-suffix correct per Rule 0001.5; reader+writer covered — Identifiable-group gap fixed in Step 6; no naming/type/missing rows; no tracker entries for this class; member type PModeGroupInAtomicSwcInstanceRef exists with XSD-shaped fields — unstamped orphan noted below, not a Rule 0001.10 placeholder in this class). Stamp decision: no deviation remains, so the stamp would be warranted at 9b — `# Spec verified:` withheld per batch instruction (stamp deferred to batch confirmation, user instruction 2026-09-24). Observations (out of scope, not blockers): (1) inherited `AbstractAccessPoint.returnValueProvision` (RETURN-VALUE-PROVISION, ABSTRACT-ACCESS-POINT group present in the MODE-SWITCH-POINT complexType) has no reader/writer coverage anywhere — the AbstractAccessPoint inherited-base gap already recorded on the ParameterAccess/InternalTriggeringPoint rows; applies equally here (Rule 0001.7, future AbstractAccessPoint drift pass; no fixture carries the element). (2) Member types `ModeGroupInAtomicSwcInstanceRef` / `PModeGroupInAtomicSwcInstanceRef` / `RModeGroupInAtomicSWCInstanceRef` (Components/InstanceRefs.py) are unstamped orphans with all-`[ ]` checklists (also recorded on the ModeAccessPoint row) — candidates for future rows. (3) Reader-side the shared `readPModeGroupInAtomicSWCInstanceRef` also reads the base-group elements BASE-REF/CONTEXT-PORT-REF (fields of the unstamped `ModeGroupInAtomicSwcInstanceRef`), but this class's writer helper emits only the P-group elements — base-group-only irefs would lose those refs on round-trip; member-type-level debt for the InstanceRefs.py future pass, NOT this class's Table 9.4 attribute (which round-trips fully, asserted in the Step 5 tests). (4) VARIATION-POINT is emitted via writeIdentifiable (IDENTIFIABLE-group position) before MODE-GROUP-IREF rather than the XSD MODE-SWITCH-POINT group tail position (sequenceOffset 10000) — generic pre-existing behavior (ParameterAccess-row disposition), round-trip lossless. (5) Aggregator `RunnableEntity.getModeSwitchPoints` still filters the `elements` registry (`sorted(filter(...))`) although `self.modeSwitchPoints` exists (Rule 0004 to-fix shape) — aggregator row, same family as the getParameterAccesses/getInternalTriggeringPoints observations, not this class.
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: touched tests 494 passed (model test_ModeDeclarationGroup.py + parser/test_arxml_parser_orchestrators.py + writer/test_writer_swc_behavior.py); full suite 11566 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11560 + 6 net new; integration round-trip 11/11 included); `uv run ruff check src tests scripts` clean; black clean on all 6 touched files (`--check`; repo-wide black NOT run — unrelated pre-existing failures at HEAD); set-based checklist==methods + coverage audit OK; plain get_type_hints pin audit OK (setter value/return + getter return); mechanical verbatim docstring diff OK (class Note + constr_1778, modeGroup Note ×3 sites, None-no-op sentence); no-`# type:` / blank-line / member-order / top-level-export (`armodel.ModeSwitchPoint`) audits OK; no `# Spec verified:` marker present (withheld). 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `AsynchronousServerCallReturnsEvent` — RTEEvent — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.10, p.541
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/RTEEvents.py
  - note (Step 1): Table confirmed at markdown L15355; p.541 via pdf_page.py. Base chain ARObject/AbstractEvent/AtpClassifier/AtpFeature/AtpStructureElement/Identifiable/MultilanguageReferrable/RTEEvent/Referrable — most-derived base RTEEvent (stamped R23-11, Table 7.9 p.541); XSD complexType ASYNCHRONOUS-SERVER-CALL-RETURNS-EVENT (AUTOSAR_00052.xsd:6588) group chain ends RTE-EVENT → ASYNCHRONOUS-SERVER-CALL-RETURNS-EVENT. Sole attr (displayed order): eventSource (AsynchronousServerCallResultPoint, 0..1, ref — member type exists stamped in ServerCall.py) → `eventSourceRef: Optional[RefType]` per ref-Kind sibling convention (startOnEventRef/triggerRef); XSD group (xsd:6565) = single optional EVENT-SOURCE-REF, DEST=ASYNCHRONOUS-SERVER-CALL-RESULT-POINT--SUBTYPES-ENUM. Class Note + constr_1940 (existence of eventSource at contract-phase generation) captured. Consume path: reader `readAsynchronousServerCallReturnsEvent` + dispatch + `createAsynchronousServerCallReturnsEvent` factory all exist and are correct; writer `writeAsynchronousServerCallReturnsEvent` is GENUINELY BUGGED — emits the inherited `AbstractEvent.activationReasonRepresentationRef` value (normally None ⇒ EVENT-SOURCE-REF silently dropped on real events; NOT an AttributeError — seed correction: the getter resolves via the AbstractEvent MRO) and its param is annotated `InitEvent`; existing writer tests mask it with MagicMock patches. Model drift: `eventSourceRef` bare-typed `RefType = None` (0..1), untyped accessors, setter lacks None no-op. Module lacks `from __future__ import annotations`; adding it trips the PEP 563 pin test on 6 pre-existing quoted top-level returns (RTEEvent ×2, SwcModeSwitchEvent ×2, WaitPoint ×2 — all same-module names) to unquote mechanically.
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red on 2 — `test_get_set_eventSourceRef` (setEventSourceRef(None) overwrites, no None no-op) and `test_get_type_hints` (accessors untyped ⇒ hints {}, field bare `RefType`); `test_initialization` passed immediately — defaults incl. inherited RTEEvent fields were already correct; old single mixed-shape test replaced with the mirrored ModeSwitchPoint style)
  - [x] Step 3 — Implement model class (Green) (note: `eventSourceRef` field/getter/setter re-typed `Optional[RefType]` per Mult. 0..1, setter gained the None no-op + `-> AsynchronousServerCallReturnsEvent` chaining; `from __future__ import annotations` added (bare self-type return, Rule 0003/0005) and the 6 pre-existing quoted `-> "RTEEvent"/"SwcModeSwitchEvent"/"WaitPoint"` returns mechanically unquoted — PEP 563 pin test green; touched test file 27 passed + full SWComponentTemplate model tree 765 passed)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) (note: old "Gets/Sets the event source reference" paraphrases wiped — class docstring = Table 7.10 Note verbatim + class-level constr_1940 appended (constr_1182/1778 precedent; markdown "AsynchronousServerCallReturnsEvent . eventSource" wrap joined per XSD qualified name); attribute Note verbatim on the inline `__init__` comment + getter; setter = Note verbatim + None-no-op sentence; `__init__` carries no docstring; 27 passed)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: Red on exactly the 2 writer-bug tests — `test_writeAsynchronousServerCallReturnsEvent` rewritten real (setEventSourceRef + assert text/DEST; old MagicMock-masked shape removed) and `test_round_trip_populated`; `test_writeAsynchronousServerCallReturnsEvent_optional_children_omitted`, `test_round_trip_empty` and `..._none` passed immediately — already correct; MagicMock mask also removed from `test_dispatches_all_event_types` (passes — inherited getter returns None, no crash, tag emitted) and the now-unused MagicMock import dropped; parser `test_readAsynchronousServerCallReturnsEvent_full` passed immediately, strengthened to the XSD DEST literal ASYNCHRONOUS-SERVER-CALL-RESULT-POINT + getDest assert)
  - [x] Step 6 — Update parser & writer (Green) (note: writer `writeAsynchronousServerCallReturnsEvent` fixed — reads `event.getEventSourceRef()` per Table 7.10 (was the inherited `AbstractEvent.getActivationReasonRepresentationRef` ⇒ wrong-field drop) and param re-annotated `AsynchronousServerCallReturnsEvent` (was `InitEvent`); parser needs NO changes — `readAsynchronousServerCallReturnsEvent` already covers EVENT-SOURCE-REF via setEventSourceRef; touched writer+parser files 487 passed)
  - [x] Step 7 — Update checklist comment (note: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 7.10, p.541` + 3 method rows in source order (`__init__`, getEventSourceRef, setEventSourceRef), 6-column format with release R23-11; reader [x] on the mutator row (setEventSourceRef — parser call site in readAsynchronousServerCallReturnsEvent), writer [x] on the getter row (getEventSourceRef — writer call site in writeAsynchronousServerCallReturnsEvent); set-based checklist==methods + coverage audit OK; marker withheld per batch instruction — see Step 8)
  - [x] Step 8 — Deviations
  - note (Step 8): no in-scope deviations remain (the sole Table 7.10 attribute eventSource is modeled via `eventSourceRef` + get/set typed `Optional[RefType]` per Mult. 0..1 and the ref-Kind sibling convention; field name verbatim + Kind-suffix correct per Rule 0001.5; reader+writer covered after the Step 6 writer fix; no naming/type/missing rows; no tracker entries for this class in method_deviation_by_class.md/_v2.md — only the distinct BswAsynchronousServerCallReturnsEvent is tracked). Stamp decision: no deviation remains, so the stamp would be warranted at 9b — `# Spec verified:` withheld per batch instruction (stamp deferred to batch confirmation, user instruction 2026-09-24). Mechanical out-of-class edit recorded: `from __future__ import annotations` added to the shared RTEEvents.py module (required for this class's bare self-type return, Rule 0003/0005) with the 6 pre-existing quoted top-level returns unquoted to bare same-module names (RTEEvent.addDisabledModeIRef/setStartOnEventRef → RTEEvent, SwcModeSwitchEvent.setActivation/addModeIRef → SwcModeSwitchEvent, WaitPoint.setTimeout/setTriggerRef → WaitPoint) — PEP 563 conformance only, no runtime behavior change, those stamped classes' checklists/rows untouched beyond the unquote (ParameterAccess-row precedent).
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: touched tests 515 passed (model test_RTEEvents.py 27 + test_pep563_annotations.py 1 + parser/test_arxml_parser_orchestrators.py 289 + writer/test_writer_swc_behavior.py 198); full suite 11571 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11566 + 5 net new; integration round-trip included); `uv run ruff check src tests scripts` clean; black clean on all 5 touched Python files (`--check`; repo-wide black NOT run — unrelated pre-existing failures at HEAD); set-based checklist==methods + coverage audit OK; plain get_type_hints pin audit OK (setter value/return + getter return); mechanical verbatim docstring diff OK (class Note + constr_1940, eventSource Note ×3 sites, None-no-op sentence); no-`# type:` / blank-line / member-order / top-level-export (`armodel.AsynchronousServerCallReturnsEvent`) audits OK; no `# Spec verified:` marker present (withheld). 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `DataReceiveErrorEvent` — RTEEvent — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.14, p.543
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/RTEEvents.py
  - note (Step 1): Table confirmed at markdown L15413; p.543 via pdf_page.py (--table 7.14). Base chain ARObject/AbstractEvent/AtpClassifier/AtpFeature/AtpStructureElement/Identifiable/MultilanguageReferrable/RTEEvent/Referrable — most-derived base RTEEvent (stamped R23-11, Table 7.9 p.541; existing `(RTEEvent)` shape correct); XSD complexType DATA-RECEIVE-ERROR-EVENT (AUTOSAR_00052.xsd:28090) group chain ends RTE-EVENT → DATA-RECEIVE-ERROR-EVENT. Sole attr (displayed order): data (VariableDataPrototype, 0..1, iref — "InstanceRef implemented by: RVariableInAtomicSwcInstanceRef"; member type exists at Components/InstanceRefs.py:452, already imported) → field name `dataIRef` verbatim-correct per iref-Kind sibling convention (ModeSwitchPoint modeGroupIRef precedent) but bare-typed 0..1, untyped accessors, setter lacks None no-op + chaining = drift; XSD group (xsd:28074) = single optional DATA-IREF, CONTEXT-R-PORT-REF (DEST=ABSTRACT-REQUIRED-PORT-PROTOTYPE--SUBTYPES-ENUM) + TARGET-DATA-ELEMENT-REF (DEST=VARIABLE-DATA-PROTOTYPE--SUBTYPES-ENUM). Class Note + constr_1944 (existence of data at contract-phase generation) captured; markdown wraps "Data ReceiveErrorEvent" / "RVariableInAtomicSwc InstanceRef" joined per XSD qualified names (prior-rows precedent). R4.3.1 Tables 7.15/7.39 (L12766/L13651) verified identical to each other but OLDER wording vs R23-11 (Note "raised by the RTE ... referenced data element", terse "Data element referenced by event") — R23-11 table wins, class syncs R23-11. Consume path: class entirely UNCONSUMED — parser `readSwcInternalBehaviorEvents` has NO DATA-RECEIVE-ERROR-EVENT branch (falls to notImplemented), writer `writeSwcInternalBehaviorEvents` has NO branch (silently dropped), SwcInternalBehavior factory `createDataReceiveErrorEvent` missing, and `DataReceiveErrorEvent` missing from the SwcInternalBehavior import block (top-level export chain broken); shared helpers `readRVariableInAtomicSwcInstanceRef`/`setRVariableInAtomicSwcInstanceRef` reusable as-is. No MagicMock masking on my writer helper (none exists; RTE-event masks removed in f1569e312; remaining parser-test MagicMocks are unrelated classes). Genuine Red pending on model (None no-op + get_type_hints pins) and reader/writer/round-trip.
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red on 2 — `test_get_set_dataIRef` (setDataIRef(None) overwrites, no None no-op) and `test_get_type_hints` (accessors untyped ⇒ hints {}); `test_initialization` passed immediately — defaults incl. inherited RTEEvent fields were already correct; old single mixed-shape test replaced with the mirrored AsynchronousServerCallReturnsEvent style)
  - [x] Step 3 — Implement model class (Green) (note: `dataIRef` field/getter/setter re-typed `Optional[RVariableInAtomicSwcInstanceRef]` per Mult. 0..1, setter gained the None no-op + `-> DataReceiveErrorEvent` chaining; touched test file 29 passed)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) (note: old "Gets/Sets the data instance reference" paraphrases wiped — class docstring = Table 7.14 Note verbatim + class-level constr_1944 appended (constr_1940 precedent; markdown "Data ReceiveErrorEvent" / "RVariableInAtomicSwc InstanceRef" wraps joined per XSD qualified names); attribute Note verbatim on the inline `__init__` comment + getter; setter = Note verbatim + None-no-op sentence; `__init__` carries no docstring; 29 passed)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: Red on all 8 new tests — parser `test_readDataReceiveErrorEvent_full` (AttributeError: factory missing) and dispatch `test_event_branches[DATA-RECEIVE-ERROR-EVENT]` (NotImplementedError: no branch, notImplemented raises); writer `test_writeDataReceiveErrorEvent` / `..._optional_children_omitted` / `..._none` + `test_round_trip_populated` / `test_round_trip_empty` (AttributeError: factory + writer helper missing — the class was entirely unconsumed on both sides))
  - [x] Step 6 — Update parser & writer (Green) (note: parser — new `readDataReceiveErrorEvent` (readRTEEvent + shared `readRVariableInAtomicSwcInstanceRef` → setDataIRef, read exactly once) + dispatch branch after DATA-RECEIVED-EVENT; writer — new `writeDataReceiveErrorEvent` (setRTEEvent + shared `setRVariableInAtomicSwcInstanceRef` via the getDataIRef getter) + isinstance branch after DataReceivedEvent; SwcInternalBehavior — new `createDataReceiveErrorEvent` factory (mirror of createDataReceivedEvent: IsElementExists + addElement + events.append) + import added to the existing RTEEvents import line (top-level export chain restored, `armodel.DataReceiveErrorEvent` resolves); touched parser+writer files 494 passed, model file 30 passed incl. the PEP 563 pin test)
  - [x] Step 7 — Update checklist comment (note: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 7.14, p.543` + 3 method rows in source order (`__init__`, getDataIRef, setDataIRef), 6-column format with release R23-11; reader [x] on the mutator row (setDataIRef — parser call site in readRVariableInAtomicSwcInstanceRef), writer [x] on the getter row (getDataIRef — writer call site in writeDataReceiveErrorEvent); marker withheld per batch instruction — see Step 8)
  - [x] Step 8 — Deviations
  - note (Step 8): no in-scope deviations remain (the sole Table 7.14 attribute data is modeled via `dataIRef` + get/set typed `Optional[RVariableInAtomicSwcInstanceRef]` per Mult. 0..1 and the spec "InstanceRef implemented by: RVariableInAtomicSwcInstanceRef"; field name verbatim + iref Kind suffix correct per Rule 0001.5; reader+writer covered after the Step 6 factory/dispatch additions — Identifiable-group elements round-trip via readRTEEvent/setRTEEvent; no naming/type/missing rows; no tracker entries for this class in method_deviation_by_class.md/_v2.md). Stamp decision: no deviation remains, so the stamp would be warranted at 9b — `# Spec verified:` withheld per batch instruction (stamp deferred to batch confirmation, user instruction 2026-09-24). Observations (out of scope, not blockers): (1) sibling `writeDataReceivedEvent` reads the field directly (`event.dataIRef`) instead of the getter — my `writeDataReceiveErrorEvent` uses `getDataIRef()` per the writer convention; the sibling drift belongs to DataReceivedEvent's own row (next in queue). (2) Shared parser helper `readRVariableInAtomicSwcInstanceRef` is annotated `parent: DataReceivedEvent` (narrow annotation on a shared helper; runtime-correct for both classes since both carry the identical DATA-IREF shape) — widening belongs to DataReceivedEvent's row. (3) Member type `RVariableInAtomicSwcInstanceRef` (Components/InstanceRefs.py:452) is an unstamped orphan with an all-`[ ]` checklist — same family as the ModeAccessPoint/ModeSwitchPoint InstanceRefs observations; not a Rule 0001.10 placeholder here (exists with XSD-shaped fields; round-trip asserted in the Step 5 tests).
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: touched tests 524 passed (model test_RTEEvents.py 30 incl. test_pep563_annotations.py + parser/test_arxml_parser_orchestrators.py 291 + writer/test_writer_swc_behavior.py 203); full suite 11580 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11571 + 9 net new; integration round-trip included); `uv run ruff check src tests scripts` clean; black clean on all 7 touched Python files (`--check`; repo-wide black NOT run — unrelated pre-existing failures at HEAD); set-based checklist==methods + coverage audit OK; plain get_type_hints pin audit OK (setter value/return + getter return); mechanical verbatim docstring diff OK (class Note + constr_1944, attribute Note ×3 sites, None-no-op sentence); no-`# type:` / blank-line / member-order / top-level-export (`armodel.DataReceiveErrorEvent`) audits OK; Rule 0013 chain check — 0 new chained mutators in the diff (pre-existing constructor-arg idioms untouched); no `# Spec verified:` marker present (withheld). 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `DataReceivedEvent` — RTEEvent — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.13, p.542
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/RTEEvents.py
  - note (Step 1): Table confirmed at markdown L15398; p.542 via pdf_page.py (--table 7.13). Base chain ARObject/AbstractEvent/AtpClassifier/AtpFeature/AtpStructureElement/Identifiable/MultilanguageReferrable/RTEEvent/Referrable — most-derived base RTEEvent (stamped R23-11, Table 7.9 p.541; existing `(RTEEvent)` shape correct); XSD complexType DATA-RECEIVED-EVENT (AUTOSAR_00052.xsd:28128) group chain ends RTE-EVENT → DATA-RECEIVED-EVENT. Sole attr (displayed order): data (VariableDataPrototype, 0..1, iref — "InstanceRef implemented by: RVariableInAtomicSwcInstanceRef"; member type exists at Components/InstanceRefs.py:452, already imported) → field name `dataIRef` verbatim-correct per iref-Kind sibling convention but bare-typed 0..1, untyped accessors, setter lacks None no-op + chaining = drift; XSD group (xsd:28112) = single optional DATA-IREF type R-VARIABLE-IN-ATOMIC-SWC-INSTANCE-REF (fixed concrete — flat read/write, same shape as DataReceiveErrorEvent). Class Note + constr_1943 (existence of data at contract-phase generation) captured; markdown wraps "Data ReceivedEvent" / "RVariableInAtomicSwc InstanceRef" joined per XSD qualified names (prior-rows precedent). R4.3.1 Tables 7.14/7.38 (L12756/L13612) verified reproductions with OLDER wording vs R23-11 ("The event is raised when the referenced data elements are received." / "Data element referenced by event") — R23-11 + XSD authoritative, class syncs R23-11. Consume path: parser `readDataReceivedEvent` + dispatch branch + `createDataReceivedEvent` factory + export chain (armodel.DataReceivedEvent) all exist and are correct; writer `writeDataReceivedEvent` exists and works but reads the field directly (`event.dataIRef`) instead of the getDataIRef() getter (seed 1 verified — convention drift, not a functional drop); shared parser helper `readRVariableInAtomicSwcInstanceRef` annotated narrowly `parent: DataReceivedEvent` while DataReceiveErrorEvent also flows through it (seed 2 verified — widen at Step 6). Existing writer test asserts only tag+DATA-IREF presence (no DEST/text — weaker than the DREE sibling); no MagicMock masking on this writer helper. Genuine Red pending model-side only (None no-op + get_type_hints pins); parser/writer coverage exists — Step 5 expects pass-immediately + strengthening of the thin writer test and the event-branches parametrize list (DATA-RECEIVED-EVENT absent).
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red on 2 — `test_get_set_dataIRef` (setDataIRef(None) overwrites, no None no-op) and `test_get_type_hints` (accessors untyped ⇒ hints {}); `test_initialization` passed immediately — defaults incl. inherited RTEEvent fields were already correct; old single mixed-shape test replaced with the mirrored DataReceiveErrorEvent style)
  - [x] Step 3 — Implement model class (Green) (note: `dataIRef` field/getter/setter re-typed `Optional[RVariableInAtomicSwcInstanceRef]` per Mult. 0..1, setter gained the None no-op + `-> DataReceivedEvent` chaining; touched test file 31 passed)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) (note: old "Gets/Sets the data instance reference" paraphrases wiped — class docstring = Table 7.13 Note verbatim + class-level constr_1943 appended (constr_1940/1944 precedent; markdown "Data ReceivedEvent" / "RVariableInAtomicSwc InstanceRef" wraps joined per XSD qualified names); attribute Note verbatim on the inline `__init__` comment + getter; setter = Note verbatim + None-no-op sentence; `__init__` carries no docstring; 31 passed)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: NO genuine Red — all new/strengthened tests passed immediately (reader/writer coverage was already complete per the Step 1 consume-path grep; the writer's direct-field read `event.dataIRef` is runtime-equivalent to the getter, no dropped fields, no MagicMock masking). New coverage per Rule 0006 asserting field values, not just presence: parser `test_readDataReceivedEvent_full` strengthened to the XSD DEST literals (R-PORT-PROTOTYPE / VARIABLE-DATA-PROTOTYPE getDest asserts, DREE-sibling shape) and `DATA-RECEIVED-EVENT` added to the `test_event_branches` parametrize list (was absent — dispatch now covered); writer `test_writeDataReceivedEvent` rewritten to the DREE-sibling shape (DATA-IREF text + DEST for both refs), new `test_writeDataReceivedEvent_optional_children_omitted` (+ pre-existing `..._none`); round-trip `TestDataReceivedEventRoundTrip` added — `test_round_trip_populated` (dataIRef refs + inherited startOnEventRef values/DESTs) and `test_round_trip_empty` (no DATA-IREF in serialized XML, reload ⇒ getDataIRef() is None); touched parser+writer files 498 passed)
  - [x] Step 6 — Update parser & writer (Green) (note: writer — `writeDataReceivedEvent` now reads `event.getDataIRef()` per the writer convention (was the direct-field read `event.dataIRef`; seed 1 verified and fixed — runtime-equivalent, no behavior change, tests passed before and after); parser — shared helper `readRVariableInAtomicSwcInstanceRef` annotation widened `parent: DataReceivedEvent` → `parent: Union[DataReceivedEvent, DataReceiveErrorEvent]` (seed 2 verified — annotation-only, `Union` added to the existing typing import; both call sites 5605/5610 runtime-correct before and after); touched parser+writer+model files 529 passed)
  - [x] Step 7 — Update checklist comment (note: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 7.13, p.542` + 3 method rows in source order (`__init__`, getDataIRef, setDataIRef), 6-column format with release R23-11; reader [x] on the mutator row (setDataIRef — parser call site in readRVariableInAtomicSwcInstanceRef), writer [x] on the getter row (getDataIRef — writer call site in writeDataReceivedEvent); marker withheld per batch instruction — see Step 8)
  - [x] Step 8 — Deviations
  - note (Step 8): no in-scope deviations remain (the sole Table 7.13 attribute data is modeled via `dataIRef` + get/set typed `Optional[RVariableInAtomicSwcInstanceRef]` per Mult. 0..1 and the spec "InstanceRef implemented by: RVariableInAtomicSwcInstanceRef"; field name verbatim + iref Kind suffix correct per Rule 0001.5; reader+writer covered — Identifiable-group elements round-trip via readRTEEvent/setRTEEvent; no naming/type/missing rows; no tracker entries for this class in method_deviation_by_class.md/_v2.md). Stamp decision: no deviation remains, so the stamp would be warranted at 9b — `# Spec verified:` withheld per batch instruction (stamp deferred to batch confirmation, user instruction 2026-09-24). Seeds from the DataReceiveErrorEvent agent: (1) VERIFIED + FIXED at Step 6 — `writeDataReceivedEvent` read the field directly (`event.dataIRef`), now `getDataIRef()` per the writer convention; (2) VERIFIED + FIXED at Step 6 — shared parser helper `readRVariableInAtomicSwcInstanceRef` annotation widened to `Union[DataReceivedEvent, DataReceiveErrorEvent]` (annotation-only). Observations (out of scope, not blockers): (1) member type `RVariableInAtomicSwcInstanceRef` (Components/InstanceRefs.py:452) is an unstamped orphan with an all-`[ ]` checklist — same family as the ModeAccessPoint/ModeSwitchPoint InstanceRefs observations; not a Rule 0001.10 placeholder here (exists with XSD-shaped fields; round-trip asserted in the Step 5 tests).
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: touched tests 529 passed (model test_RTEEvents.py 31 + test_pep563_annotations.py 1 + parser/test_arxml_parser_orchestrators.py 292 + writer/test_writer_swc_behavior.py 206); full suite 11586 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11580 + 6 net new; integration round-trip 11/11 included); `uv run ruff check src tests scripts` clean; black clean on all 6 touched Python files (`--check`; repo-wide black NOT run — unrelated pre-existing failures at HEAD); set-based checklist==methods + coverage audit OK; plain get_type_hints pin audit OK (setter value/return + getter return); mechanical verbatim docstring diff OK (class Note + constr_1943, attribute Note ×3 sites, None-no-op sentence); no-`# type:` / blank-line / member-order / top-level-export (`armodel.DataReceivedEvent`) audits OK; Rule 0013 chain check — 0 new chained mutators in the diff; no `# Spec verified:` marker present (withheld). 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `DataSendCompletedEvent` — RTEEvent — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.11, p.542
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/RTEEvents.py
  - note (Step 1): Table confirmed at markdown L15372; p.542 via pdf_page.py (--table 7.11). Base chain ARObject/AbstractEvent/AtpClassifier/AtpFeature/AtpStructureElement/Identifiable/MultilanguageReferrable/RTEEvent/Referrable — most-derived base RTEEvent (stamped R23-11, Table 7.9 p.541; existing `(RTEEvent)` shape correct); XSD complexType DATA-SEND-COMPLETED-EVENT (AUTOSAR_00052.xsd:28173) group chain ends RTE-EVENT → DATA-SEND-COMPLETED-EVENT (group xsd:28150). Sole attr (displayed order): eventSource (VariableAccess, 0..1, ref — plain EVENT-SOURCE-REF element, REF + required DEST of VARIABLE-ACCESS--SUBTYPES-ENUM whose sole enum value is VARIABLE-ACCESS) → field name `eventSourceRef` verbatim + Kind suffix correct (AsynchronousServerCallReturnsEvent precedent) but bare-typed 0..1 (`RefType = None`), untyped accessors, setter lacks None no-op + return annotation = drift; XSD group = single optional EVENT-SOURCE-REF, flat read/write. Class Note + constr_1941 (existence of eventSource at contract-phase generation) captured; markdown wraps "DataSend CompletedEvent" joined per XSD qualified names (prior-rows precedent). R4.3.1 Tables 7.12/7.36 (L12744/L13562) verified reproductions with OLDER wording vs R23-11 (7.36: "The event is raised when the referenced data elements have been sent or an error occurs." / "The variable access that triggers the event.", Mult. 1 vs R23-11 0..1; 7.12's caption even sits over a DataWriteCompletedEvent body) — R23-11 + XSD authoritative, class syncs R23-11. Consume path: fully wired — parser `readDataSendCompletedEvent` (arxml_parser.py:5647, readRTEEvent + setEventSourceRef(getChildElementOptionalRefType EVENT-SOURCE-REF)) + dispatch branch (:5685); writer `writeDataSendCompletedEvent` (arxml_writer.py:3627, setRTEEvent + setChildElementOptionalRefType via the getEventSourceRef() getter) + isinstance branch (:3659); SwcInternalBehavior `createDataSendCompletedEvent` (:1105) + `getDataSendCompletedEvents` (:1179) exist; export import present (SwcInternalBehavior/__init__.py:10; `armodel.DataSendCompletedEvent` runtime check deferred to 9a). Known-disease check: writer helper reads via the GETTER (no direct-field read disease); no MagicMock near DataSendCompletedEvent writer tests. Existing writer test asserts only tag presence and existing parser test asserts getValue only (no DEST assert — both thin, strengthen at Step 5); both use DEST='DATA-SEND-POINT' which is NOT a VARIABLE-ACCESS--SUBTYPES-ENUM value — strengthened tests use the XSD-valid literal; DATA-SEND-COMPLETED-EVENT already in `test_event_branches` parametrize. Genuine Red pending model-side only (None no-op + get_type_hints pins + Optional[RefType]); parser/writer coverage exists — Step 5 expects pass-immediately + strengthening.
  - [x] Step 1 — Sync members & description from spec
  - [x] Step 2 — Write model class unit test (Red) (note: Red on 2 — `test_get_set_eventSourceRef` (setEventSourceRef(None) overwrites, no None no-op) and `test_get_type_hints` (accessors untyped ⇒ hints {}); `test_initialization` passed immediately — defaults incl. inherited RTEEvent fields were already correct; old single mixed-shape test replaced with the mirrored DataReceivedEvent/DataReceiveErrorEvent style)
  - [x] Step 3 — Implement model class (Green) (note: `eventSourceRef` field/getter/setter re-typed `Optional[RefType]` per Mult. 0..1, setter gained the None no-op + `-> DataSendCompletedEvent` chaining; touched test file 33 passed)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) (note: old "Gets/Sets the event source reference" Google-style paraphrases wiped — class docstring = Table 7.11 Note verbatim + class-level constr_1941 appended (constr_1940/1943/1944 precedent; markdown "DataSend CompletedEvent" wrap joined per XSD qualified names); attribute Note verbatim on the inline `__init__` comment + getter; setter = Note verbatim + None-no-op sentence; `__init__` carries no docstring; 33 passed)
  - [x] Step 5 — Write reader/writer round-trip test (Red) (note: NO genuine Red — all new/strengthened tests passed immediately (reader/writer coverage was already complete and getter-correct per the Step 1 consume-path grep; no dropped fields, no MagicMock masking). New/strengthened coverage per Rule 0006 asserting field values, not just presence: writer `test_writeDataSendCompletedEvent` rewritten to the ASCRE-sibling shape (EVENT-SOURCE-REF text + DEST) with the XSD-valid DEST literal `VARIABLE-ACCESS` (was `DATA-SEND-POINT`, not a VARIABLE-ACCESS--SUBTYPES-ENUM value) and new `test_writeDataSendCompletedEvent_optional_children_omitted` (+ pre-existing `..._none`); parser `test_readDataSendCompletedEvent_full` strengthened with the DEST assert (same XSD-valid literal swap); round-trip `TestDataSendCompletedEventRoundTrip` added — `test_round_trip_populated` (eventSourceRef values/DEST + inherited startOnEventRef values/DEST) and `test_round_trip_empty` (no EVENT-SOURCE-REF in serialized XML, reload ⇒ getEventSourceRef() is None); touched parser+writer files 501 passed)
  - [x] Step 6 — Update parser & writer (Green) (note: NO source edit needed — the Step 1 consume-path audit found both helpers already spec-correct: parser `readDataSendCompletedEvent` (readRTEEvent + setEventSourceRef(getChildElementOptionalRefType "EVENT-SOURCE-REF"), mutator-based) and writer `writeDataSendCompletedEvent` (setRTEEvent + setChildElementOptionalRefType via the getEventSourceRef() GETTER — no direct-field read, no wrong-field disease, no MagicMock masking); dispatch branches + SwcInternalBehavior factory + export import pre-exist; git diff on both files is empty for this run; Rule 0013 chain check — only pre-existing constructor-arg idioms in unrelated handlers, 0 in my scope; Green carried by the Step 5 runs, 501 passed)
  - [x] Step 7 — Update checklist comment (note: `# Spec: AUTOSAR_CP_TPS_SoftwareComponentTemplate.pdf, Table 7.11, p.542` + 3 method rows in source order (`__init__`, getEventSourceRef, setEventSourceRef), 6-column format with release R23-11; reader [x] on the mutator row (setEventSourceRef — parser call site in readDataSendCompletedEvent), writer [x] on the getter row (getEventSourceRef — writer call site in writeDataSendCompletedEvent); marker withheld per batch instruction — see Step 8)
  - [x] Step 8 — Deviations
  - note (Step 8): no in-scope deviations remain (the sole Table 7.11 attribute eventSource is modeled via `eventSourceRef` + get/set typed `Optional[RefType]` per Mult. 0..1 and Kind=ref (plain REF element, DEST VARIABLE-ACCESS--SUBTYPES-ENUM); field name verbatim + Kind suffix correct per Rule 0001.5; reader+writer covered — Identifiable-group elements round-trip via readRTEEvent/setRTEEvent; no naming/type/missing rows; no tracker entries for this class in method_deviation_by_class.md/_v2.md). Stamp decision: no deviation remains, so the stamp would be warranted at 9b — `# Spec verified:` withheld per batch instruction (stamp deferred to batch confirmation, user instruction 2026-09-24). Observations (out of scope, not blockers): (1) SwcInternalBehavior aggregator `getDataSendCompletedEvents` (SwcInternalBehavior/__init__.py:1183) uses the `sorted(filter(isinstance, self.events))` registry-filter shape (Rule 0004 to-fix family) — belongs to SwcInternalBehavior's own stamped row, same family as prior rows' aggregator observations. (2) Pre-existing parser/writer tests carried the non-XSD DEST literal `DATA-SEND-POINT` (VARIABLE-ACCESS--SUBTYPES-ENUM's sole value is VARIABLE-ACCESS); corrected test-side to the XSD-valid literal this pass — no fixture or source impact.
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: touched tests 535 passed (model test_RTEEvents.py 33 + test_pep563_annotations.py 1 + parser/test_arxml_parser_orchestrators.py 292 + writer/test_writer_swc_behavior.py 209); full suite 11935 passed / 0 failed (`uv run python scripts/run_tests.py --no-coverage`, baseline 11930 + 5 net new; integration round-trip included); `uv run ruff check src tests scripts` clean; black clean on all 4 touched Python files (`--check`; repo-wide black NOT run — unrelated pre-existing failures at HEAD); set-based checklist==methods + coverage audit OK; plain get_type_hints pin audit OK (setter value/return + getter return); mechanical verbatim docstring diff OK (class Note + constr_1941, attribute Note ×3 sites, None-no-op sentence); no-`# type:` / blank-line / member-order / top-level-export (`armodel.DataSendCompletedEvent`) audits OK; Rule 0013 chain check — 0 chained mutators in scope (parser/writer untouched this run, empty diff); no `# Spec verified:` marker present (withheld). 9b deferred to batch confirmation (user instruction 2026-09-24)

- [ ] `DataWriteCompletedEvent` — RTEEvent — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.12, p.542
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/RTEEvents.py
  - [x] Step 1 — Sync members & description from spec — Table 7.12, p.542; Base chain → most-existing stamped `RTEEvent`; sole attr eventSource (VariableAccess, 0..1, ref → eventSourceRef, DEST=VARIABLE-ACCESS--SUBTYPES-ENUM per XSD group DATA-WRITE-COMPLETED-EVENT, single optional EVENT-SOURCE-REF); Note verbatim + constr_1942; R4.3.1 Tables 7.13/7.37 = older-wording reproductions
  - [x] Step 2 — Write model class unit test (Red) — TestDataWriteCompletedEvent rewritten to sibling spec-contract form: verbatim Note+constr docstring pin (inspect.cleandoc — multi-line inner indent), initialization, get/set + DEST + None no-op, plain get_type_hints pins (3 Red: docstring/None-no-op/hints; init passed — honest)
  - [x] Step 3 — Implement model class (Green) — Optional[RefType] retype (was bare RefType = None), typed accessors + chaining + None no-op; quoted self-return unquoted per repo pep563 guard (amended Rule 0003)
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — class Note + constr_1942 verbatim; XSD element documentation verbatim on inline comment + getter + setter (replaced "Gets/Sets the..." paraphrases); 6-column checklist with release column
  - [x] Step 5 — Write reader/writer round-trip test (Red) — writer full/optional-omitted/none tests, parser direct-read + EVENTS-dispatch tests; 5 Red (no factory/reader/writer at all — class was entirely unconsumed)
  - [x] Step 6 — Update parser & writer (Green) — SwcInternalBehavior.createDataWriteCompletedEvent factory (IsElementExists guard + checklist row + import); readDataWriteCompletedEvent + DATA-WRITE-COMPLETED-EVENT dispatch branch (readRTEEvent + setEventSourceRef, mirroring DataSendCompletedEvent); writeDataWriteCompletedEvent + isinstance branch; imports both sides
  - [x] Step 7 — Update checklist comment — 6-column parity checklist, Table 7.12 p.542 citation, release column; __init__ row [—] reader/writer; reader on setter row / writer on getter row; marker deferred to 9b
  - [x] Step 8 — Deviations  [none in scope: 1 attr modeled exactly; Base stamped; DEST enum = sole XSD value VARIABLE-ACCESS; no tracker entries; RteEvents module pep563-clean]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: full suite 11943 passed / 0 failed (--no-coverage, baseline 11935 + 8 net new incl. 6 round-trip + pep563 guard fix); ruff clean; black clean on all 7 touched files; integration round-trip green; 9b deferred to batch confirmation (user instruction 2026-09-24)
- [ ] `InternalTriggerOccurredEvent` — RTEEvent — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.21, p.546
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/RTEEvents.py
  - [x] Step 1 — Sync members & description from spec — Table 7.21, p.546; Base chain → most-existing stamped `RTEEvent`; sole attr eventSource (InternalTriggeringPoint, 0..1, ref → eventSourceRef, DEST=INTERNAL-TRIGGERING-POINT--SUBTYPES-ENUM per XSD group INTERNAL-TRIGGER-OCCURRED-EVENT); Note verbatim + constr_1950; R4.3.1 Tables 7.23/7.32 = older reproductions
  - [x] Step 2 — Write model class unit test (Red) — TestInternalTriggerOccurredEvent rewritten to sibling spec-contract form: verbatim Note+constr docstring pin, initialization, get/set + DEST + None no-op, plain get_type_hints pins (2 Red: docstring/hints; None-no-op + init passed — honest)
  - [x] Step 3 — Implement model class (Green) — Optional[RefType] retype (was bare RefType = None), typed accessors + chaining; None no-op pre-existed
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — class Note + constr_1950 verbatim; XSD element documentation verbatim on inline comment + getter + setter (replaced "Gets/Sets the..." paraphrases); 6-column checklist with release column
  - [x] Step 5 — Write reader/writer round-trip test (Red) — writer (value+DEST, none-guard) and parser (value) tests PRE-EXIST with value asserts; dispatch parametrize + isinstance branches pre-exist — Steps 5/6 honest pass-immediately (model tests were the genuine Reds)
  - [x] Step 6 — Update parser & writer (Green) — no changes needed: readInternalTriggerOccurredEvent (readRTEEvent + setEventSourceRef) / writeInternalTriggerOccurredEvent (getEventSourceRef via setChildElementOptionalRefType) / factory + checklist row all pre-exist correct
  - [x] Step 7 — Update checklist comment — 6-column parity checklist, Table 7.21 p.546 citation, release column; __init__ row [—] reader/writer; reader on setter row / writer on getter row; marker deferred to 9b
  - [x] Step 8 — Deviations  [none in scope: 1 attr modeled exactly; Base stamped; DEST enum per XSD; no tracker entries]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: full suite 11946 passed / 0 failed (--no-coverage, baseline 11943 + 3 net new); ruff clean; black clean on touched files; integration round-trip green; 9b deferred to batch confirmation (user instruction 2026-09-24)
- [ ] `OperationInvokedEvent` — RTEEvent — R23-11 CP_TPS_SoftwareComponentTemplate Table 7.15, p.543
  - module: M2/AUTOSARTemplates/SWComponentTemplate/SwcInternalBehavior/RTEEvents.py
  - [x] Step 1 — Sync members & description from spec — Table 7.15, p.543; Base chain → most-existing stamped `RTEEvent`; sole attr operation (ClientServerOperation, 0..1, iref → operationIRef, XSD group OPERATION-INVOKED-EVENT single OPERATION-IREF of P-OPERATION-IN-ATOMIC-SWC-INSTANCE-REF); Note verbatim + constr_1945; R4.3.1 Table 7.16 + BSW D.46 = reproductions
  - [x] Step 2 — Write model class unit test (Red) — TestOperationInvokedEvent rewritten to sibling spec-contract form: verbatim Note+constr docstring pin, initialization, get/set + None no-op, plain get_type_hints pins (3 Red: docstring/None-no-op/hints); POperationInAtomicSwcInstanceRef moved to top-level test import
  - [x] Step 3 — Implement model class (Green) — Optional[POperationInAtomicSwcInstanceRef] retype (was bare), typed accessors + chaining + None no-op
  - [x] Step 4 — Sync docstrings (wipe + rewrite) — class Note + constr_1945 verbatim; XSD element documentation verbatim on inline comment + getter + setter (replaced "Gets/Sets the..." paraphrases); 6-column checklist with release column
  - [x] Step 5 — Write reader/writer round-trip test (Red) — writer (value asserts, none-guard) + parser dispatch parametrize + readPOperationIRef coverage pre-exist; model tests were the genuine Reds — honest pass-immediately
  - [x] Step 6 — Update parser & writer (Green) — writer writeOperationInvokedEvent read the FIELD directly (event.operationIRef) instead of the getter — fixed to getOperationIRef() (convention fix, DataReceivedEvent precedent); reader/factory/dispatch verified correct
  - [x] Step 7 — Update checklist comment — 6-column parity checklist, Table 7.15 p.543 citation, release column; __init__ row [—] reader/writer; reader on setter row / writer on getter row; marker deferred to 9b
  - [x] Step 8 — Deviations  [none in scope: 1 attr modeled exactly; Base stamped; iref type per XSD fixed-concrete P-variant; no tracker entries]
  - [ ] Step 9 — Verify (9a) + confirm (9b) — 9a passed 2026-09-25: full suite 11949 passed / 0 failed (--no-coverage, baseline 11946 + 3 net new); ruff clean; black clean on touched files; integration round-trip green; 9b deferred to batch confirmation (user instruction 2026-09-24)
- [ ] `RteEventInEcuInstanceRef` — AtpInstanceRef — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/MeasurementCalibrationSupport/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)

- [ ] `VariableAccessInEcuInstanceRef` — AtpInstanceRef — source TBC (locate table at Step 1)
  - module: M2/AUTOSARTemplates/CommonStructure/MeasurementCalibrationSupport/__init__.py
  - note: deviation-tracked in method_deviation_by_class.md + method_deviation_by_class_v2.md — review entries at Step 1
  - [ ] Step 1 — Sync members & description from spec
  - [ ] Step 2 — Write model class unit test (Red)
  - [ ] Step 3 — Implement model class (Green)
  - [ ] Step 4 — Sync docstrings (wipe + rewrite)
  - [ ] Step 5 — Write reader/writer round-trip test (Red)
  - [ ] Step 6 — Update parser & writer (Green)
  - [ ] Step 7 — Update checklist comment
  - [ ] Step 8 — Deviations
  - [ ] Step 9 — Verify (9a) + confirm (9b)

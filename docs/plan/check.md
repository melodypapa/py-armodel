SomeipSdClientEventGroupTimingConfig is confirmed.
SomeipSdServerEventGroupTimingConfig is confirmed.
SomeipServiceVersion is confirmed.
DhcpServerConfiguration class docstring is partial. the member's docstring are incorrect.
CouplingPortTrafficClassAssignment is confirmed.
CanXlProps is can be Xsd verfied not Spec verified.
TagWithOptionalValue is confirmed.
ConsumedEventGroup is confirmed.
ConsumedServiceInstance is confirmed.
SomeipSdClientServiceInstanceConfig is confirmed.
Ipv4DhcpServerConfiguration is confirmed.
Ipv6DhcpServerConfiguration is confirmed.
AnyServiceInstanceId is confirmed.
AnyVersionString is confirmed.